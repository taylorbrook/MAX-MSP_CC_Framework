---
phase: quick-260331-vs8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/graph.py
  - src/maxpat/analysis.py
  - src/maxpat/patcher.py
  - src/maxpat/validation.py
  - tests/test_validation.py
autonomous: true
requirements: [DECOMPOSE-GRAPH, DECOMPOSE-ANALYSIS, MAXCLASS-VALIDATION]

must_haves:
  truths:
    - "Patcher instances still have all graph methods (downstream, upstream, signal_path, connected_components)"
    - "Patcher instances still have all analysis methods (analyze, _analyze_sections, etc.)"
    - "All existing tests pass unchanged (test_patcher.py, test_analysis.py, test_validation.py)"
    - "Non-UI objects using their name as maxclass instead of 'newobj' are flagged by validation"
  artifacts:
    - path: "src/maxpat/graph.py"
      provides: "GraphMixin class with ~325 lines of graph traversal methods"
      contains: "class GraphMixin"
    - path: "src/maxpat/analysis.py"
      provides: "AnalysisMixin class with ~500 lines of analysis methods plus constants"
      contains: "class AnalysisMixin"
    - path: "src/maxpat/patcher.py"
      provides: "Patcher inheriting GraphMixin and AnalysisMixin"
      contains: "class Patcher(GraphMixin, AnalysisMixin)"
  key_links:
    - from: "src/maxpat/patcher.py"
      to: "src/maxpat/graph.py"
      via: "mixin inheritance"
      pattern: "from src.maxpat.graph import GraphMixin"
    - from: "src/maxpat/patcher.py"
      to: "src/maxpat/analysis.py"
      via: "mixin inheritance"
      pattern: "from src.maxpat.analysis import AnalysisMixin"
    - from: "tests/test_analysis.py"
      to: "src/maxpat/patcher.py"
      via: "import SECTION_SIGNATURES"
      pattern: "from src.maxpat.patcher import.*SECTION_SIGNATURES"
---

<objective>
Decompose patcher.py (~2890 lines) by extracting graph traversal methods into graph.py (GraphMixin) and analysis methods into analysis.py (AnalysisMixin). Add maxclass validation check to validation.py. All existing tests must pass without modification.

Purpose: Reduce patcher.py from ~2890 to ~2060 lines for maintainability. Separate graph/analysis concerns. Add missing maxclass validation.
Output: graph.py, analysis.py (new modules), slimmed patcher.py, updated validation.py
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/maxpat/patcher.py
@src/maxpat/validation.py
@src/maxpat/maxclass_map.py
@src/maxpat/__init__.py
@tests/test_patcher.py
@tests/test_analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract GraphMixin and AnalysisMixin into separate modules</name>
  <files>src/maxpat/graph.py, src/maxpat/analysis.py, src/maxpat/patcher.py</files>
  <action>
1. Create `src/maxpat/graph.py` containing `class GraphMixin`:
   - Move these methods from Patcher (lines ~2066-2386): `_build_adj`, `downstream`, `upstream`, `_traverse`, `_enqueue_neighbors`, `_cross_subpatcher`, `signal_path`, `connected_components`
   - Imports needed: `from collections import deque`, `from __future__ import annotations`, `from typing import TYPE_CHECKING`
   - Use TYPE_CHECKING for Box/Patchline references to avoid circular imports: `if TYPE_CHECKING: from src.maxpat.patcher import Box, Patchline`
   - The mixin methods reference `self.boxes`, `self.lines`, `self.db` -- these are provided by Patcher at runtime, no changes needed to method bodies
   - Do NOT import Patcher -- the mixin is mixed INTO Patcher

2. Create `src/maxpat/analysis.py` containing `class AnalysisMixin` and the two module-level constants:
   - Move `SECTION_SIGNATURES` dict (lines 37-88) and `_SECTION_NAME_PRIORITY` dict (lines 93-118) to analysis.py as module-level constants
   - Move these methods from Patcher (lines ~2390-2889): `_classify_domain`, `_resolve_send_receive_pairs`, `_merge_components_by_send_receive`, `_name_section`, `_analyze_sections`, `_count_recursive`, `_analyze_complexity`, `_analyze_inventory`, `_analyze_signal_chains`, `_analyze_control_paths`, `_hierarchy_lines`, `_analyze_hierarchy`, `_analyze_parameters`, `analyze`
   - Imports needed: `from collections import Counter`, `from __future__ import annotations`, `from typing import TYPE_CHECKING`
   - Import `UI_MAXCLASSES` from maxclass_map (used by `_classify_domain`)
   - Use TYPE_CHECKING for Box reference: `if TYPE_CHECKING: from src.maxpat.patcher import Box`
   - The `_analyze_sections` method calls `self.connected_components()` (from GraphMixin) -- this works via MRO since Patcher inherits both
   - `_analyze_signal_chains` calls `self.downstream()` -- same MRO resolution

3. Update `src/maxpat/patcher.py`:
   - Add imports: `from src.maxpat.graph import GraphMixin` and `from src.maxpat.analysis import AnalysisMixin, SECTION_SIGNATURES, _SECTION_NAME_PRIORITY`
   - Change class declaration to: `class Patcher(GraphMixin, AnalysisMixin):`
   - Remove the extracted methods and constants from patcher.py (the methods between lines ~2066-2889 and the constants at lines 37-118)
   - Keep the `SECTION_SIGNATURES` and `_SECTION_NAME_PRIORITY` re-exported from patcher.py for backward compatibility (test_analysis.py imports `SECTION_SIGNATURES` from `src.maxpat.patcher`)
   - Remove now-unused imports from patcher.py: `deque` can be removed if only used by graph methods, `Counter` can be removed if only used by analysis methods
   - Keep `from collections import Counter, deque` removal conditional -- verify no other patcher.py code uses them first

IMPORTANT: The re-export of SECTION_SIGNATURES from patcher.py is critical -- test_analysis.py line 13 does `from src.maxpat.patcher import Patcher, Box, SECTION_SIGNATURES`. The import in patcher.py `from src.maxpat.analysis import AnalysisMixin, SECTION_SIGNATURES, _SECTION_NAME_PRIORITY` handles this.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_patcher.py tests/test_analysis.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>graph.py and analysis.py exist as mixin modules. Patcher inherits from both. All graph/analysis tests pass unchanged. patcher.py reduced by ~830 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add maxclass validation check</name>
  <files>src/maxpat/validation.py, tests/test_validation.py</files>
  <action>
1. Add `_validate_maxclass_usage` function to validation.py after the `_validate_objects_exist` function (~line 250):
   - Iterates over boxes in `patch_dict["patcher"]["boxes"]`
   - For each box, get `maxclass` and the object name (via `_extract_object_name`)
   - Skip structural maxclasses (inlet, outlet, patcher, bpatcher) -- already in `_STRUCTURAL_MAXCLASSES`
   - If `maxclass != "newobj"` and `maxclass` not in `UI_MAXCLASSES`: emit a warning
     - Message: f"Object '{name}' uses maxclass '{maxclass}' instead of 'newobj' -- non-UI objects should use maxclass='newobj'"
     - Level: "warning" (not error -- third-party patches may have custom maxclasses)
     - Layer: "objects"
   - Import `UI_MAXCLASSES` from maxclass_map (already imported at top of validation.py via `is_ui_object`)
   - Actually check: `UI_MAXCLASSES` is already imported. Just need to add the check.

2. Wire `_validate_maxclass_usage` into `validate_patch` function:
   - Add call after Layer 2 (object existence): `results.extend(_validate_maxclass_usage(patch_dict))`
   - This is a Layer 2 extension (object-level check), not a new layer

3. Add tests to `tests/test_validation.py`:
   - Test: non-UI object using its name as maxclass triggers warning (e.g., box with maxclass="cycle~" instead of "newobj")
   - Test: UI object using its name as maxclass does NOT trigger warning (e.g., maxclass="toggle")
   - Test: structural maxclass (inlet, outlet) does NOT trigger warning
   - Test: standard newobj box does NOT trigger warning
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_validation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Non-UI objects using their own name as maxclass (instead of "newobj") are flagged as warnings by the validation pipeline. Tests cover positive and negative cases.</done>
</task>

</tasks>

<verification>
- `python3 -m pytest tests/ -x -q` -- full test suite passes
- `python3 -c "from src.maxpat.patcher import Patcher, SECTION_SIGNATURES; p = Patcher(); print(type(p.downstream).__qualname__)"` -- prints `GraphMixin.downstream` (method from mixin)
- `python3 -c "from src.maxpat.patcher import Patcher; p = Patcher(); print(type(p.analyze).__qualname__)"` -- prints `AnalysisMixin.analyze`
- `wc -l src/maxpat/patcher.py` -- confirms ~2060 lines (down from 2889)
</verification>

<success_criteria>
- patcher.py reduced by ~830 lines
- graph.py contains GraphMixin with 8 methods
- analysis.py contains AnalysisMixin with 14 methods plus 2 constants
- All existing tests pass without modification
- New maxclass validation check catches non-UI objects using wrong maxclass
- No external API changes (all methods still on Patcher instances)
</success_criteria>

<output>
After completion, create `.planning/quick/260331-vs8-decompose-patcher-py-extract-graph-trave/260331-vs8-SUMMARY.md`
</output>
