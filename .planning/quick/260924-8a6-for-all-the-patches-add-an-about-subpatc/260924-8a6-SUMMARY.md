---
quick_id: 260924-8a6
status: complete
date: 2026-09-24
---

# Quick 260924-8a6: "p about" subpatch for every patch — SUMMARY

Added a looper-style `p about` subpatcher (title 16pt bold / section headers 13pt bold / 12pt body lines, x=20
w=700, dark 0.333 inner canvas, 0 in / 0 out, patching-mode only) to 33 user-facing main patches. Looper already had one.

- Content drafted per patch from context.md + the live patch into `about/<stem>.json` (4 parallel drafting agents,
  read-only), then applied sequentially via the Patcher API + `save_patch_roundtrip` (auto-commit per file).
- Verification per patch: exactly one `p about`; every pre-existing box dict and all lines identical before/after;
  inner comment count = title + headers + lines.
- 12 patches whose top row extended far right (kicksynth, psycography main, minitaur, performance-patch-template,
  rhythmic-sampler, simple-fm, subtractive-synth, terrain-synth, sample-layers, ji-harmonizer, timestretch, stutter)
  had the box moved to a collision-free spot inside the first ~1200px (second commit per file).
- 45 patch commits total, ending at 2be69eb. Excluded: abstractions/bpatcher children, voices, *-test, demos.
- Deviation: planner agent stalled (no output after 600s); orchestrator wrote PLAN.md directly and ran Task 2 inline.

## Drafting notes worth a follow-up (found while reading patches, not changed)
- mixer: saved file holds duplicate strip/bus bpatchers (js cleans on load); cords into strips are lost on reload.
- barnett-dbap host title comment still says v0.5 (features are v0.7.x).
- tape-wobble: adc~ unconnected; controls not in presentation.
- timestretch: Play toggle likely has no audible effect in buffer mode.
- wormhole: dials not initialised on load; fixed send~ names allow only one instance per session.
- minitaur / ji-harmonizer: no DSP toggle in presentation; minitaur status.md still says "ideation".
