---
phase: quick-260401-l3t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/aliases.json
  - .claude/max-objects/msp/objects.json
  - .claude/max-objects/overrides.json
autonomous: true
requirements: [P0-aliases, P0-msp-cleanup, P0-override-cleanup, P0-case-normalize]

must_haves:
  truths:
    - "ObjectDatabase resolves `v` to `value` and `del` to `delay`"
    - "MSP domain contains only real objects (no documentation entries)"
    - "overrides.json has no non-object entries and no case mismatches"
  artifacts:
    - path: ".claude/max-objects/aliases.json"
      provides: "Alias mappings including v and del"
      contains: "\"v\": \"value\""
    - path: ".claude/max-objects/msp/objects.json"
      provides: "MSP objects without phantom entries"
    - path: ".claude/max-objects/overrides.json"
      provides: "Override corrections with clean keys"
  key_links:
    - from: ".claude/max-objects/aliases.json"
      to: "src/maxpat/db_lookup.py"
      via: "ObjectDatabase loads and resolves aliases"
      pattern: "aliases"
    - from: ".claude/max-objects/overrides.json"
      to: "src/maxpat/db_lookup.py"
      via: "ObjectDatabase deep-merges overrides by key name"
      pattern: "overrides"
---

<objective>
Clean up P0 data correctness issues identified by the 260401-jyk database audit.

Purpose: The object database is the single source of truth for MAX object definitions. Non-real entries, missing aliases, and case mismatches degrade lookup reliability and can cause silent failures in patch generation.

Output: Three cleaned JSON files -- aliases.json, msp/objects.json, overrides.json -- all passing existing test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/max-objects/aliases.json
@.claude/max-objects/overrides.json
@.claude/max-objects/msp/objects.json
@.claude/max-objects/audit/260401-jyk-database-audit-report.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing aliases and remove non-real MSP entries</name>
  <files>.claude/max-objects/aliases.json, .claude/max-objects/msp/objects.json</files>
  <action>
1. In `.claude/max-objects/aliases.json`, add two entries to the `aliases` object:
   - `"v": "value"`
   - `"del": "delay"`

2. In `.claude/max-objects/msp/objects.json`, delete the top-level keys `"MC Wrapper Features"` and `"Snapshot Messages"` -- these are documentation entries extracted from help text, not real MSP objects.

Both edits are pure JSON key additions/removals. Preserve existing formatting (2-space indent).
  </action>
  <verify>
    <automated>python3 -c "
import json
a = json.load(open('.claude/max-objects/aliases.json'))
assert a['aliases']['v'] == 'value', 'missing v alias'
assert a['aliases']['del'] == 'delay', 'missing del alias'
m = json.load(open('.claude/max-objects/msp/objects.json'))
assert 'MC Wrapper Features' not in m, 'MC Wrapper still present'
assert 'Snapshot Messages' not in m, 'Snapshot Messages still present'
print('PASS: aliases and MSP cleanup verified')
"</automated>
  </verify>
  <done>aliases.json has 12 entries (10 existing + v + del). msp/objects.json has no documentation-only entries.</done>
</task>

<task type="auto">
  <name>Task 2: Clean overrides -- remove non-objects and normalize case</name>
  <files>.claude/max-objects/overrides.json</files>
  <action>
In `.claude/max-objects/overrides.json`, inside the `objects` dict:

1. **Remove non-real-object entries** -- delete these 5 keys that were extracted from patch metadata, not actual MAX objects:
   - `!`
   - `1`
   - `2`
   - `>p`
   - `?`

2. **Normalize case mismatches** -- the DB uses lowercase but overrides use titlecase:
   - Read the value of `Bucket`, write it under key `bucket`, delete `Bucket`
   - Read the value of `Uzi`, write it under key `uzi`, delete `Uzi`

Use Python to load, modify, and re-write the JSON to preserve structure. Use `json.dump` with `indent=2` and `ensure_ascii=False` to match existing formatting.
  </action>
  <verify>
    <automated>python3 -c "
import json
d = json.load(open('.claude/max-objects/overrides.json'))
objs = d['objects']
for bad in ['!','1','2','>p','?','Bucket','Uzi']:
    assert bad not in objs, f'{bad} still in overrides'
assert 'bucket' in objs, 'bucket override missing'
assert 'uzi' in objs, 'uzi override missing'
print(f'PASS: overrides cleaned ({len(objs)} entries)')
" && python3 -m pytest tests/test_object_schema.py -x -q</automated>
  </verify>
  <done>overrides.json has no non-object keys (!,1,2,>p,?) and bucket/uzi are lowercase. All ObjectDatabase tests pass.</done>
</task>

</tasks>

<verification>
Run the full object schema test suite to confirm no regressions:
```
python3 -m pytest tests/test_object_schema.py -v
```

Spot-check alias resolution:
```python
from src.maxpat.db_lookup import ObjectDatabase
db = ObjectDatabase()
assert db.lookup("v") is not None    # resolves to value
assert db.lookup("del") is not None  # resolves to delay
assert db.lookup("bucket") is not None  # override now matches
assert db.lookup("uzi") is not None     # override now matches
```
</verification>

<success_criteria>
- `v` and `del` aliases resolve correctly via ObjectDatabase
- No `MC Wrapper Features` or `Snapshot Messages` in MSP domain
- No `!`, `1`, `2`, `>p`, `?` keys in overrides
- `bucket` and `uzi` overrides use lowercase keys matching the DB
- `python3 -m pytest tests/test_object_schema.py` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/quick/260401-l3t-clean-object-db-add-missing-aliases-remo/260401-l3t-01-SUMMARY.md`
</output>
