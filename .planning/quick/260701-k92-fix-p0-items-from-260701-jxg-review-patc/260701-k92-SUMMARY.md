---
phase: quick-260701-k92
plan: 01
subsystem: testing, repo-hygiene, object-database
tags: [pytest, gitignore, extraction-log, p0-fixes]
requires: []
provides:
  - integration test suite executes real validation (36 TypeError failures eliminated)
  - clean git index (124 junk files untracked, 5 ignore patterns)
  - truthful extraction-log.json (3430 objects via conftest traversal)
affects: [tests, .claude/max-objects]
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - tests/test_integration_patches.py
    - .gitignore
    - .claude/max-objects/extraction-log.json
decisions:
  - "extraction-log.json now describes the committed DB (max_installation_path: .claude/max-objects), not a package-source audit run"
  - "Remaining 4 validate + 14 review integration failures are genuine patch blockers — P1 scope, deliberately not fixed here"
metrics:
  duration: ~6 min
  completed: 2026-07-01
status: complete
---

# Quick Task 260701-k92: Fix P0 Items from 260701-jxg Review Summary

One-line dropped `patch_dir` kwarg fix restores real integration validation, .gitignore + `git rm --cached` cleans 124 tracked junk files, and extraction-log.json regenerated from the committed DB (3430 objects) so test_source_coverage passes 10/10.

## Tasks Completed

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Drop removed `patch_dir` kwarg from validate_patch() call | 795395b | tests/test_integration_patches.py |
| 2 | Add 5 ignore patterns, untrack 124 junk files | a1152f9 | .gitignore + 124 index removals |
| 3 | Regenerate extraction-log.json from real database | d85289d | .claude/max-objects/extraction-log.json |

## What Changed

**Task 1 (P0-1):** `validate_patch(patch_dict, db, patch_dir=patch_path.parent)` → `validate_patch(patch_dict, db)`. The signature at src/maxpat/validation.py:114 only accepts `(patch, db, allowed_packages)`; all 36 parametrized cases were dying with TypeError before any validation ran. Now all 36 execute the pipeline: 32 pass, 4 fail on genuine blocking errors (scala-synth-voice, tape-wobble, timestretch, wormhole) — flagged P1 in the review report, out of scope here.

**Task 2 (P0-2):** Appended `__pycache__/`, `*.pyc`, `.DS_Store`, `.pytest_cache/`, `.claude/worktrees/` to .gitignore. Untracked exactly the 124 pattern-matched files via `git ls-files -z | grep -zE ... | xargs -0 git rm --cached --` (no `git add .`/`-A`, working-tree copies intact). `git ls-files` junk count is now 0; `git check-ignore` confirms patterns active (worktrees pattern verified against a file path since the dir doesn't exist inside this worktree).

**Task 3 (P0-3):** Old log claimed `total_objects: 4` with all core domains at 0 (overwritten by a single-package nn_tilde audit). Regenerated with the same traversal tests/conftest.py uses: 7 core domain objects.json + 29 per-package objects.json = 36 files, 3430 objects (max 471, msp 246, jitter 218, mc 222, gen 189, m4l 35, rnbo 560, packages 1489) — matching the plan's expected values exactly. Computed fields: variable_io_count 30, empty_inlets_count 270, empty_outlets_count 293, error_count 0. Throwaway snippet run inline; no generator script committed (Rule #5).

## Verification Results

- `pytest tests/test_integration_patches.py::test_validate_patch_no_errors`: **0 TypeErrors** (was 36), 32 passed / 4 genuine-blocker failures
- `pytest tests/test_source_coverage.py`: **10/10 passed**
- `git ls-files | grep -cE '__pycache__|\.pyc$|\.DS_Store$|\.pytest_cache/'`: **0**
- patches/.active-project.json untouched by all commits

## Full Suite Results

`python3 -m pytest` (worktree, Python 3.14 / pytest 9.0.2):

- **1994 passed, 24 failed, 4 xfailed** in 34.65s
- **Zero TypeError occurrences** — the 36 patch_dir TypeError failures are gone
- Remaining 24 failures, all pre-existing and out of P0 scope:
  - 4x test_validate_patch_no_errors — genuine blocking validation errors (P1 per jxg report)
  - 14x test_review_patch_no_blockers — genuine critic blockers (P1 per jxg report)
  - 6x community-package DB assertions (test_package_schema x3, test_validation x2, test_critics x1) — FluCoMa verified flags / community-block warnings; unrelated to the three files changed (the extraction_log fixture is only consumed by test_source_coverage, which is green)

## Deviations from Plan

None - plan executed exactly as written.

## Deferred Issues

- 4 validate + 14 review integration failures: genuine patch/critic blockers, flagged P1 in 260701-jxg-REPORT.md
- 6 community-package test failures (FluCoMa stub verified flags, community extraction warnings): pre-existing, not covered by the jxg P0 items
- `stash@{0}` exists on the shared stash list (`WIP on main: 615a6b8`) — untouched per Rule #7; flagging for user awareness

## Self-Check: PASSED

- tests/test_integration_patches.py modified: FOUND
- .gitignore patterns present: FOUND
- .claude/max-objects/extraction-log.json total_objects 3430: FOUND
- Commits 795395b, a1152f9, d85289d: FOUND
