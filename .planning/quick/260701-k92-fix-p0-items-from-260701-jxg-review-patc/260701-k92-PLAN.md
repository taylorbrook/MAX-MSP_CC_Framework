---
phase: quick-260701-k92
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_integration_patches.py
  - .gitignore
  - .claude/max-objects/extraction-log.json
autonomous: true
requirements: [P0-1, P0-2, P0-3]

must_haves:
  truths:
    - "pytest tests/test_integration_patches.py collects and runs with zero TypeError failures (the 36 kwarg failures are gone)"
    - "git ls-files returns zero entries matching __pycache__, *.pyc, .DS_Store, or .pytest_cache"
    - ".claude/worktrees/ is ignored by git (no longer shows as untracked in git status)"
    - "pytest tests/test_source_coverage.py passes all 10 tests"
  artifacts:
    - ".gitignore with 5 new ignore patterns"
    - ".claude/max-objects/extraction-log.json with total_objects matching the conftest.py traversal (~3430)"
  key_links:
    - "tests/conftest.py extraction_log fixture -> .claude/max-objects/extraction-log.json (test_extraction_log_total asserts total_objects > 1500)"
    - "tests/conftest.py all_objects fixture traverses packages/ per-package subdirs, NOT a single packages/objects.json — the regenerated log must use the same traversal"
---

<objective>
Fix the three P0 items from the 260701-jxg review report: restore integration validation coverage (36 tests failing on a removed kwarg), stop tracking build/OS junk files, and regenerate the corrupted extraction-log.json so test_source_coverage passes.

Purpose: Collapse 36 red tests to real signal, clean repo history hygiene, and make the DB scale fixture truthful.
Output: One-line test fix, updated .gitignore + untracked junk (124 files), regenerated extraction-log.json.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260701-jxg-review-repo-and-produce-improvement-repo/260701-jxg-REPORT.md
@tests/test_integration_patches.py
@tests/test_source_coverage.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop removed kwarg from validate_patch() call in integration test</name>
  <files>tests/test_integration_patches.py</files>
  <action>
In test_validate_patch_no_errors (line 66), change the call
`validate_patch(patch_dict, db, patch_dir=patch_path.parent)` to
`validate_patch(patch_dict, db)`. The current signature (src/maxpat/validation.py:114) accepts only (patch, db, allowed_packages) — the third kwarg was removed from the signature, which is why all 36 parametrized cases fail with TypeError before any validation runs. Do not touch anything else in the file (the xfail machinery and review test are fine).

Scope note: after the fix, real validation results flow again. Any genuine blocking-error failures that surface (the report flags ~14 patch-blocker failures as P1) are OUT OF SCOPE — do not fix patches or critics here. The done-condition is only that TypeError failures are eliminated.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_integration_patches.py::test_validate_patch_no_errors --tb=line -q 2>&1 | tail -20 — output must contain zero occurrences of "TypeError"</automated>
  </verify>
  <done>All 36 test_validate_patch_no_errors cases execute the validation pipeline; none fail with TypeError. Pass/fail status now reflects actual patch validation results.</done>
</task>

<task type="auto">
  <name>Task 2: Add ignore patterns and untrack the 124 junk files</name>
  <files>.gitignore</files>
  <!-- planner-discipline-allow: __pycache__|\.pyc$|\.DS_Store$|\.pytest_cache/ -->
  <action>
1. Append these five patterns to .gitignore (keep the existing _pkg-source entry):
   `__pycache__/`, `*.pyc`, `.DS_Store`, `.pytest_cache/`, `.claude/worktrees/`

2. Untrack the ~124 already-tracked junk files. Per CLAUDE.md Rule #7: NEVER `git add .`, `git add -A`, or `git stash`. Build the explicit file list from git's own index and remove from cache only:
   `git ls-files -z | grep -zE '__pycache__|\.pyc$|\.DS_Store$|\.pytest_cache/' | xargs -0 git rm --cached --`
   This stages deletions for exactly the pattern-matched tracked files (working-tree copies remain on disk). `.claude/worktrees/` is currently untracked (shows as `??` in git status), so it needs only the gitignore entry — no git rm.

3. Commit .gitignore plus the staged removals in one commit. Stage .gitignore explicitly (`git add .gitignore`); the removals are already staged by git rm --cached. Do NOT commit patches/.active-project.json (pre-existing unrelated modification — leave it alone).
  </action>
  <verify>
    <automated>test "$(git ls-files | grep -cE '__pycache__|\.pyc$|\.DS_Store$|\.pytest_cache/')" -eq 0 && git check-ignore -q .claude/worktrees && git check-ignore -q .pytest_cache</automated>
  </verify>
  <done>git ls-files matches zero junk-pattern files; git check-ignore confirms all five patterns active; git status no longer lists .claude/worktrees/ as untracked; working-tree copies of the removed files still exist on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate extraction-log.json from the real database</name>
  <files>.claude/max-objects/extraction-log.json</files>
  <action>
The current log (total_objects: 4, all core domain counts 0, timestamp from a single-package audit run) misrepresents the DB and would fail test_extraction_log_total if the fixture asserted against it honestly — the test requires total_objects > 1500.

Regenerate by computing counts with the SAME traversal tests/conftest.py uses (this is the authoritative counting method):
- Core domains: len() of the objects dict in each of .claude/max-objects/{max,msp,jitter,mc,gen,m4l,rnbo}/objects.json
- packages: sum of len() over every .claude/max-objects/packages/*/objects.json (per-package subdirectories — there is NO single packages/objects.json)

Expected values as of planning time (recompute, don't hardcode blindly — verify your computed numbers are close): max 471, msp 246, jitter 218, mc 222, gen 189, m4l 35, rnbo 560, packages 1489, TOTAL 3430.

Write the regenerated JSON preserving the existing schema keys: total_files_found (= number of objects.json files read: 7 core + count of package subdirs containing objects.json), total_objects (grand total), domain_counts (8 keys, packages = merged per-package sum), error_count: 0, errors: [], inlet_type_fallback_count / variable_io_count / empty_inlets_count / empty_outlets_count (compute from the loaded objects if cheap: variable_io_count = objects with truthy variable_io; empty_inlets_count / empty_outlets_count = objects with empty inlets/outlets arrays; inlet_type_fallback_count may stay 0 — it is not asserted by any test), extraction_timestamp (current UTC ISO-8601), max_installation_path: ".claude/max-objects" (the log now describes the committed DB, not a package-source audit).

Use a throwaway Python snippet in the scratchpad to compute and write the JSON via the Write tool or python json.dump — do NOT create a persistent generator script in the repo (CLAUDE.md Rule #5).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_source_coverage.py -q</automated>
  </verify>
  <done>All 10 test_source_coverage.py tests pass; extraction-log.json total_objects > 1500 and matches the conftest traversal count; error rate fields keep test_error_rate_under_5_percent green.</done>
</task>

</tasks>

<verification>
Final confirmation run:
1. `python3 -m pytest tests/test_integration_patches.py tests/test_source_coverage.py --tb=line -q` — zero TypeError occurrences; test_source_coverage fully green. Any remaining test_validate_patch/test_review_patch failures must be genuine validation findings (P1 scope), not TypeErrors.
2. `git ls-files | grep -cE '__pycache__|\.pyc$|\.DS_Store$|\.pytest_cache/'` returns 0.
3. `git status --short` shows no `.claude/worktrees/` untracked entry and patches/.active-project.json untouched by our commits.
</verification>

<success_criteria>
- 36 patch_dir TypeError failures eliminated from tests/test_integration_patches.py
- .gitignore contains the 5 new patterns; 124 junk files removed from the git index (working tree intact)
- extraction-log.json reflects the real DB (~3430 objects via conftest traversal) and tests/test_source_coverage.py passes 10/10
- Each task committed atomically with explicit file paths (no git add ./-A, no git stash)
</success_criteria>

<output>
Create `.planning/quick/260701-k92-fix-p0-items-from-260701-jxg-review-patc/260701-k92-SUMMARY.md` when done.
</output>
