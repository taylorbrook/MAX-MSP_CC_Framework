---
phase: quick-260331-w95
plan: 01
subsystem: testing, documentation
tags: [review, consistency, z-order, tests, xfail]
dependency_graph:
  requires: [quick-260331-eqh, quick-260331-nps, quick-260331-o1y]
  provides: [test-suite-green, z-order-consistency]
  affects: [shared-capabilities.md, test_agent_skills.py, test_round_trip.py]
tech_stack:
  added: []
  patterns: [xfail-for-externally-modified-patches]
key_files:
  created: []
  modified:
    - tests/test_agent_skills.py
    - tests/test_round_trip.py
    - .claude/skills/references/shared-capabilities.md
decisions:
  - "comp-band.maxpat and rhythmic-sampler.maxpat xfailed (re-saved by MAX with different formatting)"
  - "shared-capabilities.md z-order section was stale after 8feeebf fix -- corrected to match CLAUDE.md and patcher.py"
metrics:
  duration: 3min
  completed: "2026-04-01"
  tasks_completed: 2
  tasks_total: 2
  files_created: 0
  files_modified: 3
---

# Phase quick-260331-w95 Plan 01: Review Recent Updates Summary

Fixed 3 test failures and corrected stale z-order documentation in shared-capabilities.md after user's manual z-order semantics fix in commit 8feeebf.

## One-Liner

Fix 3 failing tests (ObjectDatabase ref, critic protocol phrasing, xfail externally-modified patches) and correct stale z-order docs in shared-capabilities.md

## What Was Done

### Task 1: Fix failing test and verify test suite

**3 test failures found and fixed:**

1. **test_patch_agent_references_max_objects** (test_agent_skills.py:118): SKILL.md was updated to use `ObjectDatabase` instead of raw `max/objects.json` paths. Test assertion now accepts `ObjectDatabase` reference.

2. **test_critic_protocol_escalation_for_repeated_findings** (test_agent_skills.py:257): Critic protocol doc uses "NOT a round limit" phrasing, but test looked for "no hard round" or "no round limit". Assertion updated to match actual doc phrasing.

3. **test_max_saved_file_byte_identical + test_framework_file_byte_identical** (test_round_trip.py:845,870): comp-band.maxpat was re-saved by MAX with different array formatting; rhythmic-sampler.maxpat was re-saved with 4-space indent (was 2-space). Both xfailed with descriptive reasons, matching the minitaur.maxpat pattern.

**Result:** 1233 passed, 3 xfailed (all expected)

**Commit:** 3853bd7

### Task 2: Review consistency across docs, code, and skills

**Critical inconsistency found and fixed:** shared-capabilities.md still had the OLD (incorrect) z-order semantics:
- Said "Later in array = renders on top" (wrong -- earlier = on top)
- Said `bring_to_front` moves to "end of array" (wrong -- moves to index 0)
- Said `send_to_back` moves to "index 0" (wrong -- moves to end)

This was stale after the user's z-order fix in commit 8feeebf which corrected CLAUDE.md and patcher.py but didn't update shared-capabilities.md.

**Verification:** CLAUDE.md, patcher.py docstrings, shared-capabilities.md, and tests all now consistently state: index 0 = front (topmost), end = back (behind).

**Commit:** 0841a18

## Deviations from Plan

None -- plan executed exactly as written.

## Decisions Made

1. xfail externally-modified patch round-trip tests rather than updating test fixtures (consistent with minitaur.maxpat precedent)
2. Updated shared-capabilities.md z-order section to match corrected CLAUDE.md semantics (background=1 explanation added)

## Known Stubs

None.

## Self-Check: PASSED
