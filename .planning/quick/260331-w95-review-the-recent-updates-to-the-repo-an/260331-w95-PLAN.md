---
phase: quick-260331-w95
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - TECHNICAL.md
autonomous: true
requirements: [DOC-UPDATE]

must_haves:
  truths:
    - "README reflects current test count (1,276), LOC (~13,200), test files (35), and module count"
    - "README milestones table includes v2.3 with the recent work (z-order API, auto-commit, MSP outlet verification, patcher decomposition, integration tests, trigger enforcement, etc.)"
    - "TECHNICAL.md reflects new modules (graph.py, utils.py, analysis.py as standalone), updated test/LOC counts, updated validation checks, z-order methods, auto-commit hooks"
    - "No stale numbers remain (old: 1,141 tests / 32 files / ~11,900 LOC)"
  artifacts:
    - path: "README.md"
      provides: "Updated public-facing documentation"
    - path: "TECHNICAL.md"
      provides: "Updated technical internals documentation"
  key_links:
    - from: "README.md"
      to: "TECHNICAL.md"
      via: "cross-reference link"
      pattern: "TECHNICAL\\.md"
---

<objective>
Update README.md and TECHNICAL.md to reflect all changes since v2.2.0 — new features, corrected stats, new modules, and a v2.3 milestone entry. Once docs are accurate, the user will commit, push, and tag to trigger a new npx release.

Purpose: The docs are the public face of the framework. Stale numbers and missing features mislead users.
Output: Updated README.md and TECHNICAL.md ready for v2.3 tag.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
@TECHNICAL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with current stats and v2.3 milestone</name>
  <files>README.md</files>
  <action>
Read README.md and make these specific updates:

**Stats updates throughout the file:**
- "~11,900 LOC" -> "~13,200 LOC" (appears in Features bullet and How It Works section)
- "1,141 tests across 32 files" -> "1,276 tests across 35 files" (appears in Project Structure comment area)
- "10 specialist agents" -> "9 specialist agents" (max-memory-agent was retired in 260322-eva; verify the existing count is correct)
- Verify the patches table is current — all 11 projects (FDNVerb, gen-eq, granularsynthtest, kicksynth, minitaur, mixer, performancepatchtest, rhythmic-sampler, scala-synth, stutter, wormhole) are listed

**Milestones table — add v2.3:**
Add a new row after v2.2:
```
| **v2.3 Reliability & Validation** | 2026-03-31 | — | — | Z-order API, auto-commit safety hooks, MSP outlet type verification (202 objects), patcher decomposition (GraphMixin/AnalysisMixin), integration tests on real patches, trigger enforcement hardening, round-trip text bug fix, external .gendsp validation, maxclass validation |
```

**How It Works section — mention auto-commit:**
In the "Direct .maxpat Editing (v2.0)" paragraph, after "survives the load-edit-save cycle", add: "Every patch save auto-commits to git for safety."

**Project Structure tree — verify accuracy:**
- src/maxpat LOC comment should say ~13,200
- tests line should say 1,276 tests across 35 files
  </action>
  <verify>
    <automated>grep -c "13,200\|13.200" README.md && grep -c "1,276" README.md && grep -c "v2.3" README.md</automated>
  </verify>
  <done>README.md has correct LOC (~13,200), test count (1,276/35), v2.3 milestone row, auto-commit mention, and no stale numbers</done>
</task>

<task type="auto">
  <name>Task 2: Update TECHNICAL.md with new modules, methods, validation checks, and stats</name>
  <files>TECHNICAL.md</files>
  <action>
Read TECHNICAL.md and make these specific updates:

**Stats updates:**
- "~11,900 LOC" -> "~13,200 LOC" (Python Engine section header)
- "1,141 tests across 32 test files" -> "1,276 tests across 35 test files" (Test Suite section)

**Core Classes (patcher.py) — add z-order methods to Patcher table:**
Add these rows to the Patcher method table:
- `bring_to_front(box)` | Move box to index 0 (renders on top)
- `send_to_back(box)` | Move box to end of array (renders behind)
- `set_z_index(box, index)` | Set explicit z-order position

**Python Engine section — note patcher decomposition:**
After the Core Classes subsection, add a brief note:
"Patcher internals are decomposed into mixins for maintainability: `GraphMixin` (`graph.py`) handles adjacency/traversal and `AnalysisMixin` (`analysis.py` standalone module) handles patch analysis. The public API is unchanged."

**New module: utils.py**
In the Python Engine section, note `utils.py` provides shared helpers like `get_box_name()`.

**Write Hooks table — update for auto-commit:**
Update the `save_patch_roundtrip` row description: "Lossless round-trip; auto-commits to git via `auto_commit_patch()`"
Update the `write_gendsp` row: "Auto-commits to git"
Update the `write_js` row: "Code validation (report-only); auto-commits to git"

**Validation Pipeline — add new checks to Layer 4 table:**
Add rows:
- `Maxclass mismatch` | `newobj` maxclass on non-UI objects that should use specific maxclass | warning
- `External .gendsp I/O mismatch` | Gen~ box I/O count doesn't match referenced .gendsp file | warning
- `MC oscillator gain staging` | MC oscillators reaching mc.dac~ without gain control | warning

**DSP Critic table — add gain staging specifics:**
Add row: `MIDI-range gain values` | Raw 0-127 values feeding *~ or gain~ without normalization | blocker

**Memory System section:**
Add a note that max-memory-agent was retired (v2.2) — the MemoryStore API and patterns.md files remain but auto-inject/write-back is handled directly by generating agents rather than a dedicated memory agent.

**Test Suite section:**
Update to: "The project includes 1,276 tests across 35 test files covering all modules."
  </action>
  <verify>
    <automated>grep -c "13,200\|13.200" TECHNICAL.md && grep -c "1,276" TECHNICAL.md && grep -c "bring_to_front\|z-order\|z.order" TECHNICAL.md && grep -c "auto_commit\|auto-commit" TECHNICAL.md</automated>
  </verify>
  <done>TECHNICAL.md has correct LOC, test count, z-order methods documented, auto-commit hooks documented, new validation checks listed, patcher decomposition noted, memory agent retirement noted</done>
</task>

</tasks>

<verification>
- `grep "11,900\|1,141\|32 test" README.md TECHNICAL.md` returns NO matches (stale numbers gone)
- `grep "13,200" README.md TECHNICAL.md` returns matches in both files
- `grep "1,276" README.md TECHNICAL.md` returns matches in both files
- `grep "v2.3" README.md` returns the milestone row
- `python3 -m pytest tests/ --co -q 2>/dev/null | tail -1` still shows 1276 (sanity check that tests weren't broken)
</verification>

<success_criteria>
- README.md and TECHNICAL.md have no stale stats (LOC, test count, test file count)
- v2.3 milestone with feature summary is in README
- All new v2.3 features are documented in TECHNICAL.md (z-order, auto-commit, patcher decomposition, new validation checks)
- Both files are ready for commit + tag
</success_criteria>

<output>
After completion, create `.planning/quick/260331-w95-review-the-recent-updates-to-the-repo-an/260331-w95-SUMMARY.md`
</output>
