---
quick_id: 260427-j4e
slug: audit-claude-md-files-against-feedback-m
description: Audit CLAUDE.md (project + global) against the 27 feedback memories and fix wrong/missing instructions at the source.
date: 2026-04-27
mode: quick
references:
  - .planning/quick/260427-hox-review-this-system-and-all-of-the-issues/260427-hox-FINDINGS.md (P0-5)
  - /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_*.md (27 files)
---

# Quick Task 260427-j4e: CLAUDE.md ↔ Feedback Memory Drift Audit

## Goal

Walk every `feedback_*.md` memory under `~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/` and check whether the project `CLAUDE.md` and global `~/.claude/CLAUDE.md` either contradict the memory or are silent on a rule the memory exists to enforce. Promote the rule to CLAUDE.md so the memory is no longer the only source of truth.

## Scope

- 27 feedback memories (the prompt said 30; current count is 27)
- File: `/Users/taylorbrook/Dev/MAX/CLAUDE.md` (project)
- File: `/Users/taylorbrook/.claude/CLAUDE.md` (global — only contains developer profile, no MAX content; no edits expected)
- No code changes. Documentation only.

## Tasks

1. Cross-reference each feedback memory against current `CLAUDE.md` content. Classify each as: matches / contradicts / gap / out-of-scope.
2. For named contradictions/gaps from the user prompt, apply edits at source:
   - GenExpr `delay()` vs `Delay.read/write`
   - gen~ Param `@` syntax
   - Comment box `#N` substitution
   - GenExpr `in1`/`out1` (verify already correct)
   - Layout spacing (verify already correct)
3. Promote additional high-recurrence gaps (M4L `param_connect`, `replace_box` orphans, `inlet~`/`outlet~` non-existence, `umenu` items format, `expr` no `clip()`, `floor~` RNBO-only, `buffer~` no `info`, `line~` comma, multislider `fetch`, maxclass=`newobj` rule, empty-I/O DB caveat).
4. Skip out-of-scope memories (DB-data fixes, niche package memories, highly DSP-specific notes).
5. Write SUMMARY.md listing every memory, its disposition, and the edit applied.

## Verification

- `grep` confirms each new rule landed in CLAUDE.md.
- File line count growth is reasonable (≈30 new lines).
- No regressions: existing rules unchanged.

## Done When

- All edits committed.
- SUMMARY.md lists each of the 27 memories with disposition.
