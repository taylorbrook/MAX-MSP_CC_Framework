---
phase: 260702-k9w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/code_validation.py
  - tests/test_code_validation.py
  - tests/test_package_schema.py
  - tests/test_validation.py
  - tests/test_critics.py
  - tests/review_blocker_allowlist.json
  - tests/test_integration_patches.py
autonomous: true
requirements:
  - FIX-GENEXPR   # 4x test_validate_patch_no_errors (validator false positives)
  - FIX-COMMUNITY # 6x community-package behavior-drift tests
  - FIX-FANOUT    # 14x test_review_patch_no_blockers (documented blocker debt)
user_setup: []

must_haves:
  truths:
    - "python3 -m pytest tests/ -q reports 0 failed (was 24 failed)."
    - "Zero committed .maxpat files are edited (hard constraint per D-fanout, regression profile)."
    - "GenExpr validator changes only suppress false positives; every existing Check 1-9 unit test still passes."
    - "The review-blocker allowlist is per-patch and signature-scoped: a NEW patch or a NEW fan-out in an existing patch is still blocked."
  artifacts:
    - tests/review_blocker_allowlist.json
    - src/maxpat/code_validation.py
  key_links:
    - "tests/test_integration_patches.py::test_review_patch_no_blockers filters blockers through review_blocker_allowlist.json keyed by (patch path, blocker signature); unmatched blockers still fail."
    - "validate_genexpr Check 6 tokenizes code_stripped (not raw code); Check 9 flags only block-assigned names read at brace depth 0."
---

<objective>
Reconcile all 24 remaining test failures so `python3 -m pytest tests/ -q` is green, honoring the locked CONTEXT decisions: fix validator/critic false positives in code (never in committed patches), reconcile community-package tests to current DB behavior, and exempt documented fan-out blocker debt via a per-patch allowlist that never weakens blocking for new patches.

Purpose: Return the suite to green after intentional DB backfills and a promoted fan-out blocker tier left tests and committed patches out of sync — without touching any working `.maxpat` file (D-fanout hard constraint; user regression profile).
Output: One validator fix (2 false-positive classes), 6 reconciled community tests, and a documented review-blocker allowlist consumed by the integration test.

IMPORTANT — diagnosis corrections found during planning (the CONTEXT under-counted root causes; scope and decision intent are unchanged — still "fix validator false positives, not patches; suite green; zero patch edits"):
- Of the 4 `test_validate_patch_no_errors` failures, only `tape-wobble` is the ALL-CAPS comment-banner misparse (Check 6). The other 3 (`scala-synth-voice`, `timestretch`, `wormhole`) are Check 9 (init-before-if/else) false positives — a block-local variable assigned-before-use inside a branch is wrongly flagged. Both classes are validator false positives; both are fixed in the validator (Task 1).
- Of the 14 `test_review_patch_no_blockers` failures, `physics-composition` carries 2 non-fan-out blockers (Bach-llll type mismatch from a `receive` object whose output llll-ness is statically unknowable through a send/receive bridge). These are also allowlisted as documented per-patch debt (Task 3), keeping ONE mechanism and zero patch edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260702-k9w-reconcile-the-20-remaining-test-failures/260702-k9w-CONTEXT.md
@CLAUDE.md

# Sources under change
@src/maxpat/code_validation.py
@src/maxpat/critics/structure_critic.py
@src/maxpat/critics/package_critic.py
@tests/test_integration_patches.py
@tests/test_code_validation.py
@tests/test_package_schema.py
@tests/test_validation.py
@tests/test_critics.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix two GenExpr validator false positives (4x test_validate_patch_no_errors)</name>
  <files>src/maxpat/code_validation.py, tests/test_code_validation.py</files>
  <behavior>
    - RED test A (Check 6 banner): a gen~ codebox whose body contains an ALL-CAPS section banner comment of the shape "// === SATURATION (Pade tanh) ===" must produce ZERO "Unknown GenExpr operator" errors from validate_genexpr. Currently the word before "(" inside the // comment is tokenized as an operator call.
    - RED test B (Check 9 assign-before-use): a codebox where a variable is assigned inside a branch and then read only inside that same/enclosing branch (e.g. an else block: "morphL = wetL * clamp(envL,0,1);" then "outL = ... + morphL * blend;") must produce ZERO "without prior init" errors. The variable is written before it is read within the block, so GenExpr compiles fine.
    - GREEN/REGRESSION: all existing TestGenExprChecks cases keep their current results — test_check9_init_before_if still flags 'y' (assigned only in-block, then READ at depth 0 via "out1 = y;"); test_check9_param_exempt, test_check9_pre_init_exempt, test_check9_single_line_if_block_false_negative, test_check9_suggestion_documents_limitations, test_checks_skip_comments unchanged.
  </behavior>
  <action>
Fix Check 6 (operator existence, code_validation.py ~lines 179-208): the false positive is that `func_pattern.finditer(code)` scans the RAW code, so an ALL-CAPS banner like `// === SATURATION (...) ===` matches `\b(\w+)\s*\(` on the word "SATURATION". Change the Check-6 token scan to iterate `code_stripped` (the already-computed `_strip_line_comments(code)` result at line ~90) instead of `code`. For consistency also build `declared_names` (the Param/History/Delay/Buffer/Data name set at ~lines 187-189) from `code_stripped`. Do NOT change the error message text or severity. This resolves tape-wobble (banners DRIFT/EQ/LFO/ROLLOFF/SATURATION/SIGNAL).

Fix Check 9 (init-before-if/else, code_validation.py ~lines 229-290): the current logic flags the ASSIGNMENT of any name written at brace depth >= 1 that was not pre-initialized at depth 0 or declared. That wrongly flags a variable that is assigned-before-use entirely within a branch (the real GenExpr "not defined" error is a READ before any assignment on the taken path, not an in-block write). Restructure to a two-pass posture over `lines`:
  Pass 1 (assignments): keep the existing depth walk and `pre_block_inits` (depth-0 left-hand-side assignments) and `declared` (Param/History/Delay/Buffer/Data names) sets. Additionally collect `block_assigned`: a mapping of name -> first line index for names assigned at depth >= 1 that are NOT in `pre_block_inits` and NOT in `declared` (skip `//`/`#`/blank lines; keep the existing `assign_pattern` anchored at line start, which already misses single-line if-blocks — preserve that documented false negative).
  Pass 2 (reads at depth 0): re-walk `lines` tracking brace depth; on a line whose depth is 0 that is NOT a comment/preprocessor/blank, extract identifier tokens from the read portion (the right-hand side when the line is an assignment, else the whole line) and, for each token that is in `block_assigned` and not in `pre_block_inits` and not a GenExpr keyword/builtin (`_GENEXPR_KEYWORDS`), record it as a genuine "read-before-init" occurrence.
  Emit the SAME error (level="error", the existing "variable '{name}' used inside if/else without prior init ({line_display}); ..." message including the D-20 "if this is a false positive" suggestion) only for such recorded names, first-error-and-stop as today, using the recorded assignment line index for `line_display`. Names that are only ever read inside the block that assigned them (morphL, n, ng) never reach depth-0 read detection and are therefore not flagged.

Add the two RED unit tests to tests/test_code_validation.py inside the existing TestGenExprChecks class (mirror the existing style; assert the specific error class is empty). Keep the banner literal in the test as a normal string; it is a test fixture, not a written patch file.

Note in the SUMMARY that the CONTEXT diagnosis ("all 4 are comment-banner misparse") was incomplete: 3 of 4 were Check-9 assign-before-use false positives; both classes fixed in the validator with zero patch edits. Commit this task's files only (no `git add -A`).
  </action>
  <verify>
    <automated>python3 -m pytest "tests/test_integration_patches.py" -k "validate_patch_no_errors" -q && python3 -m pytest tests/test_code_validation.py -q</automated>
  </verify>
  <done>The 4 test_validate_patch_no_errors parametrizations (scala-synth-voice, tape-wobble, timestretch, wormhole) pass; every TestGenExprChecks test passes including the 2 new ones; no committed .maxpat edited.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile 6 community-package tests to current DB behavior</name>
  <files>tests/test_package_schema.py, tests/test_validation.py, tests/test_critics.py</files>
  <action>
Update six tests to the intentional current DB state (community packages now carry real extracted data: e.g. Bach 195 verified, EARS 102 verified, Dada 28 verified; package_info marks all community packages extracted=true). Do NOT weaken meaningfully; preserve the behavior each test targets by adjusting preconditions/assertions. Per D-community.

1. tests/test_package_schema.py::TestCommunityPackageStubs::test_community_stubs_verified_false (~line 429): the DB now legitimately has verified=true objects. Replace the `obj.get("verified") is False` assertion with a presence/type assertion that `verified` is a bool. Update the docstring to state extracted community objects may be verified=true. (Field presence is already covered by test_community_stubs_schema; this keeps a type check without asserting a stale value.)

2. tests/test_package_schema.py::TestCommunityPackageStubs::test_community_stubs_signal_objects_have_signal_io (~line 437): fails because several community `~` objects are buffer-processing objects (control I/O), not audio-rate — e.g. FluCoMa `fluid.buf*~` (fluid.bufonsetslice~) and the ears library (ears.read~, ears.slice~) use `~` to denote buffer operations, so a signal-typed port is not required. Change the invariant to: for signal (`~`) objects that are `verified is True`, assert the object has NON-EMPTY I/O (at least one inlet OR at least one outlet) — i.e. real extracted data must be usable — instead of requiring a signal-typed port. Skip `verified` not True (known-incomplete stubs may still have empty I/O, e.g. fluid.stftpass~/fluid.waveform~). Add a comment documenting the buffer-`~` convention. This preserves the original intent (catch unusable empty-I/O entries) scoped to verified data and matches CLAUDE.md's "empty I/O = unusable" concern.

3. tests/test_package_schema.py::TestCommunityPackageStubs::test_lookup_ears (~line 566): investigate per D-community exception. Evidence: the EARS package objects.json contains `ears.slice~` (with tilde, a buffer object) but NOT a non-tilde `ears.slice`; the ears library uniformly uses the `~` suffix (ears.read~, ears.write~, ears.slice~ …). `ears.slice` (no tilde) is not a real ears object, so inventing it would violate CLAUDE.md Rule #1. Restore-vs-weaken resolution: correct the test to look up the real object `ears.slice~` (already present in the DB, so lookup returns non-None). Justify in the SUMMARY that this is a reference correction to an existing real object, not a weakening, and that no data invention was warranted.

4. tests/test_critics.py::TestPackageCritic::test_community_unextracted_warning (~line 2058): review_packages now sees FluCoMa extracted=true, so no "unextracted" warning fires. The behavior under test is "unextracted community package -> warning", which still exists. Add `db` and `monkeypatch` fixtures to the method, set FluCoMa extracted=false on the db (mirror the monkeypatch.setitem(db._package_info, "FluCoMa", {...extracted:false}) pattern used by test_no_warning_when_extracted in tests/test_validation.py ~line 1833), and pass the db in: review_packages(patch, db=db). Assertion (>=1 "unextracted" warning) stays.

5. tests/test_validation.py::TestCommunityPackageBlock::test_community_block_warning (~line 1794): same drift — fluid.ampfeature~ no longer warns because FluCoMa is extracted. Add a monkeypatch that sets FluCoMa extracted=false on the `db` fixture (inverse of the sibling test_no_warning_when_extracted), then keep the existing assertions ("FluCoMa", "not extracted", "extract_objects.py --package", "Package Manager"). This exercises the warning branch with the precondition it actually tests.

6. tests/test_validation.py::TestCommunityPackageBlock::test_ircam_spat_specific_message (~line 1851): IRCAM Spat is now extracted=true, so no warning. Add a monkeypatch setting IRCAM Spat extracted=false on the `db` fixture, keep the assertions ("IRCAM Spat", "forum.ircam.fr", "extract_objects.py --package"). spat5.panning~ already exists in the DB.

Do not edit any objects.json or overrides.json (test_lookup_ears resolves via a test reference correction, not data restore). Commit this task's files only (no `git add -A`).
  </action>
  <verify>
    <automated>python3 -m pytest "tests/test_package_schema.py::TestCommunityPackageStubs" "tests/test_validation.py::TestCommunityPackageBlock" "tests/test_critics.py::TestPackageCritic::test_community_unextracted_warning" -q</automated>
  </verify>
  <done>All 6 named community-package tests pass; sibling tests in the same classes (test_bundled_no_community_warning, test_no_warning_when_extracted, test_community_extracted_clean, all count/lookup/schema tests) remain green; no DB JSON files edited.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Documented review-blocker allowlist for 14 committed patches (14x test_review_patch_no_blockers)</name>
  <files>tests/review_blocker_allowlist.json, tests/test_integration_patches.py</files>
  <behavior>
    - After filtering through the allowlist, each of the 14 currently-failing patches yields ZERO remaining blockers, so test_review_patch_no_blockers passes for them.
    - Guardrail 1 (new patch): a patch path absent from the allowlist has NONE of its blockers filtered — still fails if it has any blocker.
    - Guardrail 2 (new fan-out in an existing patch): a blocker whose signature is not an explicit allowlist entry for that patch is NOT filtered — still fails.
    - Zero committed .maxpat files are modified.
  </behavior>
  <action>
Do NOT edit any committed `.maxpat` file (D-fanout hard constraint). Instead create tests/review_blocker_allowlist.json documenting the existing blocker debt, and make test_review_patch_no_blockers consume it.

Allowlist file (tests/review_blocker_allowlist.json): a JSON object with a top-level `_doc` string explaining it exempts ONLY existing documented review-blocker debt in committed patches (fan-out-without-trigger per CLAUDE.md Rule #4, plus a small number of Bach-llll routing-bridge false positives), is scoped per patch, and never applies to new patches or new blockers. Then a `patches` object keyed by the patch path relative to repo root (e.g. "patches/kicksynth/generated/kicksynth.maxpat"), each value a list of exemption entries. Each entry MUST cite the specific finding it exempts: `severity`, `source` (source object name), `source_id`, `outlet` (source outlet index for fan-out; use inlet for Bach-llll and record it as `inlet`), and `destinations` (list of destination object names). Populate the file by enumerating the CURRENT blockers: for each of the 14 patches load the .maxpat, run src.maxpat.critics.review_patch, and record every result with severity=="blocker" — both "Fan-out without trigger:" findings (112 total) and the 2 physics-composition "Bach llll type mismatch:" findings. Build this JSON during execution and commit only the resulting data file — do NOT commit a generator script (CLAUDE.md Rule #5).

The 14 patch paths (blocker counts for cross-check): gen-eq/generated/gen-eq.maxpat (14), gong-model/generated/gong-model.maxpat (6), intelligent-corpus-remixer/generated/intelligent-corpus-remixer.maxpat (3), kicksynth/generated/kicksynth.maxpat (8), minitaur/generated/minitaur.maxpat (36), mixer/generated/mixer-bus.maxpat (1), performancepatchtest/generated/comp-band.maxpat (5), physics-composition/generated/physics-composition.maxpat (3 = 1 fan-out + 2 Bach-llll), rhythmic-sampler/generated/rhythmic-sampler.maxpat (2), rhythmic-sampler/generated/slot.maxpat (9), scala-synth/generated/scala-synth-voice.maxpat (2), scala-synth/generated/scala-synth.maxpat (16), stutter/generated/stutter.maxpat (8), wormhole/generated/wormhole-test.maxpat (1).

Matching key: use (source_id, outlet) for fan-out entries and (source_id, inlet, sorted destinations) for Bach-llll entries — source_id is stable because committed patches are never edited. Store `source`/`destinations` names for human auditability.

Test change (tests/test_integration_patches.py::test_review_patch_no_blockers): load review_blocker_allowlist.json once (module scope). After computing `blockers`, remove a blocker only if the current patch's path has an allowlist list AND the blocker matches one of its entries. Derive the match key from the blocker by parsing its `finding` string, whose formats are fixed by the critics: fan-out findings read "Fan-out without trigger: '<name>' (<source_id>) outlet <N> connected to ..." and Bach-llll findings read "Bach llll type mismatch: '<name>' (<source_id>) connected to '<dst>' (<dst_id>) inlet <N> ...". Extract source_id and outlet/inlet via a small regex. Assert no blockers REMAIN after filtering (so an unmatched or new blocker still fails). Do not alter the existing _REVIEW_XFAILS mechanism (mixer-strip stays xfail). Keep the assertion message listing any remaining blockers.

Verify guardrails before committing: (a) after filtering, the 14 patches have 0 remaining blockers; (b) a patch path not in the allowlist, or a fabricated (source_id, outlet) not listed, is NOT filtered (spot-check in a scratch check, not committed). Note in the SUMMARY that physics-composition's 2 Bach-llll entries are a send/receive routing-bridge false positive (a `receive` object's output llll type is statically unknowable) folded into the same documented-debt allowlist rather than a critic edit, and could alternatively be fixed critic-side in a follow-up. Commit this task's files only (no `git add -A`).
  </action>
  <verify>
    <automated>python3 -m pytest "tests/test_integration_patches.py" -k "review_patch_no_blockers" -q</automated>
  </verify>
  <done>All 14 test_review_patch_no_blockers failures pass via allowlist filtering; no committed .maxpat edited; a patch/blocker absent from the allowlist is still reported as a failure (guardrail verified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | All changes are to test files, validator logic, and a static test-only JSON data file. No network, no external/user input crosses a new boundary, no package-manager installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k9w-01 | Tampering | review_blocker_allowlist.json (test-scoped exemption) | low | mitigate | Allowlist is per-patch + per-blocker-signature and only filters within test assertions; new patches/new blockers are never filtered (guardrail asserted in Task 3). Critic behavior at generation time is unchanged. |
| T-k9w-02 | Repudiation | GenExpr validator false-positive suppression | low | accept | Fixes are false-positive-only; every existing Check 1-9 unit test remains green, so genuine "not defined"/unknown-operator errors still block. No package installs → package-legitimacy gate N/A. |
</threat_model>

<verification>
Run each task's scoped selector, then the full suite gate:

- Task 1: `python3 -m pytest "tests/test_integration_patches.py" -k "validate_patch_no_errors" tests/test_code_validation.py -q`
- Task 2: `python3 -m pytest "tests/test_package_schema.py::TestCommunityPackageStubs" "tests/test_validation.py::TestCommunityPackageBlock" "tests/test_critics.py::TestPackageCritic::test_community_unextracted_warning" -q`
- Task 3: `python3 -m pytest "tests/test_integration_patches.py" -k "review_patch_no_blockers" -q`
- FINAL GATE: `python3 -m pytest tests/ -q` must report `0 failed` (was 24 failed; xfailed count unchanged).

Confirm `git status` shows no modified `patches/**/*.maxpat` files at any point.
</verification>

<success_criteria>
- `python3 -m pytest tests/ -q` reports 0 failed.
- No committed `.maxpat` file modified (verified via git status).
- GenExpr validator changes suppress only false positives; all TestGenExprChecks tests pass.
- 6 community tests reflect current intentional DB behavior; sibling tests still pass.
- review_blocker_allowlist.json exists, cites each exempted blocker, and the integration test still blocks new patches / new blockers.
- Each task committed separately (TDD-style), scoped to its own files (no `git add -A`, no `git stash`).
</success_criteria>

<output>
Create `.planning/quick/260702-k9w-reconcile-the-20-remaining-test-failures/260702-k9w-SUMMARY.md` when done. In it, document: (1) the two CONTEXT diagnosis corrections (Check-9 vs banner; physics-composition Bach-llll); (2) the test_lookup_ears resolution rationale (reference correction to ears.slice~, no data invention); (3) the review-blocker allowlist format and how the test consumes it.
</output>
