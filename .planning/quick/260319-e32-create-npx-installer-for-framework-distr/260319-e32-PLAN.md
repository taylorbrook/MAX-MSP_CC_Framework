---
phase: quick-260319-e32
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .release-ignore
  - .github/workflows/release.yml
  - installer/package.json
  - installer/bin/create-max-framework.js
  - installer/README.md
  - install.sh
  - README.md
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, DIST-04]

must_haves:
  truths:
    - "GitHub Actions builds a clean framework tarball when a version tag is pushed"
    - "npx create-max-framework my-project scaffolds a working project directory"
    - "install.sh scaffolds a working project directory identical to npx"
    - "README Quick Start shows all three installation methods"
  artifacts:
    - path: ".release-ignore"
      provides: "Exclusion list for release tarball"
      contains: ".planning/"
    - path: ".github/workflows/release.yml"
      provides: "GitHub Actions release workflow"
      contains: "softprops/action-gh-release"
    - path: "installer/package.json"
      provides: "npm package definition for create-max-framework"
      contains: "create-max-framework"
    - path: "installer/bin/create-max-framework.js"
      provides: "CLI entry point that downloads tarball and scaffolds project"
      min_lines: 100
    - path: "install.sh"
      provides: "Shell script fallback installer"
      min_lines: 40
    - path: "README.md"
      provides: "Updated Quick Start with npx, shell, and git clone options"
      contains: "npx create-max-framework"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".release-ignore"
      via: "tar --exclude-from reads ignore file"
      pattern: "exclude-from.*release-ignore"
    - from: "installer/bin/create-max-framework.js"
      to: "GitHub Releases"
      via: "HTTPS download of framework.tar.gz"
      pattern: "releases/latest/download/framework\\.tar\\.gz"
    - from: "install.sh"
      to: "GitHub Releases"
      via: "curl download of framework.tar.gz"
      pattern: "releases/latest/download/framework\\.tar\\.gz"
---

<objective>
Create the full distribution system for the MAX/MSP Claude Code Framework: a GitHub Actions release workflow that builds a clean tarball on tag push, an npx create-max-framework CLI package that downloads and scaffolds projects, a shell script fallback installer, and updated README Quick Start documentation.

Purpose: Enable users to set up the framework with a single command instead of git clone, getting a clean project without development artifacts.
Output: Release workflow, npm installer package, shell script, updated README.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260319-d83-investigate-npx-installer-for-framework-/FINDINGS.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions release workflow and release-ignore</name>
  <files>.release-ignore, .github/workflows/release.yml</files>
  <action>
Create `.release-ignore` at repo root listing paths to exclude from the distribution tarball:
```
.planning/
.git/
.github/
.pytest_cache/
tests/
.claude/max-objects/audit/
src/maxpat/__pycache__/
patches/
installer/
scripts/
.DS_Store
*.pyc
__pycache__/
```

Create `.github/workflows/release.yml` with a workflow that:
- Triggers on push of tags matching `v*`
- Runs on `ubuntu-latest`
- Checks out the repo with `actions/checkout@v4`
- Builds a tarball with: `tar czf framework.tar.gz --exclude-from=.release-ignore --transform 's,^\./,MAX-MSP_CC_Framework/,' .`
  (Note: GNU tar on ubuntu uses `--transform` not `-s` for path rewriting. The transform wraps all files under a `MAX-MSP_CC_Framework/` prefix directory so extraction produces a named folder.)
- Creates a GitHub Release using `softprops/action-gh-release@v2` with the tarball attached
- Uses `permissions: contents: write` on the job so the GITHUB_TOKEN can create releases

The workflow should be minimal and correct. Do NOT use `--exclude-from` with directories that have trailing slashes in the tar command if GNU tar on ubuntu doesn't support it -- test the syntax. The standard GNU tar format for directory exclusion in exclude files uses paths without trailing slashes but with `--anchored` or just the directory name. Use this safe pattern in .release-ignore:
```
.planning
.git
.github
.pytest_cache
tests
.claude/max-objects/audit
src/maxpat/__pycache__
patches
installer
scripts
.DS_Store
```
(No trailing slashes -- GNU tar exclude-from matches path prefixes.)
  </action>
  <verify>
    <automated>cat /Users/taylorbrook/Dev/MAX/.release-ignore && echo "---" && cat /Users/taylorbrook/Dev/MAX/.github/workflows/release.yml && python3 -c "import yaml; yaml.safe_load(open('/Users/taylorbrook/Dev/MAX/.github/workflows/release.yml'))" 2>/dev/null || echo "yaml module not available, visual check only"</automated>
  </verify>
  <done>.release-ignore exists with correct exclusions. release.yml is valid YAML, triggers on v* tags, builds tarball excluding dev files, creates GitHub Release with framework.tar.gz attached.</done>
</task>

<task type="auto">
  <name>Task 2: Create npx create-max-framework CLI package</name>
  <files>installer/package.json, installer/bin/create-max-framework.js, installer/README.md</files>
  <action>
Create `installer/` directory with the npm package for `create-max-framework`.

**installer/package.json:**
```json
{
  "name": "create-max-framework",
  "version": "1.0.0",
  "description": "Create a MAX/MSP Claude Code Framework project",
  "bin": {
    "create-max-framework": "./bin/create-max-framework.js"
  },
  "files": ["bin/", "README.md"],
  "keywords": ["max", "msp", "maxmsp", "claude", "claude-code", "audio", "music", "dsp"],
  "author": "Taylor Brook",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/taylorbrook/MAX-MSP_CC_Framework.git",
    "directory": "installer"
  }
}
```

**installer/bin/create-max-framework.js** (executable, with `#!/usr/bin/env node` shebang):

The script must handle these cases:
1. `npx create-max-framework <project-name>` -- scaffold a new project
2. `npx create-max-framework --version` / `-v` -- print version
3. `npx create-max-framework --help` / `-h` -- print usage
4. No arguments -- print usage and exit 1

Implementation details (zero external dependencies, Node.js stdlib only):
- Use `https` module (not `fetch` which may not exist in Node 16) to download the tarball
- GitHub Releases URL: `https://github.com/taylorbrook/MAX-MSP_CC_Framework/releases/latest/download/framework.tar.gz`
- Follow redirects (GitHub returns 302 to the actual CDN URL) -- implement a simple redirect follower (check response.statusCode 301/302, re-request response.headers.location), max 5 redirects
- Pipe the response to `tar xz` via `child_process.spawn` with `--strip-components=1` to remove the `MAX-MSP_CC_Framework/` prefix, extracting into the target directory
- If `tar` fails or download fails, print a clear error and exit 1
- After extraction, create `patches/` directory if it doesn't already exist
- Create `patches/.active-project.json` with `{"active_project": null}`
- Initialize a git repo: `git init` in the project directory (suppress output)
- Check Python version: spawn `python3 --version`, parse output, warn if < 3.10 or missing (do NOT exit -- just warn)
- Print success message with next steps:
  ```
  Done! Your MAX/MSP Claude Code Framework project is ready.

  Next steps:
    cd <project-name>
    claude
    /max-new my-first-patch

  Prerequisites:
    - Claude Code (https://docs.anthropic.com/en/docs/claude-code)
    - MAX 9 (https://cycling74.com/products/max)
    - Python 3.10+ (for the validation engine)
  ```

- Check if target directory already exists before doing anything -- if it does, error with "Error: Directory '<name>' already exists." and exit 1
- The script must be robust: wrap the download+extract in a try/catch, clean up the target directory on failure (rm -rf) so the user doesn't have a half-extracted mess

**installer/README.md:**
Brief readme for the npm package page:
- Title: create-max-framework
- One-line description
- Usage: `npx create-max-framework my-project`
- What it does (3 bullets)
- Link to main repo
- Prerequisites
  </action>
  <verify>
    <automated>node -c /Users/taylorbrook/Dev/MAX/installer/bin/create-max-framework.js && node /Users/taylorbrook/Dev/MAX/installer/bin/create-max-framework.js --help</automated>
  </verify>
  <done>installer/package.json defines create-max-framework with correct bin entry. CLI script parses arguments, handles --help/--version, downloads tarball with redirect following, extracts with tar, checks Python, prints next steps. Script passes Node.js syntax check and --help prints usage.</done>
</task>

<task type="auto">
  <name>Task 3: Create shell script fallback and update README Quick Start</name>
  <files>install.sh, README.md</files>
  <action>
**install.sh** at repo root (executable, `#!/usr/bin/env bash`, `set -euo pipefail`):

The shell script should mirror the npx installer's behavior:
1. Accept project name as first positional argument, error if missing with usage message
2. Check if target directory exists, error if so
3. Create the directory
4. Download and extract the release tarball:
   `curl -fsSL "https://github.com/taylorbrook/MAX-MSP_CC_Framework/releases/latest/download/framework.tar.gz" | tar xz -C "$PROJECT_NAME" --strip-components=1`
5. Create `patches/` directory and `patches/.active-project.json`
6. Run `git init` in the project directory (suppress output)
7. Check Python 3 version -- warn if < 3.10 or not found (do not exit)
8. Print the same success message with next steps as the npx installer
9. On failure (set -e will catch), clean up the partial directory

**README.md update:**

Replace the existing Quick Start section (lines 32-59, from "## Quick Start" through the "/max-build" code block) with an updated version that shows three installation options, then continues with the existing workflow steps. The new Quick Start should be:

```markdown
## Quick Start

### 1. Install

**Option A: npx (recommended)**

```bash
npx create-max-framework my-project
cd my-project
claude
```

**Option B: Shell script**

```bash
curl -fsSL https://raw.githubusercontent.com/taylorbrook/MAX-MSP_CC_Framework/main/install.sh | bash -s my-project
cd my-project
claude
```

**Option C: Git clone (for contributors)**

```bash
git clone https://github.com/taylorbrook/MAX-MSP_CC_Framework.git my-project
cd my-project
claude
```
```

Then renumber the existing steps: "2. Create a project" (was "2. Create a project"), "3. Build" (was "3. Build"), etc. -- the content of these steps stays exactly the same, just renumbered since "1. Clone and open" is now "1. Install" and the remaining steps shift accordingly. Actually the numbering stays the same since the old step 1 was "Clone and open" which is now "Install" -- steps 2-7 keep their numbers.

Do NOT change any content below the Quick Start section except the step numbering.
  </action>
  <verify>
    <automated>bash -n /Users/taylorbrook/Dev/MAX/install.sh && head -5 /Users/taylorbrook/Dev/MAX/install.sh && grep -c "npx create-max-framework" /Users/taylorbrook/Dev/MAX/README.md</automated>
  </verify>
  <done>install.sh passes bash syntax check, is executable, downloads tarball, checks Python, prints next steps. README.md Quick Start shows npx (recommended), shell script, and git clone options. All three methods point to the same GitHub release tarball URL.</done>
</task>

</tasks>

<verification>
- `.release-ignore` lists all dev-only paths for exclusion
- `.github/workflows/release.yml` is valid YAML and triggers on `v*` tags
- `installer/package.json` has correct `bin` entry and `files` field
- `installer/bin/create-max-framework.js` passes `node -c` syntax check and `--help` works
- `install.sh` passes `bash -n` syntax check
- `README.md` mentions all three installation methods
- No existing README content below Quick Start is modified (except step numbering)
</verification>

<success_criteria>
All distribution artifacts exist and pass syntax validation. The npx CLI handles argument parsing, tarball download with redirect following, extraction, Python check, and success messaging. The shell script provides equivalent functionality. README Quick Start presents npx as the recommended method with shell and git clone as alternatives.
</success_criteria>

<output>
After completion, create `.planning/quick/260319-e32-create-npx-installer-for-framework-distr/260319-e32-SUMMARY.md`
</output>
