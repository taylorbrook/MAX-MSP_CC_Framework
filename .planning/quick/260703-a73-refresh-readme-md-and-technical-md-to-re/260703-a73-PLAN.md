---
phase: quick-260703-a73
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - TECHNICAL.md
autonomous: true
requirements: [QUICK-260703-a73]
must_haves:
  truths:
    - README version-history table lists v5.0 with its five phases and ship date
    - README object/test/LOC counts match the current codebase (3,430 objects, 2,034 tests, ~18,600 LOC)
    - TECHNICAL documents the Phase 28 schema-hardening fields, the deepened validation pipeline, the six-critic roster, the dsp_sim harness, and the Phase 31 builders
    - No documentation claims the memory agent still auto-injects/writes back
  artifacts:
    - README.md
    - TECHNICAL.md
  key_links:
    - Phase 31 builder signatures in docs match src/maxpat/patcher.py exactly
    - dsp_sim references point to real modules under src/maxpat/dsp_sim/
---

<objective>
Refresh README.md (currently stuck at the v3.0/v4.0 milestone story) and TECHNICAL.md (stale object counts, "4-layer" validation, missing v5.0 subsystems, dead memory-agent claims) so both accurately describe the shipped v5.0 milestone: DB schema hardening (signal_role / domain_restricted / verified_installed), validator depth, MSP outlet coverage, the Phase 31 codified builders, and the dsp_sim pre-flight simulator.

Purpose: The two top-level docs are the front door for the framework and its contributors; they currently misstate the object count by ~1,400, describe a validation pipeline two milestones out of date, omit every v5.0 subsystem, and still describe a memory agent that no longer exists.
Output: Updated README.md and TECHNICAL.md, docs-only, committed to git.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@README.md
@TECHNICAL.md
@CLAUDE.md

# Verified ground-truth facts for this refresh (do NOT re-derive; these were
# confirmed against the codebase during planning):
#
# v5.0 = "DB Schema Hardening + Validator Depth", shipped 2026-05-01, 24 plans:
#   Phase 28 Schema Foundation (3 plans, 2026-04-28)
#   Phase 29 Validator Depth (5 plans, 2026-04-29)
#   Phase 30 MSP Outlet Coverage Sweep (4 plans, 2026-04-30)
#   Phase 31 Layout & UX Builders (7 plans, 2026-05-01)
#   Phase 32 DSP Pre-Flight Simulation (5 plans, 2026-05-01)
#
# Object DB total = 3,430:
#   core (7 domain files, 1,941): max 471, msp 246, jitter 218, mc 222,
#   gen 189, m4l 35, rnbo 560
#   packages: 1,489 objects across 29 per-package files under
#   .claude/max-objects/packages/<Name>/objects.json
#   (there is NO single .claude/max-objects/packages/objects.json anymore)
# Test suite: 2,034 tests across 46 files. src/maxpat LOC: 18,639 (~18,600).
#
# Phase 28 schema fields (db_lookup.py):
#   signal_role  - per-outlet closed enum; loader projects it onto the legacy
#                  outlet['signal'] bool via _apply_signal_role_writethrough
#                  (signal_role == "audio" -> signal True)
#   domain_restricted - per-object list of domain enums
#   verified_installed - per-object strict bool
#   Fail-fast validation: _validate_schema_extensions (runs after deep-merge).
#   New ObjectDatabase methods: get_signal_role, get_domain_restrictions,
#   is_domain_restricted, get_install_state, is_verified_installed,
#   audit_signal_role_coverage, audit_install_coverage, audit_domain_coverage,
#   audit_empty_io (with by_source).
#
# Phase 29 validation depth (validation.py layer comments):
#   Layer 1 JSON structure; Layer 2 object existence (+2b maxclass usage,
#   +2c package gating, +2d community-extracted check); Layer 3 connection
#   bounds + signal types (now consults curated signal_role); Layer 4 domain
#   rules (+4b domain-restriction guard, VALID-02); Layer 5 embedded GenExpr
#   codebox walker (VALID-04).
#
# Critics wired into review_patch() (critics/__init__.py): DSP, structure,
# layout, RNBO, external, package = 6 critics. NOTE: m4l_critic.py exists as a
# module but is NOT called by review_patch — do not claim it runs in the loop.
#
# Phase 32 dsp_sim (src/maxpat/dsp_sim/): offline numpy waveguide-stability
# harness. Public API run_simulation(patch_path, topology, params,
# sweep_param, sweep) -> SimulationReport. classify() emits 5 verdicts: PASS,
# PHASE_DRIFT, MODE_COMPETITION, NO_OSCILLATION, RUNAWAY. Topologies:
# bore_only, reed_bore, reed_bore_post_radiation. CLI:
# `python3 -m src.maxpat.dsp_sim --patch <p> --topology <t> ...` with exit
# codes mirroring verdict priority. Gated in max-dsp-agent/SKILL.md via
# tests/dsp_sim/test_<stem>.py.
#
# Phase 31 builders (patcher.py, signatures verified — copy exactly):
#   add_overlay_readout(target, *, format="%.2f", type="flonum",
#       editable=False, offset_x=0.0, offset_y=0.0) -> Box
#       (Rule #6 overlay recipe: bring_to_front + ignoreclick pass-through)
#   add_labeled_param_bank(params, x, y, *, label_side="left",
#       extra_attrs=None) -> (multislider, [comment, ...])
#       (size*24 height, contdata=1, setstyle=1, orientation=0 defaults)
#   replace_box_safe(old_box, new_name, *, args=None, rewire="auto")
#       -> EditResult (auto-rewires orphans by index on I/O match)
#   add_m4l_gen_synth(params, *, gen_varname="synth", gen_code=None)
#       -> (gen_obj, [live_dial, ...], plugout_obj)
#       (live.dial<->Param via param_connect; no gain~ before plugout~)
#
# Memory system: the max-memory-agent skill no longer exists; the MemoryStore
# library (src/maxpat/memory.py) is still present but nothing auto-injects or
# writes back. Correct any doc claiming an agent drives memory inject/write-back.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh README.md to the v5.0 story</name>
  <files>README.md</files>
  <action>
Update README.md so its numbers and milestone history reflect v5.0. Make these edits, grounded in the verified facts in the context block:

1. Milestone table (the "Development with GSD" section table that currently ends at the v4.0 Package Integration row): append a v5.0 row — `**v5.0 DB Schema Hardening + Validator Depth** | 2026-05-01 | 5 | 24 | ...` with a highlights cell naming: signal_role/domain_restricted/verified_installed schema fields, deepened validation pipeline, MSP outlet coverage sweep, Phase 31 layout/UX builders, and dsp_sim pre-flight simulation.

2. Object-count accuracy: change the object-count figure used in the intro feature bullet and in the "How It Works" / "Object Database" prose and the Project Structure comment so it reads 3,430 (the current true total). Keep the "29 packages" phrasing (that is correct). Where the intro bullet enumerates domains, keep the seven core domains plus 29 packages framing.

3. Project Structure block counts: update the `src/maxpat/` LOC annotation to ~18,600 and the `tests/` annotation to 2,034 tests.

4. Feature list: add or fold in mention of the v5.0 capabilities so a reader sees them as current features — (a) typed per-outlet signal metadata backing the connection validator, (b) codified layout/UX builders (labeled parameter banks, overlay readouts, safe box replacement, M4L gen-synth skeletons), and (c) the offline DSP pre-flight stability simulator for waveguide patches. Keep the existing "safety checks / no in-framework audio preview" note honest — dsp_sim is an offline numerical stability harness for specific waveguide topologies, not a general audio preview.

5. In "How It Works", the layer descriptions currently frame direct editing as "(v3.0)"; leave that historical label but ensure the validation-pipeline paragraph and the object-database paragraph reflect the current depth (signal_role-aware connection checks, package gating) rather than the pre-v5.0 description.

Do NOT touch any source code. Preserve existing formatting, table styles, and the tone of the document. Do not invent numbers — use only the verified figures from the context block.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=README.md; grep -q "v5.0 DB Schema Hardening" "$f"; grep -q "2026-05-01" "$f"; grep -q "3,430" "$f"; grep -q "2,034" "$f"; grep -Eq "18,6|~18,600|18,639" "$f"; grep -qi "signal_role\|signal metadata\|typed .*outlet" "$f"; grep -qi "pre-flight\|dsp_sim\|stability sim" "$f"; grep -qi "labeled parameter bank\|overlay readout\|builder" "$f"; echo README_OK'</automated>
  </verify>
  <done>README.md milestone table includes a v5.0 row with 5 phases / 24 plans / 2026-05-01; object total reads 3,430; LOC and test counts updated (~18,600 / 2,034); v5.0 capabilities (signal metadata, layout/UX builders, dsp_sim) appear in the feature narrative; no source files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh TECHNICAL.md to v5.0 internals</name>
  <files>TECHNICAL.md</files>
  <action>
Update TECHNICAL.md so the internals documentation matches the shipped v5.0 codebase. Make these edits, grounded strictly in the verified facts in the context block:

1. Object Database section — Domain Files table and total: correct the per-domain counts to max 471, msp 246, jitter 218, mc 222, gen 189, m4l 35, rnbo 560. Replace the single "packages" row + the stale grand total with an accurate description: packages now live as 29 per-package files under `.claude/max-objects/packages/<Name>/objects.json` totaling 1,489 objects, and set the grand total to 3,430. Remove the count that predates package integration.

2. Object Entry Schema — add the Phase 28 schema-hardening fields to the schema/key-fields description: `signal_role` (per-outlet closed enum; the loader projects it onto the legacy `signal` bool, so `signal: bool` is now a derived back-compat shim), `domain_restricted` (per-object list of domain enums), and `verified_installed` (per-object strict bool). Note that these are validated fail-fast at load by `_validate_schema_extensions`.

3. ObjectDatabase Class method table — add the new v5.0 methods: `get_signal_role`, `get_domain_restrictions` / `is_domain_restricted`, `get_install_state` / `is_verified_installed`, and the audit family (`audit_signal_role_coverage`, `audit_install_coverage`, `audit_domain_coverage`, `audit_empty_io` with by-source breakdown).

4. Agent System — Support Agents table: the max-memory-agent skill no longer exists. Remove the max-memory-agent row (or replace it with a note that memory persistence is a library, not an agent). Keep max-critic and max-lifecycle.

5. Python Engine — update the LOC figure to ~18,600. In the Patcher method table, add the four Phase 31 codified builders using their exact signatures from the context block: `add_overlay_readout(...)`, `add_labeled_param_bank(...)`, `replace_box_safe(...)`, and `add_m4l_gen_synth(...)`, each with a one-line purpose.

6. Validation Pipeline — replace the "4-layer" framing with the current layer set: Layer 1 JSON structure; Layer 2 object existence with sub-layers 2b (maxclass usage), 2c (package gating / defense in depth), 2d (community-package extracted check); Layer 3 connection bounds + signal-type checks that now consult the curated `signal_role`; Layer 4 domain-specific rules plus 4b domain-restriction guard; and Layer 5 embedded GenExpr codebox walker. Update the intro sentence and the architecture-overview ASCII note that call it a four-layer pipeline.

7. Critic System — document the full roster that `review_patch()` actually runs: DSP, structure, layout, RNBO, external, and package critics (six). Add a Layout Critic subsection and a Package Critic subsection consistent with the existing table style. Do NOT claim an M4L critic runs in the loop (the module exists but is not wired into review_patch).

8. Add a new top-level "DSP Pre-Flight Simulation" section documenting `src/maxpat/dsp_sim/`: the offline numpy waveguide-stability harness, its public `run_simulation(patch_path, topology, params, sweep_param, sweep) -> SimulationReport` API, the five classifier verdicts (PASS, PHASE_DRIFT, MODE_COMPETITION, NO_OSCILLATION, RUNAWAY), the three curated topologies (bore_only, reed_bore, reed_bore_post_radiation), the `python3 -m src.maxpat.dsp_sim` CLI with verdict-priority exit codes, and the max-dsp-agent gate via `tests/dsp_sim/test_<stem>.py`. Add it to the Table of Contents.

9. Memory System — correct the behavior description: there is no memory agent; the MemoryStore API (`src/maxpat/memory.py`) remains available as a library but auto-inject / auto-write-back is no longer driven by a dedicated agent. Fix the "the memory agent loads..." wording accordingly.

10. Test Suite — update the count to 2,034 tests across 46 test files.

Do NOT touch any source code. Preserve the document's heading structure, table style, and tone. Use only the verified figures and signatures from the context block — do not invent method names, counts, or behaviors.
  </action>
  <verify>
    <automated>bash -c 'set -e; f=TECHNICAL.md; grep -q "3,430" "$f"; grep -q "signal_role" "$f"; grep -q "domain_restricted" "$f"; grep -q "verified_installed" "$f"; grep -q "add_labeled_param_bank" "$f"; grep -q "add_overlay_readout" "$f"; grep -q "replace_box_safe" "$f"; grep -q "add_m4l_gen_synth" "$f"; grep -q "dsp_sim" "$f"; grep -q "run_simulation" "$f"; grep -qi "reed_bore_post_radiation" "$f"; grep -qi "PHASE_DRIFT\|MODE_COMPETITION\|NO_OSCILLATION\|RUNAWAY" "$f"; grep -qi "Layer 5\|Layer 4b\|Layer 2c" "$f"; grep -qi "layout critic\|review_layout\|Layout Critic" "$f"; grep -qi "package critic\|Package Critic" "$f"; grep -q "2,034" "$f"; echo TECHNICAL_OK'</automated>
  </verify>
  <done>TECHNICAL.md object counts total 3,430 with corrected per-domain rows; schema-hardening fields and new ObjectDatabase methods documented; four Phase 31 builders added with exact signatures; validation section describes Layers 1-5 with sub-layers; critic roster lists all six wired critics (layout + package added); a DSP Pre-Flight Simulation section exists and is in the TOC; memory-agent claims corrected; test count reads 2,034; no source files changed.</done>
</task>

</tasks>

<verification>
- Both README.md and TECHNICAL.md edited; `git diff --stat` shows only those two files changed (no source/code changes).
- README v5.0 milestone row present; object/test/LOC counts current.
- TECHNICAL documents all v5.0 subsystems (schema fields, validation depth, six critics, dsp_sim, Phase 31 builders) and drops the retired memory-agent claim.
- Phase 31 builder signatures in TECHNICAL match src/maxpat/patcher.py verbatim.
- No fabricated numbers: every figure traces to the verified context block.
</verification>

<success_criteria>
README.md and TECHNICAL.md accurately describe the shipped v5.0 milestone. A reader learns the current object count (3,430), the deepened validation pipeline, the six-critic roster, the signal_role/domain_restricted/verified_installed schema, the four codified Phase 31 builders, and the dsp_sim pre-flight simulator — with no stale references to the retired memory agent, the pre-v5.0 object total, or the "4-layer" pipeline. Docs-only: zero source-code changes.
</success_criteria>

<output>
Create `.planning/quick/260703-a73-refresh-readme-md-and-technical-md-to-re/260703-a73-SUMMARY.md` when done.
</output>
