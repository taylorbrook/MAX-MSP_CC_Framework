---
phase: quick-260703-a73
plan: 01
subsystem: docs
tags: [documentation, readme, technical, v5.0]
status: complete
requires: []
provides: [README.md, TECHNICAL.md]
affects: [README.md, TECHNICAL.md]
tech-stack:
  added: []
  patterns: []
key-files:
  created: []
  modified:
    - README.md
    - TECHNICAL.md
decisions:
  - Left the "four core layers" architecture-overview sentence unchanged — it describes the router/agents/engine/validation/critic architecture, not the validation-pipeline "4-layer" claim that item 6 targeted.
  - Replaced the retired max-memory-agent table row with a blockquote note (library, not agent) rather than deleting it silently, so readers who remember the old agent get an explicit correction.
metrics:
  duration: ~15m
  completed: 2026-07-03
  tasks: 2
  files: 2
  commits: 2
---

# Quick Task 260703-a73: Refresh README.md and TECHNICAL.md to v5.0 Summary

Refreshed the two front-door docs so they accurately describe the shipped v5.0 milestone (DB schema hardening + validator depth): corrected the object count from a stale 3,434 to the true 3,430, added the v5.0 milestone row, documented the Phase 28 schema fields, the Layers 1-5 validation pipeline, the six-critic roster, the four Phase 31 builders, and the dsp_sim pre-flight simulator, and removed all dead memory-agent claims.

## What Changed

### Task 1 — README.md (commit 39802c2)

- Appended a **v5.0 DB Schema Hardening + Validator Depth** row to the milestone table (5 phases / 24 plans / 2026-05-01) with a highlights cell naming the schema fields, deepened pipeline, MSP outlet sweep, Phase 31 builders, and dsp_sim.
- Corrected the object total from **3,434 → 3,430** everywhere (intro bullet, `/max-new` research prose, How It Works Object Database paragraph, Project Structure comment). Kept "29 packages" and reframed core domains as 7 (was "8 core domains").
- Updated Project Structure annotations: `src/maxpat/` **~18,600 LOC**, `tests/` **2,034 tests**.
- Added three feature bullets/edits: typed per-outlet `signal_role` metadata backing the connection validator; codified layout/UX builders; and the offline dsp_sim stability harness (explicitly framed as *not* a general audio preview, keeping the "no in-framework audio preview" note honest).
- Refreshed the How It Works validation and object-database paragraphs to current depth (signal_role-aware checks, package gating, domain restrictions, six critics).

### Task 2 — TECHNICAL.md (commit 2f28eb2)

- **Object Database**: corrected per-domain counts (max 471, msp 246, jitter 218, mc 222, gen 189, m4l 35, rnbo 560 = 1,941 core), replaced the stale single-`packages` row with the 29-per-package-file description (1,489 objects), grand total **3,430**.
- **Object Entry Schema**: added the Phase 28 schema-hardening fields (`signal_role` as the source of truth with `signal: bool` now a derived shim, `domain_restricted`, `verified_installed`), noting fail-fast `_validate_schema_extensions` at load.
- **ObjectDatabase Class**: added a schema-hardening accessor table (`get_signal_role`, `get_domain_restrictions`/`is_domain_restricted`, `get_install_state`/`is_verified_installed`, and the `audit_*` family including `audit_empty_io` with by-source breakdown).
- **Agent System**: removed the `max-memory-agent` row; replaced with a "no memory agent" note.
- **Python Engine**: updated LOC to ~18,600 and added the four Phase 31 builders with verbatim signatures (`add_overlay_readout`, `add_labeled_param_bank`, `replace_box_safe`, `add_m4l_gen_synth`).
- **Validation Pipeline**: replaced the "4-layer" framing (intro + ASCII note) with Layers 1-5, including sub-layers 2b (maxclass), 2c (package gating), 2d (community-extracted), signal_role-aware Layer 3, Layer 4b domain-restriction guard (VALID-02), and Layer 5 embedded GenExpr codebox walker (VALID-04).
- **Critic System**: added Layout Critic and Package Critic subsections and a Critic Loop note documenting all six wired critics; explicitly noted `m4l_critic.py` exists but is not called by `review_patch()`.
- Added a top-level **DSP Pre-Flight Simulation** section (public `run_simulation(...)` API, five classifier verdicts, three topologies, CLI with verdict-priority exit codes, max-dsp-agent test gate) and a TOC entry.
- **Memory System**: corrected to library-only — no auto-inject/write-back, manual `MemoryStore.read()`/`write()`.
- **Test Suite**: updated to 2,034 tests across 46 files and refreshed the coverage bullet list.

## Verification

Both plan `<automated>` verify blocks pass:

- README: `README_OK` (v5.0 row, 2026-05-01, 3,430, 2,034, ~18,600, signal metadata, dsp_sim, builders all present; no stale 3,434 remaining).
- TECHNICAL: `TECHNICAL_OK` (3,430, all three schema fields, all four builder names, dsp_sim/run_simulation, reed_bore_post_radiation, verdict names, Layer 5/4b/2c, Layout + Package critics, 2,034 all present).

Stale-figure sweep of TECHNICAL.md found no lingering `2,015`, `1,276`, `13,200`, `15,500`, `87`, or `4-layer` references. The only "four core layers" hit is the architecture-overview sentence, which correctly describes the architecture (not the validation pipeline) and was intentionally left.

Phase 31 builder signatures were checked against `src/maxpat/patcher.py` (lines 688, 775, 1234, 2088) and copied verbatim.

## Deviations from Plan

None — plan executed as written. One judgment call recorded in decisions: the generic "four core layers" architecture sentence was left intact since it does not make the "4-layer validation pipeline" claim item 6 targeted.

## Self-Check: PASSED

- README.md modified and committed (39802c2) — FOUND
- TECHNICAL.md modified and committed (2f28eb2) — FOUND
- No source-code files changed; `patches/.active-project.json` left unstaged as instructed.
