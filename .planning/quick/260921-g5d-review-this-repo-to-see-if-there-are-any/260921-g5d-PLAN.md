---
phase: quick-260921-g5d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/overrides.json
  - CLAUDE.md
  - .planning/quick/260921-g5d-review-this-repo-to-see-if-there-are-any/260921-g5d-REVIEW-FINDINGS.md
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03]
estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The object DB's drift against the installed Max 9.1.5 bundle is quantified with counts and named examples, not adjectives."
    - "Every object referenced by a committed .maxpat under patches/ is reported as either resolving in the DB or listed by name as unresolved."
    - "The Python test suite has a recorded pass/fail baseline before any fix and an identical-or-better result after every fix."
    - "Only maxref-unambiguous, non-behavioral fixes are applied; everything ambiguous or behavior-touching appears in the report as a recommendation instead."
    - "A prioritized findings report exists in the quick-task directory that a follow-up quick task can be cut from directly."
  artifacts:
    - ".planning/quick/260921-g5d-review-this-repo-to-see-if-there-are-any/260921-g5d-REVIEW-FINDINGS.md"
    - "scratchpad audit JSON (raw evidence backing every report claim)"
  key_links:
    - "maxref XML filenames in /Applications/Max.app/Contents/Resources/C74/docs/refpages/**/*.maxref.xml <-> object names keyed in .claude/max-objects/*/objects.json"
    - "ObjectDatabase.lookup()/audit_empty_io() <-> overrides.json deep-merge path"
    - "overrides.json edits <-> `python3 -m pytest -q` green baseline"
---

<objective>
Audit this repo against the live Max 9.1.5 installation and produce an evidence-backed, prioritized findings report, applying only the mechanical fixes that cannot change the behavior of working code.

Purpose: patch generation is only as reliable as the object DB and the toolchain behind it. Drift between the DB (extracted 2026-07-01) and the installed bundle is a silent-failure source — objects that resolve but cannot be connected, entries for objects that no longer exist, outlets typed wrong.

Output: `260921-g5d-REVIEW-FINDINGS.md` (must-fix / should-fix / nice-to-have, each with evidence + recommended action) plus a small set of verified `overrides.json` / doc corrections.

Requirement IDs (local to this quick task):
- REVIEW-01 — quantify DB drift vs installed Max 9.1.5 and patch-level object resolution
- REVIEW-02 — apply low-risk fixes only, with the test suite green before and after each
- REVIEW-03 — write the prioritized findings report
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Ground facts already established at planning time — do not re-derive:
- Installed Max is **9.1.5** (3db35fa476d), read from `/Applications/Max.app/Contents/Resources/../Info.plist` (`/Applications/Max.app/Contents/Info.plist`, key `CFBundleShortVersionString`).
- **Never run the Max binary to probe a version.** `/Applications/Max.app/Contents/MacOS/Max --version` launches the GUI and hangs.
- Core refpages: `/Applications/Max.app/Contents/Resources/C74/docs/refpages/{max-ref,msp-ref,jit-ref,m4l-ref}/*.maxref.xml` — 1175 files total under `refpages/`. Package refpages live under `/Applications/Max.app/Contents/Resources/C74/packages/*/docs/refpages/`. User packages: `~/Documents/Max 9/Packages/*/docs/refpages/`.
- DB extraction-log: `extraction_timestamp` 2026-07-01, 3430 objects, `empty_inlets_count` 270 / `empty_outlets_count` 293.
- `ObjectDatabase().audit_empty_io()` currently returns `critical`=9, `covered_by_override`=0, `variable_io_ok`=36, `by_source`=24 source labels.
- Test suite: `python3 -m pytest -q` from repo root, 2182 tests collected. No venv, no requirements file — system python3 (3.14.2) with pytest 9.0.2.
- `python3 -c "from src.maxpat.db_lookup import ObjectDatabase"` works from repo root with no PYTHONPATH tweak.
</context>

<constraints>
Hard rules for every task in this plan. Violating any of these is a failed task.

1. **Do not touch other work in the tree.** `patches/.active-project.json` and `patches/scala-synth/generated/scala-synth.maxpat` are modified by another instance's work. Never stage, commit, revert, or edit them.
2. **Never `git add .` or `git add -A`.** Stage only the explicit paths this plan names.
3. **`git stash` is prohibited** (CLAUDE.md Rule #7).
4. **No writes under `patches/`.** Patches are read-only evidence for this review.
5. **No new repo source files.** Audit scripts are throwaway and live in the scratchpad: `/private/tmp/claude-501/-Users-taylorbrook-Dev-MAX/5043f77d-441e-4a1c-a58a-ad34781e6570/scratchpad/`. Promoting the harness into `tools/` with tests is a *recommendation* for the report, not a change in this task.
6. **Stability over completeness.** Anything that changes the behavior of working code, is ambiguous in the maxref, or needs a judgment call goes into the report as a recommendation — not into the diff.
</constraints>

<tasks>

<task type="tracer">
  <name>Task 1: Audit harness — DB vs installed Max 9.1.5, patch resolution, toolchain baseline (REVIEW-01)</name>
  <files>
    /private/tmp/claude-501/-Users-taylorbrook-Dev-MAX/5043f77d-441e-4a1c-a58a-ad34781e6570/scratchpad/audit_db.py,
    /private/tmp/claude-501/-Users-taylorbrook-Dev-MAX/5043f77d-441e-4a1c-a58a-ad34781e6570/scratchpad/audit-260921-g5d.json,
    /private/tmp/claude-501/-Users-taylorbrook-Dev-MAX/5043f77d-441e-4a1c-a58a-ad34781e6570/scratchpad/pytest-baseline.txt
  </files>
  <precondition>/Applications/Max.app/Contents/Resources/C74/docs/refpages exists and contains .maxref.xml files. If absent, halt — the audit has no ground truth without the installed bundle.</precondition>
  <action>
Write ONE throwaway Python script in the scratchpad that reads the installed bundle and the repo and emits a single JSON evidence file. It is a read-only analysis script — it must not write anything inside the repo. Run it from the repo root so `from src.maxpat.db_lookup import ObjectDatabase` resolves.

The script produces these sections in `audit-260921-g5d.json`, each holding real names and counts (never just a count):

- `installed`: Max version string read from `/Applications/Max.app/Contents/Info.plist` (`CFBundleShortVersionString`, via `plistlib`), plus refpage file counts per refpages subdirectory (core `max-ref`/`msp-ref`/`jit-ref`/`m4l-ref`, each bundled package under `C74/packages/*/docs/refpages/`, and each user package under `~/Documents/Max 9/Packages/*/docs/refpages/` if that directory exists).
- `db_meta`: `extraction_timestamp` and `domain_counts` from `.claude/max-objects/extraction-log.json`, and the age in days between that timestamp and today. This is the drift headline.
- `missing_from_db`: object names that have a `.maxref.xml` in the installed bundle but do not resolve via `ObjectDatabase().lookup(name)`. Derive the candidate name from the filename by stripping the `.maxref.xml` suffix. Bucket by refpages source directory. Report the full sorted list per bucket plus the count.
- `absent_from_bundle`: object names keyed in the DB domain files that have no `.maxref.xml` anywhere under the installed bundle or user packages. Bucket by DB source file. These are the deletion/flag candidates — note in the JSON that absence of a refpage is suggestive, not proof (some objects genuinely ship without a refpage), so each entry also records whether an external binary or abstraction with a matching stem exists under `C74/externals/`, `C74/extras/`, `C74/packages/*/externals/`, or the user Packages equivalents.
- `empty_io`: the full `ObjectDatabase().audit_empty_io()` payload (all four keys including `by_source`), with each `critical` and `by_source` entry annotated with whether a matching `.maxref.xml` exists — an entry with a refpage is repairable from ground truth, one without is not.
- `patch_objects`: every object referenced by a committed `.maxpat` under `patches/`. Walk each file's JSON `boxes` recursively into nested `patcher` values, take each box's object name (the first token of `text` for `newobj`/`message`-style boxes, else the box's `maxclass`), and record for each distinct name whether it resolves via `lookup()` AND whether the resolved entry has non-empty inlets/outlets. Emit `unresolved` and `resolves_but_empty_io` lists with the patch file paths each name appeared in. Treat an empty-I/O hit as equivalent to missing (CLAUDE.md is explicit: they are indistinguishable at the connection site).
- `maxclass_check`: the set of `maxclass` values actually present across those same `.maxpat` files, split into those in `UI_MAXCLASSES` from `src/maxpat/maxclass_map.py` and those not. This is the evidence for whether `UI_MAXCLASSES` has gaps like the `jit.pwindow`/`jit.cellblock` fix in commit fc3aa27.
- `known_gaps_status`: for each DB gap CLAUDE.md asserts — `node.script` absent, `bach.llll2list` / `bach.list2llll` stale, `live.*` incompletely extracted — the current live truth: does `lookup()` return it, does it have non-empty I/O, and does a `.maxref.xml` or installed external exist. This confirms or retires each documented gap.

Then capture the toolchain baseline separately: run `python3 -m pytest -q` from the repo root and save the full output to `pytest-baseline.txt`. Record the pass/fail/xfail tallies. This is the "before" side of every fix in Task 2 — do not skip it and do not paraphrase it from memory; STATE.md's historical tallies are context, not the baseline.

Finally, do a documentation-drift read pass: for each helper, function, or path CLAUDE.md names (for example `ObjectDatabase`, `audit_empty_io`, `ensure_text_contrast`, `repair_text_contrast`, `apply_auto_styling`, `finalize_patch`, `save_patch_roundtrip`, `replace_box_safe`, `add_labeled_param_bank`, `add_overlay_readout`, `add_m4l_gen_synth`, `add_to_presentation`, `bring_to_front`, `send_to_back`, `set_z_index`, `MIN_CONTRAST_RATIO`, `UI_MAXCLASSES`, `db.audit_empty_io`, `tools/extract_pkg_io.py`, `scripts/audit_signal_role.py`), grep the repo for its definition and record present/absent in a `doc_drift` section. Use a positive grep for the definition site and record the result; do not gate on a bare count.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; python3 -c "import json,sys; d=json.load(open('/private/tmp/claude-501/-Users-taylorbrook-Dev-MAX/5043f77d-441e-4a1c-a58a-ad34781e6570/scratchpad/audit-260921-g5d.json')); req={'installed','db_meta','missing_from_db','absent_from_bundle','empty_io','patch_objects','maxclass_check','known_gaps_status','doc_drift'}; missing=req-set(d); sys.exit('MISSING SECTIONS: %s'%sorted(missing)) if missing else print('sections ok:', sorted(d)); print('max version:', d['installed'].get('version'))"</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; python3 -c "import re,sys,pathlib; t=pathlib.Path('/private/tmp/claude-501/-Users-taylorbrook-Dev-MAX/5043f77d-441e-4a1c-a58a-ad34781e6570/scratchpad/pytest-baseline.txt').read_text(); m=re.search(r'\d+ (passed|failed|error)', t); sys.exit('no pytest summary captured') if not m else print('baseline captured:', t.strip().splitlines()[-1])"</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; git status --porcelain | grep -vE '^ M (patches/\.active-project\.json|patches/scala-synth/generated/scala-synth\.maxpat)$' | grep -E '^' &amp;&amp; echo "UNEXPECTED REPO CHANGES ABOVE" &amp;&amp; exit 1 || echo "repo clean apart from pre-existing other-work modifications"</automated>
  </verify>
  <done>`audit-260921-g5d.json` exists with all nine sections populated from live reads of the Max 9.1.5 bundle and the repo; `pytest-baseline.txt` holds the full un-paraphrased suite output; the repo working tree carries no changes beyond the two pre-existing other-work modifications.</done>
</task>

<task type="auto">
  <name>Task 2: Apply only maxref-unambiguous fixes, test-verified after each (REVIEW-02)</name>
  <files>.claude/max-objects/overrides.json, CLAUDE.md</files>
  <precondition>Task 1's pytest baseline exists and its pass/fail tallies are known — a fix cannot be verified against an unknown "before".</precondition>
  <action>
Work strictly from the Task 1 evidence. A fix qualifies as low-risk ONLY if all four hold: (a) the installed maxref XML states the object's inlet/outlet count and types unambiguously, (b) the change is additive metadata in `overrides.json` or a factual doc correction, (c) it does not alter any code path's behavior, (d) it does not touch `patches/`.

Eligible fix categories, in priority order:

1. **Empty-I/O entries with a refpage.** For each `empty_io.critical` entry (and any `by_source` entry that a committed patch actually references, from Task 1's `patch_objects`) where the maxref gives unambiguous I/O, add an `overrides.json` entry populating `inlets` and `outlets` in the same shape the existing override entries use — read several existing entries first and match their field structure exactly, including whatever provenance fields they carry. Do not invent a new schema.
2. **Mis-typed MSP outlets.** Where Task 1 or the maxref shows a non-primary outlet documented as bang/float/int/index but typed as signal in the DB, correct the outlet type in `overrides.json`. CLAUDE.md is explicit that this class of bulk-extraction error is fixed in overrides, never by rerouting patches.
3. **Confirmed-stale entries.** For DB entries Task 1 proved absent from the installed bundle AND confirmed by an existing CLAUDE.md note (the bach `llll2list`/`list2llll` case), mark them in `overrides.json` using whatever "does not exist" convention the repo already has; if no such convention exists, do NOT invent one — record it as a should-fix recommendation for the report instead and leave the entry alone.
4. **Doc corrections in CLAUDE.md.** Only factual corrections backed by Task 1's `doc_drift` and `known_gaps_status`: a documented gap that Task 1 proves is already closed, a named helper that no longer exists, a version statement contradicted by the 9.1.5 bundle. Adding the confirmed installed version as a fact is in scope. Rewriting or reorganizing rules is NOT.

Everything else — `missing_from_db` bulk re-extraction, `UI_MAXCLASSES` additions, empty-I/O entries with no refpage ground truth, `node.script` admission to the DB, any change to a Python module — is out of scope for this task and becomes a report recommendation with a concrete proposed action.

Process discipline: after EACH fix (or each tight batch touching one file), re-run `python3 -m pytest -q` and compare against the Task 1 baseline. Any new failure means revert that fix immediately and demote it to a recommendation. Record in the scratchpad which fixes were applied and which were demoted, with the reason — Task 3 consumes that list.

Commit the applied fixes with an explicit path list. Stage only `.claude/max-objects/overrides.json` and `CLAUDE.md` — never a bare `git add .`. If zero fixes qualify, that is a legitimate outcome: make no commit and say so in the report.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; python3 -c "import json; json.load(open('.claude/max-objects/overrides.json')); print('overrides.json parses')"</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; python3 -m pytest -q 2>&amp;1 | tail -3</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; python3 -c "from src.maxpat.db_lookup import ObjectDatabase; a=ObjectDatabase().audit_empty_io(); print('critical:', len(a['critical']), a['critical'])"</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; git status --porcelain patches/ | grep -vE '^ M (patches/\.active-project\.json|patches/scala-synth/generated/scala-synth\.maxpat)$' | grep -E '^' &amp;&amp; echo "PATCHES TOUCHED — VIOLATION" &amp;&amp; exit 1 || echo "patches/ untouched by this task"</automated>
  </verify>
  <done>The pytest tally equals or betters the Task 1 baseline with zero new failures; `overrides.json` is valid JSON and matches the existing entry shape; `audit_empty_io()['critical']` is the same or smaller than the Task 1 value; `patches/` carries only the two pre-existing other-work modifications; applied and demoted fixes are both recorded with reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Write the prioritized findings report (REVIEW-03)</name>
  <files>.planning/quick/260921-g5d-review-this-repo-to-see-if-there-are-any/260921-g5d-REVIEW-FINDINGS.md</files>
  <action>
Write `260921-g5d-REVIEW-FINDINGS.md` in the quick-task directory, sourced entirely from the Task 1 JSON and the Task 2 applied/demoted list. Every claim carries its evidence inline — a count, a file path, a named object, or a maxref path. No adjective stands alone.

Structure:

- **Header** — installed Max 9.1.5 (with the plist path it came from), DB extraction date and age in days, test-suite baseline and post-fix tally, date of review.
- **Applied fixes** — a table of what changed, the maxref path that justified it, and the post-fix test result. If empty, say so explicitly.
- **Must-fix** — findings that currently cause or will cause silent patch-generation failures: unresolved objects referenced by committed patches, empty-I/O entries that a real patch already connects to, confirmed-stale entries still resolving via `lookup()`, any suite failure. Each with evidence plus a concrete recommended action scoped small enough to become one follow-up quick task.
- **Should-fix** — real drift with no live failure yet: the DB-vs-9.1.5 delta (both directions, with representative names and counts), `UI_MAXCLASSES` gaps, documented gaps that Task 1 proved closed or still open, `node.script` and the packages-shadowing problem, doc drift.
- **Nice-to-have** — process and tooling: promoting the scratchpad harness into `tools/` with tests so drift is measurable on demand; a re-extraction run against 9.1.5; anything that reduces future drift.
- **Explicitly not changed** — every fix demoted in Task 2 with its reason, and every category deliberately left out of scope (patches, Python modules, re-extraction). This is the section that makes the review safe to trust.

Each finding gets a stable ID (`MF-01`, `SF-01`, `NH-01`) so follow-up tasks can cite it. Sort within each tier by blast radius.

Commit the report by explicit path together with a STATE.md quick-task row for 260921-g5d. Stage only those two paths.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; python3 -c "
import pathlib, re, sys
p = pathlib.Path('.planning/quick/260921-g5d-review-this-repo-to-see-if-there-are-any/260921-g5d-REVIEW-FINDINGS.md')
t = p.read_text()
need = ['Must-fix', 'Should-fix', 'Nice-to-have', 'Explicitly not changed', '9.1.5']
miss = [n for n in need if n not in t]
sys.exit('MISSING: %s' % miss) if miss else None
ids = re.findall(r'\b(?:MF|SF|NH)-\d\d\b', t)
sys.exit('no finding IDs found') if not ids else print('report ok —', len(ids), 'findings:', sorted(set(ids)))
"</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; git log --oneline -1 -- .planning/quick/260921-g5d-review-this-repo-to-see-if-there-are-any/260921-g5d-REVIEW-FINDINGS.md | grep -E '.' || (echo "report not committed" &amp;&amp; exit 1)</automated>
    <automated>cd /Users/taylorbrook/Dev/MAX &amp;&amp; git status --porcelain | grep -vE '^ M (patches/\.active-project\.json|patches/scala-synth/generated/scala-synth\.maxpat)$' | grep -E '^' &amp;&amp; echo "UNCOMMITTED WORK REMAINS" &amp;&amp; exit 1 || echo "tree clean apart from pre-existing other-work modifications"</automated>
  </verify>
  <done>The report exists, is committed, carries all four tiers plus the applied-fixes table, states the installed 9.1.5 version and the DB age, gives every finding a stable ID with inline evidence, and lists every demoted fix with its reason. The working tree is clean apart from the two pre-existing other-work modifications.</done>
</task>

</tasks>

<threat_model>
Scope note: this task installs no packages (no npm/pip/cargo installs are in scope), integrates no external API, and touches no schema — the package-legitimacy, API-coverage, and schema-push gates do not fire. ASVS L1, block on `high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installed Max.app bundle -> repo DB | Read-only ingest of third-party XML/plist into data that steers future patch generation |
| Scratchpad audit script -> repo working tree | A throwaway script running with repo write access alongside another instance's uncommitted work |
| Findings report -> future executors | Report content becomes the authority for follow-up fix tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g5d-01 | Tampering | `.claude/max-objects/overrides.json` | high | mitigate | Only maxref-unambiguous additive entries; full `python3 -m pytest -q` compared against the Task 1 baseline after each fix; revert on any new failure |
| T-g5d-02 | Tampering | Another instance's uncommitted `patches/` files | high | mitigate | Explicit-path staging only, `git add .`/`-A` and `git stash` forbidden; a `git status --porcelain` allowlist check runs in every task's verify |
| T-g5d-03 | Denial of Service | Max.app GUI launch | medium | mitigate | Version read from `Info.plist` via `plistlib`; the Max binary is never executed |
| T-g5d-04 | Information Disclosure | Audit artifacts | low | accept | Audit JSON and pytest output stay in the session scratchpad; only the curated report enters the repo |
| T-g5d-05 | Repudiation | Findings report | medium | mitigate | Every finding carries inline evidence (path, count, object name) and a stable ID; demoted fixes are recorded with reasons |
| T-g5d-06 | Spoofing | Stale entries in the DB | medium | mitigate | Absence of a refpage is recorded as suggestive, not proof — each candidate also checks for an installed external/abstraction before any entry is flagged |
| T-g5d-SC | Tampering | package-manager installs | n/a | accept | No npm/pip/cargo install occurs in this task; system python3 + pytest already present |
</threat_model>

<verification>
- `python3 -m pytest -q` tally equals or betters the Task 1 baseline; zero new failures.
- `.claude/max-objects/overrides.json` parses as JSON and loads through `ObjectDatabase()`.
- `ObjectDatabase().audit_empty_io()['critical']` is <= 9 (the planning-time value).
- `git status --porcelain` shows nothing beyond `patches/.active-project.json` and `patches/scala-synth/generated/scala-synth.maxpat`.
- `260921-g5d-REVIEW-FINDINGS.md` exists, is committed, and every finding cites evidence.
</verification>

<success_criteria>
- DB drift against installed Max 9.1.5 is quantified in both directions with named examples.
- Every object used by a committed patch is classified: resolves / unresolved / resolves-but-empty-I/O.
- Applied fixes are maxref-backed, test-verified, and individually reversible.
- No working functionality changed; no file under `patches/` touched; no Python module modified.
- The report is actionable: each finding is scoped small enough to become one follow-up quick task.
</success_criteria>

<output>
Create `.planning/quick/260921-g5d-review-this-repo-to-see-if-there-are-any/260921-g5d-SUMMARY.md` when done.
</output>
