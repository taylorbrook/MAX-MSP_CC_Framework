---
phase: quick-260703-i0t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md
  - /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md (outside repo, not committed)
  - /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_*.md (30 deletions, outside repo)
autonomous: true
requirements: [QUICK-260703-i0t]
must_haves:
  truths:
    - "CLAUDE.md contains every agent-facing rule previously held only in feedback_*.md memories (D-01, D-02)"
    - "All 30 feedback_*.md bodies are archived verbatim in a committed repo file BEFORE any deletion (D-03)"
    - "The memory directory contains zero feedback_*.md files after execution; all 4 project_*.md files untouched (D-06)"
    - "MEMORY.md index has no lines pointing at deleted files and one new line pointing to project CLAUDE.md as the canonical MAX-rules surface (D-04)"
  artifacts:
    - ".planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md"
    - "CLAUDE.md (15 promoted nuances inserted into existing sections, D-05)"
  key_links:
    - "Archive commit MUST land before rm of memory files (deletions outside repo are unrecoverable)"
    - "Promotions (Task 2) MUST land before deletions (Task 3) so no rule ever exists on zero surfaces"
---

<objective>
De-duplicate CLAUDE.md against the 30 `feedback_*.md` MEMORY entries. CLAUDE.md becomes the single canonical rule surface (D-01). Each memory is classified DUPLICATE / PARTIAL / PERSONAL (D-02); all bodies are archived to a committed repo file before deletion (D-03); the MEMORY.md index is pruned and pointed at CLAUDE.md (D-04). CLAUDE.md edits are minimal insertions into existing sections only (D-05). `project_*.md` memories untouched (D-06).

Purpose: the same hard-won rules currently live on two surfaces; memories are private and not loaded by subagents, so drift means agents miss rules. One canonical surface eliminates drift.

Output: updated CLAUDE.md, committed archive file, cleaned memory directory + index.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

**Execution note (from CONTEXT.md):** Run WITHOUT worktree isolation — origin/main is stale, and the memory files live outside the repo where worktrees provide no isolation. Per CLAUDE.md Rule #7: never `git stash`; never `git add .` — stage explicit paths only.
</execution_context>

<context>
@./CLAUDE.md
@.planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-CONTEXT.md

Memory directory (outside repo): `/Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/`
</context>

## Classification Table (all 30 disk files — pre-derived, do NOT re-derive; D-02)

Verdicts: **DUP** = rule fully in CLAUDE.md → archive + delete. **PARTIAL** = nuance missing → apply promotion Pn (see Promotions appendix), then archive + delete. No PERSONAL files exist on disk (see anomaly note).

| # | File | Verdict | Rationale / promotion |
|---|------|---------|------------------------|
| 1 | feedback_assistance_comments.md | DUP | "Label inlets/outlets via the `comment` attribute" fully in Subpatcher Inlet/Outlet Access section |
| 2 | feedback_bach_no_llll2list.md | PARTIAL → P9 | No bach content anywhere in CLAUDE.md; agents will hit "No such object" again |
| 3 | feedback_bach_out_attr.md | PARTIAL → P9 | `@out t` rule absent from CLAUDE.md; same bach insertion |
| 4 | feedback_bpatcher_args.md | DUP | Standalone-token `#N` rule fully in Bpatcher section |
| 5 | feedback_bpatcher_numeric_args.md | PARTIAL → P8 | JSON-number-not-string args rule absent from Bpatcher section |
| 6 | feedback_buffer_info_query.md | PARTIAL → P5a | Core rule in MSP section; `peek~` + `uzi N` bridge alternative missing (one clause) |
| 7 | feedback_comment_no_hash_sub.md | DUP | Comment-boxes-don't-substitute + loadbang→message→set chain fully in Bpatcher section |
| 8 | feedback_db_empty_io.md | PARTIAL → P4a | Empty-I/O paragraph exists; missing: `by_source` key note + search-packages/overrides-before-adding-fresh rule |
| 9 | feedback_expr_no_clip.md | DUP | min(max(...)) rule verbatim in MSP section |
| 10 | feedback_floor_tilde_rnbo.md | DUP | trunc~/Gen~ fallback + check-domain rule verbatim in MSP section |
| 11 | feedback_gen_param_messages.md | DUP | Plain `param_name $1` (no @) + attrui fully in Gen~ section |
| 12 | feedback_genexpr_delay_syntax.md | DUP | Delay.read/write + init-before-if/else fully in Gen~ section |
| 13 | feedback_genexpr_io_syntax.md | DUP | `in1`/`out1` no-space rule fully in Gen~ section |
| 14 | feedback_git_stash_prohibited.md | PARTIAL → P3 | Rule #7 prohibits stash; missing: how to verify regressions without stashing |
| 15 | feedback_inlet_outlet_maxclass.md | DUP | No inlet~/outlet~ rule fully in Subpatcher section |
| 16 | feedback_layout_spacing.md | PARTIAL → P1 | 20px/15px in Rule #4; missing: ~5px companion gap (meter~ beside gain~) |
| 17 | feedback_line_tilde_comma.md | DUP | line~ single-list rule fully in MSP section |
| 18 | feedback_live_scope_tilde.md | PARTIAL → P4b | DB fix already landed; residual rule (grep patches/ before declaring object unknown) absent |
| 19 | feedback_m4l_no_gain.md | DUP | No-gain~-before-plugout~ fully in M4L section |
| 20 | feedback_m4l_param_connect.md | PARTIAL → P6 | param_connect pattern fully covered EXCEPT `parameter_initial_enable` missing from attr list (one-token fix) |
| 21 | feedback_maxclass_newobj.md | DUP | UI_MAXCLASSES-is-authoritative rule fully in Object Database section |
| 22 | feedback_maxpat_roundtrip_and_diffing.md | PARTIAL → P2 | Lossless to_dict / lossy Box.to_dict / git-show diffing absent from CLAUDE.md |
| 23 | feedback_message_box_width.md | PARTIAL → P1 | 8px/char + 25px chrome rendered-width formula absent (already in memory index, nowhere in CLAUDE.md) |
| 24 | feedback_msp_outlet_types.md | PARTIAL → P5b | Extraction bug history is stale; residual rule (stripped non-primary MSP outlet → fix overrides.json) absent |
| 25 | feedback_multislider_fetch.md | DUP | fetch/right-outlet/no-split all in Rule #4 multislider subsection |
| 26 | feedback_node_script_missing.md | PARTIAL → P7 | N4M section implies node.script is usable but it is NOT in the DB — conflicts with Rule #1 unless flagged |
| 27 | feedback_replace_box_orphans.md | DUP | Rule #8 covers orphaning + replace_box_safe in full |
| 28 | feedback_umenu_items_format.md | DUP | Comma-as-element format + runtime alternative fully in MSP section |
| 29 | feedback_waveguide_loop_phase_comp.md | PARTIAL → P10 | Zero waveguide-filter content in CLAUDE.md; 4-version debugging lesson, must survive |
| 30 | feedback_worktree_stash_danger.md | PARTIAL → P3 | Stash prohibition covered; missing: check `git stash list` after worktree merges |

**Anomaly:** `feedback_multiple_choice.md` appears in the MEMORY.md index but does NOT exist on disk (verified 2026-07-03). It is the one PERSONAL-class entry (presentation preference). Per D-02 personal entries stay untouched: leave its index line alone, do not create/delete anything for it, and note the dangling line in the SUMMARY.

<tasks>

<task type="auto">
  <name>Task 1: Archive all 30 memory bodies verbatim, commit before anything else</name>
  <files>.planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md</files>
  <action>
Create the archive file per D-03. Because bodies must be byte-verbatim (they contain their own YAML frontmatter and nested triple-backtick fences that the Write tool would have to escape fragilely), build it via bash concatenation of existing files — this is NOT a heredoc write, it is verbatim archiving:

MEM=/Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory; ARCH=/Users/taylorbrook/Dev/MAX/.planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md; { printf '# Archived feedback memories — quick-260703-i0t (2026-07-03)\n\nVerbatim bodies of the 30 feedback memory files deleted from the memory directory after promotion into CLAUDE.md (the canonical rule surface per D-01). Separator format: "===== filename =====".\n'; for f in "$MEM"/feedback_*.md; do printf '\n\n===== %s =====\n\n' "$(basename "$f")"; cat "$f"; done; } > "$ARCH"

Then commit ONLY this file: git add the archive path explicitly (never `git add .`) and commit with message `docs(quick-260703-i0t): archive 30 feedback memory bodies before dedup deletion`.

This commit MUST exist before Task 3 deletes anything — deletions outside the repo are unrecoverable.
  </action>
  <verify>
    <automated>test $(grep -c '^===== feedback_' /Users/taylorbrook/Dev/MAX/.planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md) -eq 30 && git -C /Users/taylorbrook/Dev/MAX log --oneline -1 -- .planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md | grep -q archive</automated>
  </verify>
  <done>Archive file contains exactly 30 "===== feedback_...=====" sections with verbatim bodies and is committed to the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the 15 PARTIAL nuances into CLAUDE.md (minimal insertions, D-05)</name>
  <files>CLAUDE.md</files>
  <action>
Apply promotions P1–P10 from the Promotions appendix below, using the Edit tool with the stated anchor lines. Insert ONLY the given text at the given anchors — no restructuring, no rewording of existing content, no section reordering (D-05). P9 adds a small new "### bach (package)" subsection under the existing "## Domain-Specific Rules" umbrella (inserting promoted nuance under an existing top-level section, permitted by D-05; flag in SUMMARY if the user should review).

After all 10 edits, commit ONLY CLAUDE.md with message `docs(quick-260703-i0t): promote 15 memory nuances into CLAUDE.md (canonical rule surface)`.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && grep -q '5px horizontal gap' CLAUDE.md && grep -q 'len(text) \* 8 + 25' CLAUDE.md && grep -q 'Box.to_dict()` is lossy' CLAUDE.md && grep -q 'git stash list' CLAUDE.md && grep -q 'by_source' CLAUDE.md && grep -q 'peek~` with `uzi N' CLAUDE.md && grep -q 'parameter_initial_enable' CLAUDE.md && grep -q 'node.script` is NOT in the object database' CLAUDE.md && grep -q 'JSON numbers, not strings' CLAUDE.md && grep -q 'bach (package)' CLAUDE.md && grep -q '@out t' CLAUDE.md && grep -q 'pd_samples' CLAUDE.md && grep -q 'non-primary outlet' CLAUDE.md && grep -q 'declaring an object unknown' CLAUDE.md</automated>
  </verify>
  <done>All 10 promotion edits present in CLAUDE.md; existing sections otherwise byte-identical; CLAUDE.md committed.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the 30 memory files and update MEMORY.md index (D-04, D-06)</name>
  <files>/Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md</files>
  <action>
Pre-condition: Tasks 1 and 2 committed (verify with git log before deleting).

1. Delete exactly the 30 feedback files: `rm /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_*.md`. Do NOT touch any `project_*.md` file (D-06). Note: `feedback_multiple_choice.md` does not exist on disk, so the glob covers exactly the 30 archived files — confirm the glob matched 30 before running rm (e.g. `ls ... | wc -l`).
2. Edit MEMORY.md (Edit tool, outside repo — no commit possible or needed):
   - Remove the 30 index lines for the deleted files (every line linking a feedback_*.md EXCEPT the `feedback_multiple_choice.md` line, which stays untouched per D-02/PERSONAL even though its file is missing — record the dangling-line anomaly in the SUMMARY instead of resolving it unilaterally).
   - Keep all project_*.md lines untouched.
   - Add one line at the top of the list: `- MAX object/patching/workflow rules are canonical in the project CLAUDE.md (/Users/taylorbrook/Dev/MAX/CLAUDE.md); 30 memory entries were promoted/archived there on 2026-07-03 (archive: .planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md)`
3. Nothing in this task lives inside the repo, so no git commit here; the quick-task SUMMARY commit closes out the task.
  </action>
  <verify>
    <automated>test $(ls /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_*.md 2>/dev/null | wc -l) -eq 0 && test $(ls /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_*.md | wc -l) -eq 4 && test $(grep -c 'feedback_' /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md) -eq 1 && grep -q 'canonical in the project CLAUDE.md' /Users/taylorbrook/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md</automated>
  </verify>
  <done>Zero feedback_*.md files remain; 4 project_*.md files intact; MEMORY.md has exactly one remaining feedback_ reference (the untouched multiple_choice line) plus the new CLAUDE.md canonical-pointer line.</done>
</task>

</tasks>

## Promotions Appendix (exact insertion payloads for Task 2)

Each promotion states the ANCHOR (existing CLAUDE.md text to locate) and the INSERT text. Insert text verbatim.

### P1 — Rule #4 Patch Style (layout_spacing #16 + message_box_width #23)

ANCHOR: the bullet `- Standard object spacing: ~20px vertical, ~15px horizontal gutter (matches defaults.py; user-confirmed tight spacing)` — insert immediately AFTER it:

```
- Companion pairs (e.g. `meter~` beside `gain~`) sit side-by-side at ~5px horizontal gap, not stacked below (companion detection lives in defaults.py)
- MAX renders message boxes wider than their stored `patching_rect` width. For overlap-free layout, estimate rendered width as `len(text) * 8 + 25` px of chrome and keep at least 8px between boxes — never rely on `patching_rect[2]` for spacing
```

### P2 — Rule #5 (maxpat_roundtrip_and_diffing #22)

ANCHOR: end of Rule #5, after the sentence ending `It was deprecated in milestone 2.0.` — insert as a new paragraph:

```
`Patcher.from_dict` -> `Patcher.to_dict()` round-trips are lossless (raw box JSON is preserved in `box._raw` and merged back on serialize); the standalone `Box.to_dict()` is lossy (drops `extra_attrs`/`_raw`, recomputes inlet counts, mangles message text) — never serialize a single Box directly, always go through `Patcher.to_dict()`. When investigating a suspected `.maxpat` regression, diff committed git objects (`git show A:file` vs `git show B:file`), never the working tree — a working-tree copy can be a transient degraded save from another instance and produce phantom diffs.
```

### P3 — Rule #7 (git_stash_prohibited #14 + worktree_stash_danger #30)

ANCHOR: end of the paragraph beginning `**Prohibited:** \`git stash\` during any patch workflow.` (after `...commit your current work first.`) — insert as a continuation of that paragraph or a new paragraph directly after:

```
To verify whether a change caused a failure, check out a prior commit into a temp worktree or run tests against `git show HEAD~N:path` — never stash. After any worktree merge, check `git stash list` for orphaned stashes: a `stash && merge && pop` chain short-circuits on conflict and silently strands uncommitted work.
```

### P4 — Object Database section (db_empty_io #8 = P4a, live_scope_tilde #18 = P4b)

ANCHOR: the paragraph beginning `**Verify lookup results have non-empty I/O.**`, at its end (after `...not just \`exists()\`.`) — insert these two sentences at the end of that paragraph:

```
The `by_source` key of `audit_empty_io()` enumerates empty-I/O gaps per domain/package file. Before adding an object fresh to a domain file, search the `packages/` subdirectories and `overrides.json` first — it may already exist with partial data worth merging (same-named package entries can shadow populated core entries). Before declaring an object unknown, also grep existing `.maxpat` files under `patches/` — `live.*` UI objects were incompletely extracted, and user-confirmed objects should be added to the appropriate domain file so future lookups succeed.
```

### P5 — MSP section (buffer_info_query #6 = P5a, msp_outlet_types #24 = P5b)

P5a ANCHOR: the MSP bullet about `buffer~` — extend the sentence ending `or \`jit.buffer~\` + \`jit.matrixinfo\` for dimensions` to read `or \`jit.buffer~\` + \`jit.matrixinfo\` for dimensions, or drive \`peek~\` with \`uzi N\` when an upper bound on length is known`.

P5b ANCHOR: insert a new bullet in the MSP list, directly after the `buffer~` bullet:

```
- If a control connection from an MSP object's non-primary outlet gets stripped by validation, the DB likely mis-marks that outlet as signal (bulk-extraction bug — many MSP right outlets are actually bangs/floats/indices). Fix the outlet types in `overrides.json` rather than rerouting the patch.
```

### P6 — M4L section (m4l_param_connect #20)

ANCHOR: in the M4L bullet listing the `saved_attribute_attributes.valueof` block, the parenthetical list beginning `(\`parameter_initial\`,` — insert `\`parameter_initial_enable\`, ` immediately after `` `parameter_initial`, `` so the list reads `(\`parameter_initial\`, \`parameter_initial_enable\`, \`parameter_longname\`, ...)`.

### P7 — Node for Max section (node_script_missing #26)

ANCHOR: first bullet of the `### Node for Max (N4M / node.script)` section — insert this bullet BEFORE it:

```
- **`node.script` is NOT in the object database** — per Rule #1 it cannot be used in generated patches until it is added (with verified I/O) to the DB. For tasks that would naturally use N4M (file I/O, binary parsing, npm libraries), prefer a build-time Python data tool that emits `coll`/`dict` data files the patch loads with verified objects — this is not a Rule #5 violation (Rule #5 only forbids regenerating `.maxpat` files).
```

### P8 — Bpatcher section (bpatcher_numeric_args #5)

ANCHOR: after the paragraph ending `This applies equally to newobj boxes and message boxes (\`set #1\`, \`setbuffer #1\`).` and before the `**Comment boxes do NOT perform...**` paragraph — insert:

```
**Numeric bpatcher args must be serialized as JSON numbers, not strings.** `"args": ["slot-1", "0"]` makes MAX substitute `#2` as the quoted symbol `0`, and object boxes like `-~ #2` fail with "bad arguments creating object" (once per instance). Pass Python ints — `args=[slot, 0]`, not `args=[slot, "0"]`; symbol args (e.g. `"slot-1"`) stay strings. To fix an existing patch surgically, convert numeric strings in `box._raw["args"]` (post-round-trip `box.args` is empty) and `save_patch_roundtrip`.
```

### P9 — new bach subsection (bach_no_llll2list #2 + bach_out_attr #3)

ANCHOR: insert after the end of the `### Max for Live (M4L / .amxd)` subsection (its last bullet, ending `...remaining Live-automatable.`) and before `## PD Confusion Guard`:

```
### bach (package)

- `bach.llll2list` and `bach.list2llll` do NOT exist in the installed bach package — the DB entries are stale; patches fail at load with "No such object". Extract sub-llll values with `bach.nth N` (1-based, one call per index); add `@unwrap 1` when the extracted element is itself a sublist. MAX lists connect directly to bach inlets in flat cases; for complex conversions use `bach.flat`, `bach.iter`, or `bach.pack`.
- Every bach object's `@out` attribute defaults to `n` (native), which plain MAX objects (`f`, `select`, `unpack`, `expr`) reject with "wrong message or type". Set `@out t` (text) on any bach object whose outlet feeds a non-bach consumer; the attribute string has one char per llll outlet. In text format a flat sublist emerges as a regular MAX list, so `unpack f f` works directly.
```

### P10 — Gen~ section (waveguide_loop_phase_comp #29)

ANCHOR: end of the `### Gen~ (GenExpr DSP Code)` bullet list (after the `**Setting Param values from MAX:**` bullet) — insert two bullets:

```
- **Waveguide loop filters:** resonant filters (Q >= ~1.5) go POST-LOOP (radiation/output path), never inside the feedback loop — an in-loop resonance competes with the bore's self-excited mode and pitch locks/jumps to the filter fc; no phase compensation fixes it. This includes "source-side" filters between a driver nonlinearity and the delay-line injection. Low-Q (Q <= ~1) filters may stay in-loop but must subtract analytic **phase delay** (-phi(w)/w) — never group delay (overshoots 3x+ for resonant sections) — from the target period. Onepole (b = 1-a): `pd_samples = atan(b*sin(w) / (1 - b*cos(w))) / max(w, 0.0001)`. For biquads, evaluate B(e^jw)/A(e^jw) as complex numbers, take the atan2 phase difference, unwrap past -pi, clamp compensation to a sane range (e.g. [0, 64] samples), and share coefficients between the filter and the comp block (full derivation: `.planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-archived-memories.md`).
- **Pre-flight any new in-loop waveguide filter with a numpy simulation** before committing: sweep the filter's controlling Param across its full range at several target freqs and measure the output fundamental (autocorrelation/FFT). If pitch moves more than a few cents across the sweep, the architecture is wrong. Post-loop filters need no compensation.
```

<verification>
- Archive committed BEFORE deletions (git log ordering: archive commit precedes SUMMARY commit; rm happens only in Task 3).
- CLAUDE.md diff contains ONLY the 10 promotion insertions (review `git diff HEAD~1 -- CLAUDE.md` after Task 2 — no deleted lines except where P5a/P6 extend an existing line in place).
- Memory dir: 0 feedback files, 4 project files, MEMORY.md pruned + canonical pointer added.
</verification>

<success_criteria>
- Every rule from the 30 feedback memories exists in exactly one live surface: CLAUDE.md (15 already covered, 15 promoted).
- Full verbatim bodies recoverable from the committed archive file.
- MEMORY.md index consistent with disk except the pre-existing dangling `feedback_multiple_choice.md` line (left untouched per PERSONAL classification; anomaly recorded in SUMMARY).
- Two repo commits (archive, CLAUDE.md) with explicit-path staging; no `git stash`, no `git add .`.
</success_criteria>

<output>
Create `.planning/quick/260703-i0t-de-duplicate-claude-md-against-the-30-fe/260703-i0t-SUMMARY.md` when done. Record: per-verdict counts (15 DUP / 15 PARTIAL / 0 PERSONAL on disk), the `feedback_multiple_choice.md` dangling-index anomaly, and the P9 new-subsection judgment call for user review.
</output>
