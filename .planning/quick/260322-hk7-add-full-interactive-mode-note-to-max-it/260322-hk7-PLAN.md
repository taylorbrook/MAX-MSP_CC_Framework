---
phase: quick-260322-hk7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/max-iterate.md
autonomous: true
requirements: [QUICK]
must_haves:
  truths:
    - "--full flag description mentions it requires interactive mode"
    - "Examples section notes that --full/--discuss/--plan need interactive mode"
  artifacts:
    - path: ".claude/commands/max-iterate.md"
      provides: "Updated flag documentation with interactive mode note"
      contains: "interactive"
  key_links: []
---

<objective>
Add interactive mode requirement note to --full flag documentation in max-iterate command.

Purpose: Users need to know that --full (and --discuss/--plan individually) requires interactive mode because those phases wait for user input. Running them in non-interactive contexts will hang.
Output: Updated .claude/commands/max-iterate.md
</objective>

<context>
@.claude/commands/max-iterate.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive mode notes to max-iterate.md</name>
  <files>.claude/commands/max-iterate.md</files>
  <action>
Make two edits to `.claude/commands/max-iterate.md`:

1. **Flags table** (line ~144): Update the `--full` row description to note interactive mode requirement. Also add a note row or paragraph below the table. Updated table should read:

| Flag | Description |
|------|-------------|
| --full | Run full pipeline: discuss -> research -> plan -> build (requires interactive mode) |
| --discuss | Run discuss phase only before build (requires interactive mode) |
| --research | Run research phase only before build |
| --plan | Run plan phase only before build (requires interactive mode) |

Add a note after "Flags are stripped from the change description before processing." (line 151):

"**Note:** `--full`, `--discuss`, and `--plan` require interactive mode because the discuss and plan phases wait for user input. `--research` can run non-interactively."

2. **Examples section** (around lines 165-167 where --full examples are): Add a comment line in the full pipeline examples block noting interactive mode:

```
# Full pipeline (requires interactive mode -- discuss and plan wait for user input):
```

Replace the existing comment "# Full pipeline (discuss + research + plan before edit):" with the above.
  </action>
  <verify>
    <automated>grep -c "interactive" .claude/commands/max-iterate.md | grep -q "[3-9]" && echo "PASS: interactive mentioned 3+ times" || echo "FAIL"</automated>
  </verify>
  <done>The --full flag description, the flags note paragraph, and the examples section all mention interactive mode requirement. --research is noted as the exception that can run non-interactively.</done>
</task>

</tasks>

<verification>
grep "interactive" .claude/commands/max-iterate.md shows mentions in: flag table (--full, --discuss, --plan rows), note paragraph, and examples comment.
</verification>

<success_criteria>
- --full, --discuss, and --plan flag descriptions note "requires interactive mode"
- A note paragraph explains WHY (discuss and plan phases wait for user input)
- --research is NOT marked as requiring interactive mode
- Examples section comment updated to mention interactive mode
</success_criteria>

<output>
After completion, create `.planning/quick/260322-hk7-add-full-interactive-mode-note-to-max-it/260322-hk7-SUMMARY.md`
</output>
