---
phase: quick-260401-j8z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/overrides.json
  - .claude/max-objects/msp/objects.json
autonomous: true
requirements: [FIX-INFO-OUTLETS]

must_haves:
  truths:
    - "ObjectDatabase.lookup('info~') returns exactly 10 outlets"
    - "Outlet types match real .maxpat outlettype array: float, list, float, float, float, float, float, empty, int, empty"
    - "Outlet order matches Cycling74 docs: SR, instrument info, sustain loop start/end, release loop start/end, total time, filename, channels, state"
    - "Inlet is control (not signal) — info~ responds to bang, not audio"
  artifacts:
    - path: ".claude/max-objects/overrides.json"
      provides: "Corrected info~ override with 10 outlets"
      contains: "info~"
    - path: ".claude/max-objects/msp/objects.json"
      provides: "Corrected info~ base entry with 10 outlets"
      contains: "info~"
  key_links:
    - from: ".claude/max-objects/overrides.json"
      to: "ObjectDatabase"
      via: "deep merge onto base objects"
      pattern: "info~.*outlets.*10"
---

<objective>
Fix the info~ object entry in the MSP database. The override currently has 5 outlets with wrong order/content, and the base JSON has 10 outlets but all incorrectly typed as signal. Both need correction to match the real 10-outlet layout per Cycling '74 docs.

Purpose: info~ is used in buffer~/groove~ workflows (e.g., rhythmic-sampler). Wrong outlet count/types causes incorrect connections when agents generate patches.
Output: Corrected info~ in both overrides.json and msp/objects.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/max-objects/overrides.json (lines 2950-3030 — current info~ override with 5 wrong outlets)
@.claude/max-objects/msp/objects.json (info~ entry — 10 outlets all incorrectly typed as signal)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix info~ outlets in overrides.json (primary) and msp/objects.json (base)</name>
  <files>.claude/max-objects/overrides.json, .claude/max-objects/msp/objects.json</files>
  <action>
Replace the info~ entry in overrides.json with the correct 10 outlets matching this exact order and types from Cycling '74 docs:

```json
"outlets": [
  {"id": 0, "type": "float",  "signal": false, "digest": "Sample rate (float)"},
  {"id": 1, "type": "list",   "signal": false, "digest": "Instrument info / MIDI pitch (list)"},
  {"id": 2, "type": "float",  "signal": false, "digest": "Sustain loop start in ms (float)"},
  {"id": 3, "type": "float",  "signal": false, "digest": "Sustain loop end in ms (float)"},
  {"id": 4, "type": "float",  "signal": false, "digest": "Release loop start in ms (float)"},
  {"id": 5, "type": "float",  "signal": false, "digest": "Release loop end in ms (float)"},
  {"id": 6, "type": "float",  "signal": false, "digest": "Total time of buffer in ms (float)"},
  {"id": 7, "type": "",       "signal": false, "digest": "Filename of most recently read audio file (symbol)"},
  {"id": 8, "type": "int",    "signal": false, "digest": "Number of channels (int)"},
  {"id": 9, "type": "",       "signal": false, "digest": "State / valid flag"}
]
```

Also update the `_note` to: "info~ has 10 control outlets (not signal). Order per Cycling74 docs."

Keep the existing `inlets` override (control, not signal) — that part is correct.

Update the `_manual_original` block to match the new outlets for consistency.

Then in msp/objects.json, update the info~ base entry outlets to match the same 10-outlet structure (with `signal: false` and correct types). Also fix the inlet from `signal: true` to `signal: false` and `type: ""` (info~ takes bangs, not audio).

Use Python to load, modify, and write both JSON files to preserve formatting.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -c "
from src.maxpat.db_lookup import ObjectDatabase
db = ObjectDatabase()
obj = db.lookup('info~')
outlets = obj['outlets']
assert len(outlets) == 10, f'Expected 10 outlets, got {len(outlets)}'
assert outlets[0]['type'] == 'float' and outlets[0]['digest'].startswith('Sample rate'), f'Outlet 0 wrong: {outlets[0]}'
assert outlets[1]['type'] == 'list', f'Outlet 1 should be list: {outlets[1]}'
assert outlets[6]['type'] == 'float' and 'Total time' in outlets[6]['digest'], f'Outlet 6 wrong: {outlets[6]}'
assert outlets[7]['type'] == '' and 'Filename' in outlets[7]['digest'], f'Outlet 7 wrong: {outlets[7]}'
assert outlets[8]['type'] == 'int', f'Outlet 8 should be int: {outlets[8]}'
assert outlets[9]['type'] == '' and 'State' in outlets[9]['digest'], f'Outlet 9 wrong: {outlets[9]}'
for o in outlets:
    assert o['signal'] == False, f'Outlet {o[\"id\"]} should not be signal'
types = db.get_outlet_types('info~')
assert types == ['float','list','float','float','float','float','float','','int',''], f'Outlet types wrong: {types}'
inlet = obj['inlets'][0]
assert inlet['signal'] == False, f'Inlet should not be signal'
print('ALL CHECKS PASSED: 10 outlets, correct types, correct order, control-rate')
"
    </automated>
  </verify>
  <done>ObjectDatabase.lookup('info~') returns 10 outlets with correct types matching ["float","list","float","float","float","float","float","","int",""], all signal=false, inlet is control not signal.</done>
</task>

</tasks>

<verification>
- `python3 -c "from src.maxpat.db_lookup import ObjectDatabase; db = ObjectDatabase(); print(len(db.lookup('info~')['outlets']))"` returns 10
- `python3 -c "from src.maxpat.db_lookup import ObjectDatabase; db = ObjectDatabase(); print(db.get_outlet_types('info~'))"` returns `['float', 'list', 'float', 'float', 'float', 'float', 'float', '', 'int', '']`
- Existing tests still pass: `python3 -m pytest tests/ -x -q`
</verification>

<success_criteria>
info~ entry in both overrides.json and msp/objects.json has exactly 10 control-rate outlets in the correct order per Cycling '74 documentation. ObjectDatabase returns the corrected data.
</success_criteria>

<output>
After completion, create `.planning/quick/260401-j8z-fix-info-object-outlets-in-msp-database-/260401-j8z-01-SUMMARY.md`
</output>
