---
id: 260419-vy7
type: quick-full
status: active
description: Populate I/O schemas for 11 critical empty-schema objects in overrides.json
date: 2026-04-20
must_haves:
  truths:
    - Override entries present for all 11 target objects (bpatcher, funnel, expr, expr~, codebox, codebox~, pan, pan~, xfade, xfade~, waveform~)
    - Each override entry has non-empty inlets array with required fields {id, type, signal, hot, digest}
    - Each override entry has non-empty outlets array with required fields {id, type, signal, digest}
    - Each entry carries an _audit block with source, confidence=HIGH, date=2026-04-19
    - variable_io_rules entries exist for expr, expr~, codebox, codebox~, pan, pan~, bpatcher
    - Validation command reports non-zero inlet+outlet count for all 11 objects
  artifacts:
    - .claude/max-objects/overrides.json (modified)
  key_links:
    - .claude/max-objects/overrides.json
    - /Applications/Max.app/Contents/Resources/C74/docs/refpages/ (authoritative source)
    - src/maxpat/db_lookup.py (validation target)
---

# Quick Task 260419-vy7: Populate I/O for 11 Critical Empty-Schema Objects

## Description

11 core objects in the MAX object database currently have empty `inlets`/`outlets` arrays, making them effectively unusable by the patch builder's I/O-aware code paths (validation, layout, connection checks). Populate authoritative overrides in `overrides.json` so every target reports a non-zero I/O schema.

## Scope

Target objects (verified real, verified empty after domain merge):

| Object | Current I/O | Source domain | Strategy |
|--------|-------------|---------------|----------|
| bpatcher | 0/0 | max | variable_io inherited_from_subpatch, default 1/1 |
| funnel | 0/0 | max | variable_io first_arg/fixed:1, default 2/1 |
| expr | 1/1 | max | variable_io ($N count), default 1/1 — verified against max refpage |
| expr~ | 0/0 | rnbo | variable_io ($N count), default 1/1 signal |
| codebox | 0/0 | rnbo | variable_io (in/out decls), default 1/1 |
| codebox~ | 0/0 | rnbo | variable_io (in~/out~ decls), default 1/1 signal |
| pan | 0/0 | rnbo | 2 inlets / 2 outlets (L, R) |
| pan~ | 0/0 | rnbo | 2 signal inlets / 2 signal outlets |
| xfade | 0/0 | rnbo | 3 inlets / 1 outlet |
| xfade~ | 0/0 | rnbo | 3 signal inlets / 1 signal outlet |
| waveform~ | 5/6 | msp | confirms msp values; 5/6 per refpage |

## Tasks

### Task 1: Apply overrides to overrides.json

**Files:** `.claude/max-objects/overrides.json`

**Action:** Add override entries under `objects` key for 11 target objects. Add variable_io_rules entries for new variable_io cases (expr, expr~, codebox, codebox~, pan, pan~, bpatcher).

**Authoritative sources:**
- MAX refpages under `/Applications/Max.app/Contents/Resources/C74/docs/refpages/` for bpatcher, funnel, expr, waveform~
- Task description spec for RNBO objects (codebox/pan/xfade families) — RNBO docs lack standalone refpages in installed app

**Verify:** Run the validation command — every object must report non-zero inlets and outlets.

```bash
python3 -c "from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); [print(n, len(db.lookup(n)['inlets']), len(db.lookup(n)['outlets'])) for n in ['bpatcher','funnel','expr','expr~','codebox','codebox~','pan','pan~','xfade','xfade~','waveform~']]"
```

**Done when:** validation output shows all 11 objects with >0 inlets and >0 outlets.

### Task 2: Atomic commit

**Files:** `.claude/max-objects/overrides.json`

**Action:** Commit the single overrides.json change atomically with message: `db: populate I/O for 11 critical empty-schema objects`

**Verify:** `git log -1 --stat` shows one file changed.
