---
phase: quick-260422-b0d
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - QUICK-260422-b0d-01
must_haves:
  truths:
    - "README object count reflects 3,434 objects"
    - "README package count reflects 29 packages"
    - "README test count reflects 1,589 tests"
    - "README patches table lists all 18 projects in patches/ (adds bassoon-model, intelligent-corpus-remixer, physics-composition, rhythmic-corpus-chopper)"
    - "Milestone table is unchanged"
    - "No prose/feature-list sections were rewritten"
  artifacts:
    - path: "README.md"
      provides: "Accurate stats and patch listing aligned with current repo state"
      contains: "3,434"
  key_links:
    - from: "README.md stats (object/package/test counts)"
      to: "actual repo state (.claude/max-objects/, patches/, pytest --collect-only)"
      via: "surgical Edit tool replacements"
      pattern: "3,434|29 packages|1,589 tests"
---

<objective>
Refresh stale statistics in README.md and add four missing patch projects to the patches table. All edits are surgical string replacements — no prose rewrites, no new feature bullets, no milestone-table changes.

Purpose: Keep README accurate after recent DB growth (new packages + objects), test-suite growth, and four new patch projects landed since v4.0.

Output: Updated `README.md` with corrected counts (3,434 objects, 29 packages, 1,589 tests) and 4 new rows in the Patches table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@CLAUDE.md

<!-- Pre-investigation already verified numbers. Re-run before editing to confirm nothing drifted between investigation and execution. -->

Verification commands (run once before editing to confirm current numbers match the plan):

```bash
# Object count (expected: 3,434)
python3 -c "import json, glob; print(sum(len(json.load(open(f))) for f in glob.glob('.claude/max-objects/**/objects.json', recursive=True)))"

# Package count (expected: 29)
ls .claude/max-objects/packages/ | wc -l

# Test count (expected: 1,589)
python3 -m pytest --collect-only -q 2>/dev/null | tail -3

# Verify the 4 new patch projects exist
ls patches/bassoon-model patches/intelligent-corpus-remixer patches/physics-composition patches/rhythmic-corpus-chopper
```

If any number drifts, use the fresh number in the edit (not the plan's number).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update stale counts (objects, packages, tests)</name>
  <files>README.md</files>
  <action>
Apply five surgical Edit replacements to refresh stale statistics. Verify each target string appears EXACTLY ONCE in the file before replacing (use Grep if unsure — some strings are similar but not identical).

**Edit A — line 13 (Features bullet):**
- FIND: `**2,450-object knowledge base** — verified database covering MAX, MSP, Jitter, MC, Gen~, Max for Live, RNBO, and 20 packages`
- REPLACE: `**3,434-object knowledge base** — verified database covering MAX, MSP, Jitter, MC, Gen~, Max for Live, RNBO, and 29 packages`

**Edit B — line 69 (Quick Start step 2):**
- FIND: `**Research** — looks up the best MAX objects, patterns, and techniques from the 2,450-object database`
- REPLACE: `**Research** — looks up the best MAX objects, patterns, and techniques from the 3,434-object database`

**Edit C — line 168 (How It Works → Object Database):**
- FIND: `**Object Database** — A verified knowledge base of 2,450 MAX objects (`.claude/max-objects/`) across 8 core domains plus 20 packages`
- REPLACE: `**Object Database** — A verified knowledge base of 3,434 MAX objects (`.claude/max-objects/`) across 8 core domains plus 29 packages`

**Edit D — line 181 (Project Structure comment):**
- FIND: `│   ├── max-objects/        # Object database (2,450 objects across 8 domains + 20 packages)`
- REPLACE: `│   ├── max-objects/        # Object database (3,434 objects across 8 domains + 29 packages)`

**Edit E — line 185 (Project Structure tests comment):**
- FIND: `├── tests/                  # Test suite (1,545 tests)`
- REPLACE: `├── tests/                  # Test suite (1,589 tests)`

Do NOT touch the milestone table (line 229 still says "2,450 objects" in the v4.0 row — that is a historical snapshot of what shipped at v4.0 and must be preserved).

Per D-01 (pre-investigation): apply targeted edits only, preserve all other prose.
  </action>
  <verify>
    <automated>grep -c "3,434" README.md</automated>
  </verify>
  <done>
- `grep -c "3,434" README.md` returns 3 (features bullet, quick-start step 2, how-it-works, project-structure — wait, that's 4 actually. Run `grep -n "3,434" README.md` to confirm all 4 Edit A–D sites are updated.)
- `grep -c "2,450" README.md` returns 1 (only the v4.0 milestone row retains it as a historical stat)
- `grep -c "29 packages" README.md` returns 3 (features bullet, how-it-works, project-structure)
- `grep -c "20 packages" README.md` returns 0
- `grep -c "1,589 tests" README.md` returns 1
- `grep -c "1,545 tests" README.md` returns 0
- Milestone table row for v4.0 still reads `Package-aware DB (2,450 objects)` — unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 4 new patch projects to Patches table</name>
  <files>README.md</files>
  <action>
Add four new rows to the Patches table (lines 145–160). Insert rows in alphabetical order to match the existing table's ordering:

- `bassoon-model` goes BEFORE `FDNVerb` (alphabetical: b < F case-insensitive — but existing table uses case-insensitive alpha order: FDNVerb, gen-eq, gong-model, granularsynthtest… so `bassoon-model` goes first)
- `intelligent-corpus-remixer` goes BETWEEN `gong-model` and `granularsynthtest`? Check: g-o-n-g < g-r-a-n < i-n-t, so `intelligent-corpus-remixer` goes AFTER `granularsynthtest` and BEFORE `kicksynth`
- `physics-composition` goes BETWEEN `performancepatchtest` and `rhythmic-sampler` (p-e-r < p-h-y < r)
- `rhythmic-corpus-chopper` goes BEFORE `rhythmic-sampler` (chopper < sampler alphabetically)

Preserve exact column format: `| **name** | Description | .ext1, .ext2 |` with bold project name and backtick-wrapped file extensions.

**Edit A — insert `bassoon-model` row BEFORE FDNVerb row:**
- FIND: `| **FDNVerb** | Feedback delay network reverb with 8 delay lines, Hadamard matrix, decay/diffusion/damping/freeze controls | \`.maxpat\`, \`.gendsp\` |`
- REPLACE: `| **bassoon-model** | Physical model of a bassoon with conical waveguide and reed nonlinearity for high-accuracy microtonal pitch | \`.maxpat\`, \`.gendsp\` |
| **FDNVerb** | Feedback delay network reverb with 8 delay lines, Hadamard matrix, decay/diffusion/damping/freeze controls | \`.maxpat\`, \`.gendsp\` |`

**Edit B — insert `intelligent-corpus-remixer` row BETWEEN granularsynthtest and kicksynth:**
- FIND: `| **granularsynthtest** | Granular synthesizer with Gen~ DSP engine and MC multichannel output for flexible speaker arrays | \`.maxpat\`, \`.gendsp\` |
| **kicksynth** | Kick drum synthesizer with Gen~ pitch envelopes, click/sub/noise layers, and drive/saturation | \`.maxpat\`, \`.js\` |`
- REPLACE: `| **granularsynthtest** | Granular synthesizer with Gen~ DSP engine and MC multichannel output for flexible speaker arrays | \`.maxpat\`, \`.gendsp\` |
| **intelligent-corpus-remixer** | Corpus-based concatenative synthesis using FluCoMa (MFCC + UMAP + k-means), EARS, and Odot; 2D plotter-triggered playback | \`.maxpat\`, \`.js\` |
| **kicksynth** | Kick drum synthesizer with Gen~ pitch envelopes, click/sub/noise layers, and drive/saturation | \`.maxpat\`, \`.js\` |`

**Edit C — insert `physics-composition` row BETWEEN performancepatchtest and rhythmic-sampler:**
- FIND: `| **performancepatchtest** | Live performance cue system with multiband compression, feedback delay, distortion, and soundfile playback | \`.maxpat\`, \`.gendsp\` |
| **rhythmic-sampler** | 8-slot sampler with slice-based sequencing, time-stretching, and per-slot FX | \`.maxpat\`, \`.js\` |`
- REPLACE: `| **performancepatchtest** | Live performance cue system with multiband compression, feedback delay, distortion, and soundfile playback | \`.maxpat\`, \`.gendsp\` |
| **physics-composition** | Audiovisual instrument where a 2D bouncing-ball simulation (dada.bounce) drives microtonal note generation with bach.roll score display | \`.maxpat\` |
| **rhythmic-corpus-chopper** | Sample-accurate beat slicer and re-sequencer using FluCoMa onset detection and the Rhythmic Time Toolkit for signal-rate sequencing | \`.maxpat\`, \`.js\` |
| **rhythmic-sampler** | 8-slot sampler with slice-based sequencing, time-stretching, and per-slot FX | \`.maxpat\`, \`.js\` |`

Note: Edit C inserts BOTH `physics-composition` and `rhythmic-corpus-chopper` in a single replacement because they fall between the same pair of existing rows. This reduces the Edit count and keeps the table diff clean.

Do NOT modify the column headers or any existing row. Do NOT reformat existing entries.
  </action>
  <verify>
    <automated>grep -c "^| \*\*" README.md</automated>
  </verify>
  <done>
- `grep -c "^| \*\*bassoon-model\*\*" README.md` returns 1
- `grep -c "^| \*\*intelligent-corpus-remixer\*\*" README.md` returns 1
- `grep -c "^| \*\*physics-composition\*\*" README.md` returns 1
- `grep -c "^| \*\*rhythmic-corpus-chopper\*\*" README.md` returns 1
- Patches table now contains 18 project rows (14 existing + 4 new). Count by running `awk '/^## Patches/,/^## How It Works/' README.md | grep -c "^| \*\*"` → expect 18
- Milestone table row format unchanged; GSD command table unchanged
- `ls patches/` shows all 18 projects that appear in the README table (no missing, no extras)
  </done>
</task>

</tasks>

<verification>
Run these final checks after both tasks complete:

```bash
# All three stats are fresh
grep -n "3,434" README.md              # expect: Features bullet, Quick Start step 2, How-It-Works, Project Structure
grep -n "29 packages" README.md        # expect: Features bullet, How-It-Works, Project Structure
grep -n "1,589 tests" README.md        # expect: Project Structure

# No stale numbers leaked (except the v4.0 milestone historical row)
grep -n "2,450" README.md              # expect: 1 hit — the v4.0 milestone row only
grep -n "20 packages" README.md        # expect: 0 hits
grep -n "1,545" README.md              # expect: 0 hits

# Patches table is complete and matches the filesystem
PATCHES_IN_README=$(awk '/^## Patches/,/^## How It Works/' README.md | grep -c "^| \*\*")
PATCHES_ON_DISK=$(ls -d patches/*/ 2>/dev/null | wc -l | tr -d ' ')
echo "README: $PATCHES_IN_README, Disk: $PATCHES_ON_DISK"  # expect: both 18

# Milestone table untouched
grep -n "v4.0 Package Integration" README.md   # expect: row still present, 2026-04-15 unchanged
```
</verification>

<success_criteria>
- README.md reflects accurate counts: 3,434 objects, 29 packages, 1,589 tests
- Patches table lists all 18 `patches/*/` projects in alphabetical order
- No prose rewrites outside of the replaced substrings
- Milestone table unchanged (v4.0 row retains `2,450 objects` as historical)
- Diff is minimal and surgical — a reviewer can see each change was intentional and targeted
</success_criteria>

<output>
After completion, create `.planning/quick/260422-b0d-look-over-recent-updates-to-the-system-a/260422-b0d-SUMMARY.md` documenting:
- Final grep counts for stats verification
- List of 4 new patch rows added
- Confirmation that milestone table and prose sections are untouched
- Any drift between investigation numbers and run-time numbers (should be none)
</output>
