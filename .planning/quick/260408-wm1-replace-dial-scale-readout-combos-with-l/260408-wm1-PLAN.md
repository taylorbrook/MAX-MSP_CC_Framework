---
phase: quick
plan: 260408-wm1
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/gong-model/generated/gong-model.maxpat
autonomous: true
must_haves:
  truths:
    - "Each live.dial displays its parameter name (shortname) below the dial"
    - "Each live.dial outputs values in its correct range (not 0-127)"
    - "Decay and bloom_speed dials have exponential scaling for musical feel"
    - "Modes dial outputs integers 4-32"
    - "autopattr restore values are compatible with new parameter ranges"
  artifacts:
    - path: "patches/gong-model/generated/gong-model.maxpat"
      provides: "Gong model patch with fully configured live.dial parameters"
      contains: "parameter_shortname"
  key_links:
    - from: "live.dial saved_object_attributes"
      to: "prepend objects"
      via: "outlet 0 sends scaled value directly"
      pattern: "parameter_mmin.*parameter_mmax"
---

<objective>
Populate saved_object_attributes on the 10 live.dial objects (obj-300 through obj-309) in the gong-model patch with correct parameter configuration: shortname labels, value ranges, exponents, unit styles, and initial values.

Purpose: The live.dials are already placed and wired but have empty saved_object_attributes, so they display as generic 0-127 dials with no labels or range mapping. This makes them output wrong values and show no parameter names.

Output: gong-model.maxpat with all 10 live.dials fully configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@patches/gong-model/generated/gong-model.maxpat
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate live.dial saved_object_attributes for all 10 parameters</name>
  <files>patches/gong-model/generated/gong-model.maxpat</files>
  <action>
Load the .maxpat JSON directly. For each live.dial (obj-300 through obj-309), set `saved_object_attributes` with the correct parameter configuration.

Parameter configs (varname -> attributes):

| varname | obj | shortname | mmin | mmax | type | exponent | unitstyle | initial |
|---------|-----|-----------|------|------|------|----------|-----------|---------|
| d_structure | obj-300 | structure | 0.0 | 1.0 | 0 (float) | 1.0 | 1 (float) | 0.5 |
| d_brightness | obj-301 | brightness | 0.0 | 1.0 | 0 | 1.0 | 1 | 0.5 |
| d_decay | obj-302 | decay | 0.1 | 30.0 | 0 | 3.0 | 1 | 3.93 |
| d_position | obj-303 | position | 0.0 | 1.0 | 0 | 1.0 | 1 | 0.5 |
| d_nonlinearity | obj-304 | nonlinear | 0.0 | 1.0 | 0 | 1.0 | 1 | 0.1 |
| d_hardness | obj-305 | hardness | 0.0 | 1.0 | 0 | 1.0 | 1 | 0.5 |
| d_bloom | obj-306 | bloom amt | 0.0 | 1.0 | 0 | 1.0 | 1 | 0.1 |
| d_bloom_speed | obj-307 | bloom spd | 0.1 | 5.0 | 0 | 2.0 | 1 | 0.99 |
| d_modes | obj-308 | modes | 4.0 | 32.0 | 1 (int) | 1.0 | 1 | 24 |
| d_gain | obj-309 | gain | 0.0 | 1.0 | 0 | 1.0 | 1 | 0.5 |

For each live.dial box, set:
```json
"saved_object_attributes": {
    "parameter_longname": "{shortname}",
    "parameter_shortname": "{shortname}",
    "parameter_type": {type},
    "parameter_mmin": {mmin},
    "parameter_mmax": {mmax},
    "parameter_exponent": {exponent},
    "parameter_unitstyle": {unitstyle},
    "parameter_initial_enable": 1,
    "parameter_initial": [{initial}]
}
```

Notes:
- parameter_longname must be UNIQUE across all live.dial objects in the patch (use the shortname value which is already unique)
- parameter_type 1 (int) for modes means it outputs integers only
- parameter_exponent > 1 gives more resolution at the low end (musical for decay/speed)
- The autopattr restore values already use actual parameter-range values (not 0-127) so they need NO changes

Write back the modified JSON to the same .maxpat path. Then git commit the file.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('patches/gong-model/generated/gong-model.maxpat') as f:
    p = json.load(f)
ok = True
for b in p['patcher']['boxes']:
    box = b['box']
    if box.get('maxclass') == 'live.dial':
        soa = box.get('saved_object_attributes', {})
        if not soa.get('parameter_shortname'):
            print(f'FAIL: {box[\"id\"]} missing shortname'); ok = False
        if 'parameter_mmax' not in soa:
            print(f'FAIL: {box[\"id\"]} missing mmax'); ok = False
        if 'parameter_initial' not in soa:
            print(f'FAIL: {box[\"id\"]} missing initial'); ok = False
        if soa.get('parameter_initial_enable') != 1:
            print(f'FAIL: {box[\"id\"]} initial not enabled'); ok = False
        print(f'OK: {box[\"id\"]} {soa.get(\"parameter_shortname\",\"\")} range={soa.get(\"parameter_mmin\",\"?\")}-{soa.get(\"parameter_mmax\",\"?\")} exp={soa.get(\"parameter_exponent\",\"?\")}')
if ok: print('ALL PASS')
else: print('SOME FAILED')
"
    </automated>
  </verify>
  <done>All 10 live.dial objects have populated saved_object_attributes with correct shortname, range (mmin/mmax), exponent, unit style, and initial value. Patch opens in MAX with labeled dials outputting correctly scaled values.</done>
</task>

</tasks>

<verification>
- Open gong-model.maxpat in MAX 9
- Each dial shows its label below (structure, brightness, decay, etc.)
- Each dial shows its value in the correct range
- Turning decay dial sweeps 0.1 to 30 with exponential curve
- Turning modes dial outputs integers 4 to 32
- All dials feed into their prepend -> send gong-ctrl chain correctly
</verification>

<success_criteria>
All 10 live.dial objects have complete saved_object_attributes. Patch loads in MAX with working labeled dials that output correctly ranged values.
</success_criteria>

<output>
After completion, create `.planning/quick/260408-wm1-replace-dial-scale-readout-combos-with-l/260408-wm1-SUMMARY.md`
</output>
