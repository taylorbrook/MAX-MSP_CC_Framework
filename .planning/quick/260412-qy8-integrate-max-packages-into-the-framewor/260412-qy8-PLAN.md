---
phase: 260412-qy8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v4.0-package-integration-PROPOSAL.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PKG-PROPOSAL]

must_haves:
  truths:
    - "Proposal document covers all 8+ target packages with extraction approach per package"
    - "Phase breakdown maps every locked decision from CONTEXT.md to a specific phase"
    - "Installation handling section describes the /max-new and /max-build gating workflow"
    - "PROJECT.md Active requirements reflect the upcoming v4.0 milestone scope"
    - "ROADMAP.md has a placeholder entry for v4.0 Package Integration milestone"
  artifacts:
    - path: ".planning/milestones/v4.0-package-integration-PROPOSAL.md"
      provides: "Complete milestone proposal with phases, requirements, package survey, risks"
      min_lines: 150
    - path: ".planning/PROJECT.md"
      provides: "Updated Active requirements section"
      contains: "Package Integration"
    - path: ".planning/ROADMAP.md"
      provides: "v4.0 milestone placeholder"
      contains: "v4.0"
  key_links:
    - from: ".planning/milestones/v4.0-package-integration-PROPOSAL.md"
      to: ".planning/PROJECT.md"
      via: "Requirements referenced in both"
      pattern: "PKG-"
    - from: ".planning/milestones/v4.0-package-integration-PROPOSAL.md"
      to: ".planning/ROADMAP.md"
      via: "Phase numbers referenced in roadmap placeholder"
      pattern: "Phase 2[0-9]"
---

<objective>
Write a comprehensive milestone proposal for v4.0 Package Integration that covers all target packages, extraction approaches, phase breakdown, requirements, and integration points -- then update PROJECT.md and ROADMAP.md to reflect the upcoming milestone.

Purpose: Define the full scope and phasing for integrating MAX packages (bundled + community) into the framework at full parity with core domains, so the milestone can be planned and executed.
Output: Proposal document + updated project files
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quick/260412-qy8-integrate-max-packages-into-the-framewor/260412-qy8-CONTEXT.md
@.planning/quick/260412-qy8-integrate-max-packages-into-the-framewor/260412-qy8-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write v4.0 Package Integration milestone proposal</name>
  <files>.planning/milestones/v4.0-package-integration-PROPOSAL.md</files>
  <action>
Create a comprehensive milestone proposal document at `.planning/milestones/v4.0-package-integration-PROPOSAL.md` with the following structure and content:

**1. Milestone Overview**
- Name: v4.0 Package Integration
- Goal: Integrate MAX packages into the framework at full parity with core domains -- object DB entries with validation, agent guidance, templates, critics, and install-aware gating.
- Phase numbering: Phases 20-25 (continuing from the existing Phase 19)

**2. Target Packages (two tables)**

Tier 1 -- Bundled (shipped with MAX, guaranteed installed):
| Package | Est. Objects | Type | Prefix | Extraction Method | Status |
BEAP (~172 modules, bpatcher abstractions, `bp.*`, NEW abstraction parser, not in DB)
Vizzie (~110 modules, bpatcher abstractions, `vz.*`, NEW abstraction parser, not in DB)
ableton-dsp (74 objects, compiled externals, `abl.*`, existing XML, ALREADY IN DB)
jit.mo (8 objects, compiled externals, `jit.mo.*`, existing XML, ALREADY IN DB)
Mira (2 objects, compiled externals, `mira.*`, existing XML, PARTIALLY IN DB)
Jitter Geometry (~26 objects, compiled externals, XML extraction)
Jitter Tools (~99 objects, compiled + abstractions, hybrid extraction)
RNBO (~560 objects, compiled externals, existing XML, ALREADY IN DB)

Tier 2 -- Community (require installation):
| Package | Est. Objects | Type | Prefix | Install Method | Notes |
FluCoMa (~60, compiled, `fluid.*`, Package Manager, audio analysis/ML)
CNMAT (~30, compiled, various, Package Manager/GitHub, OSC/mapping)
Odot (~40, compiled, `o.*`, Package Manager, expression-based data)
Bach (~250+, mixed, `bach.*`, Package Manager, CAC/notation -- LARGE)
IRCAM Spat (~300, compiled, `spat5.*`, IRCAM Forum license, spatialization)
ml.*/ml-lib (~8, compiled, `ml.*`, Package Manager, ML toolkit)

**3. Phase Breakdown (6 phases)**

Phase 20: DB Schema + Package Field
- Add `"package"` field to ALL existing objects (null for core, package name for packages)
- Create `package_info.json` registry (per-package metadata: name, tier, prefix, version, install method, description)
- Update `ObjectDatabase` in `db_lookup.py`: add `lookup(name, allowed_packages=)`, `list_packages()`, `get_package_objects(pkg)` methods
- Update validation to warn when using package object not in project's allowed packages
- Add `package` field to extraction pipeline output
- Requirements: PKG-01 through PKG-04

Phase 21: Bundled Package Extraction (BEAP + Vizzie)
- Build new `extract_abstractions.py` for bpatcher-based packages (cannot use XML pipeline)
- Parse help patches + open abstraction .maxpat files to extract I/O counts and signal types
- Extract ~172 BEAP modules and ~110 Vizzie modules into per-package DB files
- DB entries use `"maxclass": "bpatcher"` with `"abstraction_file"` field
- Also extract remaining bundled packages not yet in DB (Jitter Geometry, Jitter Tools)
- Verify extracted data against help patches using existing audit pipeline
- Requirements: PKG-05 through PKG-08

Phase 22: Package-Gated Generation
- Add package selection to project config (`status.md` or new `config.json`)
- Update `/max-new` to prompt user for which packages they want to use
- Update `/max-build` to prompt before generating with package objects if not decided at project creation
- Package selection gates which objects agents can use -- `ObjectDatabase` filtering
- No silent generation with unavailable packages -- always user-confirmed
- Requirements: PKG-09 through PKG-13

Phase 23: Agent Package Intelligence
- Package-specific guidance sections in all relevant agent SKILL.md files
- BEAP: modular patching patterns, CV signal conventions (0-5V range), bpatcher sizing/layout
- Vizzie: video processing chains, standard interconnection patterns
- FluCoMa: audio analysis workflows, ML pipeline patterns
- Bach: llll data type handling, never mix with standard MAX lists
- CNMAT: OSC workflows, mapping patterns
- Package-specific entries in `relationships.json` (e.g., BEAP `bp.VCA` + `bp.Oscillator`)
- Layout overrides for bpatcher-based packages (BEAP/Vizzie modules are larger than standard objects)
- Requirements: PKG-14 through PKG-18

Phase 24: Community Package Support
- Stub DB entries for all Tier 2 packages (FluCoMa, CNMAT, Bach, Odot, ml.*, IRCAM Spat)
- Stubs include: name, package, prefix, install instructions, marked `"extracted": false`
- Extraction commands for installed packages: `python extract_objects.py --package flucoma --path <path>`
- When user has package installed, extraction populates full DB entries from XML refpages
- Install guidance surfaced in agent prompts when stub objects are referenced
- Requirements: PKG-19 through PKG-22

Phase 25: Templates + Critics
- Starter templates per package (BEAP modular synth, Vizzie video chain, FluCoMa analysis, Bach notation)
- Package-aware critics: BEAP signal convention validator, Bach llll connection checker
- Template integration with `/max-new` -- project creation can scaffold from package templates
- Requirements: PKG-23 through PKG-26

**4. Requirements (PKG-01 through PKG-26)**

List each requirement with ID, description, and phase mapping. Group by phase. Every locked decision from CONTEXT.md must map to at least one requirement:

DB Schema:
- PKG-01: All package objects tagged with source package field
- PKG-02: Package registry tracks name, version, install method per package
- PKG-03: ObjectDatabase supports package-aware filtering (allowed_packages parameter)
- PKG-04: Validation warns on package objects not in project's allowed packages

Extraction:
- PKG-05: Abstraction extraction pipeline handles bpatcher-based packages (BEAP, Vizzie)
- PKG-06: BEAP modules extracted with correct I/O counts and signal types
- PKG-07: Vizzie modules extracted with correct I/O counts
- PKG-08: All bundled packages represented in DB (including Jitter Geometry, Jitter Tools)

Generation Gating:
- PKG-09: `/max-new` asks user which packages to use for the project
- PKG-10: `/max-build` prompts before generating with package objects if not decided
- PKG-11: Package selection stored in project config
- PKG-12: Object usage gated on project-level package selection
- PKG-13: No silent generation with unavailable packages

Agent Intelligence:
- PKG-14: Agent-specific guidance per package in SKILL.md files
- PKG-15: BEAP modular patching patterns documented for agents
- PKG-16: Package-specific relationships.json entries
- PKG-17: Layout overrides for bpatcher-based package objects
- PKG-18: Full parity with core domains (not just DB entries)

Community:
- PKG-19: Stub DB entries for uninstalled community packages
- PKG-20: Extraction commands for installed community packages
- PKG-21: Install guidance in agent prompts for community packages
- PKG-22: FluCoMa, CNMAT, Bach, Odot, ml.*, IRCAM Spat all have DB presence

Templates/Critics:
- PKG-23: Starter templates for common package workflows
- PKG-24: Package-aware critics (signal conventions, data type checking)
- PKG-25: Template integration with `/max-new` project scaffolding
- PKG-26: Dedicated critics for BEAP signal conventions and Bach llll handling

**5. Architecture Decisions**

Document these decisions from research:
- Per-package DB files (`packages/beap/objects.json`, etc.) rather than single monolithic packages file -- cleaner isolation, selective loading
- bpatcher objects use `"maxclass": "bpatcher"` with `"abstraction_file"` field -- patch generation uses `add_bpatcher()` not `add_box()`
- ObjectDatabase load order unchanged -- packages load early, core domains shadow duplicates
- Existing `extract_objects.py` extended for XML-bearing packages; new `extract_abstractions.py` for bpatcher packages
- Community packages use stub entries when not installed, full entries when extracted from installed packages

**6. Integration Points**

Map which existing framework components need changes:
- `db_lookup.py` -- package-aware lookup, filtering, listing
- `validation.py` -- package gating warnings
- `extract_objects.py` -- package field in output, new package path globs
- NEW `extract_abstractions.py` -- bpatcher extraction pipeline
- `packages/` directory -- split into per-package subdirectories
- `relationships.json` -- package-specific object pairs
- `overrides.json` -- package-specific overrides
- Project config -- package selection storage
- `/max-new` skill -- package selection prompt
- `/max-build` skill -- package gating check
- Agent SKILL.md files (all 6+) -- package guidance sections

**7. Risks and Open Questions**

Risks:
- Abstraction I/O type detection (signal vs control) requires parsing inner .maxpat files -- more complex than XML extraction
- Bach is very large (~250+ objects) and may need special handling
- IRCAM Spat requires paid license -- cannot be tested without subscription
- Community package XML format may vary from Cycling'74 standard
- BEAP signal conventions (0-5V CV) need verification from actual BEAP documentation

Open questions (from research):
- How to handle abstraction I/O types (signal vs control) -- need to open .maxpat and scan for inlet~/outlet~
- Should stub entries for uninstalled packages block or warn? (Recommendation: warn-only)
- Additional community packages beyond the named ones? (Claude's discretion)

**8. Assumptions Log**

Copy the 4 assumptions from RESEARCH.md (Bach object count, BEAP CV signaling, Bach lllls, community XML refpages) and add risk-if-wrong for each.

**9. Estimated Scope**

- 6 phases, ~15-20 plans total
- Phases 20-21 are foundational (sequential)
- Phases 22-23 can partially parallelize
- Phase 24 depends on Phase 20 (schema) but not Phase 21 (bundled extraction)
- Phase 25 depends on Phases 23-24

Format the document in the same style as the existing v2.0-ROADMAP.md -- markdown with tables, phase details, requirement listings, and traceability matrix.
  </action>
  <verify>
    <automated>test -f /Users/taylorbrook/Dev/MAX/.planning/milestones/v4.0-package-integration-PROPOSAL.md && wc -l /Users/taylorbrook/Dev/MAX/.planning/milestones/v4.0-package-integration-PROPOSAL.md | awk '{if ($1 >= 150) print "PASS: " $1 " lines"; else print "FAIL: only " $1 " lines"}'</automated>
  </verify>
  <done>
    - Proposal document exists at `.planning/milestones/v4.0-package-integration-PROPOSAL.md`
    - Contains all 8+ target packages with extraction approach per package
    - Contains 6 phases (20-25) with requirements mapped to each
    - All 14 locked decisions from CONTEXT.md are covered by at least one requirement
    - Contains architecture decisions, integration points, risks, and assumptions
    - At least 150 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md and ROADMAP.md for v4.0 milestone</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <action>
**PROJECT.md updates:**

1. Read current PROJECT.md
2. In the `### Active` section (currently says "Next milestone requirements TBD"), replace with:

```
### Active

- [ ] Package Integration milestone (v4.0) -- integrate MAX packages at full parity with core domains
  - Object DB entries with validation, aliases, connection checking for all bundled + community packages
  - Abstraction extraction pipeline for bpatcher-based packages (BEAP, Vizzie)
  - Package-gated generation: `/max-new` and `/max-build` prompt for package selection
  - Agent-specific guidance per package (modular patterns, signal conventions, workflow templates)
  - Community package support with stub entries and extraction commands
  - Dedicated critics and starter templates per package
  - See `.planning/milestones/v4.0-package-integration-PROPOSAL.md` for full proposal
```

3. In the `### Future` section, REMOVE the line "Template library for common MAX patterns" since templates are now part of v4.0 scope. Keep the other future items.

**ROADMAP.md updates:**

1. Read current ROADMAP.md
2. In the `## Milestones` section, add after the v3.0.0 line:
```
- **v4.0 Package Integration** -- Phases 20-25 (proposed)
```

3. After the existing `</details>` blocks for v3.0.0, add a new section:
```
### v4.0 Package Integration (Proposed)

**Milestone Goal:** Integrate MAX packages (bundled + community) into the framework at full parity with core domains -- object DB entries, extraction pipelines, package-gated generation, agent intelligence, and starter templates.

**Proposal:** `.planning/milestones/v4.0-package-integration-PROPOSAL.md`

- [ ] Phase 20: DB Schema + Package Field
- [ ] Phase 21: Bundled Package Extraction (BEAP + Vizzie)
- [ ] Phase 22: Package-Gated Generation
- [ ] Phase 23: Agent Package Intelligence
- [ ] Phase 24: Community Package Support
- [ ] Phase 25: Templates + Critics
```

4. Add rows to the Progress table for Phases 20-25 with milestone v4.0, 0 plans, status Proposed.

Do NOT change any existing completed milestone entries. Only add/update sections for v4.0.
  </action>
  <verify>
    <automated>grep -c "v4.0" /Users/taylorbrook/Dev/MAX/.planning/PROJECT.md /Users/taylorbrook/Dev/MAX/.planning/ROADMAP.md</automated>
  </verify>
  <done>
    - PROJECT.md Active section lists v4.0 Package Integration milestone scope
    - PROJECT.md Future section no longer has "Template library" (moved to v4.0)
    - ROADMAP.md Milestones section includes v4.0
    - ROADMAP.md has phase listing for Phases 20-25
    - ROADMAP.md Progress table has 6 new rows for v4.0 phases
    - No existing milestone entries modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- this is a documentation/planning task. No code execution, no external services, no user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-qy8-01 | Tampering | PROPOSAL.md | accept | Documentation only -- no runtime impact. Reviewed by user before execution. |
</threat_model>

<verification>
1. Proposal document exists and is comprehensive (150+ lines, all packages covered, all phases defined)
2. All 14 locked decisions from CONTEXT.md are traceable to specific requirements in the proposal
3. PROJECT.md and ROADMAP.md reference v4.0 milestone
4. No existing milestone entries in ROADMAP.md were modified
</verification>

<success_criteria>
- `.planning/milestones/v4.0-package-integration-PROPOSAL.md` exists with complete proposal
- All 8+ target packages documented with extraction approach
- 6 phases defined (20-25) with requirements per phase
- 26 requirements (PKG-01 through PKG-26) covering all locked decisions
- PROJECT.md Active section updated, template line moved from Future
- ROADMAP.md has v4.0 placeholder with 6 phases
</success_criteria>

<output>
After completion, create `.planning/quick/260412-qy8-integrate-max-packages-into-the-framewor/260412-qy8-SUMMARY.md`
</output>
</task>
