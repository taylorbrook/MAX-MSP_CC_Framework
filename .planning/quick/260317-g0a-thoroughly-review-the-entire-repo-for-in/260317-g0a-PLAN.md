---
phase: quick-260317-g0a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/skills/max-dsp-agent/SKILL.md
  - .claude/skills/max-patch-agent/SKILL.md
  - .claude/skills/max-ui-agent/SKILL.md
  - .claude/skills/max-ui-agent/BOUNDARIES.md
  - .claude/skills/max-router/SKILL.md
  - .claude/skills/max-lifecycle/SKILL.md
  - .claude/skills/max-lifecycle/references/project-structure.md
  - .claude/skills/max-lifecycle/references/status-tracking.md
  - .claude/skills/max-lifecycle/references/test-protocol.md
  - .claude/skills/max-critic/SKILL.md
  - .claude/skills/max-memory-agent/SKILL.md
  - .claude/skills/max-memory-agent/BOUNDARIES.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "CLAUDE.md GenExpr section distinguishes patcher objects (in 1) from codebox code (in1)"
    - "All spacing references across CLAUDE.md, skills, and boundaries match defaults.py values (v_spacing=20, h_gutter=15)"
    - "Layout engine is consistently described as row-based topological across all skills"
    - "All skill and reference files use /max-command format (not /max:command)"
    - "Stale scala-synth memory file is cleaned up or annotated as historical"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected GenExpr I/O syntax and spacing values"
    - path: ".claude/skills/max-ui-agent/BOUNDARIES.md"
      provides: "Corrected spacing values"
    - path: ".claude/skills/max-patch-agent/SKILL.md"
      provides: "Corrected layout description (row-based not column-based)"
  key_links:
    - from: "CLAUDE.md"
      to: "src/maxpat/codegen.py"
      via: "GenExpr syntax documentation"
      pattern: "in1.*out1"
    - from: "CLAUDE.md"
      to: "src/maxpat/defaults.py"
      via: "Layout spacing documentation"
      pattern: "V_SPACING.*20"
---

<objective>
Fix documented inconsistencies across CLAUDE.md, agent skills, boundaries, and reference files to ensure all documentation matches the actual code behavior and user decisions made during v2.0 development.

Purpose: Prevent agents from following stale documentation that contradicts the working codebase, which would produce incorrect patches (wrong GenExpr syntax, wrong spacing, wrong command names).
Output: All documentation files corrected and internally consistent.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/maxpat/defaults.py
@src/maxpat/codegen.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix GenExpr I/O syntax and layout spacing in CLAUDE.md and DSP agent SKILL.md</name>
  <files>CLAUDE.md, .claude/skills/max-dsp-agent/SKILL.md</files>
  <action>
Fix two documented inconsistencies that contradict the actual code:

**1. GenExpr I/O syntax in CLAUDE.md (line ~101):**
Change:
```
- GenExpr uses C-style syntax with `in 1`, `in 2` for inputs and `out 1`, `out 2` for outputs
```
To:
```
- GenExpr codebox uses C-style syntax with `in1`, `in2` for inputs and `out1`, `out2` for outputs (no space -- `in 1`/`out 1` is only for gen~ patcher objects, not codebox code)
```

**2. GenExpr I/O syntax in DSP agent SKILL.md (line ~40):**
Change:
```
- GenExpr syntax: `in 1`/`out 1` for I/O, `Param` for parameters, `History` for feedback, `Buffer`/`Data` for samples
```
To:
```
- GenExpr syntax: `in1`/`out1` for I/O (no space -- space form is for gen~ patcher objects only), `Param` for parameters, `History` for feedback, `Buffer`/`Data` for samples
```

**3. Layout spacing in CLAUDE.md Rule #4 (line ~80):**
Change:
```
- Standard object spacing: ~80-120px vertical, ~150-200px horizontal
```
To:
```
- Standard object spacing: ~20px vertical, ~15px horizontal gutter (matches defaults.py; user-confirmed tight spacing)
```

These changes align documentation with `src/maxpat/codegen.py` (uses `in1`/`out1`), `src/maxpat/code_validation.py` (validates `in1`/`out1`), and `src/maxpat/defaults.py` (V_SPACING=20, H_GUTTER=15).
  </action>
  <verify>
    <automated>grep -c "in1.*out1" CLAUDE.md && grep -c "~20px vertical" CLAUDE.md && grep -c "in1.*out1" .claude/skills/max-dsp-agent/SKILL.md && echo "All 3 fixes verified"</automated>
  </verify>
  <done>CLAUDE.md GenExpr section says in1/out1 (not in 1/out 1), CLAUDE.md spacing says ~20px/~15px (not 80-120/150-200), DSP agent SKILL.md says in1/out1.</done>
</task>

<task type="auto">
  <name>Task 2: Fix layout description, spacing in boundaries, and /max: command format across all skill files</name>
  <files>.claude/skills/max-patch-agent/SKILL.md, .claude/skills/max-ui-agent/BOUNDARIES.md, .claude/skills/max-router/SKILL.md, .claude/skills/max-lifecycle/SKILL.md, .claude/skills/max-lifecycle/references/project-structure.md, .claude/skills/max-lifecycle/references/status-tracking.md, .claude/skills/max-lifecycle/references/test-protocol.md, .claude/skills/max-critic/SKILL.md, .claude/skills/max-memory-agent/SKILL.md, .claude/skills/max-memory-agent/BOUNDARIES.md</files>
  <action>
Fix three categories of inconsistencies across skill files:

**A. Layout description in max-patch-agent SKILL.md (line ~47):**
Change `column-based layout positioning` to `row-based topological layout positioning` -- this matches the actual layout.py implementation and the max-ui-agent SKILL.md description.

**B. Spacing in max-ui-agent BOUNDARIES.md (line ~35):**
Change:
```
- Standard object spacing: ~80-120px vertical, ~150-200px horizontal (patching mode)
```
To:
```
- Standard object spacing: ~20px vertical, ~15px horizontal gutter (patching mode, per defaults.py)
```

**C. Command format: replace all `/max:` references with `/max-` in skill and reference files.**
The actual Claude Code slash commands use hyphens (`/max-build`, `/max-iterate`), not colons. Find and replace ALL occurrences of `/max:` with `/max-` in these files:
- `.claude/skills/max-router/SKILL.md`: `/max:build` -> `/max-build`, `/max:iterate` -> `/max-iterate`, `/max:verify` -> `/max-verify`, `/max:test` -> `/max-test`, `/max:status` -> `/max-status`, `/max:memory` -> `/max-memory`, `/max:switch` -> `/max-switch`, `/max:discuss` -> `/max-discuss`, `/max:research` -> `/max-research`
- `.claude/skills/max-lifecycle/SKILL.md`: `/max:new` -> `/max-new`, `/max:status` -> `/max-status`, `/max:switch` -> `/max-switch`, `/max:test` -> `/max-test`, `/max:version` -> `/max-version`
- `.claude/skills/max-lifecycle/references/project-structure.md`: `/max:new` -> `/max-new`, `/max:discuss` -> `/max-discuss`, `/max:test` -> `/max-test`
- `.claude/skills/max-lifecycle/references/status-tracking.md`: `/max:new` -> `/max-new`, `/max:discuss` -> `/max-discuss`, `/max:research` -> `/max-research`, `/max:build` -> `/max-build`, `/max:test` -> `/max-test`, `/max:verify` -> `/max-verify`, `/max:status` -> `/max-status`
- `.claude/skills/max-lifecycle/references/test-protocol.md`: `/max:test` -> `/max-test`
- `.claude/skills/max-critic/SKILL.md`: `/max:verify` -> `/max-verify`
- `.claude/skills/max-memory-agent/SKILL.md`: `/max:memory` -> `/max-memory`
- `.claude/skills/max-memory-agent/BOUNDARIES.md`: `/max:memory` -> `/max-memory`

Do NOT change:
- `.claude/commands/*.md` files (these already use the correct `/max-` format)
- Any non-command references (e.g., `.claude/max-objects/` paths, `~/.claude/max-memory/` paths)
  </action>
  <verify>
    <automated>grep -r "/max:" .claude/skills/ | grep -v "max-objects" | grep -v "max-memory/" | grep -v "/max-" | wc -l | xargs -I{} test {} -eq 0 && grep "row-based" .claude/skills/max-patch-agent/SKILL.md | head -1 && grep "~20px" .claude/skills/max-ui-agent/BOUNDARIES.md | head -1 && echo "All fixes verified"</automated>
  </verify>
  <done>No /max: command references remain in skill files (only /max- format), max-patch-agent says "row-based" not "column-based", UI agent boundaries says ~20px not ~80-120px.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate stale scala-synth memory file as historical</name>
  <files>None (user project memory file -- annotation only)</files>
  <action>
The file `~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_scala_synth_debug.md` references `build_scala_synth.py` which was removed per Rule #5. Since this is a project memory file (user's personal memory, not repo), do NOT delete it -- instead add a note at the top of the file:

After the existing YAML frontmatter, add:
```
> **Historical note (2026-03-17):** The `build_scala_synth.py` script referenced below was removed per CLAUDE.md Rule #5 (No Generator Scripts). The gen~ debug findings about codebox syntax (in1/out1, Delay.read/write, declaration ordering) remain valid and useful. To iterate on the scala-synth patch, use `/max-iterate` on the existing .maxpat files directly.
```

This preserves the valuable GenExpr debugging knowledge while flagging the stale build script reference.
  </action>
  <verify>
    <automated>grep -c "Historical note" ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_scala_synth_debug.md | xargs -I{} test {} -ge 1 && echo "Annotation verified"</automated>
  </verify>
  <done>Stale memory file annotated with historical context explaining the build script removal and pointing to /max-iterate as the current workflow.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `grep "in1" CLAUDE.md` -- confirms codebox syntax uses in1 not "in 1"
2. `grep "~20px" CLAUDE.md` -- confirms spacing matches defaults.py
3. `grep -r "/max:" .claude/skills/ | grep -v "max-objects" | grep -v "max-memory/"` -- should return 0 lines of /max: command format
4. `grep "row-based" .claude/skills/max-patch-agent/SKILL.md` -- confirms layout description consistency
5. `python3 -m pytest tests/ --tb=no -q` -- all 1151 tests still pass (documentation-only changes)
</verification>

<success_criteria>
- CLAUDE.md GenExpr section correctly documents in1/out1 for codebox (not in 1/out 1)
- CLAUDE.md and all skill/boundary files use ~20px/~15px spacing (not 80-120/150-200)
- Layout described as "row-based topological" consistently across all skills
- Zero /max: command format references remain in skill files (all use /max-)
- Stale build script reference in memory annotated as historical
- All 1151 existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/quick/260317-g0a-thoroughly-review-the-entire-repo-for-in/260317-g0a-SUMMARY.md`
</output>
