---
id: 260421-bx3
description: Audit mc.* objects for missing variable_io flags and rules
date: 2026-04-21
status: ready
source: REVIEW-260420-j15 DQ-07
must_haves:
  truths:
    - "mc.pack~, mc.unpack~, mc.combine~, mc.separate~, mc.deinterleave~, mc.interleave~, mc.gate~, mc.selector~, mc.matrix~, mc.fffb~ are argument-driven at the connection level."
    - "Most other mc.* int-arg objects (mc.dup~, mc.list~, mc.mixdown~, etc.) set channel count INSIDE a single MC outlet — not outlet count. Those stay variable_io:false."
    - "mc.pack~ uses first_arg (single int), not arg_count (unlike core pack). mc.separate~ is the only mc.* using arg_count."
  artifacts:
    - ".claude/max-objects/mc/objects.json — 10 variable_io:false → true flips"
    - ".claude/max-objects/overrides.json — 10 new variable_io_rules entries (alphabetical in mc.* block)"
    - "tests/test_db_lookup.py — 8 regression tests mirroring TC-01 / TC-02 pattern"
  key_links:
    - ".planning/quick/260421-bx3-audit-mc-objects-for-variable-io-flags-a/260421-bx3-RESEARCH.md"
    - ".planning/quick/260420-j15-review-the-objects-database-entries-and-/260420-j15-REVIEW.md (DQ-07)"
    - ".claude/max-objects/overrides.json:7745 (variable_io_rules registry)"
    - "src/maxpat/db_lookup.py (compute_io_counts / _validate_variable_io_rules)"
    - "tests/test_db_lookup.py:234 (TC-01 pattern)"
---

# PLAN 260421-bx3: mc.* variable_io Audit (DQ-07)

## Goal

Close REVIEW-260420-j15 DQ-07. Flag 10 mc.* objects as `variable_io: true` in `mc/objects.json`, add matching formula rules to `overrides.json:variable_io_rules`, and lock behavior with regression tests. Defer subpatcher-inherited mc.* (mc.gen~, mc.poly~, mc.pfft~, mc.vst~) — they need coordinated treatment with their core counterparts.

## Scope

**NARROW sweep** per RESEARCH.md recommendation. 10 objects, all verified against Cycling74 refpages or mirroring existing overrides.json rules. Risk ~0: every formula shape already validated at load time by `_validate_variable_io_rules()`.

## Tasks

### T1 — Flag flips + rule additions (single atomic commit)

**Files:**
- `.claude/max-objects/mc/objects.json`
- `.claude/max-objects/overrides.json`

**Action:**
1. In `mc/objects.json`, set `"variable_io": true` on these 10 entries (currently all `false`):
   - `mc.combine~`, `mc.deinterleave~`, `mc.fffb~`, `mc.gate~`, `mc.interleave~`, `mc.matrix~`, `mc.pack~`, `mc.selector~`, `mc.separate~`, `mc.unpack~`
2. In `overrides.json:variable_io_rules` (currently ends after `routepass` at line 7919), append 10 new entries alphabetically ordered in an mc.* block. Use the ready-to-paste JSON from RESEARCH.md (Category A). Each entry carries an `_audit` block citing `quick-260421-bx3 (DQ-07)`.

**Verify:**
- `python -c "from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); print(db.compute_io_counts('mc.pack~', ['4']))"` → `(4, 1)`
- `python -c "from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); print(db.compute_io_counts('mc.unpack~', ['6']))"` → `(1, 6)`
- `ObjectDatabase()` constructor does not raise (validates all rule formulas at load time).
- `db.lookup('mc.pack~')['variable_io'] is True` for all 10 objects.

**Done:** JSON valid (parseable), 10 flags flipped, 10 rules added, DB loads without warnings. Commit: `refactor(quick-260421-bx3): DQ-07 — flag variable_io on 10 mc.* objects + add overrides.json rules`.

### T2 — Regression tests

**Files:**
- `tests/test_db_lookup.py`

**Action:** Append 8 regression tests after the existing `test_compute_io_counts_routepass_normalized_default_outlets` block (line ~338), mirroring the TC-01 `test_compute_io_counts_*` pattern. Tests from RESEARCH.md:
- `test_compute_io_counts_mc_pack` — `(4,1)` and default `(2,1)`
- `test_compute_io_counts_mc_unpack` — `(1,6)` and default `(1,2)`
- `test_compute_io_counts_mc_separate` — `(1,3)` for 3 args, default `(1,2)`
- `test_compute_io_counts_mc_combine` — `(3,1)` for 1 arg
- `test_compute_io_counts_mc_gate` — `(2,4)` for mirror-of-gate pattern
- `test_compute_io_counts_mc_selector` — `(4,1)` for first_arg+1 pattern
- `test_compute_io_counts_mc_matrix` — `(4,6)` for first_arg/second_arg
- `test_mc_pack_is_variable_io` — flag + rule presence

Add section header comment: `# ── compute_io_counts mc.* (quick-260421-bx3 DQ-07) ────────────`.

**Verify:** `python -m pytest tests/test_db_lookup.py -x -q` all green. Existing tests (TC-01, TC-02, routepass) must still pass.

**Done:** 8 tests pass, no regressions in existing tests. Commit: `test(quick-260421-bx3): DQ-07 — regression tests for mc.* variable_io rules`.

## Non-goals

- Subpatcher-inherited mc.* (mc.gen~, mc.poly~, mc.pfft~, mc.vst~). Systemic gap — defer until core counterparts (gen~, poly~, pfft~, vst~) also get rules.
- Signal-name-string family (mc.send~, mc.receive~, mc.dac~, mc.adc~). Correctly `variable_io: false` today.
- Channel-count-inside-outlet family (mc.dup~, mc.list~, mc.mixdown~, mc.apply~, mc.chord~, mc.bands~, etc.). Correctly `variable_io: false`; int arg sets chans inside a single MC outlet, not outlet count.

## Verification signals (post-execution)

1. `ObjectDatabase()` load-time validator passes (covered by `test_load_time_validation_accepts_live_overrides`).
2. `compute_io_counts` returns correct counts for all 10 rules with both explicit and default args.
3. No change to generators / patch emitters needed — the fix is purely DB-level.
