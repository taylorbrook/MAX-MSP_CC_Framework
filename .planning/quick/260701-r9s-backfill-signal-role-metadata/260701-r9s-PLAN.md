---
phase: quick-260701-r9s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/audit_signal_role.py
  - tests/test_audit_signal_role.py
  - .claude/max-objects/overrides.json
autonomous: true
requirements: [WR-01]

must_haves:
  truths:
    - "Loading ObjectDatabase() and scanning every outlet, ZERO outlets that carry a signal_role have an empty type field (baseline measured at 703 before this plan)"
    - "Loading ObjectDatabase() and scanning every outlet, ZERO outlets that carry a signal_role have an empty digest field (baseline measured at 588 before this plan)"
    - "cmd_apply_run, when it creates a brand-new outlet entry, writes a non-empty type and digest sourced from canonical domain metadata (or a role-derived fallback) instead of the empty-string stub"
    - "The backfill only fills EMPTY type/digest fields; it never overwrites a non-empty existing value and never mutates any signal_role"
    - "Re-running the backfill a second time is a byte-stable no-op (idempotent)"
    - "The existing tests/test_audit_signal_role.py apply/idempotency/overwrite suite stays green (no regression to cmd_apply_run behavior on existing outlets)"
  artifacts:
    - "scripts/audit_signal_role.py with _canonical_outlet_meta + _resolve_outlet_meta helpers, a fixed cmd_apply_run new-outlet branch, and a backfill-metadata subcommand"
    - "tests/test_audit_signal_role.py with new tests for synthesis-path metadata population and backfill fill/no-clobber/idempotency"
    - ".claude/max-objects/overrides.json with type and digest populated on all signal_role outlets (795 outlets, verified via the loaded ObjectDatabase)"
  key_links:
    - "cmd_apply_run new-outlet branch -> _resolve_outlet_meta -> _canonical_outlet_meta(overrides_file.parent) which reads .claude/max-objects/{domain}/objects.json in DOMAIN_LOAD_ORDER"
    - "ObjectDatabase override deep-merge (db_lookup.py:152-160) REPLACES the entire outlets array, so canonical type/digest must be copied INTO the override outlet or it is lost at load — file edits alone are the fix, but verification must go through ObjectDatabase()"
    - "backfill-metadata post-write loader-acceptance check constructs ObjectDatabase() so a bad write fails the command, mirroring cmd_apply_run's existing guard"
---

<objective>
Close WR-01 (Phase 30 metadata fidelity): curated signal_role outlets in overrides.json carry empty type (703) and empty digest (588) fields because cmd_apply_run synthesizes outlet stubs with empty strings and the override deep-merge replaces the canonical outlets array wholesale, discarding the extracted type/digest. Fix the synthesis path so newly created outlets carry full metadata, add a re-runnable backfill subcommand, run it against the real overrides.json, and verify the result through the loaded ObjectDatabase (not just the file).

Purpose: signal_role outlets become fully-described (typed + human-readable digest) at the loader level, restoring the metadata fidelity Phase 30 was supposed to deliver.
Output: two helpers + a synthesis fix + a backfill subcommand in scripts/audit_signal_role.py, new unit tests, and a backfilled overrides.json.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@scripts/audit_signal_role.py
@src/maxpat/db_lookup.py
@tests/test_audit_signal_role.py

# Data facts (re-measure at runtime; do NOT hard-code these into code):
# - overrides.json currently: 795 outlets carry signal_role; 703 have empty type, 588 have empty digest.
# - signal_role exists ONLY in overrides.json (0 occurrences in any domain/objects.json), so the loaded DB's
#   signal_role outlets are exactly the override outlets — backfilling overrides covers 100% of the loaded gap.
# - Canonical domain metadata: 676/703 empty-type outlets and 583/588 empty-digest outlets have a matching
#   (object name, outlet id) entry in a domain objects.json with a non-empty type/digest to copy verbatim.
# - The remaining ~27 types / ~5 digests have no canonical (name, id) source (e.g. adstatus has an empty
#   canonical outlets array; several mc.* variants declare more outlets than their canonical entry) and need
#   the role-derived fallback.
# - The override deep-merge (db_lookup.py:152-160) REPLACES the whole `outlets` array per object; canonical
#   type/digest is shadowed at load unless it is copied into the override outlet itself.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical-metadata helpers, fix the cmd_apply_run synthesis stub, and add a backfill-metadata subcommand</name>
  <files>scripts/audit_signal_role.py, tests/test_audit_signal_role.py</files>
  <behavior>
    - _canonical_outlet_meta(db_root): reads {db_root}/{domain}/objects.json for each domain in DOMAIN_LOAD_ORDER (imported from db_lookup), plus the packages/*/objects.json subdir scan, and returns a dict mapping (object_name, outlet_id) -> (type, digest). Later-loaded domains overwrite earlier ones so core (msp/max) wins over rnbo, matching the loader's merge priority. Missing domain files are skipped without error (so isolated tmp_path test roots that only create overrides.json still work).
    - _resolve_outlet_meta(name, outlet_id, role, canon_map): returns (type, digest). type = canonical type when present and non-empty, else "signal" when role == "audio" else "control". digest = canonical digest when present and non-empty, else "Signal output" when role == "audio" else "Control output". (These fallback strings already exist verbatim in overrides.json, e.g. adc~ / adstatus, so no new vocabulary is introduced.)
    - cmd_apply_run synthesis fix: in the `target is None` new-outlet branch only, populate type and digest from _resolve_outlet_meta instead of the empty-string stub. Do NOT touch the existing-outlet branch — existing apply/idempotency/overwrite tests depend on cmd_apply_run being a byte-stable no-op when a role is already set, so filling existing empties here would break test_idempotent_apply_is_byte_stable.
    - cmd_backfill_run(overrides_file=None): walks data["objects"], and for every outlet dict that has a signal_role AND an empty type OR empty digest, fills only the empty field(s) via _resolve_outlet_meta; never clobbers a non-empty value; never touches signal_role or outlets without a signal_role. Idempotent (no change -> return 0 without writing). Atomic write via .json.tmp + replace, round-trip JSON validation before write, and a post-write ObjectDatabase() loader-acceptance check when overrides_file is the canonical default (mirror cmd_apply_run's guards). Wire it into build_parser as a `backfill-metadata` subcommand (cmd_backfill) with an optional --overrides-file arg.
    - New tests in tests/test_audit_signal_role.py: (a) applying a role to an outlet NOT already present, with a domain objects.json seeded under the isolated db_root, creates the outlet with the canonical type/digest; (b) same with no domain file present falls back to role-derived ("signal"/"Signal output" for audio, "control"/"Control output" otherwise); (c) cmd_backfill_run fills empty type/digest on a signal_role outlet and leaves a non-empty digest untouched; (d) cmd_backfill_run is a byte-stable no-op on a second run; (e) cmd_backfill_run never mutates outlets that lack a signal_role.
  </behavior>
  <action>Implement per the behavior block. Import DOMAIN_LOAD_ORDER alongside the existing ObjectDatabase / _SIGNAL_ROLE_ENUM import from src.maxpat.db_lookup. Derive db_root inside cmd_apply_run and cmd_backfill_run from overrides_file.parent so isolated test roots resolve correctly. Keep the synthesis change confined to the `target is None` branch; the description's "synthesis path" is exactly that new-outlet creation site. The backfill is scoped to outlets that carry a signal_role (WR-01 scope) — do not backfill role-less outlets. Reuse cmd_apply_run's atomic-write, round-trip-validate, and canonical-default loader-check patterns verbatim for cmd_backfill_run so behavior is consistent. Write the new tests using the existing _make_overrides / _make_isolated_db / _make_review_file fixture helpers already in the test module.</action>
  <verify>
    <automated>python -m pytest tests/test_audit_signal_role.py -q</automated>
  </verify>
  <done>All tests in tests/test_audit_signal_role.py pass, including the pre-existing apply/idempotency/overwrite suite and the new synthesis + backfill tests. scripts/audit_signal_role.py exposes a `backfill-metadata` subcommand and the two helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Run the backfill on the real overrides.json and verify through the loaded ObjectDatabase</name>
  <files>.claude/max-objects/overrides.json</files>
  <action>From the project root, capture the BEFORE gap by loading ObjectDatabase() and counting signal_role outlets with empty type and empty digest (re-measure at runtime; the 703/588 figures are the expected baseline but must be measured, not hard-coded). Run `python scripts/audit_signal_role.py backfill-metadata` to mutate .claude/max-objects/overrides.json in place. Capture the AFTER gap the same way through a freshly constructed ObjectDatabase(). Confirm the AFTER counts are both zero. Then run the backfill a second time and confirm it reports no change and leaves overrides.json byte-identical (idempotent). Do not edit overrides.json by hand — the subcommand is the only sanctioned mutation path. When committing, scope the add to exactly scripts/audit_signal_role.py, tests/test_audit_signal_role.py, and .claude/max-objects/overrides.json plus the plan/state docs; never use `git add .` or `git add -A` (CLAUDE.md Rule #7, main-checkout multi-instance safety).</action>
  <verify>
    <automated>python scripts/audit_signal_role.py backfill-metadata && python -c "import sys; sys.path.insert(0,'.'); from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); bad=[(n,o.get('id')) for n,ob in db._objects.items() for o in ob.get('outlets',[]) if isinstance(o,dict) and o.get('signal_role') is not None and (o.get('type','')=='' or o.get('digest','')=='')]; assert not bad, f'{len(bad)} signal_role outlets still empty: {bad[:10]}'; print('VERIFIED via loaded ObjectDatabase: 0 empty type/digest among signal_role outlets')"</automated>
  </verify>
  <done>A freshly constructed ObjectDatabase() reports zero signal_role outlets with an empty type and zero with an empty digest (down from the measured 703/588 baseline). A second backfill run leaves overrides.json byte-identical. overrides.json parses and the loader constructs without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → overrides.json | The backfill and the fixed apply path mutate a shared, git-tracked source-of-truth DB file consumed by every patch build. |
| overrides.json → ObjectDatabase loader | A malformed write would break DB construction for the whole project (fail-closed schema validators). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r9s-01 | Tampering | cmd_backfill_run overwrite of curator values | high | mitigate | Fill only EMPTY type/digest; never clobber a non-empty value; never mutate signal_role. Covered by the no-clobber unit test. |
| T-r9s-02 | Denial of Service | Corrupt overrides.json write breaks DB load | high | mitigate | Atomic .tmp + replace, round-trip JSON validation before write, and a post-write ObjectDatabase() loader-acceptance check on the canonical default file (mirrors cmd_apply_run). |
| T-r9s-03 | Tampering | Empty-string metadata reintroduced by future apply runs | medium | mitigate | Synthesis-path fix populates type/digest from canonical/fallback on new-outlet creation; regression guarded by the synthesis unit tests. |
| T-r9s-04 | Repudiation | Broad `git add` sweeps unrelated files into the commit | medium | mitigate | Task 2 scopes the add to the three touched files plus plan/state docs; no `git add .`/`-A` (CLAUDE.md Rule #7). |
</threat_model>

<verification>
- `python -m pytest tests/test_audit_signal_role.py -q` — full module green (existing + new tests).
- `python scripts/audit_signal_role.py backfill-metadata` — exits 0; a second run is a byte-stable no-op.
- Loaded-DB gate: constructing ObjectDatabase() yields zero signal_role outlets with empty type and zero with empty digest.
- `python -c "import json; json.load(open('.claude/max-objects/overrides.json'))"` — file remains valid JSON.
</verification>

<success_criteria>
- signal_role outlet metadata gap measured through ObjectDatabase() drops from 703 empty-type / 588 empty-digest to 0 / 0.
- cmd_apply_run's new-outlet synthesis writes full metadata; no regression to existing apply/idempotency/overwrite tests.
- A re-runnable, idempotent `backfill-metadata` subcommand exists and is covered by tests.
- Commit is scoped to the three touched files plus plan/state docs.
</success_criteria>

<output>
Create `.planning/quick/260701-r9s-backfill-signal-role-metadata/260701-r9s-SUMMARY.md` when done.
</output>