---
phase: quick-260322-c7w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/db_lookup.py
  - src/maxpat/validation.py
  - tests/test_validation.py
autonomous: true
requirements: [QUICK-260322-C7W]

must_haves:
  truths:
    - "Overridden MSP objects still get signal-to-control connections auto-removed"
    - "Non-overridden MSP objects emit a warning but the connection survives"
    - "Non-MSP objects (no tilde suffix) are unaffected by this change"
  artifacts:
    - path: "src/maxpat/db_lookup.py"
      provides: "is_overridden() method on ObjectDatabase"
      contains: "def is_overridden"
    - path: "src/maxpat/validation.py"
      provides: "Guard in _validate_connections for non-overridden MSP sources"
      contains: "is_overridden"
    - path: "tests/test_validation.py"
      provides: "Three new tests covering override guard behavior"
      contains: "non_overridden"
  key_links:
    - from: "src/maxpat/validation.py"
      to: "src/maxpat/db_lookup.py"
      via: "db.is_overridden(name) call in _validate_connections"
      pattern: "db\\.is_overridden"
---

<objective>
Guard Layer 3 signal-to-control auto-removal so that MSP objects without outlet-type overrides emit a warning instead of silently removing the connection.

Purpose: The MSP domain extraction marked ALL outlets as signal=true. Only ~42 of 248 have been corrected via overrides.json. For uncorrected objects (e.g., groove~ outlet 2), a valid control-outlet connection to a control inlet (e.g., trigger inlet 0) is incorrectly auto-removed. This guard prevents data loss until all MSP objects are corrected.

Output: Updated validation.py with override-aware guard, ObjectDatabase.is_overridden() method, and 3 new tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/maxpat/db_lookup.py
@src/maxpat/validation.py
@tests/test_validation.py
@.claude/max-objects/overrides.json (structure reference only)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ObjectDatabase.is_overridden() and guard _validate_connections()</name>
  <files>src/maxpat/db_lookup.py, src/maxpat/validation.py, tests/test_validation.py</files>
  <behavior>
    - Test 1 (overridden MSP object): Build a patch with a source object whose name ends with ~ AND is in overrides.json (e.g., "line~"), source outlettype=["signal", ""], connecting from signal outlet 0 to a control-only inlet (e.g., "print" inlet 0). Connection SHOULD be auto-removed. Verify auto_fixed result exists and line is removed.
    - Test 2 (non-overridden MSP object): Build a patch with a source object whose name ends with ~ and is NOT in overrides.json (e.g., "groove~" -- or use a fake tilde name not in overrides), source outlettype includes "signal" on the outlet being connected, connecting signal outlet to a control-only inlet. Connection SHOULD survive (NOT removed). A warning-level result SHOULD be emitted with message containing "unverified outlet type" (or similar).
    - Test 3 (non-MSP object unchanged): Build a patch with a non-tilde source (e.g., "print" with outlettype=["signal"] to simulate the check), connecting signal outlet to control-only inlet. The existing auto-removal behavior SHOULD apply unchanged (connection removed, auto_fixed=True).
  </behavior>
  <action>
    1. In `src/maxpat/db_lookup.py`:
       - In `_load()`, after the override merge loop (line ~76), build a `self._overridden_objects: set[str]` containing all object names that had overrides applied (the keys of `overrides_data["objects"]` that exist in `self._objects` AND don't start with `_`).
       - Add method `is_overridden(self, name: str) -> bool` that resolves alias then checks `self._overridden_objects`. Docstring: "Check whether an object has expert-verified overrides applied."

    2. In `src/maxpat/validation.py`, in `_validate_connections()`, inside the `if is_signal_source:` block (around line 330):
       - Before checking `_inlet_accepts_signal`, determine the source object name via `_extract_object_name(src_box)`.
       - If the source name ends with `~` AND `db.is_overridden(src_name)` is False:
         - Do NOT auto-remove the connection.
         - Emit a warning-level (not error) ValidationResult with layer="connections", message like: "Unverified outlet types: '{src_name}' outlet {src_outlet} -> '{dst_id}' inlet {dst_inlet} -- source MSP object not in overrides, connection preserved".
         - Set auto_fixed=False.
         - Skip the `_inlet_accepts_signal` check entirely (continue to next line).
       - If the source name does NOT end with `~`, OR if it does end with `~` AND is overridden: proceed with existing auto-removal logic unchanged.

    3. In `tests/test_validation.py`, add a new test class `TestLayer3OverrideGuard` with the three tests from the behavior section. Use `_make_box` and `_make_line` helpers. For test 2 (non-overridden), use a fake object name like "fakesynth~" that won't be in overrides but will be set up with outlettype=["signal", "signal"] in the box dict. The validation code reads outlettype from the box dict, not the DB, so this works.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_validation.py -x -v -k "TestLayer3OverrideGuard or TestLayer3SignalTypes" 2>&1 | tail -30</automated>
  </verify>
  <done>
    - ObjectDatabase.is_overridden() returns True for objects in overrides.json, False otherwise
    - Non-overridden MSP objects: signal-to-control connections emit warning, connection preserved in patch lines
    - Overridden MSP objects: existing auto-removal behavior unchanged
    - Non-MSP objects: existing auto-removal behavior unchanged
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_validation.py -x -v 2>&1 | tail -40
```
All tests pass including new override guard tests and all existing Layer 3 signal type tests.
</verification>

<success_criteria>
- 3 new tests pass covering: overridden MSP auto-removes, non-overridden MSP warns+preserves, non-MSP unchanged
- All 25+ existing validation tests still pass
- groove~ outlet 2 -> trigger inlet 0 pattern would no longer be silently removed
</success_criteria>

<output>
After completion, create `.planning/quick/260322-c7w-in-validation-py-validate-connections-ad/260322-c7w-SUMMARY.md`
</output>
