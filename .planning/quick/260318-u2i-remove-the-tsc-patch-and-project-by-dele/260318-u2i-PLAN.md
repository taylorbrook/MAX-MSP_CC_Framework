---
phase: quick-260318-u2i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/TSC/ (deleted)
  - README.md
autonomous: true
requirements: [QUICK-u2i]
must_haves:
  truths:
    - "patches/TSC directory no longer exists on disk or in git"
    - "README.md patches table no longer lists TSC"
  artifacts:
    - path: "README.md"
      provides: "Updated patches table without TSC entry"
      contains: "wormhole"
  key_links: []
---

<objective>
Remove the TSC patch project entirely from the repository by deleting the patches/TSC directory (33 tracked files) and removing its entry from the README.md patches table.

Purpose: User wants to clean up a patch project that is no longer needed.
Output: Clean repo with TSC fully removed from disk, git tracking, and documentation.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md (line 126 — TSC entry in patches table)
@patches/TSC/ (33 generated files to delete)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete TSC directory and remove README entry</name>
  <files>patches/TSC/, README.md</files>
  <action>
1. Remove the entire patches/TSC directory from git tracking and disk:
   `git rm -r patches/TSC/`
   This removes all 33 tracked files (generated/*.maxpat, generated/*.gendsp).

2. Edit README.md to remove line 126 which contains:
   `| **TSC** | Temporal Semiotic Composition system for corpus-based live performance with behavior modules | \`.maxpat\`, \`.gendsp\` |`
   Remove that entire table row. Do not leave a blank line in its place — the table should go directly from the stutter row to the wormhole row.

3. Verify no other references to TSC remain in core project files (README, CLAUDE.md, etc.). Planning-history files (.planning/) can retain historical references — those are fine.
  </action>
  <verify>
    <automated>test ! -d patches/TSC && ! grep -q "| \*\*TSC\*\*" README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>patches/TSC directory is fully removed from disk and git. README.md patches table has no TSC row. The table still renders correctly with all remaining 10 projects.</done>
</task>

</tasks>

<verification>
- `ls patches/TSC` returns "No such file or directory"
- `git status` shows patches/TSC files staged for deletion and README.md modified
- README.md patches table has exactly 10 rows (FDNVerb through wormhole, no TSC)
</verification>

<success_criteria>
- patches/TSC directory does not exist
- README.md contains no TSC entry
- All changes committed cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260318-u2i-remove-the-tsc-patch-and-project-by-dele/260318-u2i-SUMMARY.md`
</output>
