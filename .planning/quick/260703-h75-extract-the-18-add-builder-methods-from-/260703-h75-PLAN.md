---
phase: quick
plan: 260703-h75
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/builders.py
  - src/maxpat/patcher.py
  - src/maxpat/__init__.py
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "All 18 add_* builder methods are defined on BuildersMixin and remain callable on Patcher instances with identical behavior"
    - "The full pytest suite produces the same passed/failed/error counts as the pre-refactor baseline (zero behavior change)"
    - "src.maxpat no longer re-exports _AUTO_HIGHLIGHT; aesthetics.py still defines and uses it internally"
    - "Patchline.__init__, Box.__init__, and Patcher.__init__ carry -> None return annotations"
  artifacts:
    - path: "src/maxpat/builders.py"
      provides: "New BuildersMixin holding the 18 add_* builder methods, matching the GraphMixin/AnalysisMixin extraction pattern"
    - path: "src/maxpat/patcher.py"
      provides: "Patcher with BuildersMixin in its bases, the 18 methods removed, and 3 __init__ methods annotated -> None"
    - path: "src/maxpat/__init__.py"
      provides: "Package API without the _AUTO_HIGHLIGHT re-export"
  key_links:
    - "Patcher class bases include BuildersMixin via MRO; builders.py imports Box/Patchline without triggering a circular-import failure"
    - "In-method local imports (codegen, m4l_constants, sizing.get_bpatcher_dims, re, warnings, calculate_box_size) are preserved verbatim inside the moved method bodies"
---

<objective>
Extract the 18 `add_*` builder methods from `src/maxpat/patcher.py` into a new `BuildersMixin` in `src/maxpat/builders.py` (matching the existing `GraphMixin`/`AnalysisMixin` extraction pattern), add `-> None` return hints to the 3 methods that actually lack them, and stop re-exporting the private `_AUTO_HIGHLIGHT` from `src/maxpat/__init__.py`.

Purpose: reduce the 95 KB `patcher.py` monolith by moving the object-builder surface into its own module, and close two small typing/API-hygiene gaps — all with zero behavior change.

Output: `src/maxpat/builders.py` (new, `BuildersMixin`), a slimmer `patcher.py` (18 methods removed, bases updated, 3 `__init__` annotated), and `__init__.py` without the `_AUTO_HIGHLIGHT` import. The full test suite must pass identically (same pass/fail/error counts as before).
</objective>

<scope_verification>
Counts were verified against the current source (not guessed). The executor MUST honor these corrected numbers:

**The 18 add_* methods to MOVE** (all currently on the `Patcher` class; line numbers are pre-refactor reference points):
`add_box` (L405), `add_comment` (L446), `add_section_header` (L475), `add_subsection` (L498), `add_annotation` (L518), `add_bubble` (L541), `add_panel` (L570), `add_step_marker` (L639), `add_overlay_readout` (L688), `add_labeled_param_bank` (L775), `add_message` (L930), `add_connection` (L953), `add_subpatcher` (L1636), `add_bpatcher` (L1726), `add_gen` (L1922), `add_m4l_gen_synth` (L2088), `add_node_script` (L2204), `add_js` (L2256). "Builder" here means "every method whose name starts with `add_`" — there are exactly 18, all on `Patcher`.

**Methods that STAY in `patcher.py`** (do NOT move — the moved builders call these via `self.` at runtime, which still resolves through the mixin): `_gen_id`, `bring_to_front`, `send_to_back`, `set_z_index`, `remove_box`, `remove_connection`, `modify_box`, `replace_box`, `replace_box_safe`, `insert_into_connection`, `_find_clear_position`, `_auto_position`, `find_boxes`, `find_box`, `_box_matches`, `get_inlets`, `get_outlets`, `populate_assistance_comments`, `_populate_comments_recursive`, `from_dict`, `to_dict`, `__init__`. The `Patchline`, `EditResult`, and `Box` classes also stay.

**Return-hint reality — only 3 methods actually lack annotations, NOT ~28.** AST analysis proves exactly 3 methods have no return annotation: `Patchline.__init__` (L49), `Box.__init__` (L137), `Patcher.__init__` (L370) — each gets `-> None`. The other 25 methods flagged by a naive `grep 'def' | grep -v '->'` ARE already annotated; their `->` simply sits on a continuation line of a multi-line signature (3 real + 25 false positives = the "~28" estimate). DO NOT add annotations to any already-annotated method — that is a no-op at best and a syntax error at worst.

**`_AUTO_HIGHLIGHT` re-export removal is safe.** Repo-wide grep confirms: `__init__.py` (L27) is the only importer, it is NOT listed in `__all__`, and `aesthetics.py` both defines (L120) and uses (L136) it internally. No test imports it from the package. Removal touches only the import block in `__init__.py`.
</scope_verification>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/maxpat/patcher.py
@src/maxpat/__init__.py
@src/maxpat/graph.py
@src/maxpat/analysis.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture pre-refactor test baseline</name>
  <files>.planning/quick/260703-h75-extract-the-18-add-builder-methods-from-/260703-h75-BASELINE.txt</files>
  <action>
Before touching any source, run the full suite and record the exact result so parity can be proven after the refactor. Run `python3 -m pytest tests/ -q --tb=no` from the repo root and write its complete output (or at minimum the final summary line, e.g. "N passed, M failed, K errors in Ss") to `260703-h75-BASELINE.txt` in this quick-task directory. The suite has known pre-existing failures (community-package blocks etc.) — that is expected; the goal is to capture the current numbers, NOT to make them green. Make NO code changes in this task. This baseline file is a reference artifact for Task 3's parity gate.
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -q --tb=no 2>&1 | tee ".planning/quick/260703-h75-extract-the-18-add-builder-methods-from-/260703-h75-BASELINE.txt" | tail -1; grep -Eq "passed|failed|error" ".planning/quick/260703-h75-extract-the-18-add-builder-methods-from-/260703-h75-BASELINE.txt"</automated>
  </verify>
  <done>260703-h75-BASELINE.txt exists and contains a pytest summary line with the current passed/failed/error counts. No source files changed.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the 18 add_* methods into BuildersMixin</name>
  <files>src/maxpat/builders.py, src/maxpat/patcher.py</files>
  <action>
Create `src/maxpat/builders.py` mirroring the `GraphMixin`/`AnalysisMixin` structure (see `src/maxpat/graph.py` and `src/maxpat/analysis.py` in context): a module docstring explaining it holds the `add_*` builders extracted from `patcher.py`, `from __future__ import annotations`, the required imports, then `class BuildersMixin:` with a docstring noting it expects `self.boxes`, `self.lines`, `self.db`, and the sibling Patcher helpers (`_gen_id`, `_auto_position`, `_find_clear_position`, `bring_to_front`, `set_z_index`, `get_inlets`, `get_outlets`, `modify_box`, `add_connection`, etc.) to be provided by the host class.

Move the 18 methods listed in `<scope_verification>` from `Patcher` into `BuildersMixin` VERBATIM — preserve every line including the in-method local imports (`add_bpatcher` imports `get_bpatcher_dims` from `src.maxpat.sizing`; `add_comment`/`add_message`/`add_subpatcher` import `calculate_box_size` from `src.maxpat.sizing`; `add_gen` imports `parse_genexpr_io`/`reorder_genexpr_declarations` from `src.maxpat.codegen`, `validate_genexpr` from `src.maxpat.code_validation`, and `warnings`; `add_m4l_gen_synth` imports `ParamType`/`UnitStyle`/`ModMode` from `src.maxpat.m4l_constants`; `add_overlay_readout` imports `re`). Those local imports travel with the bodies and keep resolving — do not hoist or drop them.

`builders.py` module-level imports needed (names referenced by the moved bodies without a local import): `from typing import Any`; the defaults constants `AESTHETIC_PALETTE, BUBBLE_TOP, FONT_NAME, FONT_SIZE, FONTFACE_BOLD, FONTFACE_ITALIC, GEN_PATCHER_BGCOLOR` from `src.maxpat.defaults`; and `Box, Patchline` from `src.maxpat.patcher` (17 of the 18 methods construct `Box`, and `add_connection` constructs `Patchline`).

CIRCULAR-IMPORT HANDLING (critical — differs from Graph/AnalysisMixin, which only need `Box` under TYPE_CHECKING). Because `builders.py` imports `Box`/`Patchline` at runtime and `patcher.py` imports `BuildersMixin`, the import of `BuildersMixin` in `patcher.py` must be RELOCATED from the current top-of-file mixin import block (near the `GraphMixin`/`AnalysisMixin` imports) to immediately BEFORE the `class Patcher(...)` definition — i.e. after `Patchline`, `EditResult`, and `Box` are defined. At that point `src.maxpat.patcher` is far enough initialized that `builders.py`'s `from src.maxpat.patcher import Box, Patchline` resolves. Then change the class header to `class Patcher(GraphMixin, AnalysisMixin, BuildersMixin):`. Leave the `GraphMixin`/`AnalysisMixin` imports where they are. If the executor instead observes a circular ImportError, fall back to: import `Box`/`Patchline` under `if TYPE_CHECKING:` in `builders.py` for annotations and add a local `from src.maxpat.patcher import Box, Patchline` at the top of each method that constructs them.

Do NOT change any method body logic, signatures, or the sibling methods that stay in `patcher.py`. This is a pure move plus a bases-list edit.
  </action>
  <verify>
    <automated>python3 -c "from src.maxpat.patcher import Patcher; from src.maxpat.builders import BuildersMixin; ms=['add_box','add_comment','add_section_header','add_subsection','add_annotation','add_bubble','add_panel','add_step_marker','add_overlay_readout','add_labeled_param_bank','add_message','add_connection','add_subpatcher','add_bpatcher','add_gen','add_m4l_gen_synth','add_node_script','add_js']; assert issubclass(Patcher, BuildersMixin); bad=[(m, getattr(Patcher,m).__qualname__) for m in ms if not getattr(Patcher,m).__qualname__.startswith('BuildersMixin.')]; assert not bad, bad; assert len(ms)==18; print('OK 18 builders resolve via BuildersMixin')" && python3 -m pytest tests/test_patcher.py tests/test_aesthetics.py tests/test_analysis.py -q --tb=short</automated>
  </verify>
  <done>src/maxpat/builders.py defines BuildersMixin with all 18 add_* methods (verified via __qualname__ starting with "BuildersMixin."); Patcher inherits (GraphMixin, AnalysisMixin, BuildersMixin); patcher.py no longer defines any of the 18; importing Patcher raises no circular-import error; targeted patcher/aesthetics/analysis tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Annotate 3 __init__ methods, drop _AUTO_HIGHLIGHT re-export, prove suite parity</name>
  <files>src/maxpat/patcher.py, src/maxpat/__init__.py</files>
  <action>
Add `-> None` return annotations to the three methods that genuinely lack them: `Patchline.__init__` (L49), `Box.__init__` (L137), and `Patcher.__init__` (L370) in `src/maxpat/patcher.py`. Each has a multi-line signature — place the `-> None` on the closing line of the signature, immediately before the `:`. Touch ONLY these three; per `<scope_verification>`, every other method is already annotated and must not be modified.

In `src/maxpat/__init__.py`, remove the `_AUTO_HIGHLIGHT` name from the `from src.maxpat.aesthetics import ( ... )` block (currently the last name in that tuple). Keep every other name in that import (they are part of `__all__`). Do not add `_AUTO_HIGHLIGHT` to `__all__` — it must simply stop being pulled into the package namespace. `aesthetics.py` is unchanged; it keeps defining and using the name internally.

Then run the FULL suite and confirm the passed/failed/error counts exactly match `260703-h75-BASELINE.txt` from Task 1. Any deviation means the refactor changed behavior — investigate and fix before declaring done. Write the post-refactor output to `260703-h75-AFTER.txt` in the quick-task directory for the parity comparison.
  </action>
  <verify>
    <automated>python3 -c "import inspect, src.maxpat as m; from src.maxpat.patcher import Patchline, Box, Patcher; anns=[inspect.signature(c.__init__).return_annotation for c in (Patchline,Box,Patcher)]; assert all(a=='None' for a in anns), anns; assert not hasattr(m,'_AUTO_HIGHLIGHT'), 'still re-exported by package'; print('OK annotations + export hygiene')" && python3 -m pytest tests/ -q --tb=no 2>&1 | tee ".planning/quick/260703-h75-extract-the-18-add-builder-methods-from-/260703-h75-AFTER.txt" | tail -1 && python3 -c "import re,pathlib; d='.planning/quick/260703-h75-extract-the-18-add-builder-methods-from-'; s=lambda p:{k:int(n) for n,k in re.findall(r'(\d+)\s+(passed|failed|error|errors|skipped)',pathlib.Path(f'{d}/{p}').read_text())}; b=s('260703-h75-BASELINE.txt'); a=s('260703-h75-AFTER.txt'); norm=lambda x:(x.get('passed',0),x.get('failed',0),x.get('errors',x.get('error',0))); assert norm(b)==norm(a),(b,a); print('PARITY OK',a)"
  </verify>
  <done>Patchline.__init__, Box.__init__, Patcher.__init__ all carry -> None; `import src.maxpat` no longer exposes _AUTO_HIGHLIGHT (aesthetics.py unchanged); the full suite's passed/failed/error counts equal the Task 1 baseline exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure internal Python module reorganization. No untrusted input, no new external surface, no package installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h75-01 | Tampering | Behavior drift during method move (silent logic change) | medium | mitigate | Verbatim move only; full-suite parity gate in Task 3 must match the captured baseline exactly |
| T-h75-02 | Denial (import-time) | Circular import between builders.py and patcher.py | medium | mitigate | Relocate the BuildersMixin import below Box/Patchline definitions; Task 2 verify imports Patcher and fails loudly on ImportError |
</threat_model>

<verification>
- `python3 -c "from src.maxpat import Patcher"` imports cleanly (no circular-import error).
- All 18 add_* methods resolve on Patcher via `BuildersMixin.__qualname__`.
- The 3 `__init__` methods report `-> None`; `src.maxpat` has no `_AUTO_HIGHLIGHT` attribute.
- Full `python3 -m pytest tests/` passed/failed/error counts equal the Task 1 baseline.
</verification>

<success_criteria>
- `src/maxpat/builders.py` exists with `BuildersMixin` holding exactly the 18 add_* methods, matching the Graph/AnalysisMixin pattern.
- `patcher.py` defines none of the 18, has `class Patcher(GraphMixin, AnalysisMixin, BuildersMixin)`, and its 3 `__init__` methods are annotated `-> None`.
- `__init__.py` no longer imports `_AUTO_HIGHLIGHT`.
- Zero behavior change: full-suite pass/fail/error counts identical to baseline.
</success_criteria>

<output>
Create `.planning/quick/260703-h75-extract-the-18-add-builder-methods-from-/260703-h75-SUMMARY.md` when done.
</output>
