---
phase: quick-260317-e14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - patches/stutter/generate.py
  - ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md
  - ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_incremental_patching.md
  - ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_iterate_via_generator.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "CLAUDE.md explicitly prohibits generate.py scripts"
    - "No generate.py file exists in any patch directory"
    - "Memory files no longer reference generate.py as a valid workflow"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Rule #5: No Generator Scripts"
      contains: "No Generator Scripts"
    - path: "patches/stutter/generate.py"
      provides: "DELETED — must not exist on disk"
  key_links:
    - from: "CLAUDE.md Rule #5"
      to: "agent behavior"
      via: "project instructions loaded on every conversation"
      pattern: "No Generator Scripts"
---

<objective>
Remove the deprecated generate.py approach from the stutter patch and add safeguards to prevent agents from creating generate.py scripts in the future.

Purpose: The generate.py pattern was deprecated in milestone 2.0 (direct .maxpat editing via Patcher API is the correct approach), but the stutter patch was built after that and still got one. This means agents are not sufficiently prevented from creating them. Adding an explicit prohibition to CLAUDE.md and cleaning up the last remnant closes this gap.

Output: Updated CLAUDE.md with Rule #5, deleted generate.py, updated memory files.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@patches/stutter/generate.py
@~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rule #5 to CLAUDE.md and delete stutter generate.py</name>
  <files>CLAUDE.md, patches/stutter/generate.py</files>
  <action>
1. Edit CLAUDE.md — add a new rule after Rule #4 (Patch Style), before the "## Domain-Specific Rules" section (line 82). Insert:

```markdown
### Rule #5: No Generator Scripts

Never create `generate.py` or similar intermediary Python scripts that regenerate `.maxpat` files from scratch. The Patcher API (`src.maxpat`) is the only sanctioned way to create and edit patches. Agents build Patcher instances in-memory during the `/max-build` or `/max-iterate` commands, then write via `save_patch_roundtrip()`. There is no separate generator script to maintain or re-run.

This rule exists because the generator pattern causes regeneration to overwrite manual edits and iterate improvements. It was deprecated in milestone 2.0.
```

2. Delete `patches/stutter/generate.py` from disk and stage as a git deletion.

Note: The stutter patch's generated .maxpat and .gendsp files in `patches/stutter/generated/` are the actual artifacts and must be kept — only the generator script is removed.
  </action>
  <verify>
    <automated>grep -q "Rule #5: No Generator Scripts" CLAUDE.md && [ ! -f patches/stutter/generate.py ] && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md contains Rule #5 prohibiting generator scripts. patches/stutter/generate.py no longer exists on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Update memory files to remove generate.py references</name>
  <files>~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md, ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_incremental_patching.md, ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_iterate_via_generator.md</files>
  <action>
1. Delete `~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_incremental_patching.md` — this file describes how generate.py should do incremental patching. The entire premise is obsolete since generate.py no longer exists.

2. Delete `~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_iterate_via_generator.md` — this file says /max-iterate must update generate.py. The correct v2.0 approach is direct .maxpat editing via the Patcher API.

3. Update `~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md` — remove the two index entries:
   - Remove the line: `- [project_incremental_patching.md](project_incremental_patching.md) - generate.py must do incremental patching to preserve user/iterate changes in .maxpat files`
   - Remove the line: `- [feedback_iterate_via_generator.md](feedback_iterate_via_generator.md) - /max-iterate must update generate.py, not the .maxpat, to prevent regeneration from wiping changes`

Note: These memory files are outside the git repo (in ~/.claude/) so they are just deleted from disk, not staged for git.
  </action>
  <verify>
    <automated>[ ! -f ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/project_incremental_patching.md ] && [ ! -f ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/feedback_iterate_via_generator.md ] && ! grep -q "generate.py" ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both obsolete memory files deleted. MEMORY.md index no longer references generate.py-related entries.</done>
</task>

</tasks>

<verification>
- `grep "Rule #5" CLAUDE.md` returns the no-generator-scripts rule
- `ls patches/stutter/generate.py` returns "No such file"
- `ls patches/stutter/generated/stutter.maxpat` confirms the actual patch artifact still exists
- `grep generate.py ~/.claude/projects/-Users-taylorbrook-Dev-MAX/memory/MEMORY.md` returns no matches
</verification>

<success_criteria>
- CLAUDE.md has Rule #5 explicitly prohibiting generate.py scripts with rationale
- patches/stutter/generate.py is deleted
- patches/stutter/generated/ directory and its contents are untouched
- Two obsolete memory files are deleted and MEMORY.md index is updated
</success_criteria>

<output>
After completion, create `.planning/quick/260317-e14-remove-generate-py-approach-and-add-safe/260317-e14-SUMMARY.md`
</output>
