---
phase: 24-community-package-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/packages/FluCoMa/objects.json
  - .claude/max-objects/packages/CNMAT/objects.json
  - .claude/max-objects/packages/Bach/objects.json
  - .claude/max-objects/packages/Odot/objects.json
  - .claude/max-objects/packages/ml-lib/objects.json
  - .claude/max-objects/packages/IRCAM Spat/objects.json
  - .claude/max-objects/packages/Cage/objects.json
  - .claude/max-objects/packages/Dada/objects.json
  - .claude/max-objects/packages/EARS/objects.json
  - .claude/max-objects/packages/Rhythmic Time Toolkit/objects.json
  - .claude/max-objects/package_info.json
  - tests/test_package_schema.py
autonomous: true
requirements:
  - PKG-19
  - PKG-22

must_haves:
  truths:
    - "All 10 community packages have populated objects.json with curated stubs"
    - "Every stub entry has name, inlets, outlets, signal types, category, verified:false"
    - "ObjectDatabase loads all stubs automatically without code changes"
    - "IRCAM Spat tier corrected to community in package_info.json"
  artifacts:
    - path: ".claude/max-objects/packages/FluCoMa/objects.json"
      provides: "52 FluCoMa stub entries"
      contains: "fluid.ampfeature~"
    - path: ".claude/max-objects/packages/CNMAT/objects.json"
      provides: "~55 CNMAT stub entries"
      contains: "resonators~"
    - path: ".claude/max-objects/packages/Bach/objects.json"
      provides: "~80 Bach stub entries"
      contains: "bach.score"
    - path: ".claude/max-objects/packages/Odot/objects.json"
      provides: "~30 Odot stub entries"
      contains: "o.pack"
    - path: ".claude/max-objects/packages/ml-lib/objects.json"
      provides: "14 ml-lib stub entries"
      contains: "ml.svm"
    - path: ".claude/max-objects/packages/IRCAM Spat/objects.json"
      provides: "~25 IRCAM Spat stub entries"
      contains: "spat5.panoramix"
    - path: ".claude/max-objects/packages/Cage/objects.json"
      provides: "~30 Cage stub entries"
      contains: "cage.profile"
    - path: ".claude/max-objects/packages/Dada/objects.json"
      provides: "~15 Dada stub entries"
      contains: "dada.graph"
    - path: ".claude/max-objects/packages/EARS/objects.json"
      provides: "~30 EARS stub entries"
      contains: "ears.slice"
    - path: ".claude/max-objects/packages/Rhythmic Time Toolkit/objects.json"
      provides: "~14 RTK stub entries"
      contains: "rtk.seq~"
    - path: "tests/test_package_schema.py"
      provides: "Tests for community package stubs"
      contains: "test_community_stubs_schema"
  key_links:
    - from: ".claude/max-objects/packages/*/objects.json"
      to: "src/maxpat/db_lookup.py ObjectDatabase._load()"
      via: "Auto-loading from packages/ subdirectories"
      pattern: "pkg_root.*iterdir"
---

<objective>
Create curated stub DB entries for all 10 community packages and add tests.

Purpose: Give agents awareness of community package objects (names, I/O, signal types, categories) even when packages are not locally installed. Per D-01, stubs include approximate I/O counts and signal types -- not just names.
Output: 10 populated objects.json files, updated package_info.json, new test class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-community-package-support/24-CONTEXT.md
@.planning/phases/24-community-package-support/24-RESEARCH.md

<interfaces>
<!-- ObjectDatabase auto-loads all packages/*/objects.json -- no code changes needed -->
<!-- Stub schema must match existing ableton-dsp pattern exactly -->

From .claude/max-objects/packages/ableton-dsp/objects.json (reference schema):
Each entry has: name, maxclass, module, domain, category, digest, description,
inlets (array of {id, type, signal, digest, hot}), outlets (array of {id, type, signal, digest}),
arguments, messages, attributes, seealso, tags, min_version, verified, variable_io,
rnbo_compatible, package
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for community package stubs and populate 5 packages (FluCoMa, CNMAT, Bach, Odot, ml-lib)</name>
  <files>
    tests/test_package_schema.py,
    .claude/max-objects/packages/FluCoMa/objects.json,
    .claude/max-objects/packages/CNMAT/objects.json,
    .claude/max-objects/packages/Bach/objects.json,
    .claude/max-objects/packages/Odot/objects.json,
    .claude/max-objects/packages/ml-lib/objects.json,
    .claude/max-objects/package_info.json
  </files>
  <read_first>
    tests/test_package_schema.py,
    .claude/max-objects/package_info.json,
    .claude/max-objects/packages/ableton-dsp/objects.json,
    .planning/phases/24-community-package-support/24-RESEARCH.md
  </read_first>
  <behavior>
    - Test: All 10 community packages have non-empty objects.json (len > 0)
    - Test: Every stub entry has required fields: name, maxclass, module, domain, category, digest, inlets, outlets, verified, package
    - Test: All stubs have verified=false
    - Test: Signal objects (name ends with ~) have at least one signal inlet or outlet
    - Test: package field matches directory name exactly
    - Test: IRCAM Spat tier in package_info.json is "community" not "licensed"
    - Test: FluCoMa has >= 50 objects
    - Test: CNMAT has >= 40 objects
    - Test: Bach has >= 60 objects
    - Test: Odot has >= 25 objects
    - Test: ml-lib has >= 12 objects
    - Test: ObjectDatabase.lookup("fluid.ampfeature~") returns non-None
    - Test: ObjectDatabase.lookup("resonators~") returns non-None
    - Test: ObjectDatabase.lookup("bach.score") returns non-None
    - Test: ObjectDatabase.lookup("o.pack") returns non-None
    - Test: ObjectDatabase.lookup("ml.svm") returns non-None
  </behavior>
  <action>
1. Add a new test class `TestCommunityPackageStubs` to `tests/test_package_schema.py` with the tests described in behavior. The 10 community package names are: "FluCoMa", "CNMAT", "Bach", "Odot", "ml-lib", "IRCAM Spat", "Cage", "Dada", "EARS", "Rhythmic Time Toolkit".

2. Run tests -- they should fail (RED).

3. Create stub entries for the first 5 packages. Use the exact schema from `ableton-dsp/objects.json` entries. Every stub entry must have these fields:
   - `name`: exact object name (e.g., `fluid.ampfeature~`)
   - `maxclass`: same as name for newobj objects
   - `module`: `"msp"` if name ends with `~`, else `"max"`
   - `domain`: `"Packages"`
   - `category`: from the RESEARCH.md category tables
   - `digest`: brief one-line description
   - `description`: same as digest for stubs
   - `inlets`: array of `{id, type, signal, digest, hot}` -- id is 0-based, type is `"signal"` or `"control"`, signal is boolean, hot is true for inlet 0
   - `outlets`: array of `{id, type, signal, digest}` -- same pattern
   - `arguments`: `[]`
   - `messages`: `[]` (or known messages from RESEARCH.md like `["bang", "reset"]`)
   - `attributes`: `{}`
   - `seealso`: `[]`
   - `tags`: `[category_lowercase]`
   - `min_version`: `8`
   - `verified`: `false` (CRITICAL -- per D-03)
   - `variable_io`: `false`
   - `rnbo_compatible`: `false`
   - `package`: directory name (e.g., `"FluCoMa"`)

**FluCoMa** (52 objects, per RESEARCH.md): All `fluid.*` objects from the 6 categories listed. Signal objects (`~` suffix): 1 signal inlet (audio in) + 1 control inlet (parameters), 1 signal outlet. Buf* objects (no `~`): 1 control inlet, 1 control outlet (bang) + 1 dump outlet. Data/ML objects: 1 control inlet, 1 control outlet + 1 dump outlet.

**CNMAT** (~55 objects, per RESEARCH.md): Use BARE names (no `cnmat.` prefix -- per Pitfall 2). Objects: `OpenSoundControl`, `OSC-route`, `analyzer~`, `resonators~`, etc. Signal objects follow MSP pattern. `SDIF-buffer` etc. are control-rate.

**Bach** (~80 key objects, per RESEARCH.md): `bach.*` prefix. Math objects (`bach.+`, `bach.-`, etc.): 2 control inlets (llll), 1 control outlet (llll). List ops (`bach.rev`, `bach.flat`, etc.): 1 inlet, 1 outlet. `bach.score`, `bach.roll`: 1 inlet, 4 outlets (notation objects). I/O objects: 1 inlet, 1 outlet.

**Odot** (~30 objects, per RESEARCH.md): `o.*` prefix. All control-rate operating on odot bundles. Most: 1 inlet, 1-2 outlets. `o.route`: 1 inlet, 3 outlets (variable_io but stub with default). `o.if`: 1 inlet, 2 outlets.

**ml-lib** (14 objects, per RESEARCH.md): `ml.*` prefix. All follow same pattern: 1 control inlet (messages), 2 control outlets (result + dump). Objects: `ml.adaboost`, `ml.ann`, `ml.dtw`, `ml.gmm`, `ml.hmmc`, `ml.knn`, `ml.linreg`, `ml.logreg`, `ml.mindist`, `ml.minmax`, `ml.mulreg`, `ml.randforest`, `ml.softmax`, `ml.svm`.

4. Update `package_info.json`: change IRCAM Spat `"tier"` from `"licensed"` to `"community"` and update its `"description"` to `"Spatial audio rendering. Free download from forum.ircam.fr (account required). Copy spat5 folder to ~/Documents/Max 9/Packages/"` per RESEARCH.md.

5. Run tests again -- the 5 populated packages should pass; the other 5 still fail (GREEN for batch 1).
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python -m pytest tests/test_package_schema.py -x -q -k "community" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .claude/max-objects/packages/FluCoMa/objects.json contains "fluid.ampfeature~" and "fluid.hpss~" and "fluid.umap"
    - .claude/max-objects/packages/CNMAT/objects.json contains "resonators~" and "OpenSoundControl" and "analyzer~"
    - .claude/max-objects/packages/Bach/objects.json contains "bach.score" and "bach.+" and "bach.rev"
    - .claude/max-objects/packages/Odot/objects.json contains "o.pack" and "o.route" and "o.expr.codebox"
    - .claude/max-objects/packages/ml-lib/objects.json contains "ml.svm" and "ml.ann" and "ml.knn"
    - Every entry in all 5 files has "verified": false
    - Every entry has "domain": "Packages"
    - Signal objects (name ending ~) have at least one inlet or outlet with "signal": true
    - package_info.json IRCAM Spat entry has "tier": "community"
    - python -c "from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); assert db.lookup('fluid.ampfeature~') is not None; assert db.lookup('resonators~') is not None; assert db.lookup('bach.score') is not None; print('OK')" prints OK
  </acceptance_criteria>
  <done>First 5 community packages have curated stub entries matching ableton-dsp schema, all with verified:false. ObjectDatabase loads them automatically. Tests pass for populated packages.</done>
</task>

<task type="auto">
  <name>Task 2: Populate remaining 5 packages (IRCAM Spat, Cage, Dada, EARS, Rhythmic Time Toolkit)</name>
  <files>
    .claude/max-objects/packages/IRCAM Spat/objects.json,
    .claude/max-objects/packages/Cage/objects.json,
    .claude/max-objects/packages/Dada/objects.json,
    .claude/max-objects/packages/EARS/objects.json,
    .claude/max-objects/packages/Rhythmic Time Toolkit/objects.json
  </files>
  <read_first>
    .claude/max-objects/packages/FluCoMa/objects.json,
    .planning/phases/24-community-package-support/24-RESEARCH.md,
    tests/test_package_schema.py
  </read_first>
  <action>
Create stub entries for the remaining 5 community packages using the identical schema from Task 1. Reference RESEARCH.md for the exact object lists.

**IRCAM Spat** (~25 key objects, per RESEARCH.md): `spat5.*` prefix. Key objects: `spat5.panoramix`, `spat5.oper`, `spat5.panning~`, `spat5.binaural~`, `spat5.ambi.enc~`, `spat5.reverb~`, `spat5.early~`, `spat5.irt~`, `spat5.conv~`, `spat5.hoa.enc~`, `spat5.hoa.dec~`, `spat5.hoa.proc~`, `spat5.hoa.focus~`, `spat5.hoa.mirror~`, `spat5.hoa.srp~`, `spat5.wfs~`, `spat5.trajectories`, `spat5.periactes`, `spat5.sfplay~`, `spat5.sfrecord~`, `spat5.viewer`, `spat5.oper~`, `spat5.osc.route`, `spat5.osc.pack`, `spat5.osc.unpack`. Signal objects: 1 signal inlet + 1 control inlet (OSC), 1 signal outlet. OSC routing objects are control-rate.

**Cage** (~30 key objects, per RESEARCH.md): `cage.*` prefix. All control-rate operating on lllls. Most: 1-2 control inlets (llll), 1 control outlet (llll). Key objects from RESEARCH.md categories: pitch generation, melodic operations, harmonic analysis, rhythm, L-systems, score operations, FM/spectral. Add `"requires": ["Bach"]` field to each entry to mark the llll dependency (per RESEARCH.md Bach Ecosystem Coupling section). The DB loader ignores unknown fields so this is safe.

**Dada** (~15 objects, per RESEARCH.md): `dada.*` prefix. GUI objects that output lllls. Key objects: `dada.graph`, `dada.distances`, `dada.music~`, `dada.peanos~`, `dada.bounce`, `dada.nodes`, `dada.bodies`, `dada.polygon`, `dada.segment`, `dada.voronoi`, `dada.db`, `dada.sqlite`. Most: 1 inlet, 2 outlets. `dada.music~` is signal (1 signal outlet). Add `"requires": ["Bach"]`.

**EARS** (~30 objects, per RESEARCH.md): `ears.*` prefix. Buffer processing objects. Key objects from RESEARCH.md: `ears.readfile`, `ears.writefile`, `ears.frommax`, `ears.tomax`, `ears.slice`, `ears.join`, `ears.crop`, `ears.rotate`, `ears.reverse`, `ears.pan`, `ears.mix`, `ears.timestretch~`, `ears.pitchshift~`, `ears.resample`, `ears.loudness`, `ears.pitch`, `ears.descriptors`, `ears.spectrogram`, `ears.filter~`, `ears.reverb~`, `ears.distortion~`, `ears.compressor~`, `ears.ambi.enc~`, `ears.ambi.dec~`, `ears.synth~`, `ears.render~`, `ears.score2buf`, `ears.process~`. Buffer objects: 1 control inlet (buffer name), 1 control outlet (processed buffer). Signal objects (`~`): signal I/O. Add `"requires": ["Bach"]`.

**Rhythmic Time Toolkit** (~14 objects, per RESEARCH.md): `rtk.*` prefix. RNBO-compiled signal-rate objects. Key objects: `rtk.seq~`, `rtk.stepper~`, `rtk.arp~`, `rtk.pattern~`, `rtk.clock~`, `rtk.tap~`, `rtk.metro~`, `rtk.gate~`, `rtk.melody~`, `rtk.scale~`, `rtk.chord~`, `rtk.counter~`, `rtk.random~`, `rtk.swing~`. All signal-rate: 1-2 signal inlets, 1-2 signal outlets. Note RESEARCH.md confidence is LOW for this package -- mark stubs appropriately in digest.

Run all community stub tests to verify GREEN for all 10 packages.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python -m pytest tests/test_package_schema.py -x -q -k "community" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .claude/max-objects/packages/IRCAM Spat/objects.json contains "spat5.panoramix" and "spat5.reverb~"
    - .claude/max-objects/packages/Cage/objects.json contains "cage.profile" and "cage.retrograde" and has "requires" field
    - .claude/max-objects/packages/Dada/objects.json contains "dada.graph" and "dada.music~"
    - .claude/max-objects/packages/EARS/objects.json contains "ears.slice" and "ears.filter~"
    - .claude/max-objects/packages/Rhythmic Time Toolkit/objects.json contains "rtk.seq~" and "rtk.clock~"
    - Every entry in all 5 files has "verified": false
    - Cage, Dada, EARS entries have "requires": ["Bach"]
    - python -c "from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); assert db.lookup('spat5.panoramix') is not None; assert db.lookup('cage.profile') is not None; assert db.lookup('dada.graph') is not None; assert db.lookup('ears.slice') is not None; assert db.lookup('rtk.seq~') is not None; print('OK')" prints OK
    - python -m pytest tests/test_package_schema.py -x -q -k "community" exits 0
  </acceptance_criteria>
  <done>All 10 community packages have curated stub entries. ObjectDatabase loads them all. All community stub tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stub data accuracy | Stub I/O counts are approximate (from docs, not verified locally) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | I (Information Disclosure) | objects.json stubs | accept | Stubs contain only public object metadata from package documentation -- no secrets |
| T-24-02 | T (Tampering) | verified flag | mitigate | All stubs set verified:false; only local extraction flips to true; tests enforce this invariant |
</threat_model>

<verification>
- `python -m pytest tests/test_package_schema.py -x -q -k "community"` passes
- `python -c "from src.maxpat.db_lookup import ObjectDatabase; db=ObjectDatabase(); pkgs=db.list_packages(); assert 'FluCoMa' in pkgs; assert 'Bach' in pkgs; assert 'IRCAM Spat' in pkgs; print(f'{len(pkgs)} packages loaded')"` shows community packages loaded
- All 10 community package objects.json files are non-empty
- package_info.json IRCAM Spat tier is "community"
</verification>

<success_criteria>
All 10 community packages have curated stub entries in their objects.json files. Each entry follows the ableton-dsp schema with verified:false. ObjectDatabase auto-loads all stubs. Tests verify schema, counts, and lookups.
</success_criteria>

<output>
After completion, create `.planning/phases/24-community-package-support/24-01-SUMMARY.md`
</output>
