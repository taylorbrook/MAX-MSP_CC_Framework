---
phase: 24-layout
plan: 03
type: tdd
wave: 2
depends_on: [24-01]
files_modified:
  - src/maxpat/m4l_layout.py
  - tests/test_m4l_layout.py
autonomous: true
requirements: [LAYOUT-02]
must_haves:
  truths:
    - "Readout overlay places live.numbox on top of live.dial with ignoreclick=1"
    - "Overlay readout positioned at bottom of dial, same x coordinate"
    - "Overlay readout box appears earlier in boxes array (renders on top)"
    - "Popup panel creates a panel with hidden=1 and a show/hide toggle button"
    - "All overlay coordinates are whole integers"
  artifacts:
    - path: "src/maxpat/m4l_layout.py"
      provides: "Overlay pattern functions"
      contains: "add_readout_overlay"
    - path: "tests/test_m4l_layout.py"
      provides: "Overlay pattern tests"
      contains: "TestOverlay"
  key_links:
    - from: "src/maxpat/m4l_layout.py"
      to: "src/maxpat/sizing.py"
      via: "UI_SIZES for overlay control dimensions"
      pattern: "UI_SIZES"
---

<objective>
TDD overlay layout patterns -- readout overlays on dials/sliders and popup panels for advanced settings.

Purpose: Provides the two overlay patterns from D-06: (a) value readouts overlaid on interactive controls, and (b) hidden popup panels toggled by show/hide buttons. These are utility functions agents call when building devices, not automatically applied.
Output: Overlay helper functions added to m4l_layout.py, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-layout/24-CONTEXT.md
@.planning/phases/24-layout/24-RESEARCH.md
@.planning/phases/24-layout/24-01-SUMMARY.md
@src/maxpat/m4l_layout.py
@tests/test_m4l_layout.py
@src/maxpat/sizing.py

<interfaces>
<!-- From Plan 01 output -->

From src/maxpat/m4l_layout.py:
```python
def _set_pres_rect(box: dict, x: int, y: int, w: int, h: int) -> None:
    """Set presentation_rect with whole-pixel enforcement."""

def _get_control_size(maxclass: str) -> tuple[int, int]:
    """Get (width, height) for a live.* control."""

LAYOUT_VARNAME_PREFIX = "_layout_"
```

From src/maxpat/sizing.py:
```python
UI_SIZES["live.dial"] = (44.0, 66.0)
UI_SIZES["live.numbox"] = (56.0, 15.0)
UI_SIZES["live.slider"] = (39.0, 87.0)
```

CLAUDE.md Rule #6 (overlay readout pattern):
- Create interactive control (e.g., dial)
- Create readout display (e.g., flonum/live.numbox)
- Readout earlier in boxes array = renders on top
- Set ignoreclick=1 on readout so dial remains interactive
- Position readout overlapping the dial
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD overlay patterns -- readout overlays and popup panels</name>
  <files>src/maxpat/m4l_layout.py, tests/test_m4l_layout.py</files>
  <read_first>
    - src/maxpat/m4l_layout.py (Plan 01 output -- _set_pres_rect, _get_control_size, constants)
    - tests/test_m4l_layout.py (Plan 01 output -- test helpers)
    - src/maxpat/sizing.py (UI_SIZES for live.numbox, live.dial dimensions)
    - .planning/phases/24-layout/24-RESEARCH.md (Pattern 4: Overlay Readouts, Pitfall 3)
    - CLAUDE.md (Rule #6: Z-Order Awareness, overlay readout pattern)
  </read_first>
  <behavior>
    - Test: add_readout_overlay inserts a live.numbox dict for a live.dial
    - Test: readout has ignoreclick=1 (D-06a)
    - Test: readout has presentation=1 and whole-integer presentation_rect
    - Test: readout x matches dial x (aligned horizontally)
    - Test: readout y = dial_y + dial_h - readout_h (bottom of dial area)
    - Test: readout box inserted at index 0 in boxes array (renders on top, CLAUDE.md Rule #6)
    - Test: readout id has unique suffix to avoid collision
    - Test: readout width matches dial width (44px for live.dial), not default live.numbox width
    - Test: add_readout_overlay works for live.slider too (different dimensions)
    - Test: create_popup_panel creates a panel box with hidden=1
    - Test: popup panel has presentation=1 and presentation_rect covering device area
    - Test: popup panel has varname with _layout_ prefix for scripting
    - Test: create_popup_panel also creates a live.text toggle button (Show/Hide)
    - Test: toggle button has presentation=1 and presentation_rect
    - Test: all popup panel coordinates are whole integers
  </behavior>
  <action>
    **RED phase:** Add test classes to existing `tests/test_m4l_layout.py`:

    Test classes:
    - `TestReadoutOverlay` -- readout overlay positioning, ignoreclick, z-order, dimension matching
    - `TestPopupPanel` -- hidden panel creation, toggle button, scripting name

    Commit RED: `test(24-03): add failing tests for overlay patterns`

    **GREEN phase:** Add to `src/maxpat/m4l_layout.py`:

    Functions:
    - `add_readout_overlay(boxes: list[dict], control_box: dict, readout_id: str | None = None) -> dict` -- Creates a live.numbox overlay for a live.dial or live.slider control.
      Implementation:
      1. Get control's presentation_rect [x, y, w, h]
      2. Compute readout dimensions: width = control's width (not default live.numbox 56px), height = 15 (live.numbox height from UI_SIZES)
      3. Position readout at bottom of control: readout_y = y + h - 15
      4. Create live.numbox box dict with: `maxclass="live.numbox"`, `ignoreclick=1`, `presentation=1`, `presentation_rect=[int(x), int(readout_y), int(w), 15]`, unique `id` from readout_id or generated
      5. Insert at index 0 of boxes list (renders on top per CLAUDE.md Rule #6)
      6. Return the created readout box dict

    - `create_popup_panel(boxes: list[dict], panel_name: str, device_width: int, device_height: int = 169, button_rect: list[int] | None = None) -> tuple[dict, dict]` -- Creates a hidden popup panel and toggle button.
      Implementation:
      1. Create panel box dict: `maxclass="panel"`, `hidden=1`, `presentation=1`, `presentation_rect=[0, 0, int(device_width), int(device_height)]`, `varname=f"{LAYOUT_VARNAME_PREFIX}panel_{panel_name}"`
      2. Create live.text toggle button: `maxclass="live.text"`, `presentation=1`, `presentation_rect=button_rect or [device_width - 50, 4, 44, 15]`, `text=panel_name`, `mode=1` (toggle), `parameter_enable=1`
      3. Append panel to boxes list, append button to boxes list
      4. Return (panel_dict, button_dict) tuple

    These are utility functions called by agents when building devices, NOT automatically applied by layout_m4l_presentation(). They are part of the layout module's public API for overlay patterns.

    Commit GREEN: `feat(24-03): implement overlay patterns -- readout overlays and popup panels`

    **REFACTOR phase:** Clean up if needed. Ensure all tests pass.

    Commit REFACTOR (if changes): `refactor(24-03): clean up overlay patterns`
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_m4l_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/maxpat/m4l_layout.py` contains `def add_readout_overlay(`
    - `src/maxpat/m4l_layout.py` contains `def create_popup_panel(`
    - `src/maxpat/m4l_layout.py` contains `"ignoreclick": 1`
    - `src/maxpat/m4l_layout.py` contains `"hidden": 1`
    - `tests/test_m4l_layout.py` contains `class TestReadoutOverlay`
    - `tests/test_m4l_layout.py` contains `class TestPopupPanel`
    - `python3 -m pytest tests/test_m4l_layout.py -x -q` exits 0
    - `python3 -m pytest tests/ -x -q --ignore=tests/test_analysis.py --ignore=tests/test_generation.py` exits 0
  </acceptance_criteria>
  <done>
    Readout overlay places live.numbox on top of live.dial/slider with ignoreclick=1 and correct z-order. Popup panel creates hidden panel with toggle button and scripting name. All coordinates whole integers. Full test suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- pure in-memory dict manipulation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | accept | No security surface |
</threat_model>

<verification>
- `python3 -m pytest tests/test_m4l_layout.py -x -q` -- all layout tests pass (Plans 01, 02, 03)
- `python3 -m pytest tests/ -x -q --ignore=tests/test_analysis.py --ignore=tests/test_generation.py` -- no regressions
- Grep: `add_readout_overlay` defined in m4l_layout.py
- Grep: `create_popup_panel` defined in m4l_layout.py
- Grep: `ignoreclick` in overlay creation
</verification>

<success_criteria>
- Readout overlay correctly positioned at bottom of dial/slider with matching width
- Overlay has ignoreclick=1 and is inserted at index 0 for z-order
- Popup panel created with hidden=1, scripting name, toggle button
- All coordinates whole integers
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/24-layout/24-03-SUMMARY.md`
</output>
