---
phase: 24-layout
plan: 02
type: tdd
wave: 2
depends_on: [24-01]
files_modified:
  - src/maxpat/m4l_layout.py
  - tests/test_m4l_layout.py
autonomous: true
requirements: [LAYOUT-02]
must_haves:
  truths:
    - "Layout auto-selects tabbed pattern when controls exceed single-page capacity"
    - "Tabbed layout creates live.tab with group names as tab labels"
    - "Tab switching uses live.tab + thispatcher + script hide/show with varname scripting (D-04 unlocked — user-approved approach)"
    - "Inactive tab page controls have hidden=1"
    - "Active tab page controls have hidden=0"
    - "live.tab is positioned at top of device with livemode=1"
  artifacts:
    - path: "src/maxpat/m4l_layout.py"
      provides: "Tabbed layout pattern implementation"
      contains: "_apply_tabbed_layout"
    - path: "tests/test_m4l_layout.py"
      provides: "Tabbed layout tests"
      contains: "TestTabbedLayout"
  key_links:
    - from: "src/maxpat/m4l_layout.py"
      to: "src/maxpat/m4l_layout.py"
      via: "_select_layout_pattern returns 'tabbed' based on threshold"
      pattern: "_select_layout_pattern"
---

<objective>
TDD tabbed layout pattern -- auto-detection of when tabbing is needed, live.tab creation, thispatcher wiring for script hide/show, and tab page control visibility management.

Purpose: Handles devices with too many controls to fit on a single 169px page. Uses standard M4L tabbed device pattern (live.tab + thispatcher + scripting names) matching Ableton stock devices. This is the D-04 unlocked approach -- user approved script hide/show over bpatcher swap for simplicity.
Output: Tabbed layout functions added to m4l_layout.py, comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-layout/24-CONTEXT.md
@.planning/phases/24-layout/24-RESEARCH.md
@.planning/phases/24-layout/24-01-SUMMARY.md
@src/maxpat/m4l_layout.py
@tests/test_m4l_layout.py
@src/maxpat/sizing.py

<interfaces>
<!-- From Plan 01 output (m4l_layout.py) -->

From src/maxpat/m4l_layout.py (created in Plan 01):
```python
DEVICE_HEIGHT = 169
TOP_MARGIN = 4
BOTTOM_MARGIN = 4
LEFT_MARGIN = 4
GROUP_LABEL_HEIGHT = 18
LABEL_GAP = 4
CONTROL_H_GAP = 5
GROUP_H_GAP = 10
CONTROL_V_GAP = 4
DEFAULT_DEVICEWIDTH = 300
MAX_DEVICEWIDTH = 900
TALL_CONTROL_THRESHOLD = 100
GROUP_PRIORITY: dict[str, int]

def layout_m4l_presentation(patch_dict: dict) -> dict: ...
def _group_controls(controls: list[dict]) -> dict[str, list[dict]]: ...
def _get_control_size(maxclass: str) -> tuple[int, int]: ...
def _set_pres_rect(box: dict, x: int, y: int, w: int, h: int) -> None: ...
def _add_group_label(boxes: list, name: str, x: int, y: int, label_id: str) -> dict: ...
def _layout_column(group_name, controls, x, top_y, boxes) -> int: ...
```

From src/maxpat/sizing.py:
```python
UI_SIZES["live.tab"] = (100.0, 20.0)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD tabbed layout -- threshold detection, live.tab creation, script hide/show wiring</name>
  <files>src/maxpat/m4l_layout.py, tests/test_m4l_layout.py</files>
  <read_first>
    - src/maxpat/m4l_layout.py (Plan 01 output -- current layout engine, constants, helper functions)
    - tests/test_m4l_layout.py (Plan 01 output -- existing test helpers and patterns)
    - src/maxpat/sizing.py (UI_SIZES for live.tab dimensions)
    - .planning/phases/24-layout/24-RESEARCH.md (Pattern 3: Tab Switching via script hide/show, Pitfall 2, Pitfall 3)
  </read_first>
  <behavior>
    - Test: _select_layout_pattern returns "single" for <= 8 total controls that fit in 169px
    - Test: _select_layout_pattern returns "tabbed" for > 8 total controls (D-05)
    - Test: _select_layout_pattern returns "tabbed" when > 3 groups (research recommendation)
    - Test: tabbed layout adds a live.tab box to the patch with maxclass="live.tab"
    - Test: live.tab has livemode=1 for pixel snapping (Pitfall 6)
    - Test: live.tab presentation_rect is at top of device, y=TOP_MARGIN
    - Test: live.tab has parameter_enable=1 with parameter_type=2 (ENUM) and parameter_enum matching group names
    - Test: live.tab has parameter_longname="Page" and parameter_shortname="Page"
    - Test: tabbed layout adds thispatcher box to the patch
    - Test: tabbed layout creates select object with N outputs for N tab pages
    - Test: each tab page's controls get varname with _layout_ prefix if no existing varname (Pitfall 3)
    - Test: first tab page controls have hidden=0, other pages have hidden=1
    - Test: all tab page controls still have presentation=1 and valid presentation_rect
    - Test: tab page controls are positioned below live.tab (y starts after tab height + gap)
    - Test: wiring chain exists: live.tab -> select -> messages -> thispatcher
    - Test: message boxes contain "script show _layout_{name}" / "script hide _layout_{name}" text
    - Test: live.gain~ always placed on first page, never in tabs (Pitfall 1)
    - Test: all presentation_rect values in tabbed layout are whole integers (D-10)
    - Test: devicewidth updated to fit tab content
  </behavior>
  <action>
    **RED phase:** Add test classes to existing `tests/test_m4l_layout.py`:

    Test classes:
    - `TestLayoutPatternSelection` -- threshold detection: single vs tabbed based on control count and group count
    - `TestTabbedLayout` -- full tabbed layout: live.tab creation, page visibility, control positioning below tab
    - `TestTabWiring` -- wiring chain: live.tab -> select -> messages -> thispatcher
    - `TestTabSpecialCases` -- live.gain~ on first page, varname collision avoidance

    Helper: `_make_many_controls(n, groups)` -- creates N controls distributed across specified groups (e.g., 12 controls across 4 groups to trigger tabbed layout)

    Commit RED: `test(24-02): add failing tests for tabbed layout pattern`

    **GREEN phase:** Add to `src/maxpat/m4l_layout.py`:

    Constants:
    ```python
    TAB_CONTROL_THRESHOLD = 8      # Total controls triggering tab mode (D-05)
    TAB_GROUP_THRESHOLD = 3        # Groups triggering tab mode
    TAB_HEIGHT = 20                # live.tab presentation height
    TAB_GAP = 4                    # Gap below live.tab
    LAYOUT_VARNAME_PREFIX = "_layout_"  # Prefix for scripting names (Pitfall 3)
    ```

    Functions:
    - `_select_layout_pattern(groups: dict[str, list[dict]], patcher: dict) -> str` -- returns "single" or "tabbed". Triggers tabbed when total controls > TAB_CONTROL_THRESHOLD or len(groups) > TAB_GROUP_THRESHOLD. Also triggers tabbed if any single group has > 8 controls per D-05.
    - `_apply_tabbed_layout(groups: dict[str, list[dict]], patcher: dict) -> None` -- creates live.tab at top with group names as parameter_enum, creates thispatcher + select + message wiring chain in the patching (non-presentation) layer, positions each group's controls as a column below the tab (offset by TAB_HEIGHT + TAB_GAP from top), sets hidden=1 on all pages except first, sets varname with `_layout_` prefix on controls that lack one (Pitfall 3). Special-cases live.gain~ and live.scope~ to always be on the first page. Per D-04 (unlocked): uses script hide/show on individual controls by varname — no bpatcher swap needed.
    - `_create_tab_box(group_names: list[str], patcher: dict) -> dict` -- creates live.tab box dict with livemode=1, parameter_enable=1, parameter_type=2, parameter_enum=group_names, presentation_rect at top of device.
    - `_create_tab_wiring(group_names: list[str], patcher: dict) -> None` -- creates the patching-mode wiring chain: thispatcher box, select box with len(group_names) outlets, message boxes with "script show/hide" text, and patchlines connecting them. All wiring objects are non-presentation (no presentation=1).

    Update `layout_m4l_presentation()` to call `_select_layout_pattern()` and branch to `_apply_tabbed_layout()` when pattern is "tabbed".

    Commit GREEN: `feat(24-02): implement tabbed layout with live.tab and script hide/show`

    **REFACTOR phase:** Clean up if needed. Ensure all tests pass.

    Commit REFACTOR (if changes): `refactor(24-02): clean up tabbed layout`
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_m4l_layout.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/maxpat/m4l_layout.py` contains `def _select_layout_pattern(`
    - `src/maxpat/m4l_layout.py` contains `def _apply_tabbed_layout(`
    - `src/maxpat/m4l_layout.py` contains `def _create_tab_box(`
    - `src/maxpat/m4l_layout.py` contains `def _create_tab_wiring(`
    - `src/maxpat/m4l_layout.py` contains `TAB_CONTROL_THRESHOLD = 8`
    - `src/maxpat/m4l_layout.py` contains `"livemode": 1`
    - `src/maxpat/m4l_layout.py` contains `LAYOUT_VARNAME_PREFIX`
    - `src/maxpat/m4l_layout.py` contains `"script show`
    - `tests/test_m4l_layout.py` contains `class TestTabbedLayout`
    - `tests/test_m4l_layout.py` contains `class TestLayoutPatternSelection`
    - `python3 -m pytest tests/test_m4l_layout.py -x -q` exits 0
    - `python3 -m pytest tests/ -x -q --ignore=tests/test_analysis.py --ignore=tests/test_generation.py` exits 0
  </acceptance_criteria>
  <done>
    Tabbed layout auto-selected when controls exceed threshold. live.tab created with group names, thispatcher wiring chain generated, inactive page controls hidden. All coords whole integers. live.gain~ special-cased to first page.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries -- pure in-memory dict manipulation.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | accept | No security surface |
</threat_model>

<verification>
- `python3 -m pytest tests/test_m4l_layout.py -x -q` -- all layout tests pass (including Plan 01 tests)
- `python3 -m pytest tests/ -x -q --ignore=tests/test_analysis.py --ignore=tests/test_generation.py` -- no regressions
- Grep: `_apply_tabbed_layout` defined in m4l_layout.py
- Grep: `"live.tab"` maxclass in created tab box
- Grep: `"thispatcher"` maxclass in created wiring
</verification>

<success_criteria>
- Pattern selection correctly identifies when tabbed layout is needed
- live.tab created with correct attributes (livemode, parameter_enable, parameter_enum)
- Wiring chain (live.tab -> select -> messages -> thispatcher) generated
- Tab page controls positioned and visibility-managed (hidden attribute via script hide/show per D-04 unlocked)
- Tall controls (live.gain~) always on first page
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/24-layout/24-02-SUMMARY.md`
</output>
