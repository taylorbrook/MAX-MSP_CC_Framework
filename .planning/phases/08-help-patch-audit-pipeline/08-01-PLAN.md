---
phase: 08-help-patch-audit-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/audit/__init__.py
  - src/maxpat/audit/parser.py
  - tests/test_audit_parser.py
  - tests/fixtures/sample_help.json
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "Parser recursively descends into subpatcher tabs to find all object instances"
    - "Parser extracts outlet types, inlet/outlet counts, box widths, arguments, and connection data from each instance"
    - "Degenerate instances (label objects with no connections and mismatched I/O) are filtered out"
    - "Legitimate sink objects (print, send, dac~) with 0 outlets are preserved"
  artifacts:
    - path: "src/maxpat/audit/__init__.py"
      provides: "Package init with BoxInstance dataclass and shared types"
      contains: "class BoxInstance"
    - path: "src/maxpat/audit/parser.py"
      provides: "HelpPatchParser with recursive descent and degenerate filtering"
      exports: ["HelpPatchParser", "traverse_patcher", "parse_object_text", "is_degenerate"]
      min_lines: 80
    - path: "tests/test_audit_parser.py"
      provides: "Unit tests for recursive descent and degenerate filtering"
      min_lines: 60
    - path: "tests/fixtures/sample_help.json"
      provides: "Synthetic help file with nested subpatchers for testing"
      contains: "patcher"
  key_links:
    - from: "src/maxpat/audit/parser.py"
      to: "src/maxpat/db_lookup.py"
      via: "ObjectDatabase import for degenerate instance filtering"
      pattern: "from src\\.maxpat\\.db_lookup import ObjectDatabase|from.*db_lookup.*import"
    - from: "src/maxpat/audit/parser.py"
      to: "src/maxpat/audit/__init__.py"
      via: "BoxInstance dataclass import"
      pattern: "from.*audit.*import BoxInstance"
---

<objective>
Build the help patch parser that recursively traverses .maxhelp JSON files, extracts all object instances (including those inside subpatcher tabs), and filters out degenerate instances used as labels/decoration.

Purpose: This is the data extraction foundation -- every downstream module (analyzer, reporter, override generator) consumes the parser's output. Getting the recursive descent and filtering right here prevents cascading errors in later plans.

Output: `src/maxpat/audit/` package with parser module, BoxInstance dataclass, synthetic test fixture, and passing tests.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-help-patch-audit-pipeline/08-CONTEXT.md
@.planning/phases/08-help-patch-audit-pipeline/08-RESEARCH.md

@src/maxpat/db_lookup.py
@.claude/max-objects/overrides.json

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/maxpat/db_lookup.py:
```python
class ObjectDatabase:
    def __init__(self, db_root: str | Path | None = None): ...
    def lookup(self, name: str) -> dict | None: ...
    def exists(self, name: str) -> bool: ...
    def compute_io_counts(self, name: str, args: list[str] | None = None) -> tuple[int, int]: ...
    def get_outlet_types(self, name: str, args: list[str] | None = None) -> list[str]: ...
```

Help file JSON structure (from RESEARCH.md):
```json
{
  "patcher": {
    "boxes": [
      {"box": {"id": "obj-7", "maxclass": "newobj", "text": "cycle~ 440",
               "numinlets": 2, "numoutlets": 1, "outlettype": ["signal"],
               "patching_rect": [x, y, width, height],
               "patcher": { ... }  }}
    ],
    "lines": [
      {"patchline": {"source": ["obj-id", outlet_index], "destination": ["obj-id", inlet_index]}}
    ]
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define BoxInstance dataclass and implement recursive parser</name>
  <files>src/maxpat/audit/__init__.py, src/maxpat/audit/parser.py, tests/fixtures/sample_help.json, tests/test_audit_parser.py</files>
  <behavior>
    - Test: Parsing a help file with top-level newobj boxes extracts all instances with correct name, text, numinlets, numoutlets, outlettype, patching_rect
    - Test: Parsing a help file with subpatcher tabs (depth 1) finds instances inside the subpatcher
    - Test: Parsing a help file with nested subpatchers (depth 2) finds instances at all levels
    - Test: Non-newobj boxes (comment, message, number, button, toggle) are not extracted as object instances
    - Test: Connection data is correctly associated with each box -- connected_ids tracks boxes that appear in any patchline
    - Test: Box connections are scoped per-patcher -- boxes in different subpatchers with same ID do not cross-contaminate
    - Test: parse_object_text correctly splits "cycle~ 440" into ("cycle~", ["440"], {})
    - Test: parse_object_text handles @attribute syntax: "line~ @activeout 1" -> ("line~", [], {"activeout": "1"})
    - Test: parse_object_text handles "t b i f" -> ("t", ["b", "i", "f"], {})
    - Test: parse_object_text handles empty text -> ("", [], {})
  </behavior>
  <action>
  1. Create `src/maxpat/audit/__init__.py` with:
     - `BoxInstance` dataclass: name (str), text (str), numinlets (int), numoutlets (int), outlettype (list[str]), patching_rect (list[float]), box_id (str), depth (int), is_connected (bool), connections (list[tuple]), source_file (str, default="")
     - Module docstring explaining the audit package purpose

  2. Create `tests/fixtures/sample_help.json` -- a synthetic .maxhelp-format JSON with:
     - Top-level patcher with 2 newobj boxes (e.g., "cycle~ 440", "dac~") connected by a patchline
     - 1 comment box and 1 message box (should be ignored)
     - 1 subpatcher tab ("p basic") containing 2 more newobj boxes (e.g., "trigger b i", "print") connected by a patchline
     - Inside "p basic", a nested subpatcher ("p details") with 1 newobj box (e.g., "metro 500")
     - Use realistic box IDs ("obj-1" through "obj-10"), correct patchline format with source/destination arrays
     - Include patching_rect values and outlettype arrays on all newobj boxes

  3. Create `src/maxpat/audit/parser.py` with:
     - `parse_object_text(text: str) -> tuple[str, list[str], dict[str, str]]`: splits newobj text into (name, positional_args, attributes). Split on first `@` to separate positional from attribute args. Handle empty text edge case.
     - `traverse_patcher(patcher: dict, depth: int = 0, source_file: str = "") -> list[BoxInstance]`: recursive descent into patcher hierarchy. For each patcher scope: (a) build id_map of box_id -> box dict, (b) extract connections from lines array, (c) build connected_ids set, (d) yield BoxInstance for each maxclass=="newobj" box with non-empty text, (e) recurse into any box with a "patcher" key.
     - `HelpPatchParser` class:
       - `__init__(self)`: no state needed (stateless parser)
       - `parse_file(self, path: Path) -> list[BoxInstance]`: open JSON file, call traverse_patcher on top-level patcher, set source_file on all instances
       - `parse_directory(self, help_dir: Path) -> dict[str, list[BoxInstance]]`: glob for *.maxhelp, parse each file, return dict keyed by object name (derived from filename stem) with list of all instances found

  4. Write tests in `tests/test_audit_parser.py`:
     - Use the sample_help.json fixture
     - Test all behaviors listed above
     - Test parse_object_text with the 4 cases listed
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_audit_parser.py -x -q</automated>
  </verify>
  <done>BoxInstance dataclass defined, recursive parser extracts instances from nested subpatchers, parse_object_text handles all argument formats, all parser tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement degenerate instance filtering</name>
  <files>src/maxpat/audit/parser.py, tests/test_audit_parser.py</files>
  <behavior>
    - Test: An unconnected box whose I/O counts match the DB is NOT filtered (e.g., print with 1 inlet, 0 outlets)
    - Test: An unconnected box whose I/O counts do NOT match the DB IS filtered (e.g., a "cycle~" with numinlets=0, numoutlets=0 used as label)
    - Test: A connected box is NEVER filtered, regardless of I/O count mismatch
    - Test: An unknown object (not in DB) with no connections IS filtered
    - Test: A variable_io object with no connections is NOT filtered (can't reliably determine expected counts)
    - Test: Legitimate sink objects (dac~, print, send) with 0 outlets and no outgoing connections are NOT filtered when their counts match DB
  </behavior>
  <action>
  1. Add `is_degenerate(instance: BoxInstance, db: ObjectDatabase) -> bool` function to `parser.py`:
     - If instance.is_connected is True, return False (never filter connected instances)
     - Look up instance.name in db via db.lookup()
     - If not found in DB, return True (unknown + unconnected = degenerate)
     - If object has variable_io=True, return False (can't determine expected counts)
     - Compute expected counts via db.compute_io_counts(instance.name)
     - If instance.numinlets == expected_inlets AND instance.numoutlets == expected_outlets, return False (legitimate unconnected instance)
     - Otherwise return True (counts don't match + not connected = label/decoration)

  2. Add `filter_degenerate(instances: list[BoxInstance], db: ObjectDatabase) -> list[BoxInstance]` function:
     - Returns list of instances where is_degenerate returns False
     - Preserves order

  3. Update `HelpPatchParser.parse_file()` to accept optional `db: ObjectDatabase = None` parameter. When db is provided, apply degenerate filtering to results.

  4. Add tests for all 6 degenerate filtering behaviors to `tests/test_audit_parser.py`:
     - Use ObjectDatabase with default DB for realistic filtering
     - Create BoxInstance fixtures representing each scenario
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_audit_parser.py -x -q</automated>
  </verify>
  <done>Degenerate filtering correctly identifies label objects while preserving legitimate sinks (print, send, dac~), all parser and filtering tests pass, existing test suite unaffected</done>
</task>

</tasks>

<verification>
- All tests in tests/test_audit_parser.py pass
- Existing test suite still passes: `python3 -m pytest tests/ -x -q`
- src/maxpat/audit/ package is importable: `python3 -c "from src.maxpat.audit.parser import HelpPatchParser; print('OK')"`
- Parser can process the sample fixture: `python3 -c "from src.maxpat.audit.parser import HelpPatchParser; from pathlib import Path; p = HelpPatchParser(); r = p.parse_file(Path('tests/fixtures/sample_help.json')); print(f'{len(r)} instances found')"`
</verification>

<success_criteria>
- BoxInstance dataclass captures all metadata (name, text, I/O counts, outlettype, patching_rect, connections, depth)
- Recursive parser finds instances at all subpatcher depths
- parse_object_text correctly handles positional args, @attributes, aliases
- Degenerate filtering removes label objects while preserving legitimate sinks
- All new tests pass; all 624+ existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-help-patch-audit-pipeline/08-01-SUMMARY.md`
</output>
