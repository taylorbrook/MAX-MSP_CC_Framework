---
phase: 26-phase20-data-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/overrides.json
  - .claude/max-objects/relationships.json
  - CLAUDE.md
  - tests/test_m4l_db.py
autonomous: true
requirements: [DB-01, DB-02, DB-03, ROUTING-02]
gap_closure: true

must_haves:
  truths:
    - "ObjectDatabase.lookup('live.scope~') returns domain 'M4L' (not 'Packages')"
    - "ObjectDatabase.lookup('live.adsrui') returns dict with non-empty inlets and outlets"
    - "ObjectDatabase.lookup('live.adsr~') returns dict with non-empty inlets and outlets"
    - "relationships.json contains plugin~/plugout~, live.path/live.object, midiin/midiout pairs"
    - "CLAUDE.md contains M4L domain-specific rules section"
  artifacts:
    - path: ".claude/max-objects/overrides.json"
      provides: "Domain fix for live.scope~, I/O for live.adsrui and live.adsr~"
      contains: '"domain": "M4L"'
    - path: ".claude/max-objects/relationships.json"
      provides: "M4L object relationship pairs"
      contains: "plugin~"
    - path: "CLAUDE.md"
      provides: "M4L domain-specific rules"
      contains: "### M4L (Max for Live)"
    - path: "tests/test_m4l_db.py"
      provides: "Tests for DB-01, DB-02, DB-03"
      exports: ["TestM4LDatabase"]
  key_links:
    - from: ".claude/max-objects/overrides.json"
      to: "src/maxpat/db_lookup.py"
      via: "ObjectDatabase deep-merges overrides onto base entries"
      pattern: "live\\.scope~.*domain.*M4L"
---

<objective>
Fix M4L database gaps (DB-01, DB-02, DB-03) and add CLAUDE.md M4L rules section (ROUTING-02).

Purpose: Close 4 of 7 Phase 20 gap-closure requirements -- data fixes in overrides.json and relationships.json, plus governance documentation in CLAUDE.md.
Output: Updated overrides.json, relationships.json, CLAUDE.md, and test_m4l_db.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-phase20-data-fixes/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix live.scope~ domain and add live.adsrui/live.adsr~ I/O overrides (DB-01, DB-02)</name>
  <files>.claude/max-objects/overrides.json, tests/test_m4l_db.py</files>
  <read_first>
    - .claude/max-objects/overrides.json (current override entries, especially live.scope~ at ~line 5778)
    - .claude/max-objects/packages/objects.json (base entries for live.adsrui ~line 10580, live.adsr~ ~line 10722, live.scope~ ~line 10832)
    - src/maxpat/db_lookup.py (ObjectDatabase class, understand how overrides are deep-merged)
  </read_first>
  <behavior>
    - Test: ObjectDatabase().lookup("live.scope~") returns dict with key "domain" == "M4L"
    - Test: ObjectDatabase().lookup("live.adsrui") returns dict with len(inlets) >= 1 and len(outlets) >= 1
    - Test: ObjectDatabase().lookup("live.adsr~") returns dict with len(inlets) >= 1 and len(outlets) >= 1
    - Test: ObjectDatabase().lookup("live.adsr~") has at least one signal outlet
    - Test: ObjectDatabase().lookup("live.adsrui") has domain overridden to "M4L"
    - Test: ObjectDatabase().lookup("live.adsr~") has domain overridden to "M4L"
  </behavior>
  <action>
1. Create tests/test_m4l_db.py with a TestM4LDatabase class containing:
   - test_live_scope_domain_is_m4l: `db.lookup("live.scope~")["domain"] == "M4L"`
   - test_live_adsrui_has_io: `len(obj["inlets"]) >= 1 and len(obj["outlets"]) >= 1`
   - test_live_adsr_tilde_has_io: `len(obj["inlets"]) >= 1` and at least one signal outlet
   - test_live_adsrui_domain_is_m4l: domain == "M4L"
   - test_live_adsr_tilde_domain_is_m4l: domain == "M4L"

2. Run tests (expect RED -- domain is "Packages", I/O is empty).

3. Edit .claude/max-objects/overrides.json:
   - For "live.scope~" entry (~line 5778): Add `"domain": "M4L"` field alongside existing inlets/outlets/_audit
   - Add new "live.adsrui" entry with:
     ```json
     "live.adsrui": {
       "domain": "M4L",
       "inlets": [
         {"id": 0, "type": "list", "signal": false, "digest": "(list) ADSR values (attack, decay, sustain, release)", "hot": true}
       ],
       "outlets": [
         {"id": 0, "type": "list", "signal": false, "digest": "(list) ADSR parameter values"}
       ],
       "_audit": {
         "confidence": "LOW",
         "source": "training_knowledge",
         "note": "live.adsrui I/O from training data -- verify in MAX. 1 inlet (list/float/bang), 1 outlet (list)"
       }
     }
     ```
   - Add new "live.adsr~" entry with:
     ```json
     "live.adsr~": {
       "domain": "M4L",
       "inlets": [
         {"id": 0, "type": "signal", "signal": true, "digest": "(signal) Gate/trigger input", "hot": true},
         {"id": 1, "type": "float", "signal": false, "digest": "(float) Retrigger mode", "hot": false}
       ],
       "outlets": [
         {"id": 0, "type": "signal", "signal": true, "digest": "(signal) Envelope output"}
       ],
       "_audit": {
         "confidence": "LOW",
         "source": "training_knowledge",
         "note": "live.adsr~ I/O from training data -- verify in MAX. 1 signal inlet (gate), 1 float inlet (retrigger), 1 signal outlet (envelope)"
       }
     }
     ```
   - Place these new entries in the overrides file near the existing live.scope~ entry (in the M4L/Packages correction area, before the `_domain_rnbo` marker).

4. Run tests (expect GREEN).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_m4l_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_m4l_db.py exists and contains class TestM4LDatabase
    - overrides.json contains `"live.scope~"` entry with `"domain": "M4L"` field
    - overrides.json contains `"live.adsrui"` entry with non-empty `"inlets"` and `"outlets"` arrays
    - overrides.json contains `"live.adsr~"` entry with non-empty `"inlets"` and `"outlets"` arrays and `"domain": "M4L"`
    - python3 -m pytest tests/test_m4l_db.py -x -q exits 0
  </acceptance_criteria>
  <done>live.scope~ domain is M4L via override, live.adsrui and live.adsr~ have I/O data and domain=M4L via overrides, all verified by passing tests</done>
</task>

<task type="auto">
  <name>Task 2: Add M4L relationship entries to relationships.json (DB-03)</name>
  <files>.claude/max-objects/relationships.json, tests/test_m4l_db.py</files>
  <read_first>
    - .claude/max-objects/relationships.json (current pairs array, 19 entries)
    - tests/test_m4l_db.py (test file from Task 1 to add relationship tests)
  </read_first>
  <action>
1. Add 3 tests to tests/test_m4l_db.py in a new TestM4LRelationships class:
   - test_plugin_plugout_pair: Verify "plugin~" and "plugout~" appear together in a pairs entry with relationship "required_pair"
   - test_live_path_live_object_pair: Verify "live.path" and "live.object" appear together in a pairs entry with relationship "required_pair"
   - test_midiin_midiout_pair: Verify "midiin" and "midiout" appear together in a pairs entry with relationship "common_pair"
   Tests load relationships.json directly via json.load().

2. Run tests (expect RED -- no M4L entries).

3. Edit .claude/max-objects/relationships.json -- append these 3 entries to the end of the "pairs" array (before the closing `]`):
   ```json
   {"objects": ["plugin~", "plugout~"], "relationship": "required_pair", "note": "M4L audio I/O -- plugin~ receives audio from Live, plugout~ sends to Live"},
   {"objects": ["live.path", "live.object"], "relationship": "required_pair", "note": "Live API access -- live.path navigates the Live object model, live.object gets/sets properties"},
   {"objects": ["midiin", "midiout"], "relationship": "common_pair", "note": "M4L MIDI device I/O -- midiin receives MIDI from Live, midiout sends MIDI to Live"}
   ```

4. Run tests (expect GREEN).
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_m4l_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - relationships.json "pairs" array contains entry with "objects": ["plugin~", "plugout~"]
    - relationships.json "pairs" array contains entry with "objects": ["live.path", "live.object"]
    - relationships.json "pairs" array contains entry with "objects": ["midiin", "midiout"]
    - tests/test_m4l_db.py contains class TestM4LRelationships with 3 test methods
    - python3 -m pytest tests/test_m4l_db.py -x -q exits 0
  </acceptance_criteria>
  <done>relationships.json has 3 M4L relationship pairs with correct types, verified by tests</done>
</task>

<task type="auto">
  <name>Task 3: Add M4L domain-specific rules section to CLAUDE.md (ROUTING-02)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file -- understand section structure, especially lines 154-182 where RNBO/N4M/js sections are)
    - src/maxpat/m4l_constants.py (reference for parameter types, naming conventions)
    - src/maxpat/critics/m4l_critic.py (reference for M4L validation rules)
    - src/maxpat/project.py (reference for scaffold conventions)
  </read_first>
  <action>
Insert a new `### M4L (Max for Live)` section in CLAUDE.md after the RNBO section (after line 161, before line 163 "### Node for Max"). The section content:

```markdown
### M4L (Max for Live)

- Three device types: audio_effect (plugin~/plugout~), instrument (plugout~/midiin/midiout), midi_effect (midiin/midiout, no audio I/O)
- Every M4L device MUST contain `live.thisdevice` -- it reports device state and is the detection signal
- Use `plugin~` / `plugout~` for audio I/O (not `dac~` / `adc~`) -- these are Ableton's audio bus interface
- NO `gain~` before `plugout~` -- Ableton's channel strip handles volume control; adding gain~ causes double-volume issues
- `plugout~` is a valid terminal (like `dac~`) for DSP chain validation
- All `live.*` UI controls MUST have `parameter_enable=1` with complete `saved_attribute_attributes` (longname, shortname, type, unitstyle)
- Parameter longnames MUST be unique across the entire device -- duplicates cause Ableton crashes
- Named objects (`buffer~`, `coll`, `dict`, `send`, `receive`, `send~`, `receive~`, `value`) MUST be prefixed with `---` in M4L to avoid namespace collisions between devices
- Set `openinpresentation=1` on the patcher and `presentation=1` + `presentation_rect` on all user-facing controls
- M4L presentation view height capped at 169px -- all controls must fit within this constraint
- Use `live.banks` to organize parameters into Push controller banks (groups of 8)
- Use `create_m4l_project()` from `src.maxpat.project` to scaffold devices -- handles all boilerplate per device type
- Use `write_amxd()` from `src.maxpat.m4l_export` to export `.amxd` files with correct binary headers
```
  </action>
  <verify>
    <automated>grep -c "### M4L (Max for Live)" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains exactly one line matching "### M4L (Max for Live)"
    - The M4L section appears between the RNBO section and the Node for Max section
    - Section contains rules about plugin~/plugout~, live.thisdevice, parameter_enable, --- prefix, 169px height, no gain~ before plugout~
    - grep "plugout~" CLAUDE.md returns matches in the M4L section (not just object database listing)
    - grep "parameter_enable" CLAUDE.md returns at least one match
  </acceptance_criteria>
  <done>CLAUDE.md has M4L domain-specific rules section with all key conventions documented, positioned correctly in the Domain-Specific Rules hierarchy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries applicable -- this plan modifies local JSON data files, a local test file, and project documentation. No external inputs, no network, no user data.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | I (Information Disclosure) | overrides.json | accept | JSON data files contain no secrets; public object metadata only |
</threat_model>

<verification>
python3 -m pytest tests/test_m4l_db.py -x -q
grep "### M4L (Max for Live)" CLAUDE.md
python3 -c "from src.maxpat.db_lookup import ObjectDatabase; db = ObjectDatabase(); assert db.lookup('live.scope~')['domain'] == 'M4L'; print('DB-02 OK')"
python3 -c "from src.maxpat.db_lookup import ObjectDatabase; db = ObjectDatabase(); obj = db.lookup('live.adsrui'); assert len(obj['inlets']) >= 1; print('DB-01 OK')"
</verification>

<success_criteria>
- All test_m4l_db.py tests pass (DB-01, DB-02, DB-03)
- CLAUDE.md contains M4L rules section (ROUTING-02)
- Zero regressions in full test suite
</success_criteria>

<output>
After completion, create `.planning/phases/26-phase20-data-fixes/26-01-SUMMARY.md`
</output>
