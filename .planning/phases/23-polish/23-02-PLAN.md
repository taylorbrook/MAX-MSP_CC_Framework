---
phase: 23-polish
plan: 02
type: tdd
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/maxpat/m4l_polish.py
  - tests/test_m4l_polish.py
autonomous: true
requirements:
  - POLISH-02
  - POLISH-03
must_haves:
  truths:
    - "Parameters are grouped into banks of 8 by semantic function"
    - "Bank names auto-derived from parameter group content"
    - "Partial banks padded with empty slots (dash)"
    - "Info text describes what the parameter does using available metadata"
    - "Info text includes range from unitstyle and mmin/mmax"
    - "Existing annotations not overridden"
    - "polish_m4l_device calls naming, then banks, then info text in order"
  artifacts:
    - path: "src/maxpat/m4l_polish.py"
      provides: "Push banks, info text, and top-level polish function"
      exports: ["organize_push_banks", "populate_info_text", "polish_m4l_device"]
    - path: "tests/test_m4l_polish.py"
      provides: "Tests for POLISH-02 and POLISH-03"
      contains: "TestPushBanks"
  key_links:
    - from: "src/maxpat/m4l_polish.py"
      to: "src/maxpat/m4l_constants.py"
      via: "UnitStyle enum import"
      pattern: "from src.maxpat.m4l_constants import UnitStyle"
    - from: "src/maxpat/m4l_polish.py"
      to: "live.banks box"
      via: "_parameter_banks dict key"
      pattern: "_parameter_banks"
---

<objective>
TDD Push bank organization (POLISH-02) and info text annotations (POLISH-03), plus the top-level polish_m4l_device() compositor function.

Purpose: Push banks group parameters into banks of 8 for hardware controller display. Info text provides Ableton Info View tooltips. Together with naming from Plan 01, these complete the M4L polish pipeline.

Output: `organize_push_banks()`, `populate_info_text()`, and `polish_m4l_device()` in m4l_polish.py with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-polish/23-CONTEXT.md
@.planning/phases/23-polish/23-RESEARCH.md
@.planning/phases/23-polish/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (src/maxpat/m4l_polish.py already exists) -->

```python
# Already implemented in Plan 01:
_ABBREVIATIONS: dict[str, str]  # 20+ word->abbreviation mappings
_LIVE_NO_PARAM: frozenset  # live.* objects that don't take parameters

def derive_parameter_names(patch_dict: dict) -> dict: ...
def _collect_live_controls(boxes: list[dict]) -> list[dict]: ...
def _varname_to_longname(varname: str) -> str: ...
def _longname_to_varname(longname: str) -> str: ...
def _abbreviate_shortname(longname: str, max_len: int = 8) -> str: ...
```

From src/maxpat/m4l_constants.py:
```python
class UnitStyle(IntEnum):
    INT = 0; FLOAT = 1; TIME = 2; HERTZ = 3; DECIBEL = 4
    PERCENT = 5; PAN = 6; SEMITONES = 7; MIDI = 8; CUSTOM = 9
```

live.banks box structure (ASSUMED format -- see A1 in RESEARCH.md):
```python
{
    "id": "obj-banks",
    "maxclass": "live.banks",
    "numinlets": 1, "numoutlets": 1,
    "patching_rect": [20.0, 20.0, 315.0, 45.0],
    "_parameter_banks": {
        "banks": [
            {
                "name": "Filter",
                "parameters": ["Cutoff", "Resonance", "Drive", "-", "-", "-", "-", "-"]
            }
        ]
    }
}
```

Annotation attributes are top-level box attributes (VERIFIED in RESEARCH.md):
```python
box["annotation_name"] = "Filter Cutoff"   # Header in Info View
box["annotation"] = "Controls the lowpass filter cutoff. Range: 20-20000 Hz"  # Body
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD organize_push_banks with semantic grouping</name>
  <files>src/maxpat/m4l_polish.py, tests/test_m4l_polish.py</files>
  <read_first>
    - src/maxpat/m4l_polish.py (current state from Plan 01 -- has derive_parameter_names, _collect_live_controls)
    - tests/test_m4l_polish.py (current state from Plan 01 -- has helpers and TestNameDerivation)
    - src/maxpat/m4l_constants.py (UnitStyle enum)
    - .planning/phases/23-polish/23-RESEARCH.md (Pattern 4 for live.banks structure, Example 2 for bank keywords)
  </read_first>
  <behavior>
    RED phase -- add to tests/test_m4l_polish.py:

    class TestPushBanks:
      - test_semantic_grouping: 3 pitch params + 2 filter params -> 2 banks (D-05)
      - test_bank_padding: bank with 3 params -> padded with 5 "-" slots to fill 8 (D-07)
      - test_bank_naming: bank containing Cutoff/Resonance/Drive -> name "Filter" (D-06)
      - test_bank_naming_amp: bank containing Amp Decay/Amp Curve/Body Level -> name "Amp"
      - test_banks_of_eight: 10 params in same group -> split into 2 banks (8 + 2 padded)
      - test_main_fallback: params with no keyword match -> bank named "Main"
      - test_live_banks_box_created: if no live.banks box exists, one is added to patch
      - test_live_banks_box_reused: if live.banks already exists, its _parameter_banks is updated
      - test_params_referenced_by_longname: bank parameter entries use parameter_longname values (A2)
      - test_empty_device_no_banks: device with zero parameters -> no banks created

    GREEN phase -- add to src/maxpat/m4l_polish.py:

    _BANK_KEYWORDS dict mapping group names to keyword sets:
      "Pitch": {"pitch", "tune", "tuning", "detune", "transpose", "semitone", "cent", "octave"}
      "Amp": {"amp", "amplitude", "volume", "level", "gain", "velocity", "body"}
      "Filter": {"filter", "cutoff", "resonance", "reso", "freq", "tone", "drive", "hp", "lp", "bp"}
      "Envelope": {"envelope", "env", "attack", "decay", "sustain", "release", "adsr", "curve"}
      "Mod": {"modulation", "mod", "lfo", "rate", "depth", "amount", "speed"}
      "FX": {"delay", "reverb", "chorus", "flanger", "phaser", "distortion", "comp", "eq"}
      "Noise": {"noise", "sub"}
      "Mix": {"mix", "dry", "wet", "send", "return", "pan", "width"}

    def _classify_parameter(longname_or_varname: str) -> str:
      - Split into words (handling both spaces and underscores)
      - Score against _BANK_KEYWORDS -- prefix match gets +2 bonus
      - Return group name with highest score, or "Main" if no match

    def organize_push_banks(patch_dict: dict) -> dict:
      - Collect all live.* controls via _collect_live_controls
      - For each: extract parameter_longname from saved_attribute_attributes.valueof
      - Classify each parameter into a group via _classify_parameter
      - Group parameters: dict[group_name, list[longname]]
      - For each group, split into banks of 8. Each bank:
        {"name": group_name, "parameters": [longnames... padded with "-" to 8]}
        If group has >8, second bank is "{group_name} 2"
      - Find or create live.banks box in top-level patcher boxes
        If creating: {"id": auto, "maxclass": "live.banks", "numinlets": 1, "numoutlets": 1,
                      "outlettype": [""], "patching_rect": [20.0, 20.0, 315.0, 45.0]}
      - Set box["_parameter_banks"] = {"banks": [bank_dicts...]}
      - Return patch_dict (mutated)
  </behavior>
  <action>
    TDD RED-GREEN-REFACTOR for POLISH-02 (D-05, D-06, D-07).

    RED: Add TestPushBanks class to tests/test_m4l_polish.py with all 10 tests. Use the existing _make_live_control and _make_patch_with_controls helpers from Plan 01. For tests that check live.banks box creation, build patch dicts with live.* controls that have parameter_longname already set (since naming runs before banks in the pipeline). Run pytest -- new tests fail.

    GREEN: Add _BANK_KEYWORDS, _classify_parameter, and organize_push_banks to src/maxpat/m4l_polish.py. Implementation details:
    - _classify_parameter splits the string on both spaces and underscores, lowercases all parts, scores each word against _BANK_KEYWORDS values. The first word (prefix) gets +2 bonus if found in any keyword set.
    - organize_push_banks: use _collect_live_controls to find controls, extract longnames, group by _classify_parameter result. Then chunk each group into banks of 8.
    - To find/create live.banks box: scan boxes for maxclass=="live.banks". If not found, create one and append to patcher.boxes list. Use "obj-live-banks" as id.
    - Bank parameters are referenced by their parameter_longname strings (per A2 in research).
    - Partial banks: if a group has 3 params, pad with 5 "-" entries: ["Cutoff", "Resonance", "Drive", "-", "-", "-", "-", "-"]

    Run pytest -- all pass.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_m4l_polish.py::TestPushBanks -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_m4l_polish.py contains class TestPushBanks with at least 10 test methods
    - src/maxpat/m4l_polish.py contains `_BANK_KEYWORDS` dict with at least 8 groups
    - src/maxpat/m4l_polish.py contains `def _classify_parameter(`
    - src/maxpat/m4l_polish.py contains `def organize_push_banks(patch_dict: dict) -> dict:`
    - test_bank_padding passes: bank parameters list has exactly 8 entries
    - test_semantic_grouping passes: pitch and filter params in separate banks
    - test_params_referenced_by_longname passes: bank entries are longname strings
    - All tests in TestPushBanks pass
    - Full suite: `python3 -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>organize_push_banks groups parameters into semantic banks of 8, auto-derives bank names, pads partial banks with "-", and creates/reuses live.banks box. All bank tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD populate_info_text and polish_m4l_device compositor</name>
  <files>src/maxpat/m4l_polish.py, tests/test_m4l_polish.py</files>
  <read_first>
    - src/maxpat/m4l_polish.py (current state after Task 1 -- has naming + banks)
    - tests/test_m4l_polish.py (current state after Task 1)
    - src/maxpat/m4l_constants.py (UnitStyle enum values for range formatting)
    - .planning/phases/23-polish/23-RESEARCH.md (Example 3 for info text, Example 4 for annotation placement)
  </read_first>
  <behavior>
    RED phase -- add to tests/test_m4l_polish.py:

    class TestInfoText:
      - test_annotation_set: control with longname "Filter Cutoff", no annotation -> annotation set to descriptive text (D-08)
      - test_annotation_name_set: control with longname -> annotation_name set to longname (header in Info View)
      - test_range_formatting_hertz: unitstyle=3 (HERTZ), mmin=20, mmax=20000 -> "Range: 20-20000 Hz" in annotation (D-09)
      - test_range_formatting_decibel: unitstyle=4 (DECIBEL), mmin=-70, mmax=6 -> "Range: -70-6 dB"
      - test_range_formatting_percent: unitstyle=5 (PERCENT), mmin=0, mmax=100 -> "Range: 0-100 %"
      - test_range_formatting_midi: unitstyle=8 (MIDI) -> "Range: 0-127 (MIDI)"
      - test_no_range_when_missing: no mmin/mmax -> no "Range:" in annotation
      - test_preserves_existing_annotation: box with annotation already set -> not overridden (D-10)
      - test_preserves_existing_annotation_name: box with annotation_name set -> not overridden
      - test_annotation_is_top_level_box_attr: annotation set on box dict, NOT inside saved_attribute_attributes

    class TestPolishCompositor:
      - test_polish_calls_all_three: patch with unnamed controls -> after polish, longname derived, banks created, annotation set
      - test_polish_ordering: naming runs before banks (so banks reference correct longnames)
      - test_polish_returns_patch_dict: return value is the same dict (mutated in place)
      - test_polish_empty_patch: patch with no live.* controls -> no error, returns unchanged

    GREEN phase -- add to src/maxpat/m4l_polish.py:

    _UNIT_LABELS dict mapping UnitStyle values to display strings:
      UnitStyle.INT: "", UnitStyle.FLOAT: "", UnitStyle.TIME: "ms",
      UnitStyle.HERTZ: "Hz", UnitStyle.DECIBEL: "dB", UnitStyle.PERCENT: "%",
      UnitStyle.PAN: "(L/R)", UnitStyle.SEMITONES: "st", UnitStyle.MIDI: "(MIDI)",
      UnitStyle.CUSTOM: ""

    def _make_range_text(valueof: dict) -> str:
      - Extract mmin, mmax, unitstyle from valueof
      - If both None, return ""
      - Format as "Range: {mmin:g}-{mmax:g} {label}" where label from _UNIT_LABELS
      - Use :g format to strip trailing zeros (20.0 -> "20", 0.5 -> "0.5")

    def _make_generic_info_text(longname: str, valueof: dict) -> str:
      - Build text from available metadata: "{longname} control."
      - Append range text from _make_range_text if available
      - Return combined string. Example: "Filter Cutoff control. Range: 20-20000 Hz"

    def populate_info_text(patch_dict: dict) -> dict:
      - Collect live.* controls via _collect_live_controls
      - For each control:
        - Get longname from saved_attribute_attributes.valueof
        - If box has no "annotation" (empty or missing): set to _make_generic_info_text result (D-10)
        - If box has no "annotation_name" (empty or missing): set to longname
        - Both are TOP-LEVEL box attributes, NOT inside saved_attribute_attributes
      - Return patch_dict (mutated)

    def polish_m4l_device(patch_dict: dict) -> dict:
      - derive_parameter_names(patch_dict)   # POLISH-01 -- must run first
      - organize_push_banks(patch_dict)       # POLISH-02 -- uses longnames from step 1
      - populate_info_text(patch_dict)        # POLISH-03 -- uses longnames from step 1
      - return patch_dict
  </behavior>
  <action>
    TDD RED-GREEN-REFACTOR for POLISH-03 (D-08, D-09, D-10) and compositor (D-11, D-12).

    RED: Add TestInfoText (10 tests) and TestPolishCompositor (4 tests) to tests/test_m4l_polish.py. For info text tests, create controls with saved_attribute_attributes containing unitstyle/mmin/mmax values. Verify annotation and annotation_name are set as top-level box attributes. Run pytest -- new tests fail.

    GREEN: Add _UNIT_LABELS, _make_range_text, _make_generic_info_text, populate_info_text, polish_m4l_device to src/maxpat/m4l_polish.py.

    Key implementation details:
    - _make_range_text uses `f"{val:g}"` formatting to strip trailing zeros from floats
    - _make_generic_info_text constructs "{longname} control." as base, appends range if available
    - populate_info_text sets `box["annotation"]` and `box["annotation_name"]` directly (NOT inside saved_attribute_attributes.valueof)
    - Check `if not box.get("annotation"):` before writing (D-10 -- preserve existing)
    - polish_m4l_device is the public compositor per D-11, called explicitly by agents per D-12

    Run pytest -- all pass.
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_m4l_polish.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_m4l_polish.py contains class TestInfoText with at least 10 test methods
    - tests/test_m4l_polish.py contains class TestPolishCompositor with at least 4 test methods
    - src/maxpat/m4l_polish.py contains `def populate_info_text(patch_dict: dict) -> dict:`
    - src/maxpat/m4l_polish.py contains `def polish_m4l_device(patch_dict: dict) -> dict:`
    - src/maxpat/m4l_polish.py contains `_UNIT_LABELS` dict
    - src/maxpat/m4l_polish.py contains `def _make_range_text(valueof: dict) -> str:`
    - test_range_formatting_hertz passes: annotation contains "Range: 20-20000 Hz"
    - test_preserves_existing_annotation passes (D-10 verified)
    - test_annotation_is_top_level_box_attr passes: annotation on box dict, not inside saved_attribute_attributes
    - test_polish_calls_all_three passes: full pipeline works end to end
    - All tests in tests/test_m4l_polish.py pass
    - Full suite: `python3 -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>populate_info_text sets contextual annotations with range info per D-08/D-09/D-10. polish_m4l_device composes all three functions in correct order per D-11. All info text and compositor tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch_dict input | Untrusted dict from user-created .maxpat files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | T (Tampering) | _parameter_banks format | accept | ASSUMED format (A1). If wrong, banks won't display but no data loss. Manual testing catch. |
| T-23-04 | I (Information Disclosure) | info text content | accept | Generic fallback text contains only parameter name and range -- no sensitive data. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_m4l_polish.py -x -v` -- all tests pass including banks, info text, compositor
- `python3 -m pytest tests/ -x -q` -- full suite green
</verification>

<success_criteria>
- organize_push_banks groups parameters semantically per D-05, names banks per D-06, pads to 8 per D-07
- populate_info_text generates contextual annotations per D-08, includes range per D-09, preserves existing per D-10
- polish_m4l_device composes all three in correct order per D-11
- live.banks box created or reused with _parameter_banks attribute
- Annotations set as top-level box attributes (not inside saved_attribute_attributes)
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish/23-02-SUMMARY.md`
</output>
