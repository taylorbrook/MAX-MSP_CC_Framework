---
phase: 21-scaffold-and-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/max-router/SKILL.md
  - .claude/skills/max-router/references/dispatch-rules.md
  - .claude/skills/max-dsp-agent/SKILL.md
  - .claude/skills/max-patch-agent/SKILL.md
  - .claude/skills/max-ui-agent/SKILL.md
  - .claude/skills/max-critic/SKILL.md
autonomous: true
requirements: [ROUTING-01, ROUTING-03]

must_haves:
  truths:
    - "dispatch-rules.md contains M4L keyword section with primary and secondary keywords per D-08"
    - "max-router SKILL.md references M4L dispatch in its quick reference table"
    - "max-dsp-agent SKILL.md contains M4L signal chain rules (plugin~/plugout~ I/O, gain~/plugout~ prohibition)"
    - "max-patch-agent SKILL.md contains M4L MIDI routing section (midiin/midiout passthrough, live.path/live.object)"
    - "max-ui-agent SKILL.md contains M4L presentation section (live.* controls, parameter_enable, 169px, presentation mode)"
    - "max-critic SKILL.md contains M4L awareness note (don't flag M4L patterns as errors)"
  artifacts:
    - path: ".claude/skills/max-router/references/dispatch-rules.md"
      provides: "M4L keyword-to-agent dispatch rules"
      contains: "M4L Dispatch"
    - path: ".claude/skills/max-router/SKILL.md"
      provides: "M4L row in dispatch quick reference"
      contains: "M4L"
    - path: ".claude/skills/max-dsp-agent/SKILL.md"
      provides: "M4L signal chain rules section"
      contains: "M4L Signal Chain"
    - path: ".claude/skills/max-patch-agent/SKILL.md"
      provides: "M4L MIDI routing section"
      contains: "M4L MIDI"
    - path: ".claude/skills/max-ui-agent/SKILL.md"
      provides: "M4L presentation mode section"
      contains: "M4L Presentation"
    - path: ".claude/skills/max-critic/SKILL.md"
      provides: "M4L awareness note"
      contains: "M4L"
  key_links:
    - from: ".claude/skills/max-router/references/dispatch-rules.md"
      to: ".claude/skills/max-router/SKILL.md"
      via: "Router reads dispatch-rules.md, SKILL.md references it"
      pattern: "dispatch-rules.md"
    - from: ".claude/skills/max-router/references/dispatch-rules.md"
      to: "CLAUDE.md"
      via: "M4L dispatch rules reference CLAUDE.md M4L section"
      pattern: "CLAUDE.md"
---

<objective>
Add M4L-specific dispatch rules to the router and M4L instruction sections to 4 agent SKILL.md files + critic awareness note.

Purpose: When users request M4L device work, the router must recognize M4L keywords and dispatch with device-aware context. Agents must know M4L-specific rules (plugin~/plugout~ I/O, gain prohibition, parameter_enable, 169px height, --- prefix, MIDI passthrough).
Output: Updated dispatch-rules.md, router SKILL.md, and 4 agent SKILL.md files with M4L sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-scaffold-and-routing/21-RESEARCH.md
@.planning/phases/21-scaffold-and-routing/21-CONTEXT.md

@.claude/skills/max-router/SKILL.md
@.claude/skills/max-router/references/dispatch-rules.md
@.claude/skills/max-dsp-agent/SKILL.md
@.claude/skills/max-patch-agent/SKILL.md
@.claude/skills/max-ui-agent/SKILL.md
@.claude/skills/max-critic/SKILL.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add M4L dispatch section to dispatch-rules.md and update router SKILL.md</name>
  <files>.claude/skills/max-router/references/dispatch-rules.md, .claude/skills/max-router/SKILL.md</files>
  <read_first>
    .claude/skills/max-router/references/dispatch-rules.md
    .claude/skills/max-router/SKILL.md
    CLAUDE.md
    .planning/phases/21-scaffold-and-routing/21-CONTEXT.md
  </read_first>
  <action>
    1. **dispatch-rules.md** -- Add a new section after the existing "max-ui-agent" section and before "Ambiguity Resolution":

    ```markdown
    ### M4L Dispatch (Max for Live Devices)

    **Primary keywords:** Max for Live, M4L, Ableton device, audio effect device, instrument device, MIDI effect, amxd, live device

    **Secondary keywords:** live.dial, live.slider, live.numbox, live.toggle, live.menu, live.tab, live.text, live.adsrui, plugin~, plugout~, parameter_enable, presentation mode, devicewidth, live.thisdevice, live.path, live.object, live.banks, push controller, --- prefix

    **Dispatch strategy:** M4L tasks dispatch to existing agents with M4L context injected (per D-07, no dedicated M4L agent):
    - DSP tasks in M4L context -> max-dsp-agent + M4L signal chain rules
    - UI tasks in M4L context -> max-ui-agent + M4L presentation rules
    - MIDI routing in M4L context -> max-patch-agent + M4L MIDI rules
    - Multi-domain M4L -> standard multi-agent dispatch with M4L context on all agents

    **Context injection:** When M4L detected, inject into agent dispatch:
    - Device type (audio_effect / instrument / midi_effect) if known
    - Reminder: read CLAUDE.md M4L section for authoritative rules
    - Key constraints: --- prefix on named objects, parameter_enable=1 on live.* controls, 169px height cap, no gain~ to plugout~

    **Intent patterns:**
    - "Create a Max for Live audio effect..."
    - "Build an M4L instrument that..."
    - "Make an Ableton device..."
    - "Add live.dial controls to the device..."
    ```

    2. **dispatch-rules.md Ambiguity Resolution** -- Add M4L edge cases to the existing table:

    ```markdown
    | "M4L audio effect" | DSP + UI | DSP | M4L context injected on both agents |
    | "Ableton device with knobs" | UI (lead) + DSP | UI | M4L presentation layout is primary |
    | "M4L MIDI processor" | Patch | Patch | M4L context injected, MIDI routing domain |
    ```

    3. **SKILL.md** -- Add an M4L row to the "Quick reference" table in the "Dispatch Rules" section:

    ```markdown
    | M4L/Ableton | (multi-agent) | Max for Live, M4L, Ableton device, live.dial, plugin~, plugout~, parameter_enable |
    ```

    Also add a brief paragraph after the quick reference table:

    ```markdown
    **M4L dispatch:** When M4L keywords are detected, dispatch to the relevant domain agent(s) with M4L context injected. M4L is not a separate agent -- it augments existing agents. See `references/dispatch-rules.md` M4L section for full keyword list and context injection details.
    ```
  </action>
  <verify>
    <automated>grep -c "M4L Dispatch" .claude/skills/max-router/references/dispatch-rules.md && grep -c "M4L" .claude/skills/max-router/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - dispatch-rules.md contains section header "### M4L Dispatch (Max for Live Devices)"
    - dispatch-rules.md contains "Max for Live, M4L, Ableton device" in Primary keywords
    - dispatch-rules.md contains "live.dial, live.slider, live.numbox" in Secondary keywords
    - dispatch-rules.md contains "no dedicated M4L agent" or "existing agents" (per D-07)
    - dispatch-rules.md contains "M4L audio effect" in edge cases table
    - SKILL.md contains "M4L/Ableton" in the dispatch table
    - SKILL.md contains "M4L dispatch:" paragraph referencing dispatch-rules.md
  </acceptance_criteria>
  <done>Router dispatch-rules.md has M4L keyword section with primary/secondary keywords per D-08, and SKILL.md references M4L dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Add M4L sections to 4 agent SKILL.md files + critic awareness note</name>
  <files>.claude/skills/max-dsp-agent/SKILL.md, .claude/skills/max-patch-agent/SKILL.md, .claude/skills/max-ui-agent/SKILL.md, .claude/skills/max-critic/SKILL.md</files>
  <read_first>
    .claude/skills/max-dsp-agent/SKILL.md
    .claude/skills/max-patch-agent/SKILL.md
    .claude/skills/max-ui-agent/SKILL.md
    .claude/skills/max-critic/SKILL.md
    CLAUDE.md
    .planning/phases/21-scaffold-and-routing/21-CONTEXT.md
    src/maxpat/m4l_constants.py
  </read_first>
  <action>
    Add M4L sections to each SKILL.md. Place each M4L section after the agent's "Capabilities" section (before "Editing Existing Patches" or equivalent). Content must be domain-specific, not generic M4L rules (CLAUDE.md already has those).

    1. **max-dsp-agent/SKILL.md** -- Add section "### M4L Signal Chain Rules" (per D-09):

    ```markdown
    ### M4L Signal Chain Rules

    When generating audio for M4L devices (audio_effect or instrument):

    - **plugin~/plugout~ are the audio I/O:** audio_effect uses plugin~ (stereo input) and plugout~ (stereo output). Instruments use plugout~ only (no plugin~). Both use maxclass="newobj" -- the Box constructor handles this correctly via add_box("plugin~").
    - **NEVER connect gain~ to plugout~:** Ableton's channel strip handles device volume. The M4L critic (Phase 22) will flag gain~->plugout~ as an error. Use gain~ for internal level control between processing stages only.
    - **Stereo convention:** plugin~ has 2 outlets (L/R), plugout~ has 2 inlets (L/R). Always connect both channels for stereo audio effects.
    - **No dac~ in M4L:** M4L devices use plugout~ instead of dac~. Do not add dac~ to M4L device patches.
    - **Signal chain pattern:** plugin~ -> [processing] -> plugout~ (audio_effect) or [synthesis] -> plugout~ (instrument).
    ```

    2. **max-patch-agent/SKILL.md** -- Add section "### M4L MIDI Routing" (per D-09):

    ```markdown
    ### M4L MIDI Routing

    When generating control/MIDI flow for M4L devices (instrument or midi_effect):

    - **MIDI passthrough:** Instrument and midi_effect devices MUST have midiin connected to midiout. MIDI flows through even when the device doesn't process it -- prevents silent MIDI drops in Ableton.
    - **midiin/midiout use maxclass="newobj":** They are NOT UI objects. Use add_box("midiin") and add_box("midiout") -- the Box constructor resolves maxclass correctly.
    - **live.path / live.object:** Used together to interact with the Live API. live.path resolves an API path string (e.g., "live_set tracks 0"), live.object sends messages to the resolved object. They are a required pair -- never use one without the other.
    - **--- prefix on named objects:** All named objects (buffer~, coll, dict, send, receive, send~, receive~, value) in M4L devices MUST use --- prefix (e.g., buffer~ ---mybuffer). MAX replaces --- with a unique device instance ID at runtime, preventing name collisions when multiple instances are loaded.
    - **live.thisdevice:** Every M4L device includes live.thisdevice for device initialization. It outputs device ID and state on load.
    ```

    3. **max-ui-agent/SKILL.md** -- Add section "### M4L Presentation Mode and Controls" (per D-09):

    ```markdown
    ### M4L Presentation Mode and Controls

    When designing UI for M4L devices:

    - **Presentation mode required:** M4L devices must set openinpresentation=1 on the top-level patcher. All user-facing controls need presentation=1 and presentation_rect attributes.
    - **169px height constraint:** Ableton's device view is 169px tall. ALL presentation-mode controls must fit within this height. Controls placed below 169px are clipped and invisible.
    - **devicewidth:** Set on the patcher (not rect[2]). Default is 300px. This controls the device width in Ableton's device chain.
    - **live.* controls:** Use live.dial, live.slider, live.numbox, live.toggle, live.menu, live.tab, live.text instead of standard MAX UI objects for Ableton integration.
    - **parameter_enable=1 required:** Every live.* UI control MUST have parameter_enable=1 in its box attributes and a complete saved_attribute_attributes block:
      ```json
      {
        "saved_attribute_attributes": {
          "valueof": {
            "parameter_longname": "Unique Name",
            "parameter_shortname": "8CharMax",
            "parameter_type": 1,
            "parameter_unitstyle": 0,
            "parameter_modmode": 0
          }
        }
      }
      ```
      parameter_type: 0=INT, 1=FLOAT, 2=ENUM. parameter_unitstyle: 0=INT, 1=FLOAT, 2=TIME(ms), 3=HERTZ, 4=DECIBEL, 5=PERCENT. See src/maxpat/m4l_constants.py for all values.
    - **parameter_longname must be unique** across the entire device. Duplicate longnames cause Ableton to silently merge parameters.
    - **parameter_shortname max 8 chars:** Displayed on Push controller. Truncate or abbreviate.
    ```

    4. **max-critic/SKILL.md** -- Add brief M4L awareness note after "Capabilities" section (per D-10):

    ```markdown
    ### M4L Device Awareness

    When reviewing M4L devices (patches with openinpresentation=1 and plugin~/plugout~ or live.thisdevice):

    - **Do NOT flag plugin~/plugout~ as unknown objects** -- they are valid M4L audio I/O objects using maxclass="newobj".
    - **Do NOT flag missing dac~** -- M4L devices use plugout~ instead of dac~.
    - **Do NOT flag live.* objects as non-standard** -- live.dial, live.slider, etc. are M4L-specific controls.
    - **A dedicated M4L critic module is planned for Phase 22** -- it will handle gain~/plugout~ detection, parameter_enable validation, and device completeness checks. Until then, the general critic should not flag M4L-specific patterns as errors.
    ```
  </action>
  <verify>
    <automated>grep -l "M4L Signal Chain" .claude/skills/max-dsp-agent/SKILL.md && grep -l "M4L MIDI" .claude/skills/max-patch-agent/SKILL.md && grep -l "M4L Presentation" .claude/skills/max-ui-agent/SKILL.md && grep -l "M4L Device Awareness" .claude/skills/max-critic/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - max-dsp-agent/SKILL.md contains "### M4L Signal Chain Rules"
    - max-dsp-agent/SKILL.md contains "NEVER connect gain~ to plugout~"
    - max-dsp-agent/SKILL.md contains "plugin~/plugout~ are the audio I/O"
    - max-dsp-agent/SKILL.md contains "No dac~ in M4L"
    - max-patch-agent/SKILL.md contains "### M4L MIDI Routing"
    - max-patch-agent/SKILL.md contains "midiin connected to midiout"
    - max-patch-agent/SKILL.md contains "--- prefix on named objects"
    - max-patch-agent/SKILL.md contains "live.path / live.object"
    - max-ui-agent/SKILL.md contains "### M4L Presentation Mode and Controls"
    - max-ui-agent/SKILL.md contains "parameter_enable=1 required"
    - max-ui-agent/SKILL.md contains "169px height constraint"
    - max-ui-agent/SKILL.md contains "parameter_longname must be unique"
    - max-ui-agent/SKILL.md contains "saved_attribute_attributes"
    - max-critic/SKILL.md contains "### M4L Device Awareness"
    - max-critic/SKILL.md contains "Do NOT flag plugin~/plugout~ as unknown"
    - max-critic/SKILL.md contains "Phase 22"
  </acceptance_criteria>
  <done>Four agent SKILL.md files have domain-specific M4L instruction sections, and max-critic has M4L awareness note preventing false positives on M4L patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan -- documentation-only changes to SKILL.md and dispatch-rules.md files. No user input processed, no code execution paths modified.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | I (Information Disclosure) | SKILL.md files | accept | SKILL.md files are developer documentation, not user-facing. No secrets or PII. |
</threat_model>

<verification>
1. `grep "M4L Dispatch" .claude/skills/max-router/references/dispatch-rules.md` -- section exists
2. `grep "M4L Signal Chain" .claude/skills/max-dsp-agent/SKILL.md` -- section exists
3. `grep "M4L MIDI" .claude/skills/max-patch-agent/SKILL.md` -- section exists
4. `grep "M4L Presentation" .claude/skills/max-ui-agent/SKILL.md` -- section exists
5. `grep "M4L Device Awareness" .claude/skills/max-critic/SKILL.md` -- section exists
6. `grep "M4L" .claude/skills/max-router/SKILL.md` -- router quick reference updated
</verification>

<success_criteria>
- Router dispatch-rules.md has complete M4L keyword section with primary/secondary keywords and dispatch strategy
- Router SKILL.md quick reference table includes M4L row
- 4 agent SKILL.md files have domain-specific M4L sections (DSP: signal chain, Patch: MIDI routing, UI: presentation/controls, Critic: awareness note)
- All M4L content is consistent with CLAUDE.md M4L rules (single source of truth)
</success_criteria>

<output>
After completion, create `.planning/phases/21-scaffold-and-routing/21-02-SUMMARY.md`
</output>
