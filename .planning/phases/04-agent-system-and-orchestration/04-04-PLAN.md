---
phase: 04-agent-system-and-orchestration
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .claude/skills/max-router/SKILL.md
  - .claude/skills/max-router/references/dispatch-rules.md
  - .claude/skills/max-router/references/merge-protocol.md
  - .claude/skills/max-patch-agent/SKILL.md
  - .claude/skills/max-patch-agent/BOUNDARIES.md
  - .claude/skills/max-dsp-agent/SKILL.md
  - .claude/skills/max-dsp-agent/BOUNDARIES.md
  - .claude/skills/max-rnbo-agent/SKILL.md
  - .claude/skills/max-rnbo-agent/BOUNDARIES.md
  - .claude/skills/max-js-agent/SKILL.md
  - .claude/skills/max-js-agent/BOUNDARIES.md
  - .claude/skills/max-ext-agent/SKILL.md
  - .claude/skills/max-ext-agent/BOUNDARIES.md
  - .claude/skills/max-ui-agent/SKILL.md
  - .claude/skills/max-ui-agent/BOUNDARIES.md
  - .claude/skills/max-critic/SKILL.md
  - .claude/skills/max-critic/references/critic-protocol.md
  - .claude/skills/max-memory-agent/SKILL.md
  - .claude/skills/max-memory-agent/BOUNDARIES.md
  - .claude/skills/max-lifecycle/SKILL.md
  - .claude/skills/max-lifecycle/references/project-structure.md
  - .claude/skills/max-lifecycle/references/status-tracking.md
  - .claude/skills/max-lifecycle/references/test-protocol.md
  - tests/test_agent_skills.py
autonomous: true
requirements: [AGT-01, AGT-02]

must_haves:
  truths:
    - "6 specialist agent skills exist: Patch, DSP/Gen~, RNBO~, js/Node, Externals, UI/Layout"
    - "Router agent analyzes tasks and dispatches to correct specialist(s)"
    - "Each agent loads only domain-relevant context (not all 2,012 objects)"
    - "RNBO~ and Externals agents are stubs that return clear Phase 5 messages"
    - "Critic skill orchestrates the generate-review-revise loop using Python critic modules"
    - "Memory agent handles read/write operations across both scopes"
    - "Lifecycle agent manages project creation, status, and switching"
    - "UI/Layout agent handles presentation mode and patching mode layout"
  artifacts:
    - path: ".claude/skills/max-router/SKILL.md"
      provides: "Router agent with dispatch rules and merge protocol"
      min_lines: 40
    - path: ".claude/skills/max-patch-agent/SKILL.md"
      provides: "Patch generation specialist with Max/control context"
      min_lines: 30
    - path: ".claude/skills/max-dsp-agent/SKILL.md"
      provides: "DSP/Gen~ specialist with MSP and Gen context"
      min_lines: 30
    - path: ".claude/skills/max-rnbo-agent/SKILL.md"
      provides: "RNBO stub agent with Phase 5 deferral message"
      min_lines: 15
    - path: ".claude/skills/max-js-agent/SKILL.md"
      provides: "js/Node specialist with JavaScript context"
      min_lines: 30
    - path: ".claude/skills/max-ext-agent/SKILL.md"
      provides: "Externals stub agent with Phase 5 deferral message"
      min_lines: 15
    - path: ".claude/skills/max-ui-agent/SKILL.md"
      provides: "UI/Layout specialist with presentation mode context"
      min_lines: 30
    - path: ".claude/skills/max-critic/SKILL.md"
      provides: "Critic orchestrator with loop protocol"
      min_lines: 30
    - path: ".claude/skills/max-memory-agent/SKILL.md"
      provides: "Memory operations agent for read/write/dedup"
      min_lines: 25
    - path: ".claude/skills/max-lifecycle/SKILL.md"
      provides: "Project lifecycle management"
      min_lines: 25
    - path: "tests/test_agent_skills.py"
      provides: "Tests verifying skill file structure and required content"
  key_links:
    - from: ".claude/skills/max-router/SKILL.md"
      to: ".claude/skills/max-router/references/dispatch-rules.md"
      via: "skill reference"
      pattern: "dispatch-rules"
    - from: ".claude/skills/max-critic/SKILL.md"
      to: "src/maxpat/critics/"
      via: "references Python critic modules"
      pattern: "src/maxpat/critics"
    - from: ".claude/skills/max-memory-agent/SKILL.md"
      to: "src/maxpat/memory.py"
      via: "references memory module"
      pattern: "src/maxpat/memory"
    - from: ".claude/skills/max-lifecycle/SKILL.md"
      to: "src/maxpat/project.py"
      via: "references project module"
      pattern: "src/maxpat/project"
---

<objective>
Create all Claude Code skill definitions for the agent system -- router, 6 specialists, critic, memory, and lifecycle agents.

Purpose: Each agent is a SKILL.md that tells Claude how to behave when invoked for that domain. The router dispatches, specialists generate, critics review, memory persists, and lifecycle manages projects. This is the "brain" of the orchestration layer -- the prompts that make Claude work as domain experts.

Output: 10 skill directories under `.claude/skills/` plus validation tests.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-agent-system-and-orchestration/04-CONTEXT.md
@.planning/phases/04-agent-system-and-orchestration/04-RESEARCH.md

<interfaces>
<!-- Python modules agents reference (from Plans 01-03) -->

From src/maxpat/critics/__init__.py:
```python
from src.maxpat.critics import review_patch, CriticResult
def review_patch(patch_dict: dict, code_context: dict | None = None) -> list[CriticResult]: ...
```

From src/maxpat/memory.py:
```python
from src.maxpat.memory import MemoryStore, MemoryEntry
class MemoryStore:
    def __init__(self, scope: str = "global", project_dir: Path | None = None): ...
    def write(self, entry: MemoryEntry) -> bool: ...
    def read(self, domain: str | None = None) -> list[MemoryEntry]: ...
    def list_domains(self) -> list[str]: ...
    def delete(self, pattern: str, domain: str | None = None) -> bool: ...
```

From src/maxpat/project.py:
```python
from src.maxpat.project import create_project, get_active_project, set_active_project, read_status, update_status, list_projects
```

From src/maxpat/__init__.py:
```python
from src.maxpat import Patcher, generate_patch, write_patch, validate_file
from src.maxpat import build_genexpr, generate_gendsp, generate_n4m_script, generate_js_script
from src.maxpat import validate_genexpr, validate_js, validate_n4m
from src.maxpat import ObjectDatabase
```

Object database domain files:
- .claude/max-objects/max/objects.json (470 objects -- Patch agent)
- .claude/max-objects/msp/objects.json (248 objects -- DSP agent)
- .claude/max-objects/gen/objects.json (189 objects -- DSP agent)
- .claude/max-objects/jitter/objects.json (210 objects -- not loaded by default)
- .claude/max-objects/mc/objects.json (215 objects -- DSP agent optionally)
- .claude/max-objects/rnbo/objects.json (560 objects -- RNBO agent)
- .claude/max-objects/m4l/objects.json (33 objects -- not loaded)
- .claude/max-objects/packages/objects.json (87 objects -- not loaded by default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Router, specialist agents, and critic skill</name>
  <files>.claude/skills/max-router/SKILL.md, .claude/skills/max-router/references/dispatch-rules.md, .claude/skills/max-router/references/merge-protocol.md, .claude/skills/max-patch-agent/SKILL.md, .claude/skills/max-patch-agent/BOUNDARIES.md, .claude/skills/max-dsp-agent/SKILL.md, .claude/skills/max-dsp-agent/BOUNDARIES.md, .claude/skills/max-rnbo-agent/SKILL.md, .claude/skills/max-rnbo-agent/BOUNDARIES.md, .claude/skills/max-js-agent/SKILL.md, .claude/skills/max-js-agent/BOUNDARIES.md, .claude/skills/max-ext-agent/SKILL.md, .claude/skills/max-ext-agent/BOUNDARIES.md, .claude/skills/max-ui-agent/SKILL.md, .claude/skills/max-ui-agent/BOUNDARIES.md, .claude/skills/max-critic/SKILL.md, .claude/skills/max-critic/references/critic-protocol.md</files>
  <action>
    Create skill directories and files for all agents. Each SKILL.md follows this structure:
    - Frontmatter: name, description, allowed-tools, preconditions
    - Domain context loading instructions (which object JSON files to read)
    - Capabilities section (what this agent can do, referencing specific Python functions)
    - Output protocol (how to format and return results)
    - When to use / when NOT to use

    **max-router/SKILL.md:**
    - Analyzes user's task description for domain keywords
    - Dispatch rules: patch/object/routing -> max-patch-agent, signal/audio/synth/gen~ -> max-dsp-agent, rnbo/export/vst -> max-rnbo-agent, javascript/node/n4m/script -> max-js-agent, external/sdk/c++/compile -> max-ext-agent, layout/presentation/ui/controls -> max-ui-agent
    - Multi-domain: dispatch to multiple, merge outputs
    - References: dispatch-rules.md (detailed keyword -> agent mapping), merge-protocol.md (conflict resolution: inlet counts match, single file ownership, lead agent designation)

    **max-patch-agent/SKILL.md:**
    - Context: Read max/objects.json (470 objects). Read CLAUDE.md for rules.
    - Uses: Patcher, Box, generate_patch, write_patch from src.maxpat
    - Capabilities: MAX object routing, subpatcher organization, control flow, message passing
    - BOUNDARIES.md: Does NOT generate Gen~ code (DSP agent), does NOT handle presentation mode (UI agent), does NOT generate JavaScript (js agent)

    **max-dsp-agent/SKILL.md:**
    - Context: Read msp/objects.json + gen/objects.json. Read CLAUDE.md MSP and Gen~ sections.
    - Uses: build_genexpr, generate_gendsp, parse_genexpr_io from src.maxpat
    - Capabilities: GenExpr code, signal chain construction, gain staging, feedback loops with History/tapin~/tapout~
    - BOUNDARIES.md: Does NOT handle patch-level routing (Patch agent), does NOT handle RNBO export (RNBO agent)

    **max-rnbo-agent/SKILL.md (STUB):**
    - STUB for Phase 5. Returns message: "RNBO~ generation is planned for Phase 5. I can help design the patch structure, but export-specific features (VST3/AU, Web Audio, C++) are not yet implemented."
    - Context: Read rnbo/objects.json for compatibility checking only
    - BOUNDARIES.md: Currently limited to compatibility checking, not generation

    **max-js-agent/SKILL.md:**
    - Context: Read CLAUDE.md js and N4M sections.
    - Uses: generate_n4m_script, generate_js_script, validate_js, validate_n4m from src.maxpat
    - Capabilities: Node for Max scripts (CommonJS, max-api), js V8 scripts (inlets/outlets, handlers)
    - BOUNDARIES.md: Does NOT generate patches (Patch agent), does NOT generate GenExpr (DSP agent)

    **max-ext-agent/SKILL.md (STUB):**
    - STUB for Phase 5. Returns message: "C/C++ external development is planned for Phase 5. I can discuss external architecture, but scaffolding and code generation are not yet implemented."
    - BOUNDARIES.md: Currently informational only

    **max-ui-agent/SKILL.md:**
    - Context: Read max/objects.json (focus on UI objects: dial, slider, multislider, panel, comment, etc.). Read CLAUDE.md patch style section.
    - Uses: apply_layout from src.maxpat, presentation mode attributes
    - Capabilities: Presentation mode layout, control placement, visual hierarchy, panel organization
    - BOUNDARIES.md: Does NOT generate signal processing (DSP agent), does NOT generate code (js agent)

    **max-critic/SKILL.md:**
    - References critic-protocol.md for the generate-review-revise loop
    - Invokes `review_patch()` from src.maxpat.critics (Python, deterministic)
    - Reads CriticResult list, formats findings for generator
    - If blockers found: request revision with specific findings
    - If only warnings/notes: annotate as comment objects in .maxpat or code comments
    - Loop until clean (no blockers). Safety: escalate to user after 5 rounds with same finding.
    - critic-protocol.md: detailed loop steps, revision tracking, escalation rules
  </action>
  <verify>
    <automated>ls .claude/skills/max-router/SKILL.md .claude/skills/max-patch-agent/SKILL.md .claude/skills/max-dsp-agent/SKILL.md .claude/skills/max-rnbo-agent/SKILL.md .claude/skills/max-js-agent/SKILL.md .claude/skills/max-ext-agent/SKILL.md .claude/skills/max-ui-agent/SKILL.md .claude/skills/max-critic/SKILL.md && echo "All 8 agent skills exist"</automated>
  </verify>
  <done>All 8 agent skills created (router + 6 specialists + critic) with SKILL.md, BOUNDARIES.md (where applicable), and reference files. Each agent specifies exact context loading and Python module references.</done>
</task>

<task type="auto">
  <name>Task 2: Memory agent, lifecycle agent, and skill validation tests</name>
  <files>.claude/skills/max-memory-agent/SKILL.md, .claude/skills/max-memory-agent/BOUNDARIES.md, .claude/skills/max-lifecycle/SKILL.md, .claude/skills/max-lifecycle/references/project-structure.md, .claude/skills/max-lifecycle/references/status-tracking.md, .claude/skills/max-lifecycle/references/test-protocol.md, tests/test_agent_skills.py</files>
  <action>
    **max-memory-agent/SKILL.md:**
    - Uses: MemoryStore, MemoryEntry from src.maxpat.memory
    - Capabilities: Read memory (both scopes), write new patterns, deduplicate, delete entries
    - Auto write-back protocol: After successful generation, review output, identify patterns worth storing, check existing entries, write genuinely new ones
    - Auto-inject protocol: Before generation, load project memory (always), load global memory filtered by domain relevance
    - BOUNDARIES.md: Does NOT generate patches/code, does NOT make architectural decisions

    **max-lifecycle/SKILL.md:**
    - Uses: create_project, get_active_project, set_active_project, read_status, update_status, list_projects from src.maxpat.project
    - Uses: generate_test_checklist, save_test_results from src.maxpat.testing
    - Capabilities: Project creation with conversational kickoff, status tracking, project switching, test protocol execution
    - References:
      - project-structure.md: Standard directory layout (context.md, status.md, .max-memory/, generated/, test-results/)
      - status-tracking.md: Stage definitions (ideation, discuss, research, build, verify), progress format
      - test-protocol.md: How /max:test generates checklists, how results are recorded

    **tests/test_agent_skills.py:**
    Create tests that verify skill file structure (not behavior -- skills are prompts, not code):
    - Test all 10 skill directories exist under .claude/skills/
    - Test each SKILL.md has frontmatter with name and description
    - Test specialist agents reference correct object database files (e.g., DSP agent references msp/ and gen/)
    - Test UI agent SKILL.md exists and references presentation mode
    - Test RNBO and Externals stubs contain Phase 5 deferral messages
    - Test router references dispatch-rules.md and merge-protocol.md
    - Test critic references critic-protocol.md
    - Test BOUNDARIES.md exists for all 6 specialist agents
    - Test lifecycle references all 3 reference files
  </action>
  <verify>
    <automated>python -m pytest tests/test_agent_skills.py -x -q</automated>
  </verify>
  <done>Memory and lifecycle agents created. Validation tests confirm all 10 skill directories have correct structure, frontmatter, context references, and boundary definitions. RNBO/Externals stubs include Phase 5 deferral messages.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_agent_skills.py -v` -- all skill structure tests pass
- `ls .claude/skills/` shows 10 skill directories
- `python -m pytest tests/ -x -q` -- no regressions in existing tests
</verification>

<success_criteria>
- 10 skill directories exist: max-router, max-patch-agent, max-dsp-agent, max-rnbo-agent, max-js-agent, max-ext-agent, max-ui-agent, max-critic, max-memory-agent, max-lifecycle
- Each SKILL.md specifies domain context loading, Python module references, and output protocol
- Router includes dispatch rules and merge protocol
- Critic includes generate-review-revise loop protocol
- RNBO and Externals are stubs with Phase 5 deferral messages
- All specialists have BOUNDARIES.md defining scope
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-system-and-orchestration/04-04-SUMMARY.md`
</output>
