---
phase: 01-object-knowledge-base
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .claude/scripts/validate_db.py
  - CLAUDE.md
  - tests/test_claude_md.py
autonomous: true
requirements:
  - FRM-04
  - ODB-01

must_haves:
  truths:
    - "CLAUDE.md exists at project root with MAX/MSP development rules Claude must follow"
    - "CLAUDE.md declares the #1 rule: never guess objects -- only use objects in the database"
    - "CLAUDE.md contains domain-specific rule sections for MSP, Gen~, RNBO, and N4M"
    - "CLAUDE.md tells Claude how to access and query the object database"
    - "validate_db.py can verify the entire database meets all ODB requirements in one command"
  artifacts:
    - path: "CLAUDE.md"
      provides: "MAX/MSP development rules, conventions, and object reference guidance"
      min_lines: 100
      contains: "never guess"
    - path: ".claude/scripts/validate_db.py"
      provides: "Database validation script with --quick and --full modes"
      min_lines: 80
      exports: ["main"]
    - path: "tests/test_claude_md.py"
      provides: "Tests that CLAUDE.md has required sections"
      min_lines: 30
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/max-objects/"
      via: "references the object database location and query patterns"
      pattern: "max-objects"
    - from: ".claude/scripts/validate_db.py"
      to: ".claude/max-objects/*/objects.json"
      via: "loads and validates all domain JSON files"
      pattern: "objects\\.json"
    - from: ".claude/scripts/validate_db.py"
      to: "tests/"
      via: "--full mode runs pytest suite"
      pattern: "pytest"
---

<objective>
Create CLAUDE.md with opinionated MAX/MSP development rules and a database validation script, completing the Phase 1 knowledge base.

Purpose: CLAUDE.md is how Claude knows *how* to use the object database. Without it, the database is data without guidance. The validation script provides a single command to verify database health across all ODB requirements.
Output: CLAUDE.md at project root, validate_db.py script, test for CLAUDE.md structure.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-object-knowledge-base/01-RESEARCH.md
@.planning/phases/01-object-knowledge-base/01-CONTEXT.md
@.planning/phases/01-object-knowledge-base/01-01-SUMMARY.md
@.planning/phases/01-object-knowledge-base/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md with MAX/MSP development rules</name>
  <files>CLAUDE.md, tests/test_claude_md.py</files>
  <action>
Create `CLAUDE.md` at the project root with comprehensive MAX/MSP development rules. This file is what Claude reads before doing any MAX development work. It must be opinionated, specific, and actionable.

**Required sections (each must be a markdown ## heading):**

**## Object Database**
- Location: `.claude/max-objects/` with domain subdirectories (max, msp, jitter, mc, gen, m4l, rnbo, packages)
- Each domain has `objects.json` -- a JSON object keyed by object name
- Supplementary files: `overrides.json` (expert corrections), `aliases.json` (shortcuts), `relationships.json` (pairings), `pd-blocklist.json` (PD guard)
- How to look up an object: read the domain JSON file, find the object by name key
- How to browse by domain: list keys in the relevant domain's objects.json
- How to check RNBO compatibility: read the `rnbo_compatible` field on any object
- How to resolve aliases: check aliases.json first, then look up the canonical name
- How to check common companions: check relationships.json for the object name

**## Rules**

**### Rule #1: Never Guess Objects**
THE cardinal rule. If an object is not in the database, DO NOT use it. Do not hallucinate object names, inlet counts, outlet counts, or behaviors from training data. If unsure, look it up. If it's not there, it doesn't exist for our purposes. Flag the gap with a comment: `// UNKNOWN OBJECT: [name] -- not in database, verify manually`.

**### Rule #2: Verify Before Connect**
Before creating any connection between objects, verify:
- Source object outlet index is within bounds (check outlet count, accounting for variable_io rules)
- Destination object inlet index is within bounds (check inlet count, accounting for variable_io rules)
- Signal type compatibility: signal outlets connect to signal inlets, control outlets to control inlets
- Exception: signal/float inlets accept both signal and control connections

**### Rule #3: Hot/Cold Inlet Ordering**
When sending multiple values to an object:
- Send to cold inlets FIRST (right to left)
- Send to hot inlet LAST (leftmost, inlet 0) -- this triggers computation
- Use `trigger` (t) object for explicit fan-out ordering when timing matters
- Signal inlets are all "hot" in the audio domain -- ordering doesn't apply for MSP signal connections

**### Rule #4: Patch Style**
- Top-to-bottom signal flow (audio flows down, control flows down)
- Use explicit `trigger` objects for fan-out instead of connecting one outlet to multiple inlets
- Add `comment` objects on non-obvious connections
- Prefer named `send`/`receive` (and `send~`/`receive~`) over long patch cords that cross the patch
- Use `patcher` (subpatchers) to organize complex logic into named sections
- Standard object spacing: ~80-120px vertical, ~150-200px horizontal

**## Domain-Specific Rules**

**### MSP (Audio/Signal)**
- Always terminate signal chains with `dac~` or `*~ 0.` (multiply by zero to mute)
- Use `*~ 0.5` or `*~` with `line~` for gain control -- never connect raw oscillators to `dac~` at full volume
- Signal objects (names ending in `~`) process at audio rate -- they are always "on" once connected
- Use `snapshot~` to convert signal values to control rate for display
- Use `meter~` or `levelmeter~` for audio level monitoring
- Multichannel: `mc.` prefix objects handle multiple channels -- use `mc.pack~`/`mc.unpack~` to convert between MC and individual channels

**### Gen~ (GenExpr DSP Code)**
- GenExpr uses C-style syntax with `in 1`, `in 2` for inputs and `out 1`, `out 2` for outputs
- Use `Param` for user-controllable parameters (maps to gen~ attributes)
- Use `History` for single-sample delay (feedback loops, state)
- `Buffer` and `Data` for sample data access
- Gen~ operates at sample rate -- every operation runs once per sample
- No conditional branching cost -- both branches always execute (SIMD-friendly)
- Codebox objects embed GenExpr in `.maxpat` patches; `.gendsp` files are standalone Gen~ patchers

**### RNBO (Export-Ready Patches)**
- ONLY use objects with `rnbo_compatible: true` in the database
- RNBO uses `rnbo~` as the container object (like `gen~` but for full patches)
- RNBO patches must be self-contained -- no external file dependencies for export targets
- Export targets: VST3/AU (plugin), Web Audio (browser), C++ (embedded)
- Some RNBO objects have different behavior than their MAX counterparts (e.g., different outlet count) -- check the rnbo/ domain objects for RNBO-specific definitions
- `param` objects in RNBO map to plugin parameters for VST3/AU export

**### Node for Max (N4M / node.script)**
- node.script objects run Node.js -- use `const maxAPI = require('max-api')` for MAX communication
- `maxAPI.addHandler('message_name', callback)` to receive messages from MAX
- `maxAPI.outlet(value)` to send data back to MAX
- `maxAPI.post('message')` for console output visible in MAX
- `maxAPI.getDict('dict_name')` and `maxAPI.setDict('dict_name', data)` for Dict access
- node.script has a single inlet (messages) and configurable outlets
- Use for: file I/O, network requests, complex data processing, anything Node.js does better than MAX

**### js (V8 JavaScript / js object)**
- `js` object runs V8 JavaScript inline in MAX
- `inlets = N` and `outlets = N` to configure I/O count
- Handler functions: `bang()`, `msg_int(v)`, `msg_float(v)`, `list()`, `anything(msg, args)`
- `outlet(outlet_index, value)` to send data
- `post('message')` for console output
- Access patcher: `this.patcher.getnamed('object_name')`
- Use for: UI logic, data transformation, algorithmic composition, anything needing scripted control

**## PD Confusion Guard**
Check `.claude/max-objects/pd-blocklist.json` before using any unfamiliar object. Common confusions:
- `osc~` is PD -- use `cycle~` in MAX
- `lop~` is PD -- use `onepole~` in MAX
- `hip~` is PD -- use `onepole~` (with inversion) in MAX
- `bp~` is PD -- use `reson~` in MAX
- `tabread~` is PD -- use `index~`, `play~`, or `wave~` in MAX
- `throw~`/`catch~` is PD -- use `send~`/`receive~` in MAX
If you're about to use an object with `~` that isn't in the database, check the PD blocklist for the MAX equivalent.

**## Version Compatibility**
- Check `min_version` field before using objects
- MAX 9 objects (array.*, string.*, abl.*): only available in MAX 9+
- MC objects (mc.*): available from MAX 8.1+
- When generating patches, note the target MAX version and avoid using objects above that version

**## Variable I/O Objects**
Some objects change inlet/outlet count based on arguments. Check `variable_io` and `io_rule` fields:
- `trigger b i f` has 3 outlets (not the default 2)
- `pack 0 0 0 0` has 4 inlets (not the default 2)
- `route foo bar baz` has 4 outlets (3 match + 1 unmatched)
Always compute actual I/O count from arguments when generating connections.

**## File Conventions**
- Patches: `.maxpat` (JSON format)
- Gen~ patches: `.gendsp` (JSON format, similar structure to .maxpat)
- Node for Max: `.js` files referenced by node.script object
- js object: `.js` files referenced by js object
- Externals: `.mxo` bundles (macOS)
- Projects: `.maxproj` (project container)

Also create `tests/test_claude_md.py` (FRM-04):
- CLAUDE.md exists at project root
- Contains "## Object Database" section
- Contains "## Rules" section
- Contains "Rule #1: Never Guess Objects" (or similar phrasing with "never guess")
- Contains "## Domain-Specific Rules" section
- Contains MSP, Gen~, RNBO, and N4M subsections
- Contains "## PD Confusion Guard" section
- Contains "## Version Compatibility" section
- Contains "## Variable I/O Objects" section
- References `.claude/max-objects/` path
- File is at least 100 lines long
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_claude_md.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <done>CLAUDE.md exists at project root with all required sections. Tests confirm structure matches FRM-04 requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Create database validation script</name>
  <files>.claude/scripts/validate_db.py</files>
  <action>
Create `.claude/scripts/validate_db.py` -- a standalone validation script that checks the entire object database against all ODB requirements. This is the single command that confirms the knowledge base is healthy.

**CLI interface:**
- `python3 .claude/scripts/validate_db.py --quick` -- Fast checks (~5 seconds): file existence, schema spot-checks, count thresholds
- `python3 .claude/scripts/validate_db.py --full` -- Complete validation (~15 seconds): all quick checks + run pytest suite + detailed per-object validation
- `python3 .claude/scripts/validate_db.py --report` -- Full validation + write report to `.claude/max-objects/validation-report.json`

**Quick checks (--quick):**
1. All domain directories exist: max, msp, jitter, mc, gen, m4l, rnbo, packages
2. All domain objects.json files exist and are valid JSON
3. extraction-log.json exists
4. overrides.json exists
5. aliases.json exists
6. relationships.json exists
7. pd-blocklist.json exists
8. CLAUDE.md exists at project root
9. Total object count > 1500
10. Each domain has > 0 objects
11. Spot-check cycle~ exists in MSP with correct inlet count
12. Spot-check trigger exists in Max with variable_io: true

**Full checks (--full, includes all quick checks plus):**
1. Every object has required fields: name, maxclass, module, domain, inlets, outlets, arguments, messages, min_version, verified, rnbo_compatible, variable_io
2. Every inlet has: id, type, signal, hot
3. Every outlet has: id, type, signal
4. No raw INLET_TYPE or OUTLET_TYPE values in normalized types
5. Domain values are in allowed set: Max, MSP, Jitter, MC, Gen, M4L, Packages
6. min_version is int >= 4 and <= 9
7. MAX 9 objects (array.*, string.*, abl.*) have min_version: 9
8. At least 150 objects have rnbo_compatible: true
9. At least 10 objects have variable_io: true
10. PD blocklist has at least 15 entries
11. Aliases file has at least 5 entries
12. Relationships file has at least 10 pairings
13. Run `python3 -m pytest tests/ -x` and report pass/fail

**Output format:**
```
=== MAX Object Database Validation ===
Mode: quick|full

[PASS] Domain directories exist (7/7)
[PASS] objects.json files valid (7/7)
[PASS] Total objects: 1,847
[PASS] cycle~ found in MSP (2 inlets, 1 outlet)
[PASS] trigger found in Max (variable_io: true)
...
[FAIL] 3 objects missing rnbo_compatible field
  - max/objects.json: obscureobject1
  - max/objects.json: obscureobject2

Summary: 15/16 checks passed, 1 failed
```

Exit code: 0 if all pass, 1 if any fail.

**Report mode (--report):**
Writes `.claude/max-objects/validation-report.json` with:
```json
{
  "timestamp": "2026-03-09T...",
  "mode": "full",
  "total_checks": 16,
  "passed": 15,
  "failed": 1,
  "checks": [
    {"name": "domain_dirs", "status": "pass", "detail": "7/7"},
    ...
  ],
  "summary": {
    "total_objects": 1847,
    "by_domain": {"Max": 450, "MSP": 380, ...},
    "rnbo_compatible_count": 213,
    "variable_io_count": 15,
    "max9_object_count": 134
  }
}
```
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 .claude/scripts/validate_db.py --quick 2>&1 | tail -20</automated>
  </verify>
  <done>validate_db.py exists and runs in both --quick and --full modes. --quick completes in <5 seconds. All checks pass. Exit code is 0.</done>
</task>

</tasks>

<verification>
- `python3 .claude/scripts/validate_db.py --full` -- all checks pass, exit code 0
- `python3 -m pytest tests/ -x -v` -- all tests pass (all 8 test files)
- `cat CLAUDE.md | head -5` -- CLAUDE.md exists with MAX development rules
- `python3 .claude/scripts/validate_db.py --report && cat .claude/max-objects/validation-report.json | python3 -m json.tool | head -20` -- report generated
</verification>

<success_criteria>
- CLAUDE.md exists with all required sections: Object Database, Rules (including #1 Never Guess), Domain-Specific Rules (MSP, Gen~, RNBO, N4M), PD Confusion Guard, Version Compatibility, Variable I/O Objects
- validate_db.py --quick passes all checks in <5 seconds
- validate_db.py --full passes all checks in <15 seconds
- Full pytest suite (8 test files) passes green
- Database meets all Phase 1 success criteria from ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-object-knowledge-base/01-03-SUMMARY.md`
</output>
