---
phase: 25-templates-and-critics
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/maxpat/critics/package_critic.py
  - tests/test_critics.py
autonomous: true
requirements:
  - PKG-24
  - PKG-26

must_haves:
  truths:
    - "review_packages() returns BEAP convention warnings when BEAP chain lacks output termination"
    - "review_packages() returns BEAP warning when oscillator connects to output without VCA"
    - "review_packages() returns Bach llll blocker when non-bach outlet feeds llll inlet"
    - "review_packages() returns no findings for clean bach-to-bach connections"
    - "review_packages() returns community extraction warning for unextracted packages"
    - "BEAP checks only run when BEAP objects detected"
    - "Bach checks only run when Bach objects detected"
  artifacts:
    - path: "src/maxpat/critics/package_critic.py"
      provides: "Package critic module with review_packages(), BEAP convention checks, Bach llll checks, community extraction checks"
      exports: ["review_packages"]
    - path: "tests/test_critics.py"
      provides: "TestPackageCritic class with BEAP/Bach/community test cases"
      contains: "class TestPackageCritic"
  key_links:
    - from: "src/maxpat/critics/package_critic.py"
      to: "src/maxpat/critics/base.py"
      via: "CriticResult import"
      pattern: "from src.maxpat.critics.base import CriticResult"
    - from: "src/maxpat/critics/package_critic.py"
      to: "src/maxpat/db_lookup.py"
      via: "ObjectDatabase for package detection and inlet digest lookup"
      pattern: "from src.maxpat.db_lookup import ObjectDatabase"
---

<objective>
Build the package critic module with BEAP signal convention checks, Bach llll type mismatch detection, and community extraction warnings.

Purpose: Catch package-specific semantic errors that the generic validation pipeline misses -- BEAP convention violations produce subtle analog modeling bugs, Bach llll mismatches cause silent data corruption.
Output: `src/maxpat/critics/package_critic.py` with full test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-templates-and-critics/25-CONTEXT.md
@.planning/phases/25-templates-and-critics/25-RESEARCH.md

@src/maxpat/critics/__init__.py
@src/maxpat/critics/base.py
@src/maxpat/critics/dsp_critic.py
@src/maxpat/db_lookup.py
@src/maxpat/utils.py
@src/maxpat/validation.py
@tests/test_critics.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/maxpat/critics/base.py:
```python
class CriticResult:
    __slots__ = ("severity", "finding", "suggestion")
    def __init__(self, severity: str, finding: str, suggestion: str): ...
```

From src/maxpat/db_lookup.py:
```python
class ObjectDatabase:
    def lookup(self, name: str) -> dict | None: ...
    def exists(self, name: str) -> bool: ...
    def get_package(self, name: str) -> str | None: ...
    def get_package_info(self, package: str) -> dict | None: ...
```

From src/maxpat/utils.py:
```python
def get_box_name(box: dict) -> str:
    """For maxclass=='newobj' returns text.split()[0], else returns maxclass."""
```

From src/maxpat/critics/dsp_critic.py (pattern reference):
```python
def review_dsp(patch_dict: dict, code_context: dict | None = None) -> list[CriticResult]:
    # Builds box_lookup dict[str, dict] from boxes
    # Runs sub-checks passing box_lookup, lines, etc.
```

BEAP DB structure (from .claude/max-objects/packages/BEAP/objects.json):
- All 185 objects have "package": "BEAP", "signal_convention": "0-5V CV"
- "category" field: Oscillator (15), Output (7), Level (16, includes bp.VCA), etc.
- Output objects: bp.Stereo, bp.Mono, bp.Calibrated, bp.Calibrated_64bit, bp.M4L Out, bp.Recordr, bp.Snapshotter
- Output objects have 0 outlets -- they are signal terminators

Bach DB structure (from .claude/max-objects/packages/Bach/objects.json):
- 78 objects total, 70 have "llll" in inlet[0].digest
- Non-llll objects: bach.read, bach.write, bach.fromfile, bach.tofile, bach.list2llll, bach.llll2list, bach.explode, bach.wrap
- bach.list2llll is the ONLY converter from MAX lists to llll format

Bpatcher name resolution:
- BEAP objects in .maxpat have maxclass="bpatcher" and name in the "name" attribute (e.g., box.get("name") == "bp.Oscillator")
- get_box_name() returns "bpatcher" for these -- package critic MUST check box.get("name", "") for bpatcher maxclass
- ObjectDatabase.get_package("bp.Oscillator") returns "BEAP" -- the DB lookup works fine, the issue is extracting the name from the box dict

Validation _extract_object_name() returns None for bpatcher maxclass (it's in _STRUCTURAL_MAXCLASSES).
The package critic must implement its own bpatcher-aware name resolver.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build package_critic.py with BEAP convention checks</name>
  <files>src/maxpat/critics/package_critic.py, tests/test_critics.py</files>
  <read_first>
    src/maxpat/critics/base.py
    src/maxpat/critics/dsp_critic.py
    src/maxpat/critics/__init__.py
    src/maxpat/db_lookup.py
    src/maxpat/utils.py
    tests/test_critics.py
  </read_first>
  <behavior>
    - Test: BEAP patch with bp.Oscillator -> bp.Stereo (no VCA) produces warning about missing VCA gain staging
    - Test: BEAP patch with bp.Oscillator -> bp.VCA -> bp.Stereo produces no BEAP findings
    - Test: BEAP patch with bp.Oscillator -> bp.VCA but no bp.Stereo/bp.Mono produces warning about missing output termination
    - Test: Non-BEAP patch produces no BEAP findings (conditional dispatch per D-12)
    - Test: Mixed patch with BEAP objects AND non-BEAP objects only checks BEAP conventions for BEAP chains
  </behavior>
  <action>
    RED: Add TestPackageCritic class to tests/test_critics.py. Import review_packages from src.maxpat.critics.package_critic. Create test fixtures using the existing _make_patch() helper. BEAP fixtures use maxclass="bpatcher" with name="bp.Oscillator" etc. (matching real .maxpat structure). Write 5 test methods:
    - test_beap_missing_vca: bp.Oscillator -> bp.Stereo (direct, no VCA). Expect 1 warning about missing VCA.
    - test_beap_clean_chain: bp.Oscillator -> bp.VCA -> bp.Stereo. Expect 0 BEAP findings.
    - test_beap_missing_output: bp.Oscillator -> bp.VCA with no output module. Expect 1 warning about missing output termination.
    - test_no_beap_no_findings: Pure non-BEAP patch. Expect review_packages returns [].
    - test_beap_mixed_patch: BEAP objects + standard MSP objects. Only BEAP chains checked.

    GREEN: Create src/maxpat/critics/package_critic.py with:
    1. _get_object_name(box) helper -- returns box.get("name", "") for maxclass=="bpatcher", otherwise get_box_name(box) from utils. This is the bpatcher-aware name resolver.
    2. review_packages(patch_dict, db=None) entry point per D-10:
       - Creates ObjectDatabase if db is None (per RESEARCH.md Option A)
       - Builds box_lookup dict from boxes (same pattern as dsp_critic.py)
       - Detects packages_used set by iterating boxes and calling _get_object_name() + db.get_package()
       - If "BEAP" in packages_used: extend results with _check_beap_conventions(box_lookup, lines, db)
       - Returns list[CriticResult]
    3. _check_beap_conventions(box_lookup, lines, db) dispatches to:
       a. _check_beap_output_termination: BFS from BEAP Source/Oscillator/Random/LFO category objects through signal adjacency. If no path reaches an Output category object (bp.Stereo, bp.Mono, bp.Calibrated, bp.Calibrated_64bit, bp.M4L Out, bp.Recordr, bp.Snapshotter), emit warning severity per D-05.
       b. _check_beap_vca_staging: BFS from BEAP Oscillator category objects to Output category objects. If no Level category object (contains bp.VCA) in path, emit warning per D-05.

    BEAP-specific constants:
    ```python
    _BEAP_OUTPUT_NAMES = frozenset({
        "bp.Stereo", "bp.Mono", "bp.Calibrated",
        "bp.Calibrated_64bit", "bp.M4L Out", "bp.Recordr", "bp.Snapshotter",
    })
    _BEAP_SOURCE_CATEGORIES = frozenset({"Oscillator", "Input", "Random", "LFO"})
    _BEAP_GAIN_CATEGORIES = frozenset({"Level"})
    ```

    Signal adjacency construction: Build from patchlines the same way dsp_critic.py does in _check_gain_staging(). For bpatcher boxes, outlettype may not be in the box dict -- treat all connections FROM bpatcher boxes as potential signal connections (BEAP modules are signal processors).

    All BEAP findings use severity="warning" per D-05.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_critics.py -x -k "beap" -v</automated>
  </verify>
  <acceptance_criteria>
    - src/maxpat/critics/package_critic.py exists with review_packages() function
    - _get_object_name() handles both bpatcher (name attr) and newobj (text field) boxes
    - All 5 BEAP tests pass
    - BEAP findings have severity="warning"
    - review_packages() returns empty list for non-package patches
  </acceptance_criteria>
  <done>BEAP convention critic catches missing output termination and missing VCA gain staging with warning severity. Clean BEAP chains produce no findings. Non-BEAP patches skip BEAP checks entirely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Bach llll type checker and community extraction critic</name>
  <files>src/maxpat/critics/package_critic.py, tests/test_critics.py</files>
  <read_first>
    src/maxpat/critics/package_critic.py
    src/maxpat/validation.py
    tests/test_critics.py
  </read_first>
  <behavior>
    - Test: Non-bach object connected to bach.score inlet 0 (llll inlet) produces blocker
    - Test: bach.rev connected to bach.score inlet 0 produces no finding (bach-to-bach is fine)
    - Test: bach.list2llll connected to bach.score inlet 0 produces no finding (converter is allowed)
    - Test: Non-bach object connected to bach.list2llll inlet 0 produces no finding (list2llll accepts plain lists)
    - Test: Patch with community package objects from unextracted package produces warning
    - Test: Patch with community package objects from extracted package produces no finding
  </behavior>
  <action>
    RED: Add 6 more test methods to TestPackageCritic:
    - test_bach_llll_mismatch: "pack 0 0" (newobj) outlet 0 -> bach.score inlet 0. Expect 1 blocker with "llll type mismatch" in finding.
    - test_bach_to_bach_clean: bach.rev outlet 0 -> bach.score inlet 0. Expect 0 findings for Bach checks.
    - test_bach_list2llll_allowed: "unpack" outlet 0 -> bach.list2llll inlet 0. Expect 0 findings (list2llll is designed to accept lists).
    - test_bach_non_llll_inlet: Non-bach to bach.write (which has no llll digest on inlets). Expect 0 findings.
    - test_community_unextracted_warning: Patch with "fluid.mfcc~" (FluCoMa, extracted=false). Expect warning.
    - test_community_extracted_clean: Mock or use a package that has extracted=true. Expect 0 findings.

    Bach fixtures: bach objects use maxclass="newobj" with text="bach.score" (not bpatchers), so standard get_box_name() works. Create box dicts with id, maxclass="newobj", text="bach.score", outlettype=[""].

    GREEN: Extend package_critic.py with:
    1. In review_packages(): add "if 'Bach' in packages_used: results.extend(_check_bach_llll_types(...))" per D-12
    2. Add results.extend(_check_community_extracted(box_lookup, db)) -- always runs (lightweight early-exit)
    3. _check_bach_llll_types(box_lookup, lines, db):
       - Iterate patchlines
       - For each connection: get destination object name, look up in DB
       - If dest has package=="Bach" AND dest is NOT "bach.list2llll" AND _is_llll_inlet(dest_entry, dst_inlet) returns True AND source is NOT a Bach package object AND source is NOT "bach.list2llll":
         emit CriticResult with severity="blocker" per D-06
       - _is_llll_inlet(obj_entry, inlet_id): checks if inlet's digest contains "llll"
    4. _check_community_extracted(box_lookup, db):
       - For each box, get object name via _get_object_name(), get package via db.get_package()
       - For each unique package, check db.get_package_info(pkg)
       - If tier in ("community", "licensed") and extracted is False: emit warning
       - Track warned_packages set to warn once per package (same pattern as validation.py layer 2d)
       - Per D-07: severity="warning"

    All Bach findings use severity="blocker" per D-06.
    All community findings use severity="warning" per D-07.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_critics.py -x -k "bach or community" -v</automated>
  </verify>
  <acceptance_criteria>
    - _check_bach_llll_types function exists in package_critic.py
    - _check_community_extracted function exists in package_critic.py
    - Bach llll findings have severity="blocker"
    - Community findings have severity="warning"
    - bach.list2llll connections are NOT flagged (converter exception)
    - bach-to-bach connections are NOT flagged
    - All 6 new tests pass
    - Full test suite: python3 -m pytest tests/test_critics.py -x passes (no regressions)
  </acceptance_criteria>
  <done>Bach llll type mismatch critic catches non-bach to llll-inlet connections as blockers. Community extraction critic warns on unextracted community packages. All package critic tests pass with zero regressions in existing critic tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch_dict input | Untrusted .maxpat JSON parsed from user-provided files |
| ObjectDatabase lookups | DB entries are trusted (maintained by framework) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T (Tampering) | patch_dict structure | accept | Critics already handle missing/malformed keys with .get() defaults; no security impact from bad patch data (worst case: no findings returned) |
| T-25-02 | D (Denial of Service) | BFS graph traversal | mitigate | BFS uses visited set to prevent infinite loops from cyclic patch connections (same pattern as dsp_critic.py _check_gain_staging) |
| T-25-03 | I (Information Disclosure) | CriticResult finding text | accept | Finding text includes object names and box IDs from the patch -- this is the intended behavior for developer-facing diagnostics |
</threat_model>

<verification>
1. `python3 -m pytest tests/test_critics.py -x -v` -- all package critic tests pass
2. `python3 -m pytest tests/ -x` -- full suite passes (no regressions)
3. `grep -c "class TestPackageCritic" tests/test_critics.py` returns 1
4. `grep -c "def review_packages" src/maxpat/critics/package_critic.py` returns 1
5. `grep "severity.*warning" tests/test_critics.py` confirms BEAP tests check for warning severity
6. `grep "severity.*blocker" tests/test_critics.py` confirms Bach tests check for blocker severity
</verification>

<success_criteria>
- package_critic.py has review_packages() with conditional BEAP/Bach/community dispatch
- BEAP critic catches: missing output termination, missing VCA (both warnings)
- Bach critic catches: llll/list type mismatches (blockers)
- Community critic catches: unextracted community packages (warnings)
- All 11+ package critic tests pass
- Zero regressions in existing 56 critic tests
</success_criteria>

<output>
After completion, create `.planning/phases/25-templates-and-critics/25-01-SUMMARY.md`
</output>
