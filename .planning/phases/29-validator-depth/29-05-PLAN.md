---
phase: 29-validator-depth
plan: 05
type: execute
wave: 3
depends_on:
  - "29-02"
  - "29-04"
files_modified:
  - src/maxpat/validation.py
  - tests/test_validation.py
autonomous: true
requirements:
  - VALID-04
  - VALID-05
threat_model:
  trust_boundaries:
    - "patch JSON -> embedded codebox walker: gen~ inner patcher's codebox.code text passed verbatim to validate_genexpr"
  threats:
    - id: T-29-05
      category: T
      component: "_validate_embedded_genexpr in validation.py"
      disposition: accept
      rationale: "Codebox 'code' attribute is text only; validate_genexpr applies bounded regex (no ReDoS, no exec, no I/O). Code is never compiled or executed by the validator."

must_haves:
  truths:
    - "D-17: _validate_embedded_genexpr walks every top-level gen~ box's embedded patcher, finds boxes with maxclass='codebox' and a 'code' attribute, and runs validate_genexpr on each — emitting findings with layer='code' (existing convention). Single entry point reused; no parallel review_gendsp."
    - "D-13: validate_genexpr (Plan 02) is the SINGLE entry point — the walker calls into it; it does not re-implement Check 7/8/9 logic. Same checks fire on .gendsp via hooks.validate_code_file AND on .maxpat embedded codeboxes via this walker (parity in both directions)."
    - "A patch with a top-level gen~ box whose inner patcher contains a codebox with code 'out1 = delay(in1, 100);' produces at least one ValidationResult level='error' with layer='code' and message containing 'delay()' AND a tag identifying the gen~ box id."
    - "A patch with a gen~ codebox containing 'out1 = clip(in1, 0, 1);' produces ERROR mentioning 'clip()'."
    - "A patch with a gen~ codebox containing an init-before-if/else violation produces ERROR mentioning 'without prior init'."
    - "A patch with a clean gen~ codebox (no Check 7/8/9 violations) produces no error from this walker."
    - "A patch with no gen~ boxes or no codeboxes produces no error from this walker (graceful empty case)."
    - "D-19: The walker preserves the level and auto_fixed flags from the underlying validate_genexpr finding (severity contract carried through unchanged)."
  artifacts:
    - path: "src/maxpat/validation.py"
      provides: "_validate_embedded_genexpr walker function + call site in validate_patch after _validate_domain_restrictions"
      contains: "_validate_embedded_genexpr"
    - path: "tests/test_validation.py"
      provides: "TestEmbeddedGenExpr class with 5+ tests"
      contains: "class TestEmbeddedGenExpr"
  key_links:
    - from: "validate_patch"
      to: "_validate_embedded_genexpr"
      via: "results.extend(...) call after _validate_domain_restrictions in orchestrator"
      pattern: "_validate_embedded_genexpr\\(patch_dict, db\\)"
    - from: "_validate_embedded_genexpr"
      to: "validate_genexpr (code_validation.py)"
      via: "deferred import + per-codebox call producing ValidationResult findings"
      pattern: "from src.maxpat.code_validation import validate_genexpr"
---

<objective>
Wire embedded gen~ codeboxes inside .maxpat patches to validate_genexpr (the same pipeline .gendsp files already use via hooks.validate_code_file). This achieves VALID-04 parity — both .gendsp files AND .maxpat-embedded codeboxes get Check 7/8/9 (delay/clip/init-before-if-else) plus existing Checks 1-6. Top-level gen~ boxes only (no recursion, mirrors Plan 04's D-07 scope rule).

Purpose: VALID-04 (DSP critic rigor parity for embedded codeboxes — patch-side half), VALID-05 (severity contract preserved through the walker).

Output: validation.py gains _validate_embedded_genexpr function + call site in validate_patch after _validate_domain_restrictions. test_validation.py gains TestEmbeddedGenExpr class with at least 5 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-validator-depth/29-CONTEXT.md
@.planning/phases/29-validator-depth/29-RESEARCH.md
@CLAUDE.md
@src/maxpat/validation.py
@src/maxpat/code_validation.py
@src/maxpat/hooks.py
@tests/test_validation.py
@tests/test_code_validation.py

<interfaces>
After Plan 02 lands, validate_genexpr in code_validation.py returns a list of ValidationResult with layer="code". It now includes Checks 1-6 (existing) plus Phase 29 Checks 7 (delay() rejection, ERROR), 8 (clip() rejection, ERROR), and 9 (init-before-if/else, ERROR).

In validation.py, the ValidationResult class has slots (layer, level, message, auto_fixed). After Plan 04 the validate_patch orchestrator's last call before this plan's insertion is:

    results.extend(_validate_domain_restrictions(patch_dict, db))

Plan 05 adds:

    results.extend(_validate_embedded_genexpr(patch_dict, db))

Walker contract per RESEARCH.md "Embedded Codebox Walker" (flat top-level only):

  For each top-level box where maxclass is gen~ (or newobj with text starting "gen~"):
    Look in box["patcher"]["boxes"] for boxes with maxclass == "codebox"
    For each codebox with a "code" string attribute:
      Run validate_genexpr(code, db=db)
      Tag findings with the gen~ box id for debuggability

Existing Layer 4 GenExpr checks (NOT removed this plan — duplicate-emission acknowledged per RESEARCH.md R1):
- _check_genexpr_io_syntax (validation.py:954) fires on embedded codeboxes
- _check_genexpr_delay_usage (validation.py:979) fires on embedded codeboxes

Both continue to fire alongside the new walker. Cleanup is OUT OF SCOPE this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _validate_embedded_genexpr walker plus call site in validate_patch</name>
  <files>src/maxpat/validation.py</files>

  <read_first>
    - src/maxpat/validation.py (especially lines 84-145 validate_patch orchestration; the new function lives at the bottom of the file as a sibling Layer 5)
    - src/maxpat/code_validation.py validate_genexpr signature and Checks 7/8/9 (after Plan 02)
    - .planning/phases/29-validator-depth/29-CONTEXT.md D-13, D-17 (locked walker behavior — single entry point, ValidationResult layer="code")
    - .planning/phases/29-validator-depth/29-RESEARCH.md "VALID-04: Embedded Codebox Walker" (full implementation block plus flat-top-level rationale)
  </read_first>

  <action>
1. Add `_validate_embedded_genexpr` function in validation.py. Place it AFTER `_validate_domain_restrictions` (Plan 04) at the bottom of the file, with its own header:

```
# ===========================================================================
# Layer 5: Embedded GenExpr Codebox Walker (Phase 29 / VALID-04 / D-17)
# ===========================================================================

def _validate_embedded_genexpr(
    patch_dict: dict,
    db: ObjectDatabase,
) -> list[ValidationResult]:
    """Walk top-level gen~ boxes; for each embedded codebox with a 'code'
    attribute, run validate_genexpr (which carries Checks 1-9 including
    the new Phase 29 Checks 7/8/9 — delay() / clip() / init-before-if/else).

    Findings flow through the ValidationResult pipeline (consistent with
    .gendsp files going through hooks.validate_code_file). Tags each
    finding with the gen~ box id for debuggability.

    Top-level only — does not recurse into nested gen~ subpatchers (D-07
    scope-rule alignment with the domain-restriction guard). Survey of 28
    real .maxpat files showed zero nested gen~ inside gen~; recursion is a
    one-line change if Phase 30 surfaces a real case.

    Note (RESEARCH.md R1): existing Layer 4 helpers
    `_check_genexpr_io_syntax` and `_check_genexpr_delay_usage` ALSO fire on
    embedded codeboxes — duplicate emission for `delay(` is acknowledged
    and accepted this phase. Consolidating to a single channel is OUT OF
    SCOPE here; Phase 33-class cleanup.
    """
    # Deferred import — code_validation imports validation, so avoid a
    # circular import at module top.
    from src.maxpat.code_validation import validate_genexpr

    results: list[ValidationResult] = []
    for box_entry in patch_dict["patcher"]["boxes"]:
        box = box_entry.get("box", {})
        # gen~ at the outer level can carry maxclass='newobj' with text
        # 'gen~ ...' OR maxclass='gen~' directly (subpatcher container —
        # see _validate_maxclass_usage at line ~283 for the existing
        # convention check). Accept both shapes.
        maxclass = box.get("maxclass", "")
        text = box.get("text", "")
        is_gen_tilde = (
            maxclass == "gen~"
            or (maxclass == "newobj" and text.startswith("gen~"))
        )
        if not is_gen_tilde:
            continue
        inner = box.get("patcher")
        if not inner:
            continue
        gen_id = box.get("id", "<unknown>")
        for inner_entry in inner.get("boxes", []):
            inner_box = inner_entry.get("box", {})
            if inner_box.get("maxclass") != "codebox":
                continue
            code = inner_box.get("code")
            if not code:
                continue
            for r in validate_genexpr(code, db=db):
                results.append(ValidationResult(
                    r.layer, r.level,
                    f"gen~ '{gen_id}' codebox: {r.message}",
                    auto_fixed=r.auto_fixed,
                ))
    return results
```

2. Wire it into `validate_patch` orchestration. Find Plan 04's call site at line approximately 145:

```
    # Layer 4b: Domain restriction guard (Phase 29 / VALID-02)
    results.extend(_validate_domain_restrictions(patch_dict, db))
```

Add IMMEDIATELY AFTER it:

```
    # Layer 5: Embedded GenExpr codebox walker (Phase 29 / VALID-04)
    results.extend(_validate_embedded_genexpr(patch_dict, db))
```

**Constraints:**
- DO NOT remove `_check_genexpr_io_syntax` or `_check_genexpr_delay_usage` — RESEARCH.md R1 explicitly leaves the duplicate-emission cleanup to a future phase.
- DO NOT recurse into nested gen~ inside gen~. The outer for loop iterates `patch_dict["patcher"]["boxes"]` once at the top level only.
- DO NOT use `dsp_critic._extract_codebox_code` — RESEARCH.md "Reuse note" mandates inlining the codebox search to (a) handle multiple codeboxes per gen~, (b) capture gen~ box id for the tag, (c) avoid importing from critics/.
- The deferred `from src.maxpat.code_validation import validate_genexpr` import inside the function is intentional — `code_validation.py` already imports `ValidationResult` from `validation.py`, so a top-level import here would be circular.
- Re-emitting findings with a `ValidationResult(...)` constructor (not appending the original) lets us tag the gen~ box id while preserving level/auto_fixed values from validate_genexpr.
- Layer label stays `"code"` (matches validate_genexpr convention) — NOT a new "embedded" layer.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_validation.py::TestEmbeddedGenExpr -x -v</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "def _validate_embedded_genexpr" src/maxpat/validation.py` returns 0
    - `grep -q "Layer 5: Embedded GenExpr Codebox Walker" src/maxpat/validation.py` returns 0 (section header)
    - `grep -q "results.extend(_validate_embedded_genexpr" src/maxpat/validation.py` returns 0 (call site in validate_patch)
    - `grep -q "from src.maxpat.code_validation import validate_genexpr" src/maxpat/validation.py` returns 0 (deferred import inside walker)
    - `grep -q "gen~ '{gen_id}' codebox:" src/maxpat/validation.py` returns 0 (tagged message format) — note: f-string literal pattern, may need slight escape adjustment depending on grep flavor; alternative: grep for `codebox: {r.message}`
    - `python3 -m pytest tests/test_validation.py::TestEmbeddedGenExpr -x -q` passes (5+ tests added in Task 2)
    - `python3 -m pytest tests/test_validation.py -x -q` full file passes (existing tests stay green)
    - `python3 -m pytest -x -q` full suite passes
    - Manual probe: `python3 -c "from src.maxpat.validation import validate_patch; from src.maxpat.db_lookup import ObjectDatabase; p={'patcher':{'boxes':[{'box':{'id':'g1','maxclass':'newobj','text':'gen~','numinlets':0,'numoutlets':1,'outlettype':['signal'],'patcher':{'boxes':[{'box':{'id':'cb1','maxclass':'codebox','code':'out1 = delay(in1, 100);'}}],'lines':[]}}}],'lines':[]}}; r=validate_patch(p, db=ObjectDatabase()); print([str(x) for x in r if 'delay()' in x.message])"` prints at least one finding mentioning `delay()` AND `gen~ 'g1'`
  </acceptance_criteria>

  <done>
    `_validate_embedded_genexpr` exists in validation.py as a sibling Layer 5; iterates only top-level gen~ boxes; runs validate_genexpr on each embedded codebox's code attribute; tags findings with the gen~ box id; the call site in validate_patch runs after `_validate_domain_restrictions`. All existing tests stay green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TestEmbeddedGenExpr class to tests/test_validation.py</name>
  <files>tests/test_validation.py</files>

  <read_first>
    - tests/test_validation.py (existing fixture pattern at lines 16-58 — `db`, `_make_patch_dict`, `_make_box`, `_make_line`)
    - src/maxpat/validation.py _validate_embedded_genexpr (just-added)
    - src/maxpat/code_validation.py validate_genexpr (Plan 02)
    - .planning/phases/29-validator-depth/29-RESEARCH.md "Validation Architecture" TestEmbeddedGenExpr row
  </read_first>

  <behavior>
    - Test 1 (test_embedded_delay_emits_error): A patch with a top-level gen~ whose inner patcher has a codebox with code containing `delay(` produces at least one ValidationResult with level="error", message containing "delay()" AND `gen~ 'g1'` (or whatever id was set).
    - Test 2 (test_embedded_clip_emits_error): Same shape, code contains `clip(` → ERROR mentioning "clip()".
    - Test 3 (test_embedded_init_before_if_emits_error): Code contains an if-block-only assignment → ERROR mentioning "without prior init".
    - Test 4 (test_clean_codebox_silent): A codebox with valid code (e.g., `out1 = in1 * 0.5;`) produces no walker error.
    - Test 5 (test_no_gen_no_findings): A patch with no gen~ boxes produces no walker findings.
    - Test 6 (test_gen_without_codebox_silent): A gen~ box whose inner patcher has no codeboxes produces no walker findings.
    - Test 7 (test_severity_carried_through): The walker preserves the level and auto_fixed values from validate_genexpr (e.g., Check 7 ERROR comes through as ERROR with auto_fixed=False).
    - Test 8 (test_box_id_in_tag): The tagged message contains the gen~ box id verbatim, so callers can grep on it for multi-gen~ patches.
  </behavior>

  <action>
Add a `TestEmbeddedGenExpr` class to `tests/test_validation.py`. Place it AFTER `TestDomainGuard` (Plan 04). Use a helper to build the embedded-gen~ patch shape:

```
# ===========================================================================
# Layer 5: Embedded GenExpr Codebox Walker (Phase 29 / VALID-04 / D-17)
# ===========================================================================

class TestEmbeddedGenExpr:
    """VALID-04 / D-17: embedded gen~ codeboxes inside .maxpat files get
    Checks 1-9 from validate_genexpr piped through as ValidationResult
    findings, tagged with the gen~ box id. Top-level only (D-07 scope
    alignment with the domain guard)."""

    def _patch_with_gen_codebox(self, code, gen_id="g1"):
        """Build a minimal patch with one top-level gen~ wrapping a single
        codebox containing the given code."""
        gen_box = {
            "box": {
                "id": gen_id,
                "maxclass": "newobj",
                "text": "gen~",
                "numinlets": 0,
                "numoutlets": 1,
                "outlettype": ["signal"],
                "patching_rect": [0, 0, 80, 22],
                "patcher": {
                    "boxes": [
                        {"box": {
                            "id": "cb1",
                            "maxclass": "codebox",
                            "code": code,
                        }}
                    ],
                    "lines": [],
                },
            }
        }
        return {"patcher": {"boxes": [gen_box], "lines": []}}

    def test_embedded_delay_emits_error(self, db):
        """delay( in embedded codebox triggers Check 7 via the walker."""
        patch = self._patch_with_gen_codebox("out1 = delay(in1, 100);")
        results = validate_patch(patch, db=db)
        delay_errors = [
            r for r in results
            if r.level == "error" and "delay()" in r.message
            and "gen~ 'g1'" in r.message
        ]
        assert len(delay_errors) >= 1, [r.message for r in results]

    def test_embedded_clip_emits_error(self, db):
        """clip( in embedded codebox triggers Check 8 via the walker."""
        patch = self._patch_with_gen_codebox("out1 = clip(in1, 0., 1.);")
        results = validate_patch(patch, db=db)
        clip_errors = [
            r for r in results
            if r.level == "error" and "clip()" in r.message
            and "gen~ 'g1'" in r.message
        ]
        assert len(clip_errors) >= 1, [r.message for r in results]

    def test_embedded_init_before_if_emits_error(self, db):
        """Variable assigned only inside if/else triggers Check 9."""
        code = "if (in1 > 0) {\n    y = in1 * 2;\n}\nout1 = y;"
        patch = self._patch_with_gen_codebox(code)
        results = validate_patch(patch, db=db)
        init_errors = [
            r for r in results
            if r.level == "error" and "without prior init" in r.message
            and "gen~ 'g1'" in r.message
        ]
        assert len(init_errors) >= 1, [r.message for r in results]

    def test_clean_codebox_silent(self, db):
        """A codebox with no Check 7/8/9 violations produces no walker error."""
        patch = self._patch_with_gen_codebox("out1 = in1 * 0.5;")
        results = validate_patch(patch, db=db)
        walker_findings = [
            r for r in results
            if "gen~ 'g1' codebox:" in r.message
            and r.level == "error"
        ]
        # Walker may emit 'info' (Check 3 in/out detection) but no errors
        assert walker_findings == [], [r.message for r in walker_findings]

    def test_no_gen_no_findings(self, db):
        """Patch with no gen~ boxes produces no walker findings."""
        boxes = [
            _make_box("c1", text="cycle~ 440",
                      numinlets=2, numoutlets=1, outlettype=["signal"]),
        ]
        patch = _make_patch_dict(boxes=boxes, lines=[])
        results = validate_patch(patch, db=db)
        walker_findings = [
            r for r in results
            if "codebox:" in r.message
        ]
        assert walker_findings == []

    def test_gen_without_codebox_silent(self, db):
        """gen~ with an inner patcher that has no codeboxes produces no
        walker findings."""
        gen_box = {
            "box": {
                "id": "g1",
                "maxclass": "newobj",
                "text": "gen~",
                "numinlets": 0,
                "numoutlets": 1,
                "outlettype": ["signal"],
                "patcher": {
                    "boxes": [
                        {"box": {
                            "id": "in1",
                            "maxclass": "inlet",
                            "numinlets": 0,
                            "numoutlets": 1,
                        }}
                    ],
                    "lines": [],
                },
            }
        }
        patch = {"patcher": {"boxes": [gen_box], "lines": []}}
        results = validate_patch(patch, db=db)
        walker_findings = [
            r for r in results
            if "gen~ 'g1' codebox:" in r.message
        ]
        assert walker_findings == []

    def test_severity_carried_through(self, db):
        """Walker preserves level and auto_fixed from validate_genexpr.
        D-19: Check 7 is ERROR with auto_fixed=False."""
        patch = self._patch_with_gen_codebox("out1 = delay(in1, 100);")
        results = validate_patch(patch, db=db)
        target = next(
            (r for r in results
             if "delay()" in r.message and "gen~ 'g1'" in r.message),
            None,
        )
        assert target is not None, [r.message for r in results]
        assert target.level == "error"
        assert target.auto_fixed is False

    def test_box_id_in_tag(self, db):
        """Two distinct gen~ boxes produce two distinct tagged findings,
        each citing the correct gen~ id."""
        gen_box_a = {
            "box": {
                "id": "alpha",
                "maxclass": "newobj",
                "text": "gen~",
                "numinlets": 0,
                "numoutlets": 1,
                "outlettype": ["signal"],
                "patcher": {
                    "boxes": [
                        {"box": {
                            "id": "cba",
                            "maxclass": "codebox",
                            "code": "out1 = delay(in1, 100);",
                        }}
                    ],
                    "lines": [],
                },
            }
        }
        gen_box_b = {
            "box": {
                "id": "beta",
                "maxclass": "newobj",
                "text": "gen~",
                "numinlets": 0,
                "numoutlets": 1,
                "outlettype": ["signal"],
                "patcher": {
                    "boxes": [
                        {"box": {
                            "id": "cbb",
                            "maxclass": "codebox",
                            "code": "out1 = clip(in1, 0., 1.);",
                        }}
                    ],
                    "lines": [],
                },
            }
        }
        patch = {"patcher": {"boxes": [gen_box_a, gen_box_b], "lines": []}}
        results = validate_patch(patch, db=db)
        alpha_findings = [
            r for r in results
            if "gen~ 'alpha'" in r.message and "delay()" in r.message
        ]
        beta_findings = [
            r for r in results
            if "gen~ 'beta'" in r.message and "clip()" in r.message
        ]
        assert len(alpha_findings) >= 1, [r.message for r in results]
        assert len(beta_findings) >= 1, [r.message for r in results]
```

**Constraints:**
- DO NOT add overrides.json fixtures — the gen~ object already exists in the production DB.
- The codebox `code` attribute is a plain string; `validate_genexpr` regex passes operate on it directly.
- DO NOT use `tmp_path` here — the walker operates on dicts in memory; no file I/O needed.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_validation.py::TestEmbeddedGenExpr -x -v</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "class TestEmbeddedGenExpr" tests/test_validation.py` returns 0
    - `grep -q "def test_embedded_delay_emits_error" tests/test_validation.py` returns 0
    - `grep -q "def test_embedded_clip_emits_error" tests/test_validation.py` returns 0
    - `grep -q "def test_embedded_init_before_if_emits_error" tests/test_validation.py` returns 0
    - `grep -q "def test_clean_codebox_silent" tests/test_validation.py` returns 0
    - `grep -q "def test_no_gen_no_findings" tests/test_validation.py` returns 0
    - `grep -q "def test_gen_without_codebox_silent" tests/test_validation.py` returns 0
    - `grep -q "def test_severity_carried_through" tests/test_validation.py` returns 0
    - `grep -q "def test_box_id_in_tag" tests/test_validation.py` returns 0
    - `python3 -m pytest tests/test_validation.py::TestEmbeddedGenExpr -x -q` passes (8 tests)
    - `python3 -m pytest tests/test_validation.py -x -q` full file passes (TestRoleAwareValidation + TestDomainGuard + TestEmbeddedGenExpr + existing all green)
    - `python3 -m pytest -x -q` full suite passes
  </acceptance_criteria>

  <done>
    `TestEmbeddedGenExpr` class exists with at least 8 tests covering: delay/clip/init Check 7-9 fires through walker; clean codebox silent; no-gen no-findings; gen-without-codebox silent; severity carried through (ERROR + auto_fixed=False); box id correctly tagged across multiple gen~ boxes. All tests pass; full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch JSON -> embedded codebox walker | gen~ inner patcher's codebox `code` text passed verbatim to `validate_genexpr`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | T (Tampering) | `_validate_embedded_genexpr` walker in validation.py | accept | Codebox `code` attribute is text only; `validate_genexpr` applies bounded regex (no ReDoS, no `exec`, no I/O). The walker never compiles or executes the GenExpr code. ASVS L1 input-validation upstream by Layers 1/2 of the pipeline. No auth or PII surface. |
</threat_model>

<verification>
- `python3 -m pytest tests/test_validation.py::TestEmbeddedGenExpr -x -q` passes (8 new tests)
- `python3 -m pytest tests/test_validation.py -x -q` full file passes (TestRoleAwareValidation + TestDomainGuard + TestEmbeddedGenExpr + existing all green)
- `python3 -m pytest -x -q` full suite green (regression anchor: no test_code_validation.py / test_schema_extensions.py failures)
- Manual probe: invoke `validate_patch` on a patch with an embedded gen~ codebox containing `delay(` and confirm the result includes a tagged `gen~ '<id>' codebox: delay() is not supported...` finding.
</verification>

<success_criteria>
- VALID-04 (parity half) satisfied: embedded gen~ codeboxes inside `.maxpat` files run through `validate_genexpr` and emit the same Check 7/8/9 ERRORs that `.gendsp` files already emit via `hooks.validate_code_file`.
- VALID-05 satisfied for embedded family: severity is carried through unchanged from `validate_genexpr` — Check 7/8/9 stay ERROR; existing Checks 2/4 stay WARNING.
- D-13 (single entry point: `validate_genexpr`), D-17 (walker wires embedded codeboxes this phase, layer="code") enforced.
- Top-level only (D-07 alignment with domain guard) — no recursion regressions possible.
- Existing 84+ test_validation.py + new TestRoleAwareValidation + TestDomainGuard + new TestEmbeddedGenExpr all green.
- Full pytest suite green; phase ready for `/gsd-verify-work`.
</success_criteria>

<output>
After completion, create `.planning/phases/29-validator-depth/29-05-SUMMARY.md`
</output>
