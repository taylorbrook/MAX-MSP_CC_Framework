---
phase: 12-pipeline-integration-agent-updates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/max-patch-agent/SKILL.md
  - .claude/skills/max-dsp-agent/SKILL.md
  - .claude/skills/max-rnbo-agent/SKILL.md
  - .claude/skills/max-js-agent/SKILL.md
  - .claude/skills/max-ext-agent/SKILL.md
  - .claude/skills/max-ui-agent/SKILL.md
  - tests/test_agent_skills.py
autonomous: true
requirements: [AGNT-01, AGNT-02]

must_haves:
  truths:
    - "All 6 specialist agent SKILL.md files document aesthetic capabilities"
    - "Each agent SKILL.md lists Patcher methods for explicit styling (add_section_header, add_subsection, add_annotation, add_bubble, add_panel, add_step_marker)"
    - "Each agent SKILL.md lists aesthetics.py helpers (set_canvas_background, set_object_bgcolor, auto_size_panel, is_complex_patch)"
    - "Each agent SKILL.md documents LayoutOptions and generate_patch() layout_options parameter"
    - "Each agent SKILL.md notes that canvas bg and dac~/loadbang highlighting are auto-applied"
    - "DSP agent curated object lists are verified against audit corrections (no significant changes needed)"
    - "All existing test_agent_skills.py assertions still pass"
  artifacts:
    - path: ".claude/skills/max-patch-agent/SKILL.md"
      provides: "Aesthetic capabilities documentation for patch agent"
      contains: "Aesthetic Capabilities"
    - path: ".claude/skills/max-dsp-agent/SKILL.md"
      provides: "Aesthetic capabilities documentation for DSP agent"
      contains: "Aesthetic Capabilities"
    - path: ".claude/skills/max-rnbo-agent/SKILL.md"
      provides: "Aesthetic capabilities documentation for RNBO agent"
      contains: "Aesthetic Capabilities"
    - path: ".claude/skills/max-js-agent/SKILL.md"
      provides: "Aesthetic capabilities documentation for JS agent"
      contains: "Aesthetic Capabilities"
    - path: ".claude/skills/max-ext-agent/SKILL.md"
      provides: "Aesthetic capabilities documentation for ext agent"
      contains: "Aesthetic Capabilities"
    - path: ".claude/skills/max-ui-agent/SKILL.md"
      provides: "Aesthetic capabilities documentation for UI agent"
      contains: "Aesthetic Capabilities"
    - path: "tests/test_agent_skills.py"
      provides: "Tests validating aesthetic sections exist in all 6 agents"
      contains: "test_specialist_has_aesthetic_capabilities"
  key_links:
    - from: ".claude/skills/max-patch-agent/SKILL.md"
      to: "src/maxpat/aesthetics.py"
      via: "documentation references"
      pattern: "set_canvas_background"
    - from: ".claude/skills/max-dsp-agent/SKILL.md"
      to: "src/maxpat/defaults.py"
      via: "documentation references"
      pattern: "LayoutOptions"
---

<objective>
Update all 6 specialist agent SKILL.md files with aesthetic capability documentation and cross-reference DSP agent curated object lists against audit corrections. Add test assertions to verify the new content.

Purpose: Agents need to know about aesthetic methods (comment styling, panels, layout options) so they can use them when generating patches. The DB layer handles outlet type corrections transparently, so no per-agent outlet type documentation is needed (per user decision).
Output: 6 updated SKILL.md files, updated test_agent_skills.py with new assertions.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pipeline-integration-agent-updates/12-CONTEXT.md
@.planning/phases/12-pipeline-integration-agent-updates/12-RESEARCH.md

@.claude/skills/max-patch-agent/SKILL.md
@.claude/skills/max-dsp-agent/SKILL.md
@.claude/skills/max-rnbo-agent/SKILL.md
@.claude/skills/max-js-agent/SKILL.md
@.claude/skills/max-ext-agent/SKILL.md
@.claude/skills/max-ui-agent/SKILL.md
@tests/test_agent_skills.py

<interfaces>
<!-- Existing test patterns the executor must preserve. -->

From tests/test_agent_skills.py:
```python
SPECIALIST_AGENTS = [
    "max-patch-agent",
    "max-dsp-agent",
    "max-rnbo-agent",
    "max-js-agent",
    "max-ext-agent",
    "max-ui-agent",
]

# Existing assertions that MUST still pass:
# - test_patch_agent_uses_add_connection: "add_connection" must be in patch agent
# - test_patch_agent_write_patch_signature: "write_patch(patcher" must be in patch agent
# - test_dsp_agent_build_genexpr_signature: "build_genexpr(params, code_body" must be in DSP agent
# - test_rnbo_agent_has_api_refs: "add_rnbo" and "validate_rnbo_patch" must be in RNBO agent
# - test_ext_agent_has_api_refs: "scaffold_external" and "build_external" must be in ext agent
# - test_rnbo_agent_not_stub: no "stub", "phase 5 deferral", "not yet implemented" in RNBO
# - test_ext_agent_not_stub: no "stub", "phase 5 deferral", "not yet implemented" in ext
```

Aesthetic methods to document (from 12-CONTEXT.md locked decisions):
```
Patcher methods: add_section_header, add_subsection, add_annotation, add_bubble, add_panel, add_step_marker
Aesthetics helpers: set_canvas_background, set_object_bgcolor, auto_size_panel, is_complex_patch
LayoutOptions fields: v_spacing, h_gutter, grid_snap, inlet_align, comment_gap
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add aesthetic capabilities section to all 6 agent SKILL.md files</name>
  <files>.claude/skills/max-patch-agent/SKILL.md, .claude/skills/max-dsp-agent/SKILL.md, .claude/skills/max-rnbo-agent/SKILL.md, .claude/skills/max-js-agent/SKILL.md, .claude/skills/max-ext-agent/SKILL.md, .claude/skills/max-ui-agent/SKILL.md</files>
  <action>
Add a new `### Aesthetic Capabilities` section to each of the 6 specialist agent SKILL.md files. Place it within the existing `## Capabilities` hierarchy, AFTER the last existing `###` subsection in Capabilities and BEFORE `## Output Protocol`.

The section content is identical across all 6 agents (per CONTEXT.md: "All 6 agents get aesthetic capability documentation"). Use this exact content:

```markdown
### Aesthetic Capabilities

**Auto-applied by generate_patch() -- no manual calls needed:**
- Canvas background color (off-white with blue tint) applied automatically
- `dac~` and `loadbang` objects highlighted with subtle background colors
- Existing user-set bgcolor is never overwritten

**Patcher methods for explicit styling:**
- `add_section_header(text)` -- 16pt bold colored header with background (for patch sections)
- `add_subsection(text)` -- 12pt bold dark gray label (for subsection grouping)
- `add_annotation(text, target=box)` -- 10pt italic light gray note (for inline documentation)
- `add_bubble(text, bubbleside=1)` -- comment with arrow pointer (for callout notes)
- `add_panel(x, y, w, h, gradient=True)` -- background panel for visual grouping
- `add_step_marker(number, x, y)` -- numbered amber circle (for step-by-step patches)

**Aesthetics helpers (from `src.maxpat.aesthetics`):**
- `set_canvas_background(patcher, color=None)` -- override default canvas color
- `set_object_bgcolor(box, palette_key=None, color=None)` -- highlight specific objects
- `auto_size_panel(boxes, padding=18)` -- compute panel rect to enclose a group of boxes
- `is_complex_patch(patcher)` -- heuristic: True if 10+ boxes or has subpatchers

**Layout options:**
- `generate_patch(patcher, layout_options=LayoutOptions(...))` -- customize layout
- Key fields: `v_spacing` (vertical gap), `h_gutter` (horizontal gap), `grid_snap` (15px grid), `inlet_align` (cable straightening), `comment_gap` (annotation offset)
```

Additionally, for the **DSP agent only**, verify the curated object lists in "Signal Chain Construction" section against audit corrections. The cross-reference check confirms NO significant corrections exist for the listed objects in overrides.json (no outlet type or I/O count changes). No updates needed to the curated lists per CONTEXT.md: "DB layer is enough for outlet type corrections."

IMPORTANT: Do NOT modify any existing content in the SKILL.md files. Only ADD the new section. All existing assertions in test_agent_skills.py depend on specific strings remaining present. Read each file, find the insertion point (after last ### in Capabilities, before ## Output Protocol), and add the section.

IMPORTANT: Update the `generate_patch(patcher)` reference in the patch agent's "Key Functions" section to `generate_patch(patcher, layout_options=None)` to reflect the new signature. Do the same in any other agent that references `generate_patch()` in its existing content.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_agent_skills.py -x --tb=short -q</automated>
  </verify>
  <done>All 6 SKILL.md files have "### Aesthetic Capabilities" section with Patcher methods, aesthetics helpers, and LayoutOptions documentation. All existing test_agent_skills.py assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test assertions for aesthetic capabilities in agent SKILL.md files</name>
  <files>tests/test_agent_skills.py</files>
  <behavior>
    - Test: All 6 specialist agents have "Aesthetic Capabilities" in their SKILL.md
    - Test: All 6 specialist agents reference "add_section_header" (Patcher method)
    - Test: All 6 specialist agents reference "set_canvas_background" (aesthetics helper)
    - Test: All 6 specialist agents reference "LayoutOptions" (layout configuration)
    - Test: All 6 specialist agents reference "auto-applied" or "auto_size_panel" (auto-styling note)
  </behavior>
  <action>
In `tests/test_agent_skills.py`, add new parametrized tests after the existing specialist agent tests:

```python
@pytest.mark.parametrize("agent_name", SPECIALIST_AGENTS)
def test_specialist_has_aesthetic_capabilities(agent_name: str) -> None:
    """Each specialist agent SKILL.md must have an Aesthetic Capabilities section."""
    content = _read_skill(agent_name)
    assert "Aesthetic Capabilities" in content, (
        f"{agent_name}/SKILL.md missing 'Aesthetic Capabilities' section"
    )


@pytest.mark.parametrize("agent_name", SPECIALIST_AGENTS)
def test_specialist_references_patcher_styling_methods(agent_name: str) -> None:
    """Each specialist agent must reference Patcher aesthetic methods."""
    content = _read_skill(agent_name)
    assert "add_section_header" in content, (
        f"{agent_name}/SKILL.md missing add_section_header reference"
    )
    assert "add_panel" in content, (
        f"{agent_name}/SKILL.md missing add_panel reference"
    )


@pytest.mark.parametrize("agent_name", SPECIALIST_AGENTS)
def test_specialist_references_aesthetics_helpers(agent_name: str) -> None:
    """Each specialist agent must reference aesthetics.py helpers."""
    content = _read_skill(agent_name)
    assert "set_canvas_background" in content, (
        f"{agent_name}/SKILL.md missing set_canvas_background reference"
    )
    assert "set_object_bgcolor" in content, (
        f"{agent_name}/SKILL.md missing set_object_bgcolor reference"
    )


@pytest.mark.parametrize("agent_name", SPECIALIST_AGENTS)
def test_specialist_references_layout_options(agent_name: str) -> None:
    """Each specialist agent must reference LayoutOptions for layout configuration."""
    content = _read_skill(agent_name)
    assert "LayoutOptions" in content, (
        f"{agent_name}/SKILL.md missing LayoutOptions reference"
    )
```

Run the full test suite to verify all existing + new assertions pass.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_agent_skills.py -x --tb=short -q</automated>
  </verify>
  <done>test_agent_skills.py has 4 new parametrized tests (24 total new assertions across 6 agents) validating aesthetic capabilities content. All assertions pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `python3 -m pytest tests/test_agent_skills.py -x --tb=short -q` -- all agent skill tests pass
2. Spot check: `grep -l "Aesthetic Capabilities" .claude/skills/max-*/SKILL.md | wc -l` returns 6
3. `python3 -m pytest tests/ -x --tb=short -q` -- full suite green (847+ tests)
</verification>

<success_criteria>
- All 6 specialist agent SKILL.md files have "### Aesthetic Capabilities" section
- Section documents Patcher methods, aesthetics helpers, LayoutOptions, and auto-applied styling
- DSP agent curated object lists verified against audit (no changes needed)
- All existing test_agent_skills.py assertions still pass (no regressions)
- 4 new parametrized test functions added (24 total assertions across 6 agents)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-pipeline-integration-agent-updates/12-02-SUMMARY.md`
</output>
