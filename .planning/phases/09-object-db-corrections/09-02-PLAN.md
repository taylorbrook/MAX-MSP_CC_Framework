---
phase: 09-object-db-corrections
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .claude/max-objects/overrides.json
autonomous: false
requirements:
  - DBCX-01
  - DBCX-02
  - DBCX-03
  - DBCX-04

must_haves:
  truths:
    - "overrides.json contains ~230+ entries (23 existing + 211 audit proposals minus unresolved conflicts)"
    - "db_lookup.py returns corrected outlet types for HIGH-confidence objects (e.g., adsr~ outlets are signal+control, not all signal)"
    - "Objects that previously had empty inlets/outlets now return populated I/O arrays from db.lookup()"
    - "All 750 previously-passing tests still pass after merge (zero regressions)"
    - "overrides.json entries are grouped by domain with _domain_header separator keys"
    - "The 8 conflict objects have been reviewed and resolved with field-level decisions"
  artifacts:
    - path: ".claude/max-objects/overrides.json"
      provides: "Production overrides with audit corrections merged"
      contains: "_domain_msp"
  key_links:
    - from: ".claude/max-objects/overrides.json"
      to: "src/maxpat/db_lookup.py"
      via: "db_lookup.py reads overrides.json at startup and deep-merges onto domain objects"
      pattern: "overrides_path.*read_text"
---

<objective>
Execute the merge: resolve the 8 conflicts with user review, run the merger against real data, and verify zero regressions.

Purpose: This is the data correction step. Plan 01 built the tool; this plan uses it to actually modify overrides.json with verified corrections. The 8 conflict objects require user review (showing manual vs audit values inline). After merge, the full test suite must pass to confirm no regressions.

Output: Updated `.claude/max-objects/overrides.json` with ~230+ entries organized by domain.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-object-db-corrections/09-CONTEXT.md
@.planning/phases/09-object-db-corrections/09-RESEARCH.md
@.planning/phases/09-object-db-corrections/09-01-SUMMARY.md
@src/maxpat/audit/merger.py
@.claude/max-objects/overrides.json
@.claude/max-objects/audit/proposed-overrides.json

<interfaces>
<!-- From 09-RESEARCH.md conflict analysis -- executor needs this for conflict resolution -->

8 Conflict Objects (per research analysis):

1. coll: Keep manual inlets (expert, 2 hot inlets). Adopt audit outlets (4 outlets).
2. curve~: Keep manual outlets (signal + control bang). Adopt audit inlets.
3. info~: Keep manual outlets (5 expert outlets with good digests). Adopt audit inlets.
4. line~: Keep manual outlets (signal + control bang). Adopt audit inlets.
5. stash~: GENUINE DISAGREEMENT -- manual says outlet 1 is control ("Index (int)"), audit says signal (HIGH, 5 instances, 100% agreement). Present to user.
6. stretch~: Keep manual outlets (signal + control bang). Adopt audit inlets if present.
7. thispoly~: Keep manual outlets (2 expert control outlets). Adopt audit inlets if present.
8. vst~: Keep manual outlets (2 signal + 2 control). Adopt audit inlets.

From src/maxpat/audit/merger.py (created in Plan 01):
```python
class OverrideMerger:
    def __init__(self, proposed_path: Path, overrides_path: Path, db: ObjectDatabase): ...
    def detect_conflicts(self) -> dict[str, dict]: ...
    def merge(self, conflict_resolutions: dict | None = None) -> dict: ...
    def write(self, merged: dict) -> None: ...
```

conflict_resolutions format:
```python
{
    "coll": {"adopt_fields": ["outlets"]},
    "curve~": {"adopt_fields": ["inlets"]},
    "stash~": {"adopt_fields": ["outlets"]},  # or empty if user keeps manual
    ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve conflicts and execute merge</name>
  <files>.claude/max-objects/overrides.json</files>
  <action>
Run the merger to produce the updated overrides.json. This task requires presenting the 8 conflicts to the user for review.

**Step 1: Baseline test run**
Run `python3 -m pytest --tb=short -q` and record the pass count (expect 750+ passed, 3 pre-existing failures).

**Step 2: Detect and present conflicts**
Use OverrideMerger to detect conflicts. For each of the 8 conflicts, print a formatted comparison showing:
- Object name
- Manual entry (current overrides.json)
- Audit proposal (from proposed-overrides.json)
- Audit confidence, instance count, and agreement percentage
- Recommended resolution from research analysis (see interfaces section above)

Present all 8 inline. For 7 of the 8, the research has clear recommendations. For stash~ (genuine disagreement), present both sides with the evidence: manual says outlet 1 is control ("Index (int)"), audit says signal with HIGH confidence (5 instances, 100% agreement from Cycling '74's own help patches). Recommend adopting audit data for stash~ given the HIGH confidence and 100% agreement from the authoritative source.

**Step 3: Build conflict resolutions dict**
Based on the research analysis and user approval:
- coll: `{"adopt_fields": ["outlets"]}` -- take audit outlets (4), keep manual inlets (2 hot)
- curve~: `{"adopt_fields": ["inlets"]}` -- take audit inlets, keep manual outlets (signal + bang)
- info~: `{"adopt_fields": ["inlets"]}` -- take audit inlets, keep manual outlets (5 expert)
- line~: `{"adopt_fields": ["inlets"]}` -- take audit inlets, keep manual outlets (signal + bang)
- stash~: `{"adopt_fields": ["outlets"]}` -- take audit outlets if user approves, else skip
- stretch~: `{"adopt_fields": ["inlets"]}` -- take audit inlets if present, keep manual outlets
- thispoly~: `{"adopt_fields": ["inlets"]}` -- take audit inlets, keep manual outlets (2 expert)
- vst~: `{"adopt_fields": ["inlets"]}` -- take audit inlets, keep manual outlets (4 expert)

**Step 4: Execute merge**
```python
from pathlib import Path
from src.maxpat.audit.merger import OverrideMerger
from src.maxpat.db_lookup import ObjectDatabase

db = ObjectDatabase()
merger = OverrideMerger(
    proposed_path=Path(".claude/max-objects/audit/proposed-overrides.json"),
    overrides_path=Path(".claude/max-objects/overrides.json"),
    db=db,
)
merged = merger.merge(conflict_resolutions=resolutions)
merger.write(merged)
```

**Step 5: Verify merge output**
- Count total entries in merged objects dict (should be ~230+)
- Verify domain grouping: check for _domain_max, _domain_msp, etc. separator keys
- Verify version_map and variable_io_rules preserved
- Spot-check 3-5 merged entries by loading with ObjectDatabase and checking corrected values
- Document the 121 uncovered empty-I/O objects: add `_uncovered_empty_io` metadata key at top level listing object names for future work, OR write to a separate report. Use discretion -- a top-level underscore key is cleanest since db_lookup skips underscore keys at the objects level but this is a file-level key.

**Step 6: Post-merge regression test**
Run `python3 -m pytest --tb=short -q` and confirm same pass count as baseline. Any new failures must be investigated and fixed before proceeding.
  </action>
  <verify>
    <automated>python3 -m pytest --tb=short -q</automated>
  </verify>
  <done>overrides.json contains ~230+ entries grouped by domain. All 750+ previously-passing tests still pass. version_map and variable_io_rules preserved. All 8 conflicts resolved with field-level merge.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify merged overrides.json</name>
  <files>.claude/max-objects/overrides.json</files>
  <action>
User reviews the merged overrides.json to confirm correctness:
1. Open `.claude/max-objects/overrides.json` and verify domain section headers (_domain_max, _domain_msp, etc.)
2. Check original manual entries (buffer~, info~, line~, etc.) still have expert digest strings
3. Check resolved conflict entries have _manual_original field preserving old values
4. Check audit entries have _audit metadata (confidence, instances, agreement, source)
5. Run `python3 -m pytest --tb=short -q` and confirm 750+ passed, 3 pre-existing failures only
6. Quick spot check with ObjectDatabase to verify corrected values are returned by db.lookup()
  </action>
  <verify>
    <automated>python3 -m pytest --tb=short -q</automated>
  </verify>
  <done>User approves the merged overrides.json. All tests pass. Corrections are live in the production database.</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest --tb=short -q` -- 750+ passed, 3 pre-existing failures, no new failures (DBCX-03)
2. `python3 -c "import json; d=json.load(open('.claude/max-objects/overrides.json')); print(len([k for k in d['objects'] if not k.startswith('_')]))"` -- shows ~230+ non-header entries (DBCX-01, DBCX-02)
3. `python3 -c "import json; d=json.load(open('.claude/max-objects/overrides.json')); print([k for k in d['objects'] if k.startswith('_domain')])"` -- shows domain headers (DBCX-04)
4. `python3 -c "import json; d=json.load(open('.claude/max-objects/overrides.json')); print('version_map' in d, 'variable_io_rules' in d)"` -- both True
</verification>

<success_criteria>
- overrides.json has ~230+ object entries (23 existing + 211 audit - unresolved conflicts)
- All entries grouped by domain with _domain_header separator keys
- All 8 conflicts resolved with field-level merge (expert digests preserved, audit I/O data adopted)
- version_map and variable_io_rules sections preserved exactly
- Full test suite: 750+ passed, 3 pre-existing failures, zero new failures
- User has verified the merged file
</success_criteria>

<output>
After completion, create `.planning/phases/09-object-db-corrections/09-02-SUMMARY.md`
</output>
