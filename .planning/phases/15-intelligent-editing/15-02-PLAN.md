---
phase: 15-intelligent-editing
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - src/maxpat/patcher.py
  - tests/test_patcher.py
autonomous: true
requirements: [ED-02, ED-05]

must_haves:
  truths:
    - "User can insert an object into an existing connection and the original connection is replaced by source->new->dest wiring"
    - "Insert affects ALL connections between the source and destination (e.g., stereo connections)"
    - "Inserted object is auto-positioned below the source object with standard spacing and grid snap"
    - "New boxes added without explicit position get placed at center of patcher visible rect"
    - "Collision detection nudges new objects right then down until clear space is found"
    - "All positions snap to MAX's 15px grid"
    - "I/O mismatch on insert returns EditResult with orphaned connections, not a hard failure"
  artifacts:
    - path: "src/maxpat/patcher.py"
      provides: "insert_into_connection(), _auto_position(), _find_clear_position() methods"
      contains: "def insert_into_connection"
    - path: "tests/test_patcher.py"
      provides: "TestInsertIntoConnection and TestAutoPosition test classes"
      contains: "class TestInsertIntoConnection"
  key_links:
    - from: "insert_into_connection"
      to: "add_box + remove_connection + add_connection"
      via: "composes existing CRUD primitives"
      pattern: "self\\.add_box.*self\\.remove_connection.*self\\.add_connection"
    - from: "_auto_position"
      to: "patching_rect"
      via: "collision detection against existing box rects"
      pattern: "_find_clear_position"
    - from: "_find_clear_position"
      to: "15px grid"
      via: "round(x / 15.0) * 15.0 snap"
      pattern: "round.*15"
---

<objective>
Add auto-positioning helpers and insert_into_connection() to the Patcher class.

Purpose: Enable smart object placement (ED-05) and atomic connection insertion (ED-02). Auto-positioning is used by insert_into_connection and can also improve add_box for cases where no explicit coordinates are given. insert_into_connection is the key workflow operation for splicing objects into signal chains.

Output: _auto_position(), _find_clear_position() private helpers, insert_into_connection() public method, with full test coverage.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-intelligent-editing/15-CONTEXT.md
@.planning/phases/15-intelligent-editing/15-RESEARCH.md
@.planning/phases/15-intelligent-editing/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (must exist before this plan runs) -->

From src/maxpat/patcher.py (EditResult from Plan 01):
```python
@dataclass
class EditResult:
    box: Box
    orphaned: list[dict]  # Each: {"source_id", "source_outlet", "dest_id", "dest_inlet"}
```

From src/maxpat/patcher.py (existing CRUD methods):
```python
class Patcher:
    def add_box(self, name: str, args: list[str] | None = None, x: float = 0.0, y: float = 0.0) -> Box: ...
    def add_connection(self, src_box: Box, src_outlet: int, dst_box: Box, dst_inlet: int, ...) -> Patchline: ...
    def remove_connection(self, src_box: Box, src_outlet: int, dst_box: Box, dst_inlet: int) -> None: ...
```

From src/maxpat/defaults.py:
```python
V_SPACING = 20
class LayoutOptions:
    grid_size: float = 15.0
```

From src/maxpat/patcher.py (Box):
```python
class Box:
    patching_rect: list[float]  # [x, y, w, h]
    numinlets: int
    numoutlets: int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto-positioning helpers with tests</name>
  <files>src/maxpat/patcher.py, tests/test_patcher.py</files>
  <behavior>
    - _find_clear_position(x, y, w, h, exclude_box=None) returns (x, y) snapped to 15px grid with no overlap
    - When proposed position overlaps an existing box, nudges right by 15px; if x > 1200, wraps to next row
    - Exclude_box parameter prevents a box from colliding with itself during reposition
    - _auto_position(box, near_box=None) positions box below near_box with V_SPACING gap if near_box given
    - _auto_position with no near_box positions at center of patcher visible rect
    - All returned positions are snapped to 15px grid
    - Collision padding of 5px around boxes for readability
  </behavior>
  <action>
    1. Add two private helper methods to Patcher class:

    `_find_clear_position(self, x, y, w, h, exclude_box=None) -> tuple[float, float]`:
    - Snap x, y to 15px grid
    - Loop up to 50 attempts: check if rect (x, y, w+COLLISION_PAD, h+COLLISION_PAD) overlaps any box in self.boxes (excluding exclude_box if provided)
    - Overlap check: two rects overlap if not (right1 < left2 or left1 > right2 or bottom1 < top2 or top1 > bottom2), with COLLISION_PAD=5.0 added to both sides
    - If overlap: nudge x += 15.0; if x > 1200, reset x to original snapped x, nudge y += 15.0
    - Return (x, y)

    `_auto_position(self, box, near_box=None) -> None`:
    - If near_box: ideal_x = near_box.patching_rect[0], ideal_y = near_box.patching_rect[1] + near_box.patching_rect[3] + V_SPACING
    - If no near_box: ideal_x = center of self.props["rect"][2] / 2, ideal_y = center of self.props["rect"][3] / 2
    - Call _find_clear_position with ideal coords and box dimensions
    - Update box.patching_rect[0], [1] with result
    - Sync box._raw if present

    2. Add COLLISION_PAD = 5.0 constant near the top of patcher.py (with other constants or in the mutation section).

    3. Write tests FIRST (RED), then implement (GREEN):
    - TestAutoPosition class with ~8 tests: grid snap, collision nudge right, collision wrap to next row, near_box positioning, center positioning, exclude_box self-collision avoidance, no boxes (trivial case), padding respected
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_patcher.py -k "TestAutoPosition" -x -q</automated>
  </verify>
  <done>Auto-positioning finds clear positions with grid snap and collision avoidance. All position tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insert_into_connection() with tests</name>
  <files>src/maxpat/patcher.py, tests/test_patcher.py</files>
  <behavior>
    - insert_into_connection(source, dest, "mul~", args=["0.5"]) inserts *~ 0.5 between source and dest
    - Original connection(s) between source and dest are removed
    - New connections: source->new_box and new_box->dest are created for each original connection
    - ALL matching connections between source and dest are affected (stereo: outlet 0->inlet 0 AND outlet 1->inlet 1 both get insert)
    - One shared inserted object is wired to all matching connections (not separate objects per connection)
    - Inserted box is auto-positioned below source with V_SPACING gap, snapped to 15px grid
    - If inserted object has fewer inlets/outlets than connections found, wire as many connections as fit and return the rest as orphaned in EditResult (per CONTEXT.md: report mismatch, let caller decide -- NOT a hard fail)
    - Returns EditResult with new box and orphaned list (empty when all connections fit, populated when I/O capacity exceeded)
    - Raises ValueError only for structural errors: no connections exist between source and dest, or source/dest not in patcher
  </behavior>
  <action>
    1. Add insert_into_connection() method to Patcher class (after replace_box, in the mutation section):
    ```
    def insert_into_connection(
        self,
        source: Box,
        dest: Box,
        name: str,
        args: list[str] | None = None,
    ) -> EditResult:
    ```

    Implementation:
    - Find ALL patchlines where source_id == source.id AND dest_id == dest.id
    - If none found, raise ValueError("No connection found between {source.id} and {dest.id}")
    - Create new box via self.add_box(name, args=args)
    - Auto-position below source: call self._auto_position(new_box, near_box=source)
    - Check I/O capacity: compare len(matching) against new_box.numinlets and new_box.numoutlets
    - Wire connections that fit: for each connection at index i where i < numinlets and i < numoutlets, remove old connection, add source->new_box and new_box->dest connections
    - Collect connections that do NOT fit (i >= numinlets or i >= numoutlets) into the orphaned list as dicts with source_id, source_outlet, dest_id, dest_inlet -- these old connections are removed but NOT rewired through the new box
    - Return EditResult(box=new_box, orphaned=orphaned_list)

    Per CONTEXT.md locked decision: "If the inserted object has fewer inlets/outlets than connections being inserted into, report the mismatch and ask how to proceed rather than partial insert or hard fail." This means: wire what fits, return what does not fit as orphaned connections in EditResult so the caller has full information to decide next steps. This is consistent with how modify_box handles orphaned connections. Reserve ValueError for structural errors only (missing source/dest, no connections between them).

    Per CONTEXT.md locked decisions:
    - Insert affects ALL matching connections between source and dest
    - One shared inserted object, not separate objects per connection
    - Auto-position below source with standard spacing (not midpoint)

    2. Write tests FIRST (RED), then implement (GREEN):
    - TestInsertIntoConnection class with ~9 tests: basic single insert, stereo (2 connections) insert, position below source, grid snap, no connection raises ValueError, I/O mismatch returns EditResult with orphaned (NOT ValueError), args forwarded to new box, connections properly rewired (verify old removed, new present), orphaned list contains correct connection details for excess connections
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_patcher.py -k "TestInsertIntoConnection" -x -q</automated>
  </verify>
  <done>insert_into_connection splices a new box into all connections between source and dest. Wires as many connections as the inserted object can handle; excess connections returned as orphaned in EditResult for caller to decide. Positioned below source, grid-snapped. ValueError only for structural errors (no connections found). All tests green.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_patcher.py tests/test_round_trip.py -x -q
```
All existing tests still pass. New TestAutoPosition and TestInsertIntoConnection tests all green.
</verification>

<success_criteria>
- insert_into_connection splices new box into all connections between source and dest
- Stereo (multi-connection) insert works with one shared inserted object
- I/O mismatch returns EditResult with orphaned connections (not ValueError) per user decision
- ValueError reserved for structural errors only (no connections found, box not in patcher)
- Auto-positioning places new objects below source or at patcher center
- Collision detection nudges right then down until clear
- All positions snap to 15px grid
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/15-intelligent-editing/15-02-SUMMARY.md`
</output>
