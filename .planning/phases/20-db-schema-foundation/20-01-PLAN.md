---
phase: 20-db-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/package_info.json
  - .claude/max-objects/packages/ableton-dsp/objects.json
  - .claude/max-objects/packages/Mira/objects.json
  - .claude/max-objects/packages/jit.mo/objects.json
  - .claude/max-objects/packages/BEAP/objects.json
  - .claude/max-objects/packages/Vizzie/objects.json
  - .claude/max-objects/packages/objects.json
  - .claude/max-objects/jitter/objects.json
autonomous: true
requirements:
  - DBSI-01
  - DBSI-02
  - DBSI-05
  - DBSI-06

must_haves:
  truths:
    - "Package registry describes all known packages with required metadata fields"
    - "Every package object carries a 'package' field matching its source package name"
    - "Package objects live in per-package subdirectories under packages/"
    - "Core domain objects are unchanged (no package field added)"
    - "jit.mo.sin is migrated from jitter domain to jit.mo package"
    - "Empty placeholder directories exist for BEAP, Vizzie, and other known packages"
  artifacts:
    - path: ".claude/max-objects/package_info.json"
      provides: "Package registry with metadata for all known packages"
      contains: "ableton-dsp"
    - path: ".claude/max-objects/packages/ableton-dsp/objects.json"
      provides: "77 Ableton DSP objects with package tags"
      min_lines: 10
    - path: ".claude/max-objects/packages/Mira/objects.json"
      provides: "2 Mira objects with package tags"
      min_lines: 3
    - path: ".claude/max-objects/packages/jit.mo/objects.json"
      provides: "9 jit.mo objects with package tags"
      min_lines: 5
    - path: ".claude/max-objects/packages/BEAP/objects.json"
      provides: "Empty placeholder for Phase 21 extraction"
      contains: "{}"
    - path: ".claude/max-objects/packages/Vizzie/objects.json"
      provides: "Empty placeholder for Phase 21 extraction"
      contains: "{}"
  key_links:
    - from: ".claude/max-objects/packages/ableton-dsp/objects.json"
      to: ".claude/max-objects/package_info.json"
      via: "package field matches registry key"
      pattern: '"package": "ableton-dsp"'
---

<objective>
Migrate the monolithic packages/objects.json into per-package subdirectories, create the package registry, and prepare placeholder directories for future extraction.

Purpose: Establish the data foundation that ObjectDatabase API extensions (Plan 02) will load from. All JSON data must be in place before the code changes.
Output: Per-package subdirectories with tagged objects, package_info.json registry, empty placeholders for unextracted packages, monolithic file deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-db-schema-foundation/20-CONTEXT.md
@.planning/phases/20-db-schema-foundation/20-RESEARCH.md
@src/maxpat/db_lookup.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package_info.json registry and empty placeholder directories</name>
  <files>
    .claude/max-objects/package_info.json,
    .claude/max-objects/packages/BEAP/objects.json,
    .claude/max-objects/packages/Vizzie/objects.json,
    .claude/max-objects/packages/FluCoMa/objects.json,
    .claude/max-objects/packages/CNMAT/objects.json,
    .claude/max-objects/packages/Bach/objects.json,
    .claude/max-objects/packages/Odot/objects.json,
    .claude/max-objects/packages/ml-lib/objects.json,
    .claude/max-objects/packages/IRCAM Spat/objects.json,
    .claude/max-objects/packages/RNBO Guitar/objects.json,
    .claude/max-objects/packages/Cage/objects.json,
    .claude/max-objects/packages/Dada/objects.json,
    .claude/max-objects/packages/EARS/objects.json,
    .claude/max-objects/packages/Rhythmic Time Toolkit/objects.json
  </files>
  <read_first>
    .claude/max-objects/packages/objects.json,
    .planning/phases/20-db-schema-foundation/20-CONTEXT.md,
    .planning/phases/20-db-schema-foundation/20-RESEARCH.md
  </read_first>
  <action>
Create `.claude/max-objects/package_info.json` with the following exact content structure. Keys use MAX's exact Packages folder names per D-01. Each entry has `name`, `tier`, `prefix`, `version`, `install_method`, `description`, `object_count`, and `extracted` fields per D-02.

Bundled packages (tier: "bundled", install_method: "bundled"):
- `"ableton-dsp"`: prefix `"abl."`, version `"9.0"`, description `"Ableton audio effects and synthesizer building blocks"`, object_count 77, extracted true
- `"Mira"`: prefix `"mira."`, version `"1.3"`, description `"iPad remote control for MAX patches"`, object_count 2, extracted true
- `"jit.mo"`: prefix `"jit.mo."`, version `"9.0"`, description `"Jitter motion and animation utilities"`, object_count 9, extracted true
- `"BEAP"`: prefix `"bp."`, version `"1.0"`, description `"BPATCHER Electro-Acoustic Patching -- modular synthesis modules"`, object_count 0, extracted false
- `"Vizzie"`: prefix `"vz."`, version `"2.0"`, description `"Video processing and effects modules"`, object_count 0, extracted false

Community packages (tier: "community", install_method: "package_manager"):
- `"FluCoMa"`: prefix `"fluid."`, version `""`, description `"Fluid Corpus Manipulation -- audio decomposition and analysis"`, object_count 0, extracted false
- `"CNMAT"`: prefix `"cnmat."`, version `""`, description `"CNMAT Externals -- OSC, resonance models, descriptors"`, object_count 0, extracted false
- `"Bach"`: prefix `"bach."`, version `""`, description `"Computer-aided composition with musical notation"`, object_count 0, extracted false
- `"Odot"`: prefix `"o."`, version `""`, description `"OSC-based dynamic typing and expression language"`, object_count 0, extracted false
- `"ml-lib"`: prefix `"ml."`, version `""`, description `"Machine learning objects for MAX"`, object_count 0, extracted false

Licensed packages (tier: "licensed", install_method: "installer"):
- `"IRCAM Spat"`: prefix `"spat5."`, version `""`, description `"Spatial audio rendering and room acoustics"`, object_count 0, extracted false

Other bundled/community:
- `"RNBO Guitar"`: prefix `"rnbo.guitar."`, version `"9.0"`, install_method `"bundled"`, tier `"bundled"`, description `"RNBO-based guitar effects"`, object_count 0, extracted false
- `"Cage"`: prefix `"cage."`, version `""`, install_method `"package_manager"`, tier `"community"`, description `"Advanced computer-aided composition"`, object_count 0, extracted false
- `"Dada"`: prefix `"dada."`, version `""`, install_method `"package_manager"`, tier `"community"`, description `"Data-driven and dynamic approaches"`, object_count 0, extracted false
- `"EARS"`: prefix `"ears."`, version `""`, install_method `"package_manager"`, tier `"community"`, description `"Electroacoustic and audio research synthesis"`, object_count 0, extracted false
- `"Rhythmic Time Toolkit"`: prefix `"rtk."`, version `""`, install_method `"package_manager"`, tier `"community"`, description `"Rhythmic and time structure utilities"`, object_count 0, extracted false

Create empty `{}` placeholder `objects.json` files in per-package subdirectories for every unextracted package (BEAP, Vizzie, FluCoMa, CNMAT, Bach, Odot, ml-lib, IRCAM Spat, RNBO Guitar, Cage, Dada, EARS, Rhythmic Time Toolkit). Each file contains only `{}`.
  </action>
  <verify>
    <automated>python3 -c "
import json
from pathlib import Path

root = Path('.claude/max-objects')

# Check package_info.json exists and has required structure
info = json.loads((root / 'package_info.json').read_text())
assert len(info) >= 16, f'Expected >= 16 packages, got {len(info)}'
for key, entry in info.items():
    for field in ['name', 'tier', 'prefix', 'version', 'install_method', 'description', 'object_count', 'extracted']:
        assert field in entry, f'{key} missing field: {field}'
    assert entry['name'] == key, f'{key} name mismatch'

# Check ableton-dsp entry
assert info['ableton-dsp']['tier'] == 'bundled'
assert info['ableton-dsp']['prefix'] == 'abl.'
assert info['ableton-dsp']['object_count'] == 77

# Check placeholder dirs exist
for pkg in ['BEAP', 'Vizzie', 'FluCoMa', 'CNMAT', 'Bach']:
    p = root / 'packages' / pkg / 'objects.json'
    assert p.exists(), f'{p} not found'
    data = json.loads(p.read_text())
    assert data == {}, f'{p} should be empty dict'

print('PASS: package_info.json and placeholders valid')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.claude/max-objects/package_info.json` exists with 16+ entries
    - Each entry contains fields: name, tier, prefix, version, install_method, description, object_count, extracted
    - `info["ableton-dsp"]["prefix"]` equals `"abl."`
    - `info["BEAP"]["extracted"]` equals `false`
    - `packages/BEAP/objects.json` exists and contains `{}`
    - `packages/Vizzie/objects.json` exists and contains `{}`
    - `packages/FluCoMa/objects.json` exists and contains `{}`
    - All 13 unextracted package placeholder directories created
  </acceptance_criteria>
  <done>package_info.json exists with all 16+ known packages, empty placeholder subdirectories created for all unextracted packages</done>
</task>

<task type="auto">
  <name>Task 2: Split monolithic packages/objects.json into per-package subdirectories with package tags</name>
  <files>
    .claude/max-objects/packages/ableton-dsp/objects.json,
    .claude/max-objects/packages/Mira/objects.json,
    .claude/max-objects/packages/jit.mo/objects.json,
    .claude/max-objects/packages/objects.json,
    .claude/max-objects/jitter/objects.json
  </files>
  <read_first>
    .claude/max-objects/packages/objects.json,
    .claude/max-objects/jitter/objects.json,
    .planning/phases/20-db-schema-foundation/20-RESEARCH.md
  </read_first>
  <action>
Write a Python migration script and execute it. The script must:

1. Read `.claude/max-objects/packages/objects.json` (87 objects).
2. Read `.claude/max-objects/jitter/objects.json` and extract `jit.mo.sin`.
3. Allocate objects to packages per D-07 and research recommendations:
   - `ableton-dsp`: All 74 `abl.*` objects + `live.adsrui`, `live.adsr~`, `live.scope~` = 77 objects
   - `Mira`: `mira.motion`, `mira.multitouch` = 2 objects
   - `jit.mo`: `jit.mo.field`, `jit.mo.fieldmask`, `jit.mo.func`, `jit.mo.join`, `jit.mo.time` (from packages) + `jit.bang`, `jit.framecount`, `jit.line` (from packages) + `jit.mo.sin` (from jitter) = 9 objects
4. For each object, add `"package": "<package_name>"` field (per D-01 exact names: `"ableton-dsp"`, `"Mira"`, `"jit.mo"`). Do NOT add a package field to core objects (D-03).
5. For `jit.mo.sin`, also change `"domain": "Jitter"` to `"domain": "Packages"`.
6. Write each package's objects to `packages/<pkg>/objects.json` with sorted keys, 2-space indent.
7. Remove `jit.mo.sin` from `.claude/max-objects/jitter/objects.json` and rewrite that file.
8. Delete the monolithic `.claude/max-objects/packages/objects.json` file.

Verify totals: 77 + 2 + 9 = 88 objects total across all three package files.

After the script runs successfully, delete the script itself (it is one-time migration tooling, not production code per Rule #5).
  </action>
  <verify>
    <automated>python3 -c "
import json
from pathlib import Path

root = Path('.claude/max-objects')

# Monolithic file must be gone
assert not (root / 'packages' / 'objects.json').exists(), 'packages/objects.json still exists'

# Check ableton-dsp
abl = json.loads((root / 'packages' / 'ableton-dsp' / 'objects.json').read_text())
assert len(abl) == 77, f'ableton-dsp: expected 77, got {len(abl)}'
assert 'abl.device.autofilter~' in abl
assert 'live.adsrui' in abl
assert 'live.adsr~' in abl
assert 'live.scope~' in abl
for name, obj in abl.items():
    assert obj.get('package') == 'ableton-dsp', f'{name} missing package tag'

# Check Mira
mira = json.loads((root / 'packages' / 'Mira' / 'objects.json').read_text())
assert len(mira) == 2, f'Mira: expected 2, got {len(mira)}'
for name, obj in mira.items():
    assert obj.get('package') == 'Mira', f'{name} missing package tag'

# Check jit.mo
jitmo = json.loads((root / 'packages' / 'jit.mo' / 'objects.json').read_text())
assert len(jitmo) == 9, f'jit.mo: expected 9, got {len(jitmo)}'
assert 'jit.mo.sin' in jitmo, 'jit.mo.sin not migrated'
assert jitmo['jit.mo.sin']['domain'] == 'Packages', 'jit.mo.sin domain not updated'
for name, obj in jitmo.items():
    assert obj.get('package') == 'jit.mo', f'{name} missing package tag'

# Check jit.mo.sin removed from jitter
jitter = json.loads((root / 'jitter' / 'objects.json').read_text())
assert 'jit.mo.sin' not in jitter, 'jit.mo.sin still in jitter/objects.json'

# Total
total = len(abl) + len(mira) + len(jitmo)
assert total == 88, f'Total: expected 88, got {total}'

print('PASS: migration complete, 88 objects in 3 packages')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `packages/objects.json` (monolithic) no longer exists
    - `packages/ableton-dsp/objects.json` contains exactly 77 objects, all with `"package": "ableton-dsp"`
    - `packages/Mira/objects.json` contains exactly 2 objects, all with `"package": "Mira"`
    - `packages/jit.mo/objects.json` contains exactly 9 objects, all with `"package": "jit.mo"`
    - `jit.mo.sin` is NOT in `jitter/objects.json`
    - `jit.mo.sin` IS in `packages/jit.mo/objects.json` with `"domain": "Packages"`
    - No core domain files (max/, msp/, jitter/, etc.) have a `"package"` field added to any object
    - Total: 88 package objects across 3 subdirectories
  </acceptance_criteria>
  <done>Monolithic packages/objects.json deleted, 88 objects split across 3 per-package subdirectories with correct package tags, jit.mo.sin migrated from jitter domain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON files -> ObjectDatabase | Data files loaded and trusted by the application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | package_info.json | accept | JSON files are version-controlled, not user-editable at runtime |
| T-20-02 | Information Disclosure | package_info.json | accept | No secrets -- only package metadata (names, prefixes, versions) |
</threat_model>

<verification>
After both tasks complete:
1. `packages/objects.json` (monolithic) must not exist
2. `packages/ableton-dsp/objects.json` has 77 objects with `"package": "ableton-dsp"`
3. `packages/Mira/objects.json` has 2 objects with `"package": "Mira"`
4. `packages/jit.mo/objects.json` has 9 objects with `"package": "jit.mo"`
5. `package_info.json` has 16+ entries with all required fields
6. Empty placeholders exist for BEAP, Vizzie, and 11 community packages
7. `jit.mo.sin` removed from jitter domain
8. Existing tests still pass: `python -m pytest tests/test_object_schema.py tests/test_domain_classification.py -x -q`
</verification>

<success_criteria>
- 88 package objects tagged and split across per-package subdirectories
- package_info.json created with full registry
- Monolithic packages/objects.json deleted
- jit.mo.sin migrated from jitter to jit.mo
- Empty placeholders ready for Phase 21 extraction
- All existing tests pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/20-db-schema-foundation/20-01-SUMMARY.md`
</output>
