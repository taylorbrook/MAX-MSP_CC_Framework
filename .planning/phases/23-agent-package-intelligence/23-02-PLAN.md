---
phase: 23-agent-package-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/max-objects/PACKAGES.md
  - .claude/max-objects/relationships.json
  - tests/test_package_schema.py
autonomous: true
requirements: [PKG-15, PKG-16]
must_haves:
  truths:
    - "PACKAGES.md contains BEAP signal conventions (0-5V CV, +/-1 audio, 1V/oct pitch)"
    - "PACKAGES.md contains at least 3 BEAP canonical template chains with module lists and connection order"
    - "PACKAGES.md contains Vizzie templates with Jitter matrix conventions"
    - "PACKAGES.md contains functional role tables for BEAP and Vizzie"
    - "relationships.json has 15-25 new package pairs with 'package' field"
    - "Existing 19 core pairs in relationships.json are unchanged"
  artifacts:
    - path: ".claude/max-objects/PACKAGES.md"
      provides: "Shared package reference document for all agents"
      contains: "Signal Conventions"
    - path: ".claude/max-objects/relationships.json"
      provides: "Package relationship pairs alongside core pairs"
      contains: "\"package\":"
    - path: "tests/test_package_schema.py"
      provides: "Tests for PACKAGES.md existence and relationship package field"
      contains: "test_packages_md_exists"
  key_links:
    - from: ".claude/max-objects/PACKAGES.md"
      to: ".claude/max-objects/packages/BEAP/objects.json"
      via: "References BEAP module names that exist in DB"
      pattern: "bp\\."
    - from: ".claude/max-objects/relationships.json"
      to: ".claude/max-objects/packages/"
      via: "Package pairs reference objects that exist in DB"
      pattern: "\"package\":"
---

<objective>
Create the shared PACKAGES.md reference document with BEAP/Vizzie conventions and canonical templates, and add package relationship entries to relationships.json.

Purpose: Agents need a concise reference for package-specific conventions (signal levels, functional roles, template chains) so they generate idiomatic package patches. Relationship entries let agents suggest correct module companions.

Output: PACKAGES.md (150-250 lines), relationships.json with 15-25 package pairs, and new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-agent-package-intelligence/23-CONTEXT.md
@.planning/phases/23-agent-package-intelligence/23-RESEARCH.md

<interfaces>
<!-- Key data from DB that informs PACKAGES.md content -->

BEAP categories (22): Analysis, Effects, Envelope, Filter, Input, LFO, Level, MIDI, Misc, Mixers, Oscillator, Output, Quantizer, Random, Scope, Sequencer, Serialosc, Waveshapers

Vizzie categories (8): Control, Effect, Generate, Input, Mix-Composite, Output, Transform, Utility

BEAP signal conventions:
- All CV signals: 0 to +5V range
- Audio signals: +/-1 range (standard MSP)
- 1V/oct pitch tracking on CV1 inputs
- Gate signals: 0V (off) to +5V (on)

Vizzie I/O convention:
- Matrix and control types (NOT signal)
- Modules pass Jitter matrices between them

Key BEAP template modules:
- bp.Keyboard: 0 in / 4 out (pitch CV, gate, velocity, aftertouch)
- bp.Oscillator: 6 in / 2 out (CV1 1V/oct, CV2, CV3, FM, PWM, Sync)
- bp.VCA: 2 in / 1 out (signal, CV)
- bp.LPF: 5 in / 1 out (signal, CV freq, CV res, ...)
- bp.ADSR: 2 in / 1 out (gate, retrigger)
- bp.LFO: 0 in / 5 out (multiple waveforms)
- bp.Stereo: 2 in / 0 out
- bp.Mono: 1 in / 0 out

Key Vizzie template modules:
- vz.playr: 7 in / 2 out (video file player)
- vz.grabbr: 2 in / 1 out (camera input)
- vz.blurrr, vz.scramblr, vz.delayr (effects)
- vz.viewr: 1 in / 0 out (display)
- vz.projectr: 4 in / 1 out (fullscreen)
- vz.recordr: 3 in / 0 out (save)

Existing relationships.json format:
```json
{"objects": ["tapin~", "tapout~"], "relationship": "required_pair", "note": "..."}
```

New entries add "package" field:
```json
{"objects": ["bp.Keyboard", "bp.Oscillator"], "relationship": "cv_pair", "package": "BEAP", "note": "..."}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PACKAGES.md shared reference document</name>
  <files>.claude/max-objects/PACKAGES.md</files>
  <read_first>
    - .claude/max-objects/packages/BEAP/objects.json (scan categories, bpatcher_dimensions, signal_convention fields)
    - .claude/max-objects/packages/Vizzie/objects.json (scan categories, dimensions)
    - .claude/max-objects/package_info.json (package registry for tier/description)
    - .planning/phases/23-agent-package-intelligence/23-CONTEXT.md (D-01 through D-06)
    - .planning/phases/23-agent-package-intelligence/23-RESEARCH.md (Pattern 3, Example 4, Example 5)
  </read_first>
  <action>
Create `.claude/max-objects/PACKAGES.md` with the following structure (target 150-250 lines, per D-01):

**Section 1: Overview**
- Brief intro: this file is the shared reference for all agents when generating package patches
- Scope: bundled packages only (per D-02): BEAP, Vizzie, jit.mo, Jitter Geometry, Jitter Tools, ableton-dsp, Mira, maxforlive-elements
- Instruction: use `ObjectDatabase.lookup(name)` for individual object details; this file covers patterns and conventions

**Section 2: Signal Conventions (per D-03)**
- BEAP subsection:
  - CV signals: 0 to +5V range
  - Audio signals: +/-1 range (standard MSP)
  - 1V/oct pitch tracking on CV1 inputs
  - Gate signals: 0V (off) to +5V (on)
  - Always terminate chains with bp.Stereo or bp.Mono
  - Use bp.VCA for gain control (never connect oscillator directly to output)
- Vizzie subsection (per D-06):
  - All data flows as Jitter matrices between modules
  - Matrix inlets accept `jit_matrix` type
  - Control inlets accept int/float messages
  - No audio signal processing -- pure video/visual domain

**Section 3: Functional Roles (per D-05)**
- BEAP table with 5 roles:
  | Role | Categories | Key Modules |
  | Sources | Oscillator, Input, Noise, Random | bp.Oscillator, bp.Keyboard, bp.Noise, bp.FM |
  | Processors | Filter, Effects, Level, Waveshapers | bp.LPF, bp.VCA, bp.Chorus, bp.Feedback Delay |
  | Modulators | LFO, Envelope | bp.LFO, bp.ADSR, bp.AD, bp.Envelope Follower |
  | Output | Output | bp.Stereo, bp.Mono |
  | Utility | Scope, Misc, Sequencer, Mixers, MIDI, Analysis | bp.Scope, bp.Sequencer, bp.Audio Mixer |

- Vizzie table with 6 roles:
  | Role | Categories | Key Modules |
  | Sources | Input, Generate | vz.playr, vz.grabbr, vz.1easemappr |
  | Effects | Effect, Transform | vz.blurrr, vz.scramblr, vz.delayr |
  | Control | Control | vz.fadr, vz.slidr, vz.knobz |
  | Compositing | Mix-Composite | vz.chromakeyr, vz.mixxr |
  | Output | Output | vz.viewr, vz.projectr, vz.recordr |
  | Utility | Utility | vz.presettr, vz.timeliner |

**Section 4: Canonical Templates (per D-03, D-04, D-06)**
At least 5 BEAP templates:

1. **Subtractive Synthesizer**
   Modules: bp.Keyboard -> bp.Oscillator -> bp.LPF -> bp.VCA -> bp.Stereo
   Modulation: bp.ADSR -> bp.VCA (CV), bp.LFO -> bp.LPF (CV2)
   Connections (numbered with outlet/inlet indices):
   1. bp.Keyboard outlet 0 (pitch CV) -> bp.Oscillator inlet 0 (CV1)
   2. bp.Oscillator outlet 0 (signal) -> bp.LPF inlet 0 (signal input)
   3. bp.LPF outlet 0 -> bp.VCA inlet 0 (signal input)
   4. bp.VCA outlet 0 -> bp.Stereo inlet 0
   5. bp.Keyboard outlet 1 (gate) -> bp.ADSR inlet 0 (gate)
   6. bp.ADSR outlet 0 -> bp.VCA inlet 1 (CV)
   7. bp.LFO outlet 0 -> bp.LPF inlet 1 (CV2 cutoff modulation)

2. **FM Synthesizer**
   Modules: bp.Keyboard -> bp.FM -> bp.VCA -> bp.Stereo
   Modulation: bp.ADSR -> bp.VCA (CV)
   Connection list

3. **Sequenced Patch**
   Modules: bp.Sequencer -> bp.Oscillator -> bp.LPF -> bp.VCA -> bp.Stereo
   Modulation: bp.AD -> bp.VCA (CV), Clock from bp.Sequencer
   Connection list

4. **Audio Effect Chain**
   Modules: (audio input) -> bp.Chorus -> bp.Feedback Delay -> bp.Stereo
   Connection list (no keyboard, processes existing audio)

5. **Analysis/Visualization**
   Modules: (audio input) -> bp.Scope, bp.32-band analysis
   Connection list

At least 3 Vizzie templates:

1. **Video Effects Chain**
   Modules: vz.playr -> vz.blurrr -> vz.chromakeyr -> vz.viewr
   Connection list

2. **Live Camera Processing**
   Modules: vz.grabbr -> vz.scramblr -> vz.delayr -> vz.viewr
   Connection list

3. **VJ Setup (Multi-source)**
   Modules: vz.playr + vz.grabbr -> vz.mixxr -> vz.projectr
   Connection list

**Section 5: Bpatcher Conventions**
- BEAP modules are bpatchers that use `bp.` prefix
- Vizzie modules are bpatchers that use `vz.` prefix
- All BEAP/Vizzie modules have measured `bpatcher_dimensions` in DB -- use `add_bpatcher(object_name="bp.X")` for auto-sizing
- BEAP standard module height: 116px (most modules)
- Vizzie standard module height: varies more widely

**Section 6: Other Bundled Packages (brief)**
Brief notes on jit.mo, Jitter Geometry, Jitter Tools, ableton-dsp, Mira, maxforlive-elements -- these are NOT bpatcher-based, they are standard newobj objects. Reference their DB entries for details.
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
p = Path('.claude/max-objects/PACKAGES.md')
assert p.exists(), 'PACKAGES.md missing'
content = p.read_text()
lines = content.splitlines()
assert 100 < len(lines) < 400, f'Expected 100-400 lines, got {len(lines)}'
assert 'Signal Conventions' in content
assert '0-5V' in content or '0 to +5V' in content
assert 'bp.Oscillator' in content
assert 'bp.VCA' in content
assert 'bp.Stereo' in content
assert 'vz.playr' in content
assert 'vz.viewr' in content
assert 'Functional Roles' in content or 'Functional Role' in content
assert 'Subtractive' in content
print(f'PACKAGES.md: {len(lines)} lines, all checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/max-objects/PACKAGES.md` exists
    - File contains "Signal Conventions" heading
    - File contains "0-5V" or "0 to +5V" (BEAP CV convention)
    - File contains "+/-1" or "plus/minus 1" (audio convention)
    - File contains "Functional Roles" or "Functional Role"
    - File contains at least 5 occurrences of "bp.Oscillator" (templates reference it)
    - File contains at least 3 occurrences of "vz.playr" or "vz.viewr"
    - File contains "Subtractive" (first template name)
    - File is between 100 and 400 lines
  </acceptance_criteria>
  <done>PACKAGES.md exists with signal conventions, functional role tables, 5+ BEAP templates, 3+ Vizzie templates, bpatcher conventions, and other package notes.</done>
</task>

<task type="auto">
  <name>Task 2: Add package relationship entries and tests</name>
  <files>.claude/max-objects/relationships.json, tests/test_package_schema.py</files>
  <read_first>
    - .claude/max-objects/relationships.json (full file, 24 lines -- understand existing format)
    - .claude/max-objects/packages/BEAP/objects.json (verify module names for pairs)
    - .claude/max-objects/packages/Vizzie/objects.json (verify module names for pairs)
    - tests/test_package_schema.py (existing tests to understand patterns)
    - .planning/phases/23-agent-package-intelligence/23-CONTEXT.md (D-07, D-08)
  </read_first>
  <action>
**Part A -- relationships.json: Add 20-25 package pairs (per D-07, D-08)**

Read the existing relationships.json. Add new entries to the `"pairs"` array with the `"package"` field. Keep all 19 existing core pairs unchanged. Use these relationship types for packages:
- `signal_chain` -- audio signal flow order (BEAP)
- `cv_pair` -- CV modulation source/destination (BEAP)
- `matrix_chain` -- Jitter matrix flow (Vizzie)
- `required_pair` -- must be used together

BEAP pairs (15-18 entries):
```json
{"objects": ["bp.Keyboard", "bp.Oscillator"], "relationship": "cv_pair", "package": "BEAP", "note": "Keyboard pitch CV (outlet 0) to Oscillator CV1 (inlet 0) for 1V/oct tracking"}
{"objects": ["bp.Oscillator", "bp.VCA"], "relationship": "signal_chain", "package": "BEAP", "note": "Oscillator signal output to VCA signal input for volume control"}
{"objects": ["bp.Oscillator", "bp.LPF"], "relationship": "signal_chain", "package": "BEAP", "note": "Oscillator to low-pass filter for subtractive synthesis"}
{"objects": ["bp.LPF", "bp.VCA"], "relationship": "signal_chain", "package": "BEAP", "note": "Filter output to VCA for volume envelope"}
{"objects": ["bp.VCA", "bp.Stereo"], "relationship": "signal_chain", "package": "BEAP", "note": "VCA output to stereo output module"}
{"objects": ["bp.VCA", "bp.Mono"], "relationship": "signal_chain", "package": "BEAP", "note": "VCA output to mono output module"}
{"objects": ["bp.ADSR", "bp.VCA"], "relationship": "cv_pair", "package": "BEAP", "note": "ADSR envelope CV to VCA CV input for amplitude shaping"}
{"objects": ["bp.Keyboard", "bp.ADSR"], "relationship": "cv_pair", "package": "BEAP", "note": "Keyboard gate (outlet 1) to ADSR gate input (inlet 0)"}
{"objects": ["bp.LFO", "bp.LPF"], "relationship": "cv_pair", "package": "BEAP", "note": "LFO to filter cutoff CV for wobble/sweep effects"}
{"objects": ["bp.LFO", "bp.VCA"], "relationship": "cv_pair", "package": "BEAP", "note": "LFO to VCA CV for tremolo effect"}
{"objects": ["bp.LFO", "bp.Oscillator"], "relationship": "cv_pair", "package": "BEAP", "note": "LFO to oscillator CV2 for vibrato"}
{"objects": ["bp.Sequencer", "bp.Oscillator"], "relationship": "cv_pair", "package": "BEAP", "note": "Sequencer pitch output to oscillator CV1 for melodic sequences"}
{"objects": ["bp.FM", "bp.VCA"], "relationship": "signal_chain", "package": "BEAP", "note": "FM oscillator output to VCA for volume control"}
{"objects": ["bp.Keyboard", "bp.FM"], "relationship": "cv_pair", "package": "BEAP", "note": "Keyboard pitch CV to FM oscillator for played FM synthesis"}
{"objects": ["bp.Noise", "bp.LPF"], "relationship": "signal_chain", "package": "BEAP", "note": "Noise through filter for filtered noise sounds"}
{"objects": ["bp.Envelope Follower", "bp.VCA"], "relationship": "cv_pair", "package": "BEAP", "note": "Envelope follower CV output to VCA for dynamics following"}
```

Vizzie pairs (5-7 entries):
```json
{"objects": ["vz.playr", "vz.viewr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Video file player to display window"}
{"objects": ["vz.grabbr", "vz.viewr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Camera capture to display window"}
{"objects": ["vz.playr", "vz.blurrr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Video source to blur effect"}
{"objects": ["vz.playr", "vz.mixxr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Video source to mixer for compositing"}
{"objects": ["vz.grabbr", "vz.mixxr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Camera source to mixer for compositing"}
{"objects": ["vz.mixxr", "vz.viewr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Mixer composite output to display"}
{"objects": ["vz.mixxr", "vz.projectr"], "relationship": "matrix_chain", "package": "Vizzie", "note": "Mixer composite to fullscreen projector output"}
```

Write the full JSON with all original pairs plus new pairs to relationships.json.

**Part B -- tests/test_package_schema.py: Add relationship and PACKAGES.md tests**

Add two new test classes at the end of the file:

1. `TestPackageRelationships`:
   - `test_relationships_has_package_pairs`: Load relationships.json, count entries with `"package"` field, assert >= 15
   - `test_package_pairs_have_required_fields`: Every entry with `"package"` must also have `"objects"`, `"relationship"`, `"note"`
   - `test_core_pairs_unchanged`: Entries without `"package"` field must be >= 19 (original count)
   - `test_package_pair_objects_in_db`: For each package pair, both objects must exist in the DB (import ObjectDatabase, look up each)
   - `test_relationship_types_valid`: All `"relationship"` values must be one of: `required_pair`, `common_pair`, `equivalent`, `required_group`, `signal_chain`, `cv_pair`, `matrix_chain`

2. `TestPackagesReference`:
   - `test_packages_md_exists`: `.claude/max-objects/PACKAGES.md` must exist
   - `test_packages_md_has_signal_conventions`: Content must contain "Signal Conventions" and "0-5V" or "0 to +5V"
   - `test_packages_md_has_beap_templates`: Content must contain "Subtractive" and "bp.Oscillator"
   - `test_packages_md_has_vizzie_templates`: Content must contain "vz.playr" and "vz.viewr"
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_package_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/max-objects/relationships.json` contains `"package":` (new field on package entries)
    - `python3 -c "import json; d=json.load(open('.claude/max-objects/relationships.json')); pairs=d['pairs']; pkg=[p for p in pairs if 'package' in p]; print(len(pkg))"` outputs >= 15
    - `python3 -c "import json; d=json.load(open('.claude/max-objects/relationships.json')); pairs=d['pairs']; core=[p for p in pairs if 'package' not in p]; print(len(core))"` outputs >= 19
    - `tests/test_package_schema.py` contains `class TestPackageRelationships`
    - `tests/test_package_schema.py` contains `class TestPackagesReference`
    - `tests/test_package_schema.py` contains `test_packages_md_exists`
    - `python3 -m pytest tests/test_package_schema.py -x -q` exits 0
  </acceptance_criteria>
  <done>relationships.json has 15-25 package pairs with "package" field alongside unchanged core pairs. PACKAGES.md tests verify existence and key content. All schema tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PACKAGES.md -> agent context | Agents read this doc and follow its conventions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | I (Information Disclosure) | PACKAGES.md | accept | Contains only MAX/MSP technical conventions, no secrets |
| T-23-04 | T (Tampering) | relationships.json | accept | Version-controlled JSON, no user input; validated by tests |
</threat_model>

<verification>
```bash
python3 -m pytest tests/test_package_schema.py -x -q
python3 -c "from pathlib import Path; assert Path('.claude/max-objects/PACKAGES.md').exists()"
```
</verification>

<success_criteria>
- PACKAGES.md exists with BEAP signal conventions, functional roles, 5 BEAP templates, 3 Vizzie templates
- relationships.json has 15-25 package pairs with "package" field
- Original 19 core pairs unchanged
- All package schema tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-agent-package-intelligence/23-02-SUMMARY.md`
</output>
