---
phase: 04-agent-system-and-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/critics/__init__.py
  - src/maxpat/critics/base.py
  - src/maxpat/critics/dsp_critic.py
  - src/maxpat/critics/structure_critic.py
  - tests/test_critics.py
autonomous: true
requirements: [AGT-03, AGT-04, AGT-05]

must_haves:
  truths:
    - "review_patch() returns CriticResult findings from both DSP and structure critics"
    - "DSP critic catches gen~ I/O mismatches between codebox and wrapper box"
    - "DSP critic catches missing gain staging before dac~"
    - "DSP critic catches audio rate consistency issues"
    - "Structure critic catches fan-out without trigger objects"
    - "Structure critic catches hot/cold inlet ordering issues"
    - "Critics extend but do not duplicate existing validation.py checks"
  artifacts:
    - path: "src/maxpat/critics/__init__.py"
      provides: "review_patch() public API combining both critics"
      exports: ["review_patch", "CriticResult"]
    - path: "src/maxpat/critics/base.py"
      provides: "CriticResult dataclass with severity/finding/suggestion"
      exports: ["CriticResult"]
    - path: "src/maxpat/critics/dsp_critic.py"
      provides: "review_dsp() checking signal flow, gen~ I/O, gain staging"
      exports: ["review_dsp"]
    - path: "src/maxpat/critics/structure_critic.py"
      provides: "review_structure() checking fan-out, hot/cold ordering, subpatcher org"
      exports: ["review_structure"]
    - path: "tests/test_critics.py"
      provides: "Tests for both critics with specific patch fixtures"
  key_links:
    - from: "src/maxpat/critics/__init__.py"
      to: "src/maxpat/critics/dsp_critic.py"
      via: "import review_dsp"
      pattern: "from src.maxpat.critics.dsp_critic import review_dsp"
    - from: "src/maxpat/critics/__init__.py"
      to: "src/maxpat/critics/structure_critic.py"
      via: "import review_structure"
      pattern: "from src.maxpat.critics.structure_critic import review_structure"
    - from: "src/maxpat/critics/dsp_critic.py"
      to: "src/maxpat/codegen.py"
      via: "parse_genexpr_io for gen~ I/O matching"
      pattern: "from src.maxpat.codegen import parse_genexpr_io"
---

<objective>
Build the critic system -- Python modules that perform semantic/architectural review of generated patches, extending the existing validation pipeline.

Purpose: Critics catch design problems a MAX expert would notice (missing gain staging, gen~ I/O mismatches, fan-out without trigger) that the mechanical validation pipeline does not detect. They are the "knowledgeable colleague reviewing your patch" per user decision.

Output: `src/maxpat/critics/` package with DSP and structure critics, plus comprehensive tests.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/maxpat/__init__.py
@src/maxpat/validation.py
@src/maxpat/codegen.py
@src/maxpat/patcher.py
@src/maxpat/db_lookup.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/maxpat/validation.py:
```python
class ValidationResult:
    __slots__ = ("layer", "level", "message", "auto_fixed")
    def __init__(self, layer: str, level: str, message: str, auto_fixed: bool = False): ...

def validate_patch(patch, db: ObjectDatabase | None = None) -> list[ValidationResult]: ...
def has_blocking_errors(results: list[ValidationResult]) -> bool: ...
```

From src/maxpat/codegen.py:
```python
def parse_genexpr_io(code: str) -> tuple[int, int]:
    """Return (num_inputs, num_outputs) from GenExpr code."""
```

From src/maxpat/__init__.py:
```python
def generate_patch(patcher: Patcher) -> tuple[dict, list[ValidationResult]]: ...
```

.maxpat dict structure (patch_dict):
```python
{
    "patcher": {
        "boxes": [{"box": {"id": "obj-1", "maxclass": "cycle~", "text": "cycle~ 440", "numinlets": 2, "numoutlets": 1, ...}}, ...],
        "lines": [{"patchline": {"source": ["obj-1", 0], "destination": ["obj-2", 0]}}, ...]
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create critic base and DSP critic</name>
  <files>src/maxpat/critics/__init__.py, src/maxpat/critics/base.py, src/maxpat/critics/dsp_critic.py, tests/test_critics.py</files>
  <behavior>
    - CriticResult has severity ("blocker", "warning", "note"), finding (str), suggestion (str)
    - review_dsp(patch_dict, code_context=None) returns list[CriticResult]
    - gen~ I/O mismatch: when GenExpr code has 3 inputs but gen~ box has 2 inlets -> blocker
    - gen~ I/O mismatch: when GenExpr outputs don't match gen~ box outlets -> blocker
    - Missing gain staging: oscillator (cycle~, saw~, rect~, tri~, noise~, pink~) connected directly to dac~/ezdac~ without *~ or gain~ in between -> warning
    - Audio rate consistency: control-rate object (non-~ object) connected to a signal inlet where a signal source is expected -> warning
    - review_patch(patch_dict, code_context=None) returns combined results from both critics
  </behavior>
  <action>
    Create `src/maxpat/critics/` directory with `__init__.py`, `base.py`, and `dsp_critic.py`.

    `base.py`: Define CriticResult dataclass (or simple class with __slots__) with severity, finding, suggestion fields. Include __repr__ for readable output.

    `dsp_critic.py`: Implement `review_dsp(patch_dict: dict, code_context: dict | None = None) -> list[CriticResult]` with these checks:
    1. `_check_gen_io_match`: Find gen~ boxes in patch, use `parse_genexpr_io` from codegen to count I/O in GenExpr code, compare with box numinlets/numoutlets. Return blocker if mismatch.
    2. `_check_gain_staging`: Build adjacency graph from patchlines. BFS from oscillator objects (cycle~, saw~, rect~, tri~, noise~, pink~) to dac~/ezdac~. If path exists without passing through *~ or gain~, return warning.
    3. `_check_audio_rate_consistency`: For each patchline, check if source is non-signal object (no ~ suffix in text) connecting to a signal inlet on destination. Use box text to determine signal vs control. Return warning for mismatches.

    NOTE: Do NOT duplicate checks already in validation.py Layer 4 (unterminated signal chains, feedback loops). Focus on semantic issues validation misses.

    `__init__.py`: Import review_dsp, review_structure (stub for now -- import guarded), CriticResult. Define `review_patch()` that calls both critics and returns combined list.

    Write tests in `tests/test_critics.py` FIRST (TDD). Build minimal patch_dict fixtures:
    - Fixture with gen~ box + GenExpr code with mismatched I/O counts
    - Fixture with cycle~ directly connected to dac~ (no gain staging)
    - Fixture with proper gain staging (cycle~ -> *~ 0.5 -> dac~) -- should return no gain staging warnings
    - Fixture with control-to-signal connection

    Run tests (RED), then implement, then run tests (GREEN).
  </action>
  <verify>
    <automated>python -m pytest tests/test_critics.py -x -q</automated>
  </verify>
  <done>review_dsp catches gen~ I/O mismatches (blocker), missing gain staging (warning), and audio rate consistency issues (warning). Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create structure critic</name>
  <files>src/maxpat/critics/structure_critic.py, src/maxpat/critics/__init__.py, tests/test_critics.py</files>
  <behavior>
    - review_structure(patch_dict) returns list[CriticResult]
    - Fan-out without trigger: outlet connected to 2+ destinations without being a trigger/t object -> warning
    - Hot/cold inlet ordering: when multiple patchlines target different inlets of the same object, check if cold inlets (index > 0) are fed from the same timing source as hot inlet (index 0) without trigger for ordering -> warning
    - Missing trigger for multi-destination: bang/message connected to 2+ objects without explicit trigger -> warning
    - review_patch combines both DSP and structure critics
  </behavior>
  <action>
    Implement `src/maxpat/critics/structure_critic.py` with `review_structure(patch_dict: dict) -> list[CriticResult]`:

    1. `_check_fan_out_without_trigger`: Count connections per (source_id, outlet_index). If any source has 2+ destinations and source is NOT a trigger/t object, return warning with suggestion to use trigger object.

    2. `_check_hot_cold_ordering`: For each destination box that receives connections to multiple inlets, check if the source sending to inlet 0 (hot) comes from a trigger object that also feeds the cold inlets. If not (i.e., separate sources feed hot and cold inlets of the same box without trigger-based ordering), return warning. Skip signal (~) connections (all hot in audio domain per Rule #3).

    3. `_check_redundant_connections`: Detect duplicate patchlines (same source -> same destination). Return warning.

    Update `src/maxpat/critics/__init__.py` to import review_structure properly (remove stub guard). Ensure review_patch() calls both review_dsp and review_structure.

    Add tests to `tests/test_critics.py`:
    - Fixture with one outlet connected to 3 inlets (no trigger) -> warning
    - Fixture with trigger object fanning out -> no fan-out warning
    - Fixture with hot/cold ordering issue -> warning
    - Fixture with duplicate patchlines -> warning
    - Fixture with clean patch (no structure issues) -> empty results
    - Test review_patch() combines results from both critics
  </action>
  <verify>
    <automated>python -m pytest tests/test_critics.py -x -q</automated>
  </verify>
  <done>review_structure catches fan-out without trigger, hot/cold ordering issues, and redundant connections. review_patch combines both DSP and structure critics. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_critics.py -v` -- all critic tests pass
- `python -m pytest tests/ -x -q` -- existing tests still pass (no regressions)
- `from src.maxpat.critics import review_patch, CriticResult` imports cleanly
</verification>

<success_criteria>
- review_patch() returns combined CriticResult list from DSP and structure critics
- DSP critic catches gen~ I/O mismatches, missing gain staging, audio rate issues
- Structure critic catches fan-out without trigger, hot/cold ordering, duplicate connections
- All tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-system-and-orchestration/04-01-SUMMARY.md`
</output>
