---
phase: 04-agent-system-and-orchestration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/project.py
  - src/maxpat/testing.py
  - tests/test_project.py
  - tests/test_testing.py
autonomous: true
requirements: [FRM-01, FRM-03, FRM-06]

must_haves:
  truths:
    - "Creating a project creates the full directory structure under patches/"
    - "Each project has its own context.md, status.md, .max-memory/, generated/, test-results/"
    - "Active project is tracked in patches/.active-project.json"
    - "Project status tracks stage (ideation, discuss, research, build, verify) and progress"
    - "Test protocol generates numbered checklist with actions and expected results"
    - "Test results can be stored per project"
  artifacts:
    - path: "src/maxpat/project.py"
      provides: "Project creation, active tracking, status read/write, switching"
      exports: ["create_project", "get_active_project", "set_active_project", "read_status", "update_status"]
    - path: "src/maxpat/testing.py"
      provides: "Manual test checklist generation from patch structure"
      exports: ["generate_test_checklist", "save_test_results"]
    - path: "tests/test_project.py"
      provides: "Tests for project CRUD and status tracking"
    - path: "tests/test_testing.py"
      provides: "Tests for test checklist generation"
  key_links:
    - from: "src/maxpat/project.py"
      to: "patches/.active-project.json"
      via: "json read/write"
      pattern: "active-project.json"
    - from: "src/maxpat/project.py"
      to: "patches/{name}/"
      via: "directory creation"
      pattern: "Path.*patches"
    - from: "src/maxpat/testing.py"
      to: "patches/{name}/test-results/"
      via: "markdown file write"
      pattern: "test-results"
---

<objective>
Build project lifecycle infrastructure -- directory isolation, status tracking, and manual test protocol generation.

Purpose: Each MAX project lives in its own directory with independent context, state, and generated files. The test protocol bridges the gap between automated validation and manual MAX testing by generating step-by-step checklists.

Output: `src/maxpat/project.py` for project management, `src/maxpat/testing.py` for test checklist generation, plus tests.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-agent-system-and-orchestration/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project creation and active project tracking</name>
  <files>src/maxpat/project.py, tests/test_project.py</files>
  <behavior>
    - create_project("my-synth", base_dir) creates patches/my-synth/ with context.md, status.md, .max-memory/patterns.md, generated/, test-results/
    - create_project validates name: lowercase alphanumeric + hyphens only, rejects spaces/uppercase/special chars
    - create_project raises ValueError if project already exists
    - set_active_project("my-synth", base_dir) writes {"name": "my-synth", "activated": "ISO-date"} to patches/.active-project.json
    - get_active_project(base_dir) reads .active-project.json and returns dict, or None if no active project
    - get_active_project returns None if the project dir referenced by .active-project.json doesn't exist (desync detection)
    - status.md initialized with: stage=ideation, progress=empty, created=ISO-date
    - read_status(project_dir) parses status.md and returns dict with stage, progress, created fields
    - update_status(project_dir, stage=None, progress=None) updates specific fields in status.md
    - list_projects(base_dir) returns list of project names (directories under patches/ excluding hidden files)
  </behavior>
  <action>
    Write tests FIRST in `tests/test_project.py`:
    - Test create_project creates all expected directories and files
    - Test create_project with invalid name raises ValueError
    - Test create_project with existing name raises ValueError
    - Test set_active_project writes correct JSON
    - Test get_active_project reads JSON and returns dict
    - Test get_active_project returns None when no .active-project.json
    - Test get_active_project returns None when referenced project dir missing (desync)
    - Test read_status returns initialized fields
    - Test update_status changes stage field
    - Test list_projects returns directory names
    - Use tmp_path fixture for all operations

    Then implement `src/maxpat/project.py`:

    `create_project(name: str, base_dir: Path) -> Path`:
    - Validate name with regex `^[a-z0-9]+(-[a-z0-9]+)*$`
    - Create directory tree: `{base_dir}/patches/{name}/` with subdirs
    - Initialize context.md (empty template), status.md (stage: ideation), .max-memory/patterns.md (empty), generated/, test-results/
    - Return project path

    `set_active_project(name: str, base_dir: Path) -> None`:
    - Write JSON to `{base_dir}/patches/.active-project.json`

    `get_active_project(base_dir: Path) -> dict | None`:
    - Read JSON, validate project dir exists, return dict or None

    `read_status(project_dir: Path) -> dict`:
    - Parse status.md (YAML frontmatter or simple key-value markdown)

    `update_status(project_dir: Path, **kwargs) -> None`:
    - Read, update fields, write back

    `list_projects(base_dir: Path) -> list[str]`:
    - List directories in patches/ excluding dotfiles
  </action>
  <verify>
    <automated>python -m pytest tests/test_project.py -x -q</automated>
  </verify>
  <done>Project creation, active project tracking, status read/write, and listing all work correctly. Desync detection catches missing project directories. Tests pass with tmp_path isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manual test checklist generation</name>
  <files>src/maxpat/testing.py, tests/test_testing.py</files>
  <behavior>
    - generate_test_checklist(patch_dict, patch_name) returns markdown string with numbered test steps
    - Each test has: number, descriptive name, action (what to do in MAX), expected result
    - Checklist includes setup section (open file in MAX, turn audio on if audio objects present)
    - If patch has dac~/ezdac~: generates audio output test
    - If patch has notein/midiin: generates MIDI input test
    - If patch has toggle/button: generates UI interaction test
    - If patch has gen~: generates gen~ processing test
    - save_test_results(project_dir, test_name, results_md) writes to test-results/ directory
  </behavior>
  <action>
    Write tests FIRST in `tests/test_testing.py`:
    - Test checklist for patch with cycle~ -> *~ -> dac~ includes audio output test
    - Test checklist for patch with notein includes MIDI test
    - Test checklist for patch with toggle includes UI test
    - Test checklist includes setup section with file path and audio status
    - Test checklist format has numbered tests with Action/Expected markers
    - Test save_test_results writes file to correct location
    - Test generate for empty patch returns minimal checklist (just open in MAX)

    Then implement `src/maxpat/testing.py`:

    `generate_test_checklist(patch_dict: dict, patch_name: str, patch_path: str = "") -> str`:
    - Scan boxes in patch_dict for object types
    - Build setup section: open file in MAX, audio on if signal objects present
    - For each detected object type, add relevant test step:
      - dac~/ezdac~: "Toggle audio on. Expected: Sound from speakers"
      - notein/midiin: "Play MIDI note. Expected: Pitch change / note display"
      - toggle/button: "Click the toggle/button. Expected: State change / downstream trigger"
      - gen~: "Enable audio. Expected: gen~ processes signal correctly"
      - number/flonum: "Double-click number box and type value. Expected: Value propagates"
      - metro: "Turn on metro. Expected: Regular bangs at specified interval"
    - Format as numbered markdown with [ ] Pass [ ] Fail checkboxes
    - Return complete markdown string

    `save_test_results(project_dir: Path, test_name: str, results_md: str) -> Path`:
    - Write to `{project_dir}/test-results/{test_name}.md`
    - Return file path
  </action>
  <verify>
    <automated>python -m pytest tests/test_testing.py -x -q</automated>
  </verify>
  <done>generate_test_checklist produces numbered step-by-step test checklists based on patch contents. save_test_results stores results in project directory. Tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_project.py tests/test_testing.py -v` -- all tests pass
- `python -m pytest tests/ -x -q` -- no regressions
- `from src.maxpat.project import create_project, get_active_project` imports cleanly
- `from src.maxpat.testing import generate_test_checklist` imports cleanly
</verification>

<success_criteria>
- Project directories created with full structure (context.md, status.md, .max-memory/, generated/, test-results/)
- Active project tracking works with desync detection
- Status tracking supports stage transitions and progress updates
- Test checklist generation produces specific, actionable steps based on patch contents
- All tests pass with filesystem isolation
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-system-and-orchestration/04-03-SUMMARY.md`
</output>
