---
phase: 04-agent-system-and-orchestration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/memory.py
  - tests/test_memory.py
autonomous: true
requirements: [AGT-06, AGT-07]

must_haves:
  truths:
    - "Memory entries can be written to and read from project-specific markdown files"
    - "Memory entries can be written to and read from global markdown files"
    - "Writing a duplicate pattern does not create a second entry"
    - "Memory entries include date, context, and actionable rule"
    - "Entries can be listed, read, and deleted"
    - "Domain-filtered reading returns only relevant entries"
  artifacts:
    - path: "src/maxpat/memory.py"
      provides: "Memory read/write/dedup/delete/list operations for both scopes"
      exports: ["MemoryStore", "MemoryEntry"]
    - path: "tests/test_memory.py"
      provides: "Tests for memory CRUD and deduplication"
  key_links:
    - from: "src/maxpat/memory.py"
      to: "~/.claude/max-memory/"
      via: "pathlib file read/write"
      pattern: "Path.*max-memory"
    - from: "src/maxpat/memory.py"
      to: "patches/{project}/.max-memory/"
      via: "pathlib file read/write"
      pattern: "Path.*max-memory"
---

<objective>
Build the persistent memory system that stores learned patterns across sessions and projects.

Purpose: Agents accumulate expertise -- preferences like "user prefers line~ for gain control" or "always include meter~ on output" -- stored as structured markdown, auto-injected at task start, and deduplicated on write-back.

Output: `src/maxpat/memory.py` module with MemoryStore class and comprehensive tests.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-agent-system-and-orchestration/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryEntry and MemoryStore with read/write/list</name>
  <files>src/maxpat/memory.py, tests/test_memory.py</files>
  <behavior>
    - MemoryEntry has fields: pattern (str), domain (str), observed (date str), context (str), rule (str)
    - MemoryStore(scope="global") reads/writes to ~/.claude/max-memory/{domain}/patterns.md
    - MemoryStore(scope="project", project_dir=path) reads/writes to {project_dir}/.max-memory/patterns.md
    - store.write(entry) appends a structured markdown section to the appropriate file
    - store.read(domain=None) returns list[MemoryEntry] from file(s), optionally filtered by domain
    - store.list_domains() returns list of domains with entries
    - store.delete(pattern_name, domain) removes a specific entry
    - Files created automatically if they don't exist
    - Markdown format: ## Pattern: {name}\n- **Observed:** {date}\n- **Domain:** {domain}\n- **Context:** {context}\n- **Rule:** {rule}
  </behavior>
  <action>
    Write tests FIRST in `tests/test_memory.py`:
    - Test global store write creates file and appends entry
    - Test project store write creates file in project dir
    - Test read returns written entries as MemoryEntry objects
    - Test read with domain filter returns only matching domain
    - Test list_domains returns unique domains
    - Test delete removes specific entry by pattern name
    - Test write to non-existent directory creates it
    - Use tmp_path fixture for all file operations (no real filesystem side effects)

    Then implement `src/maxpat/memory.py`:

    `MemoryEntry`: Simple dataclass with pattern, domain, observed, context, rule fields.

    `MemoryStore`:
    - `__init__(self, scope: str = "global", project_dir: Path | None = None)`: Set base path. Global = `~/.claude/max-memory/`, project = `{project_dir}/.max-memory/`.
    - `_file_path(self, domain: str) -> Path`: Returns `{base}/{domain}/patterns.md` for global, `{base}/patterns.md` for project (project memory is flat, one file).
    - `write(self, entry: MemoryEntry) -> bool`: Append entry as markdown section. Create dirs/file if needed. Return True if written, False if deduped (Task 2).
    - `read(self, domain: str | None = None) -> list[MemoryEntry]`: Parse markdown file(s) into MemoryEntry list. Filter by domain if specified.
    - `list_domains(self) -> list[str]`: Return unique domain names from entries.
    - `delete(self, pattern: str, domain: str | None = None) -> bool`: Remove entry by pattern name. Return True if found and removed.

    Markdown parsing: Split file by `## Pattern:` headers, extract fields from `- **Key:** value` lines.
  </action>
  <verify>
    <automated>python -m pytest tests/test_memory.py -x -q</automated>
  </verify>
  <done>MemoryStore reads, writes, lists, and deletes memory entries in structured markdown format at both global and project scopes. Tests pass with tmp_path isolation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Memory deduplication on write</name>
  <files>src/maxpat/memory.py, tests/test_memory.py</files>
  <behavior>
    - Writing an entry with same pattern name and domain as existing entry skips the write (returns False)
    - Writing an entry with same domain but different pattern name succeeds
    - Writing an entry with same pattern name but different domain succeeds (different scope)
    - Dedup is by domain + pattern name (case-insensitive), not exact text matching
    - store.write returns False when entry is a duplicate, True when written
  </behavior>
  <action>
    Add dedup tests to `tests/test_memory.py`:
    - Write entry A (domain=dsp, pattern="prefer line~ for gain"), write entry A again -> second write returns False, file has only one entry
    - Write entry A (domain=dsp), write entry B (domain=dsp, different pattern) -> both exist
    - Write entry A (domain=dsp), write entry A (domain=ui) -> both exist (different domain)
    - Case insensitive: write "Prefer Line~", then "prefer line~" -> dedup triggers
    - After dedup, read() returns only unique entries

    Update `MemoryStore.write()`:
    1. Before appending, call `self.read(domain=entry.domain)`
    2. Check if any existing entry has same pattern name (case-insensitive)
    3. If duplicate found, return False without writing
    4. If genuinely new, append and return True
  </action>
  <verify>
    <automated>python -m pytest tests/test_memory.py -x -q</automated>
  </verify>
  <done>Memory deduplication prevents duplicate entries by domain + pattern name. Dedup is case-insensitive. write() returns False for duplicates, True for new entries. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_memory.py -v` -- all memory tests pass
- `python -m pytest tests/ -x -q` -- no regressions
- `from src.maxpat.memory import MemoryStore, MemoryEntry` imports cleanly
</verification>

<success_criteria>
- MemoryStore supports both global (~/.claude/max-memory/) and project ({dir}/.max-memory/) scopes
- Read, write, list, and delete operations work correctly
- Deduplication prevents duplicate entries by domain + pattern name
- All entries stored as structured markdown with date, context, and rule
- Tests pass with filesystem isolation (tmp_path)
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-system-and-orchestration/04-02-SUMMARY.md`
</output>
