---
phase: 04-agent-system-and-orchestration
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - .claude/skills/max-critic/SKILL.md
  - .claude/skills/max-critic/references/critic-protocol.md
  - .claude/skills/max-memory-agent/SKILL.md
  - .claude/skills/max-memory-agent/BOUNDARIES.md
  - .claude/skills/max-lifecycle/SKILL.md
  - .claude/skills/max-lifecycle/references/project-structure.md
  - .claude/skills/max-lifecycle/references/status-tracking.md
  - .claude/skills/max-lifecycle/references/test-protocol.md
  - tests/test_agent_skills.py
autonomous: true
requirements: [AGT-01, AGT-03]

must_haves:
  truths:
    - "Critic skill orchestrates the generate-review-revise loop using Python critic modules"
    - "Critic loop runs until clean with no hard round limit -- escalation triggers only for repeated identical findings"
    - "Memory agent handles read/write operations across both scopes"
    - "Lifecycle agent manages project creation, status, and switching"
    - "Validation tests confirm all 10 skill directories have correct structure"
  artifacts:
    - path: ".claude/skills/max-critic/SKILL.md"
      provides: "Critic orchestrator with loop protocol"
      min_lines: 30
    - path: ".claude/skills/max-critic/references/critic-protocol.md"
      provides: "Detailed loop steps, revision tracking, escalation rules"
      min_lines: 20
    - path: ".claude/skills/max-memory-agent/SKILL.md"
      provides: "Memory operations agent for read/write/dedup"
      min_lines: 25
    - path: ".claude/skills/max-lifecycle/SKILL.md"
      provides: "Project lifecycle management"
      min_lines: 25
    - path: "tests/test_agent_skills.py"
      provides: "Tests verifying skill file structure and required content"
  key_links:
    - from: ".claude/skills/max-critic/SKILL.md"
      to: "src/maxpat/critics/"
      via: "references Python critic modules"
      pattern: "src/maxpat/critics"
    - from: ".claude/skills/max-memory-agent/SKILL.md"
      to: "src/maxpat/memory.py"
      via: "references memory module"
      pattern: "src/maxpat/memory"
    - from: ".claude/skills/max-lifecycle/SKILL.md"
      to: "src/maxpat/project.py"
      via: "references project module"
      pattern: "src/maxpat/project"
---

<objective>
Create the critic, memory, and lifecycle agent skill definitions plus validation tests for all 10 agents.

Purpose: These three agents handle quality assurance (critic), accumulated expertise (memory), and project management (lifecycle). The validation tests confirm that all 10 skill directories (7 from Plan 04 + 3 from this plan) have the correct structure and content.

Output: 3 skill directories under `.claude/skills/` plus `tests/test_agent_skills.py`.
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-agent-system-and-orchestration/04-CONTEXT.md
@.planning/phases/04-agent-system-and-orchestration/04-RESEARCH.md

<interfaces>
<!-- Python modules these agents reference (from Plans 01-03) -->

From src/maxpat/critics/__init__.py:
```python
from src.maxpat.critics import review_patch, CriticResult
def review_patch(patch_dict: dict, code_context: dict | None = None) -> list[CriticResult]: ...
```

From src/maxpat/memory.py:
```python
from src.maxpat.memory import MemoryStore, MemoryEntry
class MemoryStore:
    def __init__(self, scope: str = "global", project_dir: Path | None = None): ...
    def write(self, entry: MemoryEntry) -> bool: ...
    def read(self, domain: str | None = None) -> list[MemoryEntry]: ...
    def list_domains(self) -> list[str]: ...
    def delete(self, pattern: str, domain: str | None = None) -> bool: ...
```

From src/maxpat/project.py:
```python
from src.maxpat.project import create_project, get_active_project, set_active_project, read_status, update_status, list_projects
```

From src/maxpat/testing.py:
```python
from src.maxpat.testing import generate_test_checklist, save_test_results
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Critic, memory, and lifecycle agent skills</name>
  <files>.claude/skills/max-critic/SKILL.md, .claude/skills/max-critic/references/critic-protocol.md, .claude/skills/max-memory-agent/SKILL.md, .claude/skills/max-memory-agent/BOUNDARIES.md, .claude/skills/max-lifecycle/SKILL.md, .claude/skills/max-lifecycle/references/project-structure.md, .claude/skills/max-lifecycle/references/status-tracking.md, .claude/skills/max-lifecycle/references/test-protocol.md</files>
  <action>
    **max-critic/SKILL.md:**
    - References critic-protocol.md for the generate-review-revise loop
    - Invokes `review_patch()` from src.maxpat.critics (Python, deterministic)
    - Reads CriticResult list, formats findings for generator
    - If blockers found: request revision with specific findings
    - If only warnings/notes: annotate as comment objects in .maxpat or code comments
    - Loop runs until clean (no blockers) per user decision -- there is NO hard round limit
    - critic-protocol.md: detailed loop steps, revision tracking. Escalation to user triggers ONLY when the same identical finding persists across 5 consecutive revisions (indicating the generator cannot resolve it), NOT as a general round cap. Document this distinction clearly.

    **max-memory-agent/SKILL.md:**
    - Uses: MemoryStore, MemoryEntry from src.maxpat.memory
    - Capabilities: Read memory (both scopes), write new patterns, deduplicate, delete entries
    - Auto write-back protocol: After successful generation, review output, identify patterns worth storing, check existing entries, write genuinely new ones
    - Auto-inject protocol: Before generation, load project memory (always), load global memory filtered by domain relevance
    - BOUNDARIES.md: Does NOT generate patches/code, does NOT make architectural decisions

    **max-lifecycle/SKILL.md:**
    - Uses: create_project, get_active_project, set_active_project, read_status, update_status, list_projects from src.maxpat.project
    - Uses: generate_test_checklist, save_test_results from src.maxpat.testing
    - Capabilities: Project creation with conversational kickoff, status tracking, project switching, test protocol execution
    - References:
      - project-structure.md: Standard directory layout (context.md, status.md, .max-memory/, generated/, test-results/)
      - status-tracking.md: Stage definitions (ideation, discuss, research, build, verify), progress format
      - test-protocol.md: How /max:test generates checklists, how results are recorded
  </action>
  <verify>
    <automated>for f in .claude/skills/max-critic/SKILL.md .claude/skills/max-memory-agent/SKILL.md .claude/skills/max-lifecycle/SKILL.md; do test -f "$f" && grep -q "^name:" "$f" && grep -q "^description:" "$f" || echo "FAIL: $f missing or lacks frontmatter"; done && echo "All 3 agent skills verified"</automated>
  </verify>
  <done>Critic, memory, and lifecycle agents created. Critic protocol explicitly documents that escalation is for repeated identical findings only, not a general round limit per user decision. Memory agent has auto write-back and auto-inject protocols. Lifecycle agent references all 3 reference files.</done>
</task>

<task type="auto">
  <name>Task 2: Skill validation tests for all 10 agents</name>
  <files>tests/test_agent_skills.py</files>
  <action>
    Create `tests/test_agent_skills.py` with tests verifying skill file structure (not behavior -- skills are prompts, not code):
    - Test all 10 skill directories exist under .claude/skills/ (max-router, max-patch-agent, max-dsp-agent, max-rnbo-agent, max-js-agent, max-ext-agent, max-ui-agent, max-critic, max-memory-agent, max-lifecycle)
    - Test each SKILL.md has frontmatter with name and description
    - Test specialist agents reference correct object database files (e.g., DSP agent references msp/ and gen/)
    - Test UI agent SKILL.md exists and references presentation mode
    - Test RNBO and Externals stubs contain Phase 5 deferral messages
    - Test router references dispatch-rules.md and merge-protocol.md
    - Test critic references critic-protocol.md
    - Test critic-protocol.md documents escalation for repeated identical findings, not as a round limit
    - Test BOUNDARIES.md exists for all 6 specialist agents
    - Test lifecycle references all 3 reference files
  </action>
  <verify>
    <automated>python -m pytest tests/test_agent_skills.py -x -q</automated>
  </verify>
  <done>Validation tests confirm all 10 skill directories have correct structure, frontmatter, context references, boundary definitions, and escalation semantics. RNBO/Externals stubs include Phase 5 deferral messages.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_agent_skills.py -v` -- all skill structure tests pass
- `ls .claude/skills/` shows 10 skill directories (7 from Plan 04 + 3 from this plan)
- `python -m pytest tests/ -x -q` -- no regressions in existing tests
</verification>

<success_criteria>
- 3 additional skill directories exist: max-critic, max-memory-agent, max-lifecycle
- Combined with Plan 04, all 10 skill directories are complete
- Critic protocol documents escalation for repeated identical findings only (not a general round limit)
- Validation tests confirm structure and cross-references for all 10 agents
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-system-and-orchestration/04-05-SUMMARY.md`
</output>
