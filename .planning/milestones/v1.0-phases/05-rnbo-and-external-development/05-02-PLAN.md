---
phase: 05-rnbo-and-external-development
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/maxpat/ext_templates.py
  - src/maxpat/externals.py
  - tests/test_externals.py
autonomous: true
requirements: [EXT-01, EXT-02, EXT-03, EXT-04]

must_haves:
  truths:
    - "External scaffolding creates correct Min-DevKit directory structure (source/, help/, CMakeLists.txt)"
    - "Three archetypes generate correct C++ code: message/data, DSP/signal, scheduler"
    - "CMakeLists.txt includes correct min-api paths and module configuration"
    - "DSP archetype generates sample_operator with operator() perform method"
    - "Message archetype generates inlet<>/outlet<>/message<> with MIN_FUNCTION handlers"
    - "Scheduler archetype generates timer<> with attribute<> for interval"
    - "Basic .maxhelp patch generated alongside every external"
    - "Intent-driven generation produces tailored code matching user description"
  artifacts:
    - path: "src/maxpat/ext_templates.py"
      provides: "C++ code templates for three Min-DevKit archetypes"
      exports: ["render_message_template", "render_dsp_template", "render_scheduler_template"]
    - path: "src/maxpat/externals.py"
      provides: "External scaffolding, code generation, help patch generation"
      exports: ["scaffold_external", "generate_external_code", "generate_help_patch"]
    - path: "tests/test_externals.py"
      provides: "Tests for scaffolding, all three archetypes, CMake, help patch"
  key_links:
    - from: "src/maxpat/externals.py"
      to: "src/maxpat/ext_templates.py"
      via: "scaffold_external calls render_*_template for C++ source"
      pattern: "render_.*_template"
    - from: "src/maxpat/externals.py"
      to: "src/maxpat/patcher.py"
      via: "generate_help_patch uses Patcher/Box to build .maxhelp"
      pattern: "Patcher|write_patch"
---

<objective>
C/C++ external project scaffolding and code generation via Min-DevKit.

Purpose: Generate complete Min-DevKit external project directories with CMakeLists.txt, C++ source code for three archetypes (message, DSP, scheduler), and help patches. This is the second of two independent Phase 5 workstreams (RNBO and Externals can run in parallel). Build and compilation handled in Plan 03.

Output: `src/maxpat/ext_templates.py` (C++ templates), `src/maxpat/externals.py` (scaffolding + generation), `tests/test_externals.py` (comprehensive tests).
</objective>

<execution_context>
@/Users/taylorbrook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taylorbrook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-rnbo-and-external-development/05-RESEARCH.md

Source files to read before implementing:
@src/maxpat/patcher.py (Patcher/Box for help patch generation)
@src/maxpat/hooks.py (write_patch for help patch file writing)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/maxpat/patcher.py:
```python
class Patcher:
    def __init__(self): ...
    def add_box(self, name: str, args=None, x=0.0, y=0.0) -> Box: ...
    def connect(self, source: Box, outlet: int, dest: Box, inlet: int) -> Patchline: ...

class Box:
    name: str
    id: str
    # Box.__new__(Box) pattern for structural objects
```

From src/maxpat/hooks.py:
```python
def write_patch(patcher: Patcher, path: str | Path, validate: bool = True) -> list[ValidationResult]: ...
```

From 05-RESEARCH.md -- Generated External Project Layout:
```
patches/my-project/externals/my-ext/
  CMakeLists.txt          # Top-level CMake (includes min-api)
  source/
    my-ext.cpp            # Object source code
    my-ext_test.cpp       # Optional Catch unit test
  help/
    my-ext.maxhelp        # Help patch (generated .maxpat)
  min-devkit/             # git submodule (or symlink for dev)
  build/                  # CMake build output
```

From 05-RESEARCH.md -- CMakeLists.txt template:
```cmake
cmake_minimum_required(VERSION 3.19)
get_filename_component(PROJECT_NAME ${CMAKE_CURRENT_SOURCE_DIR} NAME)
project(${PROJECT_NAME})
set(MIN_API_DIR "${CMAKE_CURRENT_SOURCE_DIR}/min-devkit/source/min-api")
include("${MIN_API_DIR}/script/min-pretarget.cmake")
set(SOURCE_FILES ${CMAKE_CURRENT_SOURCE_DIR}/source/${PROJECT_NAME}.cpp)
add_library(${PROJECT_NAME} MODULE ${SOURCE_FILES})
include("${MIN_API_DIR}/script/min-posttarget.cmake")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: C++ templates for three Min-DevKit archetypes</name>
  <files>src/maxpat/ext_templates.py, tests/test_externals.py</files>
  <behavior>
    - render_message_template(name, description, inlets, outlets, handlers) returns valid C++ with #include "c74_min.h", class declaration, inlet<>/outlet<>, message<> handlers with MIN_FUNCTION
    - render_dsp_template(name, description, num_inputs, num_outputs, params) returns C++ with sample_operator<N,M>, operator() perform method, optional dspsetup
    - render_scheduler_template(name, description, interval_default, attributes) returns C++ with timer<>, attribute<> for interval, message<> for start/stop
    - All templates include MIN_EXTERNAL(name) macro at end
    - All templates use C++17 features and Min-DevKit patterns (not classic Max SDK)
    - render_cmake_template(name) returns CMakeLists.txt content with correct min-api paths
    - render_test_template(name) returns basic Catch2 test scaffold
  </behavior>
  <action>
    Create `src/maxpat/ext_templates.py` with:

    1. **render_message_template(name, description, inlets, outlets, handlers)** -- generates C++ source for a message/data processing external:
       - `#include "c74_min.h"` and `using namespace c74::min;`
       - Class inheriting from `object<name>`
       - MIN_DESCRIPTION, MIN_TAGS ("utility"), MIN_AUTHOR ("Generated by MAX Framework")
       - inlet<> and outlet<> declarations based on inlets/outlets list (each has a comment string)
       - message<> handlers for each entry in handlers list (e.g., "bang", "number", "list", "anything") with MIN_FUNCTION bodies containing TODO comments
       - MIN_EXTERNAL(name) at end
       - Use Python f-strings for template rendering (not Jinja2 -- per Research "Don't Hand-Roll")

    2. **render_dsp_template(name, description, num_inputs, num_outputs, params)** -- generates C++ for DSP/signal external:
       - Class inheriting from `object<name>, public sample_operator<num_inputs, num_outputs>`
       - MIN_TAGS set to "audio"
       - Signal inlets/outlets with "(signal)" comment
       - `samples<num_outputs> operator()(sample input)` perform method with TODO body
       - Optional `message<> dspsetup` for sample rate access
       - attribute<> for each param entry (name, default, min, max)

    3. **render_scheduler_template(name, description, interval_default, attributes)** -- generates C++ for scheduler external:
       - MIN_TAGS set to "time"
       - timer<> member with MIN_FUNCTION that sends bang and reschedules
       - attribute<double> for interval with range
       - message<> for int toggle (start/stop)
       - Additional attribute<> for each attributes entry

    4. **render_cmake_template(name)** -- generates CMakeLists.txt:
       - cmake_minimum_required(VERSION 3.19)
       - Project name from directory
       - MIN_API_DIR pointing to min-devkit/source/min-api
       - min-pretarget.cmake include
       - Source files from source/ directory
       - add_library MODULE
       - min-posttarget.cmake include

    5. **render_test_template(name)** -- generates basic Catch2 test scaffold for the external

    Create `tests/test_externals.py` with initial tests:
    - test_message_archetype (renders valid C++ with inlet<>, outlet<>, message<>, MIN_EXTERNAL)
    - test_dsp_archetype (renders C++ with sample_operator, operator(), signal outlets)
    - test_scheduler_archetype (renders C++ with timer<>, attribute<>, toggle message)
    - test_cmake_generation (renders CMakeLists.txt with correct min-api paths)
    - test_all_templates_include_c74_min (every template has #include "c74_min.h")
    - test_all_templates_have_min_external (every template ends with MIN_EXTERNAL)
    - test_dsp_params (DSP template includes attribute<> for each param)
    - test_message_custom_handlers (message template includes user-specified handler names)

    Write tests FIRST, then implement to make them pass.

    IMPORTANT: Do NOT generate classic Max SDK C patterns (class_new/class_addmethod). Min-DevKit uses declarative C++17 patterns exclusively (per user decision and Research anti-patterns).
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_externals.py -x -v -k "template or archetype or cmake"</automated>
  </verify>
  <done>Three C++ archetype templates render correct Min-DevKit C++17 code. CMakeLists.txt template has correct min-api paths. All generated code includes c74_min.h, proper class hierarchy, and MIN_EXTERNAL macro. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: External scaffolding and help patch generation</name>
  <files>src/maxpat/externals.py, tests/test_externals.py</files>
  <behavior>
    - scaffold_external(project_dir, name, archetype, description, **kwargs) creates directory structure: source/, help/, CMakeLists.txt
    - scaffold_external writes C++ source file using appropriate template
    - scaffold_external generates .maxhelp patch using existing Patcher/write_patch pipeline
    - generate_help_patch(name, archetype, inlets, outlets) returns a Patcher with the external object, comment, number boxes demonstrating I/O
    - Help patch is valid .maxpat JSON (can open in MAX)
    - Intent-driven: scaffold_external accepts description and passes to template for MIN_DESCRIPTION and handler naming
    - Does NOT run git submodule or cmake (that is Plan 03)
  </behavior>
  <action>
    Create `src/maxpat/externals.py` with:

    1. **scaffold_external(project_dir, name, archetype, description, **kwargs)** -- creates the full directory structure:
       - `project_dir / name /` as the external root
       - `source/` subdirectory with `{name}.cpp` (rendered from appropriate template)
       - `help/` subdirectory with `{name}.maxhelp` (generated via Patcher)
       - `CMakeLists.txt` (rendered from cmake template)
       - archetype must be one of: "message", "dsp", "scheduler"
       - kwargs passed through to template (inlets, outlets, handlers, num_inputs, num_outputs, params, interval_default, attributes)
       - Returns a dict with paths: {"root": ..., "source": ..., "cmake": ..., "help": ...}
       - Does NOT create min-devkit/ submodule (handled in Plan 03 build step) or build/ directory

    2. **generate_help_patch(name, archetype, inlets, outlets)** -- builds a Patcher for the .maxhelp file:
       - Comment object with external name and description
       - The external object itself (using Box.__new__ since it is a custom external not in DB)
       - For message archetype: button -> external -> display (demonstrating bang I/O)
       - For DSP archetype: noise~ or cycle~ -> external -> dac~ (demonstrating signal chain)
       - For scheduler archetype: toggle -> external -> print (demonstrating timer output)
       - Returns Patcher (caller writes via write_patch)

    3. **generate_external_code(name, archetype, description, **kwargs)** -- generates just the C++ code string without file I/O (for programmatic use). Delegates to ext_templates.py render functions.

    Extend `tests/test_externals.py` with:
    - test_scaffold_structure (scaffold_external creates source/, help/, CMakeLists.txt)
    - test_scaffold_message (message archetype produces correct directory + files)
    - test_scaffold_dsp (DSP archetype produces correct directory + files)
    - test_scaffold_scheduler (scheduler archetype produces correct directory + files)
    - test_help_patch_valid_json (generated .maxhelp is valid JSON with patcher structure)
    - test_help_patch_contains_external (help patch has the external object box)
    - test_scaffold_returns_paths (returns dict with root, source, cmake, help paths)
    - test_generate_external_code (returns C++ string matching template output)
    - test_scaffold_invalid_archetype (raises ValueError for unknown archetype)

    Write tests FIRST, then implement to make them pass.

    Use tmp_path pytest fixture for all file system tests (no side effects on real filesystem).
  </action>
  <verify>
    <automated>cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_externals.py -x -v</automated>
  </verify>
  <done>scaffold_external creates correct Min-DevKit directory structure with source, CMakeLists.txt, and .maxhelp. Help patches are valid .maxpat JSON. All three archetypes scaffold correctly. Tests pass.</done>
</task>

</tasks>

<verification>
All external scaffolding tests pass:
```bash
cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/test_externals.py -v
```

Full test suite still passes (no regressions):
```bash
cd /Users/taylorbrook/Dev/MAX && python3 -m pytest tests/ -x -q
```
</verification>

<success_criteria>
- Three C++ archetypes render correct Min-DevKit C++17 code (no classic Max SDK patterns)
- scaffold_external creates the documented directory structure with all required files
- CMakeLists.txt includes correct min-api paths and module configuration
- .maxhelp patches are valid JSON that can open in MAX
- Intent-driven: description flows through to MIN_DESCRIPTION and code structure
- All new tests pass, existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/05-rnbo-and-external-development/05-02-SUMMARY.md`
</output>
