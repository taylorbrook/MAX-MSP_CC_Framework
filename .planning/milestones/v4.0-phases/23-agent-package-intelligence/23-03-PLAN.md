---
phase: 23-agent-package-intelligence
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - .claude/skills/max-patch-agent/SKILL.md
  - .claude/skills/max-dsp-agent/SKILL.md
  - .claude/skills/max-ui-agent/SKILL.md
  - .claude/skills/max-rnbo-agent/SKILL.md
  - .claude/skills/max-js-agent/SKILL.md
  - tests/test_agent_skills.py
  - tests/test_package_schema.py
autonomous: true
requirements: [PKG-14, PKG-18]
must_haves:
  truths:
    - "Each of the 5 agent SKILL.md files has a Package Intelligence section"
    - "max-patch-agent has BEAP modular patterns and Vizzie chain guidance"
    - "max-dsp-agent has BEAP CV signal conventions"
    - "max-ui-agent has bpatcher layout rules with object_name parameter"
    - "max-rnbo-agent explicitly notes BEAP/Vizzie are NOT RNBO-compatible"
    - "Tests verify package intelligence sections exist in all 5 agents"
    - "Parity test confirms package objects have same coverage level as core objects"
  artifacts:
    - path: ".claude/skills/max-patch-agent/SKILL.md"
      provides: "BEAP modular patterns, Vizzie chain guidance"
      contains: "Package Intelligence"
    - path: ".claude/skills/max-dsp-agent/SKILL.md"
      provides: "BEAP CV signal conventions"
      contains: "Package Intelligence"
    - path: ".claude/skills/max-ui-agent/SKILL.md"
      provides: "Bpatcher layout rules"
      contains: "Package Intelligence"
    - path: ".claude/skills/max-rnbo-agent/SKILL.md"
      provides: "BEAP/Vizzie NOT RNBO-compatible warning"
      contains: "Package Intelligence"
    - path: ".claude/skills/max-js-agent/SKILL.md"
      provides: "Package-aware scripting notes"
      contains: "Package Intelligence"
    - path: "tests/test_agent_skills.py"
      provides: "Tests for package intelligence sections"
      contains: "test_specialist_has_package_intelligence"
    - path: "tests/test_package_schema.py"
      provides: "Parity test for package coverage"
      contains: "test_package_parity"
  key_links:
    - from: ".claude/skills/max-patch-agent/SKILL.md"
      to: ".claude/max-objects/PACKAGES.md"
      via: "Reference to shared package doc"
      pattern: "PACKAGES\\.md"
    - from: ".claude/skills/max-ui-agent/SKILL.md"
      to: "src/maxpat/patcher.py"
      via: "Documents add_bpatcher(object_name=) parameter"
      pattern: "object_name"
---

<objective>
Add per-agent package intelligence sections to all 5 specialist SKILL.md files and create parity verification tests.

Purpose: Each agent needs domain-specific package guidance beyond the shared PACKAGES.md. The patch agent needs BEAP modular patterns, the DSP agent needs CV conventions, the UI agent needs bpatcher sizing rules, the RNBO agent needs incompatibility warnings, and the JS agent needs light package-aware scripting notes.

Output: Updated SKILL.md files for 5 agents, new tests for package intelligence coverage and package-vs-core parity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-agent-package-intelligence/23-CONTEXT.md
@.planning/phases/23-agent-package-intelligence/23-RESEARCH.md
@.planning/phases/23-agent-package-intelligence/23-01-SUMMARY.md
@.planning/phases/23-agent-package-intelligence/23-02-SUMMARY.md

<interfaces>
<!-- Existing SKILL.md section patterns and what Plan 01/02 produced -->

Agent SKILL.md structure (each has):
- YAML frontmatter (name, description, allowed-tools, preconditions)
- Domain Context Loading section
- Capabilities section
- Output Protocol sections
- When to Use / When NOT to Use sections

Plan 01 output:
- add_bpatcher(object_name="bp.Oscillator") auto-sizes from DB
- get_bpatcher_dims(name) in sizing.py returns (width, height) or None
- Adaptive layout spacing for tall bpatcher rows

Plan 02 output:
- .claude/max-objects/PACKAGES.md with signal conventions, functional roles, templates
- relationships.json with 15-25 package pairs tagged with "package" field
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Package Intelligence sections to 5 agent SKILL.md files</name>
  <files>.claude/skills/max-patch-agent/SKILL.md, .claude/skills/max-dsp-agent/SKILL.md, .claude/skills/max-ui-agent/SKILL.md, .claude/skills/max-rnbo-agent/SKILL.md, .claude/skills/max-js-agent/SKILL.md, tests/test_agent_skills.py</files>
  <read_first>
    - .claude/skills/max-patch-agent/SKILL.md (full file)
    - .claude/skills/max-dsp-agent/SKILL.md (full file)
    - .claude/skills/max-ui-agent/SKILL.md (full file)
    - .claude/skills/max-rnbo-agent/SKILL.md (full file)
    - .claude/skills/max-js-agent/SKILL.md (full file)
    - .claude/max-objects/PACKAGES.md (created by Plan 02, to reference correctly)
    - tests/test_agent_skills.py (existing tests, understand patterns)
  </read_first>
  <action>
Add a `## Package Intelligence` section to each of the 5 SKILL.md files. Insert it before the "Editing Existing Patches" section (or before "Output Protocol" if no editing section). Each section starts with the shared reference, then adds agent-specific guidance.

**All 5 agents get this common preamble:**
```markdown
## Package Intelligence

When generating patches with package objects (BEAP, Vizzie, etc.), read `.claude/max-objects/PACKAGES.md` for:
- Signal conventions (BEAP: 0-5V CV, +/-1 audio; Vizzie: Jitter matrices)
- Functional roles and canonical module selection
- Template signal chains with connection order
```

**max-patch-agent specific additions (per D-03, D-04, D-05):**
```markdown
### BEAP Modular Patching

BEAP modules are bpatchers that emulate analog modular synthesis:
- Use `add_bpatcher(object_name="bp.Oscillator", filename="bp.Oscillator.maxpat")` for auto-sized placement
- Follow signal chain order: Sources -> Processors -> Output (see PACKAGES.md templates)
- Always terminate with bp.Stereo or bp.Mono -- never leave signal chains unterminated
- CV connections carry control voltage (0-5V), separate from audio signal paths
- Use bp.VCA for all gain control -- never connect oscillators directly to output
- Keyboard provides pitch CV (outlet 0), gate (outlet 1), velocity (outlet 2), aftertouch (outlet 3)

### Vizzie Video Chains

Vizzie modules pass Jitter matrices (video frames) between bpatchers:
- Use `add_bpatcher(object_name="vz.playr", filename="vz.playr.maxpat")` for auto-sized placement
- Follow matrix chain order: Sources -> Effects -> Compositing -> Output
- Always terminate with vz.viewr (window) or vz.projectr (fullscreen)
- Control inlets accept int/float messages for parameter adjustment
```

**max-dsp-agent specific additions:**
```markdown
### BEAP and MSP Integration

BEAP modules use MSP signals internally but with modular conventions:
- BEAP CV range is 0 to +5V (NOT standard MSP +/-1) -- do not mix raw MSP signals into BEAP CV inputs without scaling
- When building hybrid patches (MSP + BEAP), use `*~ 5.0` to scale MSP (+/-1) to BEAP CV range, or `*~ 0.2` for BEAP-to-MSP
- BEAP 1V/oct pitch tracking: each volt = one octave. bp.Keyboard outputs calibrated pitch CV.
- gen~ codebox output can feed BEAP modules if properly scaled to 0-5V range
- BEAP modules are NOT gen~ objects -- they are bpatchers containing MSP signal chains
```

**max-ui-agent specific additions:**
```markdown
### Bpatcher Layout for Package Modules

Package bpatchers have specific dimensions that differ from the default 200x100:
- Use `add_bpatcher(object_name="bp.Oscillator", filename="bp.Oscillator.maxpat")` -- the `object_name` parameter triggers DB-driven auto-sizing
- BEAP standard height: 116px (most modules), widths vary from 52px (bp.Mono) to 895px
- Vizzie dimensions vary more widely (71-738px wide, 57-517px tall)
- When placing package bpatchers in presentation mode, use the actual dimensions from DB for `presentation_rect` sizing
- The layout engine applies adaptive vertical spacing for rows containing tall bpatchers (>100px) -- extra gap = (height - 100) * 0.1
- For manual positioning, use `get_bpatcher_dims("bp.Oscillator")` from `src.maxpat.sizing` to query dimensions
```

**max-rnbo-agent specific additions:**
```markdown
### Package Compatibility Warning

BEAP and Vizzie modules are NOT RNBO-compatible:
- BEAP bpatchers contain MSP abstractions that cannot be exported to VST3/AU/Web targets
- Vizzie uses Jitter (video) which has no RNBO equivalent
- If user requests "RNBO synth like BEAP", build equivalent signal chains using RNBO-compatible objects (rnbo~ internal patching with cycle~, biquad~, etc.)
- jit.mo, Jitter Geometry, Jitter Tools are also NOT RNBO-compatible (Jitter domain)
- Only ableton-dsp objects marked `rnbo_compatible: true` in the DB can be used in RNBO patches
```

**max-js-agent specific additions (light touch):**
```markdown
### Package-Aware Scripting

When scripting interactions with package modules:
- BEAP/Vizzie bpatchers expose parameters via their inlets -- use `this.patcher.getnamed()` to find bpatcher boxes and send messages to their inlets
- BEAP CV values are 0-5V floats -- scale accordingly when sending from js
- Vizzie control inlets accept standard int/float messages
- Use `ObjectDatabase.get_package_objects("BEAP")` to programmatically list available modules
```

**Tests -- add to tests/test_agent_skills.py:**

Add a new parametrized test at the end of the file:

```python
PACKAGE_AGENTS = [
    "max-patch-agent",
    "max-dsp-agent",
    "max-ui-agent",
    "max-rnbo-agent",
    "max-js-agent",
]

@pytest.mark.parametrize("agent_name", PACKAGE_AGENTS)
def test_specialist_has_package_intelligence(agent_name: str) -> None:
    """Each specialist agent with package relevance must have Package Intelligence section."""
    content = _read_skill(agent_name)
    assert "Package Intelligence" in content, (
        f"{agent_name}/SKILL.md missing 'Package Intelligence' section"
    )

@pytest.mark.parametrize("agent_name", PACKAGE_AGENTS)
def test_specialist_references_packages_md(agent_name: str) -> None:
    """Each agent with package intelligence must reference PACKAGES.md."""
    content = _read_skill(agent_name)
    assert "PACKAGES.md" in content, (
        f"{agent_name}/SKILL.md missing reference to PACKAGES.md"
    )
```

Plus agent-specific content tests:
```python
def test_patch_agent_has_beap_patterns() -> None:
    content = _read_skill("max-patch-agent")
    assert "BEAP" in content and "bp.Stereo" in content

def test_dsp_agent_has_cv_conventions() -> None:
    content = _read_skill("max-dsp-agent")
    assert "0-5V" in content or "0 to +5V" in content

def test_ui_agent_has_bpatcher_layout() -> None:
    content = _read_skill("max-ui-agent")
    assert "object_name" in content and "auto-siz" in content.lower()

def test_rnbo_agent_has_incompatibility_warning() -> None:
    content = _read_skill("max-rnbo-agent")
    assert "NOT RNBO-compatible" in content or "not RNBO-compatible" in content
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_agent_skills.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/max-patch-agent/SKILL.md` contains "Package Intelligence"
    - `.claude/skills/max-patch-agent/SKILL.md` contains "bp.Stereo"
    - `.claude/skills/max-dsp-agent/SKILL.md` contains "Package Intelligence"
    - `.claude/skills/max-dsp-agent/SKILL.md` contains "0-5V" or "0 to +5V"
    - `.claude/skills/max-ui-agent/SKILL.md` contains "Package Intelligence"
    - `.claude/skills/max-ui-agent/SKILL.md` contains "object_name"
    - `.claude/skills/max-rnbo-agent/SKILL.md` contains "Package Intelligence"
    - `.claude/skills/max-rnbo-agent/SKILL.md` contains "NOT RNBO-compatible"
    - `.claude/skills/max-js-agent/SKILL.md` contains "Package Intelligence"
    - `tests/test_agent_skills.py` contains "test_specialist_has_package_intelligence"
    - `tests/test_agent_skills.py` contains "test_specialist_references_packages_md"
    - `python3 -m pytest tests/test_agent_skills.py -x -q` exits 0
  </acceptance_criteria>
  <done>All 5 agent SKILL.md files have Package Intelligence sections with domain-specific guidance. Tests verify sections exist and contain correct content.</done>
</task>

<task type="auto">
  <name>Task 2: Package-vs-core parity verification tests</name>
  <files>tests/test_package_schema.py</files>
  <read_first>
    - tests/test_package_schema.py (full file after Plan 02 additions)
    - .claude/max-objects/packages/BEAP/objects.json (verify field coverage)
    - .claude/max-objects/packages/Vizzie/objects.json (verify field coverage)
    - .claude/max-objects/relationships.json (verify package pairs exist)
    - .claude/max-objects/PACKAGES.md (verify it exists)
    - .claude/skills/max-patch-agent/SKILL.md (verify Package Intelligence section)
  </read_first>
  <action>
Add a new test class `TestPackageParity` to `tests/test_package_schema.py` that verifies package objects have the same coverage level as core domain objects (PKG-18).

Parity checks:

1. **DB field parity**: Every BEAP/Vizzie object must have these fields (same as core objects): `name`, `maxclass`, `domain`, `inlets`, `outlets`. Plus package-specific: `package`, `bpatcher_dimensions`, `category`.

2. **Relationship coverage**: BEAP and Vizzie must each have at least 5 entries in relationships.json (core has 19 total).

3. **Agent guidance coverage**: At least 3 of the 5 specialist agents must reference "PACKAGES.md" in their SKILL.md.

4. **PACKAGES.md template coverage**: PACKAGES.md must contain at least 3 template sections (headers starting with "###" that contain module connection lists).

5. **Dimension coverage**: At least 90% of BEAP objects and 90% of Vizzie objects must have non-null `bpatcher_dimensions` with both values > 0.

```python
class TestPackageParity:
    """PKG-18: Package objects have same coverage level as core domains."""

    @pytest.fixture(scope="class")
    def db(self):
        from src.maxpat.db_lookup import ObjectDatabase
        return ObjectDatabase()

    def test_beap_objects_have_required_fields(self, db):
        """BEAP objects must have all standard fields plus package-specific fields."""
        required = ["name", "maxclass", "domain", "inlets", "outlets", "package", "bpatcher_dimensions", "category"]
        objs = db.get_package_objects("BEAP")
        assert len(objs) >= 180
        for obj in objs:
            for field in required:
                assert field in obj, f"BEAP object '{obj.get('name', '?')}' missing field '{field}'"

    def test_vizzie_objects_have_required_fields(self, db):
        """Vizzie objects must have all standard fields plus package-specific fields."""
        required = ["name", "maxclass", "domain", "inlets", "outlets", "package", "bpatcher_dimensions", "category"]
        objs = db.get_package_objects("Vizzie")
        assert len(objs) >= 100
        for obj in objs:
            for field in required:
                assert field in obj, f"Vizzie object '{obj.get('name', '?')}' missing field '{field}'"

    def test_beap_dimension_coverage(self, db):
        """At least 90% of BEAP objects must have valid bpatcher_dimensions."""
        objs = db.get_package_objects("BEAP")
        with_dims = [o for o in objs if o.get("bpatcher_dimensions") and len(o["bpatcher_dimensions"]) >= 2 and o["bpatcher_dimensions"][0] > 0 and o["bpatcher_dimensions"][1] > 0]
        coverage = len(with_dims) / len(objs) if objs else 0
        assert coverage >= 0.9, f"BEAP dimension coverage {coverage:.1%} < 90%"

    def test_vizzie_dimension_coverage(self, db):
        """At least 90% of Vizzie objects must have valid bpatcher_dimensions."""
        objs = db.get_package_objects("Vizzie")
        with_dims = [o for o in objs if o.get("bpatcher_dimensions") and len(o["bpatcher_dimensions"]) >= 2 and o["bpatcher_dimensions"][0] > 0 and o["bpatcher_dimensions"][1] > 0]
        coverage = len(with_dims) / len(objs) if objs else 0
        assert coverage >= 0.9, f"Vizzie dimension coverage {coverage:.1%} < 90%"

    def test_relationship_coverage_beap(self, db_root):
        """BEAP must have at least 5 relationship entries."""
        data = json.loads((db_root / "relationships.json").read_text())
        beap_pairs = [p for p in data["pairs"] if p.get("package") == "BEAP"]
        assert len(beap_pairs) >= 5, f"Expected >= 5 BEAP relationship pairs, got {len(beap_pairs)}"

    def test_relationship_coverage_vizzie(self, db_root):
        """Vizzie must have at least 3 relationship entries."""
        data = json.loads((db_root / "relationships.json").read_text())
        vizzie_pairs = [p for p in data["pairs"] if p.get("package") == "Vizzie"]
        assert len(vizzie_pairs) >= 3, f"Expected >= 3 Vizzie relationship pairs, got {len(vizzie_pairs)}"

    def test_agent_guidance_coverage(self):
        """At least 3 specialist agents must reference PACKAGES.md."""
        skills_dir = Path(__file__).resolve().parent.parent / ".claude" / "skills"
        agents = ["max-patch-agent", "max-dsp-agent", "max-ui-agent", "max-rnbo-agent", "max-js-agent"]
        count = 0
        for agent in agents:
            content = (skills_dir / agent / "SKILL.md").read_text()
            if "PACKAGES.md" in content:
                count += 1
        assert count >= 3, f"Expected >= 3 agents referencing PACKAGES.md, got {count}"

    def test_packages_md_has_templates(self, db_root):
        """PACKAGES.md must have at least 3 template sections with connection lists."""
        content = (db_root / "PACKAGES.md").read_text()
        # Count template sections (### headers containing common template words)
        import re
        templates = re.findall(r'^###\s+.*(?:Synth|Chain|Setup|Effect|Analysis|Camera|VJ)', content, re.MULTILINE)
        assert len(templates) >= 3, f"Expected >= 3 template sections, got {len(templates)}: {templates}"
```
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_package_schema.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_package_schema.py` contains `class TestPackageParity`
    - `tests/test_package_schema.py` contains `test_beap_objects_have_required_fields`
    - `tests/test_package_schema.py` contains `test_beap_dimension_coverage`
    - `tests/test_package_schema.py` contains `test_relationship_coverage_beap`
    - `tests/test_package_schema.py` contains `test_agent_guidance_coverage`
    - `tests/test_package_schema.py` contains `test_packages_md_has_templates`
    - `python3 -m pytest tests/test_package_schema.py -x -q` exits 0
    - `python3 -m pytest tests/ -x -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Parity tests confirm BEAP/Vizzie objects have full DB field coverage, relationship entries, agent guidance, dimension data, and template documentation -- matching core domain coverage level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md -> agent behavior | Agent reads SKILL.md and follows its instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | I (Information Disclosure) | SKILL.md package sections | accept | Contains only MAX/MSP technical conventions, no secrets or credentials |
| T-23-06 | S (Spoofing) | Agent following wrong conventions | accept | Conventions are verified by tests; incorrect guidance caught during patch validation |
</threat_model>

<verification>
```bash
python3 -m pytest tests/test_agent_skills.py tests/test_package_schema.py -x -q
python3 -m pytest tests/ -x -q  # Full suite
```
</verification>

<success_criteria>
- All 5 agent SKILL.md files have Package Intelligence sections
- max-patch-agent documents BEAP modular patterns and Vizzie chains
- max-dsp-agent documents CV signal conventions
- max-ui-agent documents bpatcher sizing with object_name parameter
- max-rnbo-agent warns BEAP/Vizzie are NOT RNBO-compatible
- Parity tests confirm package objects have same coverage as core domains
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-agent-package-intelligence/23-03-SUMMARY.md`
</output>
